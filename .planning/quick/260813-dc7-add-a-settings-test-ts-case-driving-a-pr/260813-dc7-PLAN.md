---
phase: quick-260813-dc7
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/frontend/src/stores/settings.test.ts
  - .planning/phases/01-restore-the-verification-signal/01-VALIDATION.md
autonomous: true
requirements: [SIG-01]
gap_closure: true
closes_gap: "01-VERIFICATION.md G1 — the forwarding direction of readBrowserStorageItem is mutation-survivable"

must_haves:
  truths:
    - "A test drives a present CAIDO_AUTHENTICATION JSON token through readBrowserStorageItem and asserts the trimmed accessToken — not the raw JSON — reaches syncCaidoSessionToken"
    - "Re-applying the mutation `if (key !== \"__never__\") return undefined;` to readBrowserStorageItem makes that new test FAIL, proven by execution, then reverted"
    - "The malformed-JSON branch (settings.ts:105-111) is covered: empty token pushed AND a warning toast raised"
    - "The non-string getItem return arm (settings.ts:41 ternary false arm) is covered"
    - "The three existing guard tests (SIG-01e/f/g) still pass unchanged — the shared beforeEach behaviour is not altered for them"
    - "01-VALIDATION.md row SIG-01h no longer claims pre-existing vi.stubGlobal coverage"
    - "Full suite green at 23 files / 134 tests and `pnpm lint` at 0 errors / 0 warnings"
    - "packages/frontend/src/stores/settings.ts is byte-identical to HEAD when the plan ends"
  artifacts:
    - path: "packages/frontend/src/stores/settings.test.ts"
      provides: "Forward-direction coverage of the storage guard"
      min_lines: 350
      contains: "forwards the parsed accessToken"
    - path: ".planning/phases/01-restore-the-verification-signal/01-VALIDATION.md"
      provides: "A validation contract that no longer asserts coverage it does not have"
      contains: "forwards the parsed accessToken"
  key_links:
    - from: "packages/frontend/src/stores/settings.test.ts"
      to: "readBrowserStorageItem"
      via: "vi.stubGlobal('window', { localStorage: { getItem } }) returning a JSON string"
      pattern: "JSON\\.stringify\\(\\{ accessToken"
    - from: "packages/frontend/src/stores/settings.test.ts"
      to: "mockSdk.backend.syncCaidoSessionToken"
      via: "await store.syncCaidoRuntimeContext()"
      pattern: "syncCaidoSessionToken\\).toHaveBeenCalledWith\\(\"tok-123\"\\)"
---

<objective>
Close gap G1 from `01-VERIFICATION.md`: `readBrowserStorageItem()` in
`packages/frontend/src/stores/settings.ts` is mutation-survivable in the forwarding
direction. Inserting `if (key !== "__never__") return undefined;` at the top of that
function leaves the full suite green at 23 files / 131 tests. Every existing guard test
(SIG-01e/f/g) asserts `syncCaidoSessionToken` was called with `""` — which is exactly what
a permanently broken read also produces — and the shared `beforeEach` at
`settings.test.ts:118-122` stubs `getItem` to return `null`. Repo-wide, the string
`CAIDO_AUTHENTICATION` appears exactly ONCE, in production code at `settings.ts:93`. No
test anywhere drives a present token through the path.

This is a **missing-test gap, not a broken-code gap**. `settings.ts:99-113` is correct by
inspection. The tolerance direction is already pinned — the verifier measured that
reverting the guard to the pre-phase unguarded read DOES fail 3/3 guard tests.

Purpose: make the phase's primary production change falsifiable in the direction that
matters. A regression to an always-empty token makes `startMcpServer` abort at
`index.ts:1697-1702` — total loss of the MCP bridge, which is CLAUDE.md's stated core value.

Output: three appended test cases in `settings.test.ts`, an executed-and-reverted mutation
proof, and a corrected SIG-01h row in `01-VALIDATION.md`.

**Hard scope fence — read before touching anything:**
- Do NOT modify `packages/frontend/src/stores/settings.ts` or any other production file.
  The ONLY contact with `settings.ts` is the temporary, reverted mutation in Task 1's
  verify step. It is deliberately absent from `files_modified` because it must end
  byte-identical to HEAD.
- The new tests are **append-only** to `settings.test.ts`. Do NOT restructure the shared
  `beforeEach` (lines 117-141) or `afterEach` (143-145) — that would change behaviour for
  the other ten tests. Override the stub locally inside each new test instead, exactly as
  the three existing guard tests already do.
- Do NOT edit `01-VERIFICATION.md`. Re-verification owns that file.
- The repo root carries an untracked `IMPROVEMENT-PLAN.md` (see `.planning/STATE.md:112`).
  Use targeted `git add <path>` when committing. Never `git add -A`.

**Measured baseline on this tree (Node 26.7.0, vitest 4.0.18):**
`settings.test.ts` = 10 tests · full suite = 23 files / 131 tests · `pnpm lint` = exit 0,
0 errors / 0 warnings.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@packages/frontend/src/stores/settings.ts
@packages/frontend/src/stores/settings.test.ts
@.planning/phases/01-restore-the-verification-signal/01-VALIDATION.md
@.planning/phases/01-restore-the-verification-signal/01-VERIFICATION.md
</context>

<interface_context>
Facts you need so you do not go hunting for them:

- `syncCaidoSessionToken` is **not** exposed on the store's public surface
  (`settings.ts:616-639` returns `syncCaidoHistoryContext` and `syncCaidoRuntimeContext`,
  not `syncCaidoSessionToken`). Drive the path through `await store.syncCaidoRuntimeContext()`,
  exactly as the three existing guard tests do. The thing you assert on is the RPC mock
  `mockSdk.backend.syncCaidoSessionToken`, which is a different object.
- `syncCaidoRuntimeContext()` calls `syncCaidoSessionToken()` and then
  `syncCaidoHistoryContext()`. The latter is already fully mocked in `beforeEach`
  (`getCurrentFilter` → `undefined`, `getQuery`/`getScopeId` → `""`), so it will not
  interfere.
- The toast call shape is `sdk.window.showToast(message, { variant: "warning" })`
  (`settings.ts:107-109`), where message is
  `` `Failed to read the current Caido session token: ${String(error)}` ``.
  The mock is `mockSdk.window.showToast`, reset in `beforeEach` at line 140.
- `vi.stubGlobal(name, value)` types `value` as `unknown`, so returning a number from a
  stubbed `getItem` needs no cast. Test files are excluded from `pnpm typecheck` anyway
  (`packages/frontend/tsconfig.json` line 8), but they ARE linted.
- ESLint applies `@vitest/eslint-plugin` recommended rules to `**/*.test.ts`
  (`eslint.config.mjs`), including `no-identical-title` and `expect-expect`. Every new
  `it()` needs a unique title and at least one `expect`.
- `settings.test.ts` carries no `@vitest-environment` docblock, so it runs in the `node`
  environment and never touches `vitest.setup.ts`'s storage shim. It stubs `window` itself.
  This independence is a verified must-have from plan 01-01 (P1.4) — preserve it by
  stubbing `window` rather than reaching for ambient storage.
</interface_context>

<tasks>

<task type="auto">
  <name>Task 1: Append three falsifying cases to settings.test.ts and prove the mutant dies</name>
  <files>packages/frontend/src/stores/settings.test.ts</files>
  <behavior>
    - Test A (the mutant-killer): storage holds `{"accessToken":"  tok-123  "}` → `syncCaidoSessionToken` receives `"tok-123"`, the trimmed value, not the raw JSON string.
    - Test A also pins the key: `getItem` was called with `"CAIDO_AUTHENTICATION"`.
    - Test B: storage holds a non-JSON string → `syncCaidoSessionToken` receives `""` AND a warning toast is raised.
    - Test C: `getItem` returns a non-string → `syncCaidoSessionToken` receives `""`.
  </behavior>
  <action>
    Append three `it(...)` blocks to `packages/frontend/src/stores/settings.test.ts`, placed
    after the last existing test (the one titled "treats a storage object without getItem as
    no token", which currently ends at line 331) and before the file's closing `});` at line
    332. Leave lines 1-331 untouched — in particular do not alter the shared `beforeEach` or
    `afterEach`. Each new test calls `vi.stubGlobal("window", ...)` itself, which replaces the
    `beforeEach` stub for that test only; the existing `afterEach` `vi.unstubAllGlobals()`
    cleans it up.

    Test A — title exactly `forwards the parsed accessToken when storage holds a real token`.
    Stub `window` with a `localStorage` whose `getItem` is a `vi.fn()` returning
    `JSON.stringify({ accessToken: "  tok-123  " })`. Hold that spy in a local const so you
    can assert on it. Create the store with `useSettingsStore()`, then
    `await store.syncCaidoRuntimeContext()`. Assert `mockSdk.backend.syncCaidoSessionToken`
    was called with `"tok-123"` — the trimmed accessToken, NOT the raw JSON. Then assert the
    `getItem` spy was called with `"CAIDO_AUTHENTICATION"`, which pins the key name that
    today appears exactly once in the whole repo (production code only). Lead the test with a
    `//` comment stating that this is the case that kills the always-`undefined` mutant and
    that every other test in this file asserts `""`, which a permanently broken read also
    produces.

    Test B — title exactly `pushes an empty token and warns when the stored value is not
    valid JSON`. Stub `getItem` to return the literal string `not-json`. After
    `await store.syncCaidoRuntimeContext()`, assert `mockSdk.backend.syncCaidoSessionToken`
    was called with `""` AND that `mockSdk.window.showToast` was called with a message
    matching `expect.stringContaining("Failed to read the current Caido session token")` and
    a second argument of `{ variant: "warning" }`. This covers `settings.ts:105-111`, which
    the verifier recorded as zero-coverage. Lead with a `//` comment noting that the toast
    assertion is what makes this test die under the mutation too — a broken read returns
    early at `settings.ts:94-97` and never reaches the parse.

    Test C — title exactly `treats a non-string getItem return as no token`. Stub `getItem`
    to return the number `42`. Assert `mockSdk.backend.syncCaidoSessionToken` was called with
    `""`. This covers the false arm of the `settings.ts:41` ternary. Lead with a `//` comment
    noting that a page-injected or polyfilled storage can return a non-string and that this
    test intentionally survives the always-`undefined` mutation because it asserts `""` —
    it covers a different arm, not the forwarding direction.

    Match the file's existing conventions throughout: double quotes, 2-space indent,
    `async () => {` bodies, a leading `//` comment explaining WHY (as SIG-01e/f/g do), and a
    blank line separating arrange / act / assert. Do not import anything new — `vi`,
    `expect`, `it`, `useSettingsStore` and `mockSdk` are all already in scope.

    Do NOT touch `packages/frontend/src/stores/settings.ts` while implementing. Its only
    role is the temporary mutation in the verify step, which is applied and reverted there.
  </action>
  <verify>
    <automated>
      # 1. The new tests pass. 10 -> 13.
      pnpm exec vitest run packages/frontend/src/stores/settings.test.ts
      # EXPECT: 13 passed (13). If it says 10, nothing was appended.

      # 2. FALSIFIABILITY PROOF — this is the whole point of the task.
      #    A test that cannot fail repeats the exact defect being closed.
      #    2a. Apply the mutation to the production file (temporary, reverted at 2e).
      perl -0pi -e 's/(function readBrowserStorageItem\(key: string\): string \| undefined \{\n)/$1  if (key !== "__never__") return undefined;\n/' packages/frontend/src/stores/settings.ts

      #    2b. Prove the mutation actually landed. Verified: this inserts exactly one line
      #        at settings.ts:35, immediately inside the function body.
      git diff --stat -- packages/frontend/src/stores/settings.ts
      # EXPECT: "1 insertion(+)". If 0, the regex missed and the entire proof below is void —
      # stop, fix the mutation, do not interpret a passing test as a closed gap.

      #    2c. The new forwarding test MUST now fail.
      pnpm exec vitest run packages/frontend/src/stores/settings.test.ts -t "forwards the parsed accessToken"
      # EXPECT: non-zero exit, 1 failed. AssertionError: expected "" to be "tok-123".
      # If this PASSES, the test is not falsifiable and the gap is NOT closed.

      #    2d. Whole-file shape under mutation.
      pnpm exec vitest run packages/frontend/src/stores/settings.test.ts
      # EXPECT: exactly 2 failed | 11 passed. Test A dies on the token value; Test B dies on
      # the missing toast (a broken read returns early at settings.ts:94-97 and never parses).
      # Test C survives by design — it asserts "". Seeing 1 failed means Test B is missing
      # its showToast assertion.

      #    2e. Revert. The production file must end byte-identical to HEAD.
      git checkout -- packages/frontend/src/stores/settings.ts
      git diff --quiet -- packages/frontend/src/stores/settings.ts && echo "REVERTED CLEAN"
      # EXPECT: "REVERTED CLEAN". This gate is mandatory — an unreverted mutation ships a
      # deliberately broken token read.

      # 3. Full suite, post-revert. 131 -> 134.
      pnpm exec vitest run
      # EXPECT: 23 files passed, 134 tests passed, 0 failed.

      # 4. Lint gate stays at 0/0.
      pnpm lint
      # EXPECT: exit 0, no output.
    </automated>
  </verify>
  <done>
    `settings.test.ts` holds 13 tests. The full suite is 23 files / 134 tests, 0 failed.
    `pnpm lint` exits 0. The falsifiability proof was executed and recorded: with the
    mutation applied the file reported 2 failed / 11 passed, and after `git checkout --`
    the file is byte-identical to HEAD (`git diff --quiet` exits 0). The three existing
    guard tests (SIG-01e/f/g) and the shared `beforeEach`/`afterEach` are unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 2: Correct the SIG-01h row and the false-pass table in 01-VALIDATION.md</name>
  <files>.planning/phases/01-restore-the-verification-signal/01-VALIDATION.md</files>
  <action>
    Make three edits to
    `.planning/phases/01-restore-the-verification-signal/01-VALIDATION.md`. Nothing else in
    the repo changes in this task.

    Edit 1 — the SIG-01h row at line 65. Today it reads: behaviour "The existing happy path
    (real token in storage) still works", automated command
    `pnpm exec vitest run packages/frontend/src/stores/settings.test.ts`, and `Exists?` cell
    `✅ exists (\`vi.stubGlobal\`)`. That `Exists?` claim is factually wrong — the referenced
    `vi.stubGlobal` at lines 118-122 returns `null`. Replace the row, keeping all seven
    columns and the leading/trailing pipes intact. New behaviour text: a present
    `CAIDO_AUTHENTICATION` token is parsed and its trimmed `accessToken` is forwarded, not
    the raw JSON. New automated command:
    `pnpm exec vitest run packages/frontend/src/stores/settings.test.ts -t "forwards the parsed accessToken"`.
    New `Exists?` cell: mark it `❌ Wave 0` and state plainly that it was wrongly recorded as
    `✅ exists (vi.stubGlobal)` when that stub returns `null`, and that it was added by
    gap-closure `260813-dc7`. Leave the Req column as `SIG-01`, the Type column as `unit`,
    and the Status column as `⬜ pending` — every one of the 23 sibling rows is `⬜ pending`,
    because this file is a contract, not a results log. Do not add a row and do not remove
    one: the map must stay at exactly 24 assertions, matching the "24 assertions" sentence at
    line 54 and the sign-off checkbox at line 135.

    Edit 2 — the `## Wave 0 Requirements` bullet at line 112, which currently reads
    "`packages/frontend/src/stores/settings.test.ts` — three guard cases: absent / throwing /
    no-`getItem` (SIG-01e/f/g)". Extend it to six cases: keep the three tolerance cases and
    add present-token forwarding, malformed JSON, and non-string return, attributing the new
    three to SIG-01h.

    Edit 3 — add one row to the `## What would constitute a false pass` table, immediately
    after the last existing `| SIG-01 |` row (currently line 96, the `it.only` row) so it
    stays inside the SIG-01 group. The mechanism to record is the one the verifier actually
    measured: every guard test asserts `syncCaidoSessionToken` was called with `""`, which is
    precisely what a permanently broken read also produces, so the guard's forwarding
    direction survives any mutation and the suite stays green at 131 tests. The
    counter-measure column: SIG-01h drives a present token and asserts the trimmed
    `accessToken`, and it is falsified by re-applying `if (key !== "__never__") return undefined;`
    to `readBrowserStorageItem`. Keep the row to the table's three columns.

    Do NOT touch the frontmatter, the "24 assertions" count, `01-VERIFICATION.md`, or any
    other planning document. Re-verification owns `01-VERIFICATION.md`.
  </action>
  <verify>
    <automated>
      V=.planning/phases/01-restore-the-verification-signal/01-VALIDATION.md

      # The false claim is gone from the SIG-01h row (line-scoped, so the corrective
      # narrative mentioning vi.stubGlobal cannot self-invalidate this gate).
      grep 'SIG-01h' "$V" | grep -c '✅ exists'
      # EXPECT: 0

      # The row now names the real test and is marked as a Wave 0 item.
      grep 'SIG-01h' "$V" | grep -c 'forwards the parsed accessToken'   # EXPECT: 1
      grep 'SIG-01h' "$V" | grep -c '❌ Wave 0'                          # EXPECT: 1

      # Table shape survived: still 7 columns (awk -F'|' counts 9 with the empty ends).
      awk -F'|' '/^\| SIG-01h /{print NF}' "$V"
      # EXPECT: 9

      # The assertion map is still exactly 24 rows — no row added or dropped.
      grep -cE '^\| (\*\*)?(SIG-0[0-9][a-z]|CMP-inv|V2-inv)' "$V"
      # EXPECT: 24

      # The false-pass table gained exactly one SIG-01 row (4 -> 5).
      grep -cE '^\| SIG-01 \| ' "$V"
      # EXPECT: 5

      # Wave 0 bullet now covers the forwarding cases.
      grep -n 'settings.test.ts' "$V" | grep -c 'SIG-01h'
      # EXPECT: 1

      # Nothing outside the two planned files moved. IMPROVEMENT-PLAN.md is the
      # pre-existing untracked file from STATE.md:112 and is expected here.
      git status --porcelain
      # EXPECT: exactly two ' M' entries (settings.test.ts, 01-VALIDATION.md) plus
      # '?? IMPROVEMENT-PLAN.md'. No modification to settings.ts.
    </automated>
  </verify>
  <done>
    The SIG-01h row states the forwarding behaviour, names the `-t "forwards the parsed
    accessToken"` command, and is marked `❌ Wave 0` with the reason the old `✅ exists`
    claim was wrong. The map still holds 24 assertions with 7 intact columns. The false-pass
    table has 5 SIG-01 rows. The Wave 0 bullet lists six `settings.test.ts` cases.
    `git status --porcelain` shows only `settings.test.ts`, `01-VALIDATION.md`, and the
    pre-existing untracked `IMPROVEMENT-PLAN.md`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundary is introduced. This plan adds test code and corrects a planning
document; no production code, no network surface, and no new dependency reaches the
shipped bundle.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-Q1-01 | Tampering | `packages/frontend/src/stores/settings.ts` during the Task 1 falsifiability proof | mitigate | The proof deliberately breaks the token read. If the revert is skipped or partial, a permanently-empty `CAIDO_AUTHENTICATION` ships and `startMcpServer` aborts at `index.ts:1697-1702` — total MCP loss. Mitigated by a mandatory `git checkout --` followed by a `git diff --quiet` gate that must exit 0, a `git status --porcelain` check in Task 2, and by deliberately excluding `settings.ts` from `files_modified` so any diff on it is an out-of-contract change. |
| T-Q1-02 | Tampering | package-manager supply chain | accept (N/A) | No task runs `pnpm add` / `npm install` / any package install. Every identifier used (`vi`, `expect`, `it`, `useSettingsStore`, `mockSdk`) is already imported in `settings.test.ts`. No Package Legitimacy Audit is required because no package enters the tree. |
| T-Q1-03 | Information Disclosure | the `tok-123` literal in Test A | accept | Synthetic, non-secret, and structurally identical to the fake paths and IDs already committed in this file (`/usr/local/bin/claude`, `project-1`). It exercises the trim path and carries no real credential material. |
| T-Q1-04 | Repudiation | the mutation proof itself | mitigate | A proof asserted but not run is indistinguishable from no proof — which is the exact defect this plan closes. Mitigated by requiring the `git diff --stat` landing check (2b) before the failure assertion (2c), so a silently-unapplied mutation cannot be misread as a surviving test. The observed counts (2 failed / 11 passed) must be recorded in the SUMMARY. |

**Fail-closed invariant preserved (V2-inv):** none of the three new tests changes
`settings.ts`, so the `pushCaidoSessionToken("")` → `startMcpServer` abort path at
`index.ts:1696-1702` is untouched. Tests B and C assert that empty-token sink directly.
</threat_model>

<verification>
Run from the repo root on Node 26.7.0 (the version that reproduces the original bug):

1. `pnpm exec vitest run packages/frontend/src/stores/settings.test.ts` → 13 passed
2. `pnpm exec vitest run` → 23 files / 134 tests, 0 failed (baseline was 131)
3. `pnpm lint` → exit 0, 0 errors / 0 warnings
4. `git diff --quiet -- packages/frontend/src/stores/settings.ts` → exit 0 (no production change)
5. `git status --porcelain` → only `settings.test.ts`, `01-VALIDATION.md`, and the
   pre-existing untracked `IMPROVEMENT-PLAN.md`
6. The mutation proof from Task 1 step 2 was executed, its observed counts recorded, and
   the mutation reverted

Commit with targeted paths only — never `git add -A`, because `IMPROVEMENT-PLAN.md` sits
untracked at the repo root (`.planning/STATE.md:112`).
</verification>

<success_criteria>
- [ ] `settings.test.ts` gained exactly three tests, appended, with lines 1-331 unchanged
- [ ] Test A asserts `syncCaidoSessionToken` received `"tok-123"` (trimmed) and that
      `getItem` was called with `"CAIDO_AUTHENTICATION"`
- [ ] Test B asserts both the `""` push and the `showToast` warning
- [ ] Test C asserts `""` for a non-string `getItem` return
- [ ] Under the mutation `if (key !== "__never__") return undefined;` the file reports
      2 failed / 11 passed — the forwarding direction is now falsifiable
- [ ] The mutation was reverted; `settings.ts` is byte-identical to HEAD
- [ ] Full suite 23 files / 134 tests green; `pnpm lint` exit 0 at 0/0
- [ ] `01-VALIDATION.md` SIG-01h no longer says `✅ exists`, names the real test command,
      and is marked `❌ Wave 0`
- [ ] The assertion map still holds exactly 24 rows; the false-pass table gained one SIG-01 row
- [ ] No production file and no other planning document changed
</success_criteria>

<output>
Create `.planning/quick/260813-dc7-add-a-settings-test-ts-case-driving-a-pr/260813-dc7-SUMMARY.md` when done.

Record in the SUMMARY, as measured values rather than claims:
- the observed test counts at each step (file 10 → 13, suite 131 → 134)
- the exact mutated-run output (`N failed | M passed`) — this is the evidence that closes gap G1
- confirmation that `git diff --quiet -- packages/frontend/src/stores/settings.ts` exited 0
  after the revert
</output>
