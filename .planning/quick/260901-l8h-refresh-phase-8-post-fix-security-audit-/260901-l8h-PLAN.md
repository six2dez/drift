---
phase: quick-260901-l8h
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/08-process-lifecycle/08-SECURITY.md
  - .planning/phases/08-process-lifecycle/threat-register-gate.sh
  - .planning/phases/08-process-lifecycle/08-VERIFICATION-4.md
  - .planning/quick/260901-l8h-refresh-phase-8-post-fix-security-audit-/260901-l8h-VERIFICATION.md
  - .planning/STATE.md
autonomous: true
requirements: [LIF-01, LIF-02]
gap_closure: false
evidence_only: true
post_fix_audit: true

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The Phase 8 security audit is current at package commit 415ae7e4704cc23f30dcd9ff959672ed10622ad6, with the exact packages tree, SHA-256 census, and zero later package commits recomputed from Git."
    - "The audited 12a7136d..415ae7e production delta is classified as the SC-4 completion-order repair: recursive token-root removal is completion-barrier-authorized, while uncertain completion retains the root and blocks replacement startup."
    - "The canonical register remains exactly T-08-01 through T-08-94 plus T-08-SC; the repair updates existing lifecycle mitigations and does not manufacture T-08-95."
    - "AR-05 remains one of 13 current residual decisions, narrowed by a dated marked correction: 415ae7e closes recursive generation-root cleanup for ROADMAP SC-4, while closeCliSession, deleteChat, and the startup sweep still remove files after kill/reap issuance rather than observed completion."
    - "The standing threat gate, its self-test/self-scan, and the independent 32-case matrix pass after changing only the package-history census that the new package commit genuinely invalidated."
    - "08-VERIFICATION-4.md records human_needed at 17/20: SC-4 is verified, SC-1/SC-2/SC-3 remain behavior-unverified, and the only two remaining acquisitions are native Windows and real-Caido Codex causality."
    - "LIF-01/LIF-02 and the four protected tracked artifacts remain unchanged; a deterministic type/path/content manifest proves every pre-existing untracked regular file, symlink target, and derived directory shape is identical, while gsd-quick Step 7 changes only the Quick Tasks Completed table and narrative Last activity line in STATE.md."
  artifacts:
    - path: ".planning/phases/08-process-lifecycle/08-SECURITY.md"
      provides: "Post-fix Phase 8 threat audit, current package baseline, and narrowed current AR-05 boundary"
      contains: "415ae7e4704cc23f30dcd9ff959672ed10622ad6"
    - path: ".planning/phases/08-process-lifecycle/threat-register-gate.sh"
      provides: "Standing Git/citation gate calibrated to the 61-package-commit census"
      contains: "EXPECTED_FORMER_PACKAGE_COMMITS=61"
    - path: ".planning/phases/08-process-lifecycle/08-VERIFICATION-4.md"
      provides: "Additive current Phase 8 verdict after the cleanup completion repair and real-Caido confirmation"
      contains: "17/20"
    - path: ".planning/quick/260901-l8h-refresh-phase-8-post-fix-security-audit-/260901-l8h-VERIFICATION.md"
      provides: "Raw commands, outputs, hashes, package census, canonical untracked manifest, preservation checks, and audit classification"
      contains: "untracked_manifest_match: true"
    - path: ".planning/STATE.md"
      provides: "Orchestrator-owned quick-task completion row and Last activity metadata"
      contains: "260901-l8h"
  key_links:
    - from: "packages/backend/src/index.ts"
      to: ".planning/phases/08-process-lifecycle/08-SECURITY.md"
      via: "the generation-scoped cleanup barrier and fail-closed removal transition are audited against the exact committed delta"
      pattern: "pendingMcpRuntimeCleanup"
    - from: ".planning/phases/08-process-lifecycle/08-SECURITY.md"
      to: ".planning/phases/08-process-lifecycle/threat-register-gate.sh"
      via: "audited_at_head, packages tree, SHA-256 census, and package-commit count are recomputed rather than trusted"
      pattern: "check_audit_integrity"
    - from: ".planning/quick/260831-vep-create-a-faithful-completion-order-harne/completion-order.harness.ts"
      to: ".planning/phases/08-process-lifecycle/08-VERIFICATION-4.md"
      via: "three favorable production-wired observations plus the resolved real-Caido checkpoint close SC-4 without widening the claim"
      pattern: "requirementSatisfied"
    - from: ".planning/phases/08-process-lifecycle/08-VERIFICATION-4.md"
      to: ".planning/REQUIREMENTS.md"
      via: "the additive verdict explicitly preserves both unchecked lifecycle requirements and their two external evidence boundaries"
      pattern: "LIF-01|LIF-02"
    - from: ".planning/quick/260901-l8h-refresh-phase-8-post-fix-security-audit-/260901-l8h-VERIFICATION.md"
      to: ".planning/STATE.md"
      via: "gsd-quick Step 7 records the verified task status without changing Phase 8 position, progress, or requirement state"
      pattern: "Quick Tasks Completed"
    - from: "the pre-existing untracked working tree"
      to: ".planning/quick/260901-l8h-refresh-phase-8-post-fix-security-audit-/260901-l8h-VERIFICATION.md"
      via: "a NUL-safe lstat manifest hashes regular-file bytes and symlink targets and records directory shape before/after"
      pattern: "untracked_manifest_match"
---

<objective>
Refresh Phase 8's security and verification truth after the committed cleanup-completion repair.

Purpose: Make the current records agree with the package users would run: ROADMAP SC-4's
generation-root completion-order gap is closed at `415ae7e`, AR-05 remains explicit for the
unprotected removal paths, and the two genuinely external lifecycle acquisitions remain open
without being conflated with the repair.

Output: A current security baseline, a still-falsifiable standing gate, a raw quick-task evidence
record, additive `08-VERIFICATION-4.md`, and the required gsd-quick completion metadata in
`STATE.md`. No production code, requirement status, historical verifier, project contract, or
pre-existing untracked artifact is in mutable scope.
</objective>

<execution_context>
@/Users/six2dez/.codex/gsd-core/workflows/execute-plan.md
@/Users/six2dez/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/STATE.md
@.planning/debug/resolved/cleanup-completion-before-rm.md
@.planning/phases/08-process-lifecycle/08-VERIFICATION-3.md
@.planning/phases/08-process-lifecycle/08-SECURITY.md
@.planning/phases/08-process-lifecycle/threat-register-gate.sh
@.planning/phases/08-process-lifecycle/threat-register-gate.test.sh
@.planning/phases/08-process-lifecycle/verdict-gate.sh
@.planning/quick/260831-vep-create-a-faithful-completion-order-harne/260831-vep-VERIFICATION.md
@packages/backend/src/index.ts
@packages/backend/src/index.source.test.ts
@packages/backend/src/mcp-lifecycle.ts
@packages/backend/src/mcp-lifecycle.test.ts
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Rebase the executable security audit on the cleanup-completion fix</name>
  <files>
    .planning/phases/08-process-lifecycle/08-SECURITY.md
    .planning/phases/08-process-lifecycle/threat-register-gate.sh
  </files>
  <action>
    Audit the committed package delta `12a7136d358786b15d377c43ed3ba234cc0004b8..415ae7e4704cc23f30dcd9ff959672ed10622ad6`
    file by file. The measured census is one package commit, four backend source/test files, 443
    insertions, 39 deletions, and no dependency-manifest change. Confirm the production chain:
    provider exit, tree-killer settlement, and orphan-reap settlement feed the generation-scoped
    barrier; only its ready disposition reaches recursive removal; refusal, non-zero exit, error,
    staleness, or completion timeout retains the token-bearing root and prevents replacement start.
    Keep automation evidence and the user's real-Caido Stop/restart confirmation as separate claims.

    Refresh `08-SECURITY.md` at the exact package boundary. Set `audited_at_head` to the full
    `415ae7e` commit, `package_tree_at_audit` to
    `8ec7d8d8f47b9406f844c4223b4d04d9171ffaa5`, the package tree SHA-256 census to
    `d404134003d4a37b6424ac294a0e640e14d3f602da8c51a7ec60424f8b6f27bd`, the former-baseline
    package-commit census to 61, and later package commits to zero. Update the opening audit note,
    the current lifecycle mitigation text (especially T-08-12/T-08-13), the final package-delta
    audit, audit trail, sign-off, and any stale statement-order-only conclusion that is current
    prose rather than preserved superseded history.

    Preserve the canonical 94 numeric rows, T-08-SC, the 44-row T-08-51..T-08-94 ledger, all
    severities/dispositions unrelated to this repair, accepted-high T-08-47 / AR-06, and all 13
    current accepted residual decisions. The code audit closes an already allocated ROADMAP SC-4
    boundary and introduces no distinct threat, so do not allocate or cite a new numeric ID.

    Keep AR-05 current and preserve its original rationale. Add one dated marked correction whose
    boundaries are mechanically unmistakable. It must state that `415ae7e` protects only
    `cleanupMcpRuntimeGeneration`'s recursive generation-root removal: provider exit, tree-killer
    completion, and orphan-reap completion settle the callback barrier before that root is removed,
    which is the session-finalize / `stopMcpServer` behavior ROADMAP SC-4 scores. It must also state
    what remains accepted: `closeCliSession` and `deleteChat` still issue `killTree` and the idle reap
    before fire-and-forget removal of their per-session activity/approval files, and
    `sweepOrphanedMcpTempDirs` still issues the class-wide reap before recursively removing stale
    roots without observing scan/kill completion. Do not generalize the barrier to those paths, do
    not label AR-05 resolved, and do not reduce the accepted-residual count below 13. Update current
    T-08-12/T-08-13 and sign-off prose with the same exact boundary while leaving superseded history
    intact.

    In `threat-register-gate.sh`, change only the package-history census constant from 60 to 61.
    Leave `REGISTER_MAX=94`, `LEDGER_MIN=51`, `EXPECTED_LEDGER_COUNT=44`, citation sentinels,
    aggregate logic, confidentiality behavior, and the independent test script unchanged. The
    fixture derives this census from the production constant, so no fixture rewrite is justified.
  </action>
  <verify>
    <automated>
      test "$(git rev-list --count d2d502b..415ae7e -- packages/)" -eq 61
      test "$(git log -1 --format=%H 415ae7e -- packages/)" = 415ae7e4704cc23f30dcd9ff959672ed10622ad6
      test "$(git rev-parse 415ae7e:packages)" = 8ec7d8d8f47b9406f844c4223b4d04d9171ffaa5
      test "$(git ls-tree -r 415ae7e -- packages | shasum -a 256 | awk '{print $1}')" = d404134003d4a37b6424ac294a0e640e14d3f602da8c51a7ec60424f8b6f27bd
      bash .planning/phases/08-process-lifecycle/threat-register-gate.sh
      bash .planning/phases/08-process-lifecycle/threat-register-gate.sh --self-test
      bash .planning/phases/08-process-lifecycle/threat-register-gate.sh --self-scan
      bash .planning/phases/08-process-lifecycle/threat-register-gate.test.sh
    </automated>
  </verify>
  <done>
    The security artifact is current at the latest package-changing commit, narrows AR-05 without
    closing or erasing it, retains 13 current residual decisions and the exact 94+SC register, and
    every production/self/independent threat gate is green with the 61-commit census.
  </done>
</task>

<task type="auto">
  <name>Task 2: Publish the additive 17/20 verifier from fresh post-fix evidence</name>
  <files>
    .planning/phases/08-process-lifecycle/08-VERIFICATION-4.md
    .planning/quick/260901-l8h-refresh-phase-8-post-fix-security-audit-/260901-l8h-VERIFICATION.md
    .planning/STATE.md
  </files>
  <action>
    Before writing, capture Git blob hashes for the four protected tracked inputs. Their
    planning-time hashes are: PROJECT.md
    `3375850f4940dad4914e30ed32860f590286d329`, historical 08-VERIFICATION.md
    `2b9c8991f94934a0fded1dc1134678175135b44c`, 08-VERIFICATION-2.md
    `dc3a9f9a341829c9357a0d05aeb6422a4342c407`, and 08-VERIFICATION-3.md
    `1f69cac956256a0809711ba64c7ead0f4fcaa040`. If any differs before execution, stop rather than
    treating a concurrent user edit as this task's input. Recompute them after writing and record
    equality in the quick verification artifact.

    A path-only untracked census is insufficient. Before creating any output, generate a canonical
    manifest from a fresh NUL-delimited `git status --porcelain=v1 -z --untracked-files=all` observation.
    Exclude only the intended new outputs under
    `.planning/quick/260901-l8h-refresh-phase-8-post-fix-security-audit-/` and the intended new
    `.planning/phases/08-process-lifecycle/08-VERIFICATION-4.md`. For every remaining `??` entry,
    use `lstat` without following symlinks and record a JSON object containing the JSON-escaped
    repository-relative path, entry type, and numeric mode. For a regular file add the SHA-256 of
    its bytes; for a symlink add the SHA-256 of the link-target string without recording that target;
    for each ancestor directory add a directory entry so path/directory shape is represented.
    Refuse unsupported filesystem types. Sort objects by the UTF-8 bytes of the relative path and
    serialize with stable key order. This may be implemented by one temporary Node program under a
    `mktemp -d` root; it must never print file bytes or link targets.

    Embed the sanitized pre-run manifest (paths, types, modes, hashes only) in
    `260901-l8h-VERIFICATION.md`. After all intended outputs exist, regenerate it with the identical
    program and exclusions, require byte-for-byte equality with `cmp`, and record both manifest
    SHA-256 values, entry counts by type, and `untracked_manifest_match: true`. Keep temporary copies
    until comparison succeeds, then remove only that explicit `mktemp` root. Use targeted staging
    only; never stage the repository wholesale, and do not push.

    Re-run the dedicated completion-order harness three times and preserve each JSON observation.
    Re-run the focused lifecycle suites, the full suite, typecheck, lint, build/ZIP, both verdict-gate
    modes, and all threat-gate modes. Record exact commands, exit codes, observed counts, package
    commit/tree/digest values, production delta statistics, UAT provenance from the resolved debug
    session, and file hashes in `260901-l8h-VERIFICATION.md`. Do not replace live-runtime evidence
    with the harness: the oracle proves the completion predicate under a controlled adverse schedule;
    the user confirmation proves the real Caido Stop/restart workflow no longer reproduces the defect.

    Create additive `08-VERIFICATION-4.md`; do not edit any earlier verifier. It supersedes the
    current conclusions of `08-VERIFICATION-3.md` and records `human_needed`, score 17/20,
    `behavior_unverified: 3`, no actionable gaps, package head `415ae7e`, and the refreshed secured
    audit. Promote SC-4 from failed to verified using the unchanged favorable assertion, three stable
    harness runs, focused barrier tests, revert/reconfirm evidence in the resolved debug record, and
    the user's real-Caido confirmation. Update SC-5 to the fresh observed regression count (currently
    805 passed and 8 native-Windows skips), and update the security-current truth to the new package
    boundary without changing the other 16 verified truths. State why this score and AR-05 coexist:
    ROADMAP SC-4 is specifically the session-finalize / `stopMcpServer` recursive generation-root
    path now protected by the barrier, while AR-05 remains a narrowed current residual for
    `closeCliSession`, `deleteChat`, and the startup sweep removal paths described in the security
    artifact. Do not imply that the favorable harness exercised those residual paths.

    Keep SC-1, SC-2, and SC-3 explicitly `PRESENT_BEHAVIOR_UNVERIFIED`. Retain exactly two external
    acquisitions: the native two-case Windows run, and cancel plus absolute-timeout causality on a
    Drift-spawned Codex turn in real Caido. Keep LIF-01 and LIF-02 unchecked and explain that SC-4's
    repair supplies neither native Windows behavior nor provider-independent POSIX causality. Do not
    update PROJECT.md, REQUIREMENTS.md, ROADMAP.md, review files, or any historical verification
    artifact.

    Reserve `.planning/STATE.md` for the gsd-quick Step 7 orchestrator after executor and verifier
    completion; the executor must not edit it early. Step 7 may make only the workflow-owned metadata
    changes: upgrade the existing Quick Tasks Completed header/separator to include `Status`, leave
    the predecessor row's new Status cell empty, append exactly one `260901-l8h` row with the actual
    executor commit, verification status, and directory, and replace the narrative `Last activity:` line under Current
    Position with the completion entry dated 2026-09-01. Do not change STATE frontmatter, Current
    Position phase/plan/status, progress, decisions, blockers, or any Phase 8/requirement status.
    After the orchestrator edit, require `state.load` to parse and review the zero-context diff so
    every changed STATE line belongs to the table schema/row or the one Last activity line.
  </action>
  <verify>
    <automated>
      for run in 1 2 3; do pnpm exec vitest run --config .planning/quick/260831-vep-create-a-faithful-completion-order-harne/vitest.harness.config.ts --reporter=verbose || exit 1; done
      pnpm exec vitest run packages/backend/src/index.source.test.ts packages/backend/src/mcp-lifecycle.test.ts packages/backend/src/kill-plan.test.ts
      pnpm exec vitest run
      pnpm typecheck
      pnpm lint
      pnpm build
      test -f dist/plugin_package.zip
      bash .planning/phases/08-process-lifecycle/verdict-gate.sh
      bash .planning/phases/08-process-lifecycle/verdict-gate.sh --self-test
      bash .planning/phases/08-process-lifecycle/threat-register-gate.sh
      bash .planning/phases/08-process-lifecycle/threat-register-gate.sh --self-test
      bash .planning/phases/08-process-lifecycle/threat-register-gate.sh --self-scan
      bash .planning/phases/08-process-lifecycle/threat-register-gate.test.sh
      test "$(rg -c '^- \[ \] \*\*LIF-0[12]\*\*:' .planning/REQUIREMENTS.md)" -eq 2
      test "$(git hash-object .planning/PROJECT.md)" = 3375850f4940dad4914e30ed32860f590286d329
      test "$(git hash-object .planning/phases/08-process-lifecycle/08-VERIFICATION.md)" = 2b9c8991f94934a0fded1dc1134678175135b44c
      test "$(git hash-object .planning/phases/08-process-lifecycle/08-VERIFICATION-2.md)" = dc3a9f9a341829c9357a0d05aeb6422a4342c407
      test "$(git hash-object .planning/phases/08-process-lifecycle/08-VERIFICATION-3.md)" = 1f69cac956256a0809711ba64c7ead0f4fcaa040
      test "$(rg -c '^untracked_manifest_match: true$' .planning/quick/260901-l8h-refresh-phase-8-post-fix-security-audit-/260901-l8h-VERIFICATION.md)" -eq 1
      node /Users/six2dez/.codex/gsd-core/bin/gsd-tools.cjs query state.load >/dev/null
      git diff --check -- .planning/phases/08-process-lifecycle/08-SECURITY.md .planning/phases/08-process-lifecycle/threat-register-gate.sh .planning/phases/08-process-lifecycle/08-VERIFICATION-4.md .planning/quick/260901-l8h-refresh-phase-8-post-fix-security-audit-/260901-l8h-VERIFICATION.md .planning/STATE.md
    </automated>
  </verify>
  <done>
    The new verifier is additive, current, and evidence-bounded at 17/20; SC-4 is closed without
    closing LIF-01/LIF-02 or the remaining three runtime truths, all release/security gates pass, and
    protected tracked inputs and the content-bearing untracked manifest are preserved exactly; Step
    7 changes only its one STATE row/table schema and Last activity metadata.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed package tree → security record | Git-derived package identity and code semantics become the basis for a secured claim. |
| controlled harness / user UAT → phase verdict | Automated causal scheduling and real-runtime workflow evidence support different claim widths. |
| executor staging → dirty user-owned planning tree | Targeted audit artifacts coexist with modified and untracked files outside task ownership; preservation requires content/type evidence, not names alone. |
| gsd-quick Step 7 → project state | The orchestrator records quick-task completion without changing Phase 8 progress or requirement truth. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-Q-L8H-01 | Repudiation | package audit baseline | high | mitigate | Recompute latest package commit, tree, SHA-256 census, and post-audit count from Git; require live/self/matrix gates. |
| T-Q-L8H-02 | Tampering | historical and untracked user inputs | high | mitigate | Pin protected blob hashes; compare a canonical regular-file/symlink/directory manifest; stage paths individually. |
| T-Q-L8H-03 | Repudiation | SC-4 and AR-05 verdict width | high | mitigate | Verify only ROADMAP SC-4's generation-root path; keep AR-05 current and explicitly enumerate the unprotected per-session/startup removals. |
| T-Q-L8H-04 | Elevation of Privilege | LIF requirement closure | high | mitigate | Require both requirement checkboxes to remain unchecked and preserve the two external acquisitions in the additive verifier. |
| T-Q-L8H-05 | Tampering | STATE.md workflow metadata | medium | mitigate | Limit Step 7 to the table schema/one task row and narrative Last activity; parse state and inspect a zero-context diff. |
| T-Q-L8H-SC | Tampering | package-manager supply chain | low | accept | This audit installs no dependency and must leave all dependency manifests unchanged. |
</threat_model>

<verification>
Completion requires agreement between four independent surfaces: the exact Git package census, the
production-wired completion-order oracle, the standing security/verdict gates and red matrices, and
the additive verifier's requirement boundary. Green static/unit gates cannot substitute for the two
remaining native/runtime acquisitions.
</verification>

<success_criteria>
- `08-SECURITY.md` is secured at `415ae7e`, tree `8ec7d8d8...`, digest `d4041340...`, 61 former-baseline package commits, and zero later package commits.
- AR-05 remains current but is narrowed by a dated marked correction: the generation-root path is protected, while close/delete/startup removals retain the completion-order residual; all 13 current residual decisions remain.
- Threat live/self/self-scan/independent gates and verdict live/self-test all pass.
- The completion harness passes three times with all completion flags and `requirementSatisfied=true`.
- Full Vitest, focused lifecycle tests, typecheck, lint, and build/ZIP pass with exact observed results recorded.
- `08-VERIFICATION-4.md` is current at 17/20 and lists only Windows execution and real-Caido Codex causality as remaining acquisitions.
- LIF-01/LIF-02 and protected historical files remain unchanged; the canonical untracked type/content manifest compares byte-for-byte before/after.
- STATE.md changes only through gsd-quick Step 7: Status table schema, one `260901-l8h` row, and one Last activity line; Phase 8 progress/status and requirements remain untouched.
- Nothing is pushed.
</success_criteria>

## Multi-Source Coverage Audit

| SOURCE | ID | Feature / Requirement | Task | Status | Notes |
|--------|----|-----------------------|------|--------|-------|
| GOAL | — | Refresh the post-fix Phase 8 security audit and additive verifier | 1, 2 | COVERED | Exact package/security chain plus current phase verdict. |
| REQ | LIF-01 | Native Windows cancel/timeout tree termination | 2 | COVERED | Preserved open; exact native acquisition retained. |
| REQ | LIF-02 | POSIX provider/MCP causal termination | 2 | COVERED | Preserved open; real-Caido Codex causal acquisition retained. |
| RESEARCH | — | Caido child callbacks cannot be awaited safely inside the initiating RPC | 1 | COVERED | Audit requires the callback-driven barrier and non-starving Stop boundary. |
| CONTEXT | — | Audit only production delta 12a7136d..415ae7e | 1 | COVERED | Commit, files, insertions/deletions, tree, and digest are explicit. |
| CONTEXT | — | Keep exact register range unless a distinct threat exists | 1 | COVERED | Existing lifecycle threats absorb the repair; no new ID is allocated. |
| CONTEXT | — | Mark AR-05's current boundary honestly | 1 | COVERED | Dated marked narrowing keeps all 13 current decisions and enumerates the remaining removal paths. |
| CONTEXT | — | Apply gsd-quick Step 7 metadata | 2 | COVERED | Only table schema/row and narrative Last activity are authorized in STATE.md. |
| CONTEXT | — | Preserve historical verifiers, requirements, dirty inputs, and untracked files; no push | 2 | COVERED | Blob pins plus a canonical type/content manifest and targeted staging enforce the boundary. |

<output>
After execution, create `260901-l8h-SUMMARY.md` beside this plan. The quick verification artifact
is authored by Task 2. Let gsd-quick Step 7 perform only its scoped STATE.md completion metadata;
do not claim Phase 8/LIF-01/LIF-02 complete.
</output>
