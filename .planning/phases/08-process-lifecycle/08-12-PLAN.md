---
phase: 08-process-lifecycle
plan: 12
type: execute
wave: 7
gap_closure: true
depends_on: ["08-11"]
files_modified:
  - packages/backend/src/kill-plan.ts
  - packages/backend/src/kill-plan.test.ts
  - packages/backend/src/kill-tree.posix.test.ts
  - packages/backend/src/index.ts
autonomous: true
requirements: [LIF-01, LIF-02]

estimate:
  tokens: 78000
  raw_tokens: 78000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`classifyLivenessObservation` returns exactly one of `alive`, `dead`, `inconclusive`, and returns `inconclusive` — never `dead` — for every input shape that is not a confident observation: the enumerator failed to spawn, timed out, exited with an unrecognised or absent code, produced no matching row while exiting successfully, or produced a row whose pid does not equal the requested pid."
    - "A pid whose decimal representation is a prefix, suffix or substring of the requested pid is NOT treated as a match: `classifyLivenessObservation` compares parsed integers, so a row for pid 4428 answers nothing about pid 44284."
    - "`classifyLivenessObservation` locates the target row by pid equality anywhere in the enumerator's stdout, not by line index: a leading blank line, a warning line, or the target row appearing last yields the same verdict as the target row appearing first."
    - "`classifyLivenessObservation` returns `inconclusive` for empty stdout, for a single blank line, and for absent/null exit code, and `dead` only for the two shapes that positively establish absence: a non-matching successful enumeration, or a matching row whose process state marks it a zombie."
    - "`buildLivenessProbePlan` refuses a pid that is undefined, zero, negative or non-integer with a stated refusal reason, and refuses on win32 with `unsupported-platform`, mirroring `buildOrphanKillPlan`'s refusal ladder."
    - "`buildLivenessProbePlan` emits a POSITIVE single-pid operand and no process-group reference anywhere in its argv."
    - "`formatSpikeVerdict` renders the `inconclusive` verdict as a string containing neither the word used for the favourable outcome nor the word used for the unfavourable one, so a reader cannot mistake a non-answer for either verdict."
    - "`packages/backend/src/kill-plan.ts`, `packages/backend/src/index.ts` and `packages/backend/src/kill-tree.posix.test.ts` each state A1's verdict as withdrawn, with the 2026-08-27 reading preserved as a block-quoted supersession and the 2026-08-27 A6 verdict unchanged."
    - "`kill-plan.ts` still declares exactly one import, `./platform` — the module's self-enforcing claim, unchanged by the new helpers."
    - "The full vitest suite count does not decrease from its 695-test / 686-passing / 9-skipped baseline; every test added by this plan executes on this host."
    - "The Stop/Send pair in `ChatInput.vue` keeps exactly one `Stop` label and one `Send` label swapped by `v-if`/`v-else`, with no spinner, progress bar, toast, `loading` binding or `:disabled` binding added on the cancel path (UI-SPEC E1 loading, R-01/R-07)."
    - "A failed cancel does not alter the Stop/Send button pair; the failure surfaces on the `CliStatus` reason line and never as a button-level affordance (UI-SPEC E1 error, R-04/R-05)."
    - "The Stop and Send labels remain fixed string literals rather than bound expressions, so neither can truncate or reflow (UI-SPEC E1 long-text)."
    - "`CliStatus` renders a yellow dot with `Streaming...` while streaming and a green dot with `Ready` otherwise, and both cancel and timeout return it to `Ready` because both clear `isStreaming` (UI-SPEC E2 loading, R-06)."
    - "The `CliStatus` error branch renders the backend `reason` verbatim on its small text line for the two strings this phase produces — the timeout string and the user-cancellation string (UI-SPEC E2 error, R-04/R-05)."
    - "Exactly one `[Cancelled]` message is appended to the chat transcript after a cancel (UI-SPEC E3 populated, R-02)."
    - "A partially streamed turn keeps its streamed prefix on cancel; the prefix is not discarded (UI-SPEC E3 partial, R-02)."
    - "Zero extra messages for a cancelled session, one message for the next turn on that same session, and a non-cancelled session still surfaces its error (UI-SPEC E3 zero-one-many, R-03)."
    - "No skeleton and no placeholder is introduced for an in-flight turn; the streamed prefix is the in-flight presentation (UI-SPEC E3 loading, R-02)."
    - "A cancelled session suppresses its late error while a non-cancelled session still surfaces one (UI-SPEC E3 error, R-03)."
    - "A cancel against an empty transcript still appends exactly one `[Cancelled]` message; no separate empty state exists or is added (UI-SPEC E3 empty)."
    - "MCP failure continues to surface through the Settings status label and the health-check card; this phase changes neither (UI-SPEC E4 error)."
    - "The App MCP badge keeps its closed set of five fixed labels, with no separate loading presentation and no error-specific presentation added (UI-SPEC E5 loading and E5 error)."
    - statement: "An unusually long backend session `reason` clips without breaking the CliStatus strip's single-line layout or shifting the chat transcript"
      verification: backstop
    - statement: "The Settings MCP Start/Stop button's in-flight state during an MCP start or stop does not regress; no pending state is added"
      verification: backstop
  artifacts:
    - "packages/backend/src/kill-plan.ts — `classifyLivenessObservation`, `buildLivenessProbePlan`, `formatSpikeVerdict`, types `LivenessVerdict` and `LivenessObservation`; A1 LIVE VERDICTS block corrected"
    - "packages/backend/src/kill-plan.test.ts — the three-valued liveness contract, including a totality case over the whole input domain"
    - "packages/backend/src/index.ts — A1 marked correction at the single-pid rung and at the getDiagnostics breadcrumb"
    - "packages/backend/src/kill-tree.posix.test.ts — A1 marked correction in the vehicle-caveat header"
  key_links:
    - "`classifyLivenessObservation` → the three-valued verdict the fixed A1 probe consumes (plan 08-15's patch file)"
    - "`buildLivenessProbePlan` → a spawned enumerator, replacing the absent `process.kill` primitive as the liveness source"
    - "`formatSpikeVerdict` → the diagnostics string a maintainer reads on real hardware (plan 08-17)"
    - "`kill-plan.ts` LIVE VERDICTS block ↔ `08-SPIKE.md` — the file says to keep the two consistent"
  prohibitions:
    - statement: "No gate-output check is written as a bare `verdict-gate.sh | grep -c <file> | grep -qx 0`. That shape is satisfied by an EMPTY match set: a gate that syntax-errors, aborts early under `set -u`, or whose ERE matches nothing prints nothing, the count is 0, and the check goes green — the vacuous-gate defect this whole round exists to eliminate, in the plans that fix it. Every gate-output check in this plan captures the gate output ONCE and asserts, against that SAME capture, both LIVENESS (exactly three `== ARM ` header lines, which the gate prints unconditionally because `fail()` sets a flag rather than exiting) AND the target-file absence. Do not copy the weak form back in."
    - statement: "No `cannot tell` answer is coalesced into either verdict, anywhere on the liveness path — not in the classifier, not in the formatter, not in the diagnostics string, not in a document cell. An unavailable primitive or an unspawnable enumerator produces `inconclusive` and nothing else."
    - statement: "No existing test is removed, skipped, relaxed or converted to a weaker assertion. The suite count does not go down."
    - statement: "No file under `packages/frontend` or `packages/shared` is modified. The cancel and timeout surfaces visible to the user are unchanged by construction, not by inspection."
    - statement: "A6's FALSIFIED verdict is not reverted, softened or bundled into the A1 retraction in any of the three source files this plan edits."
---

<objective>
Fix the defect itself, and correct the three source files that carry its output.

Gap 1's third `missing` item: *"with `process.kill` absent, liveness must be determined by a SPAWN
(`kill -0 <pid>` or `pgrep`) and a THREE-valued result — alive / dead / cannot-tell — with
cannot-tell reported as `inconclusive`, never folded into either verdict. A `?? false` on an
unavailable primitive is the bug."*

The probe is not in HEAD and must not return there. But the *decision logic* it got wrong belongs in
a pure helper where a test can reach it — the same argument that put the pid refusal in
`buildKillTreePlan` rather than at its call site, and the same argument `classifyOrphanScanOutcome`
already makes for itself. This plan ships that helper, unit-proven with a stated red input in both
directions, so plan 08-15's probe patch composes a tested classifier instead of re-deriving one.

Then it lands the A1 marked correction in the three source carriers `verdict-gate.sh` will still be
naming after plan 08-11: `kill-plan.ts`, `index.ts` and `kill-tree.posix.test.ts`.

Purpose: gap 1 `missing` items 2 (source carriers) and 3 (the probe fix).
Output: a tested three-valued liveness determination, and three source files that state A1's actual
verdict.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md

@.planning/phases/08-process-lifecycle/08-VERIFICATION.md
@.planning/phases/08-process-lifecycle/08-PATTERNS.md
@packages/backend/src/kill-plan.ts
@packages/backend/src/kill-plan.test.ts
</context>

## Artifacts this phase produces

Symbols, files and identifiers created by **this plan**:

- `packages/backend/src/kill-plan.ts`: `LivenessVerdict` (type), `LivenessObservation` (type),
  `LivenessProbeRefusal` (type, if a distinct refusal union is warranted),
  `classifyLivenessObservation` (function), `buildLivenessProbePlan` (function),
  `formatSpikeVerdict` (function).
- `packages/backend/src/kill-plan.test.ts`: new `describe` blocks for the three helpers, including a
  totality case over the classifier's whole input domain.
- No new files. No new CLI flags. No frontmatter, config or schema fields.

Symbols created by **sibling plans in this gap-closure set**: `STALE_VERDICT_ERE`, `READING_ERE`,
`RETRACTION_TOKEN`, `SUMMARY_KNOWN_UNPINNED`, `requires_retraction` (08-11, `verdict-gate.sh`);
`lastOrphanReap`, `recordOrphanReapOutcome` (08-15, `index.ts`);
`.planning/phases/08-process-lifecycle/a1-probe-fix.patch` (08-15, new file).

<flagged_assumptions>
Surfaced rather than dropped, per the spec-less probe fallback. This phase has no SPEC.md, so the
edge probe ran against the requirement text directly and produced four rows; three are authored into
`must_haves.truths` above and one is unresolved.

1. **LIF-01 / category `unclassified` / status `unresolved`.** The edge probe could not classify
   LIF-01 and an unclassified row is never auto-resolved. Planner assumption: LIF-01 is the Windows
   half of this phase, its only open item is the terminal verb "terminates", and `08-VERIFICATION.md`
   defers that to Phase 9 SC-4 explicitly. No edge predicate is authored for LIF-01 in this
   gap-closure round, and none is invented. If Phase 9 closes SC-4 without producing one, LIF-01's
   edge behaviour remains unspecified and that is a known hole, not an oversight.

2. **UI-SPEC E2 / `overflow` / status `unresolved`.** The `CliStatus` reason line is `truncate` with
   no tooltip and no `title` attribute, so a long backend `reason` is silently cut with no way to
   read the rest. Pre-existing, not introduced by this phase, and out of scope: both strings this
   phase produces fit. Surfaced here so it is a known assumption rather than a later discovery.
   Fixing it would require touching `packages/frontend`, which this plan's prohibitions forbid.
</flagged_assumptions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: A three-valued liveness determination, in a pure helper a test can reach</name>

  <read_first>
    - `packages/backend/src/kill-plan.ts` — `buildOrphanKillPlan` (the refusal ladder and the
      positive-pid commentary to mirror), `classifyOrphanScanOutcome` and `OrphanScanNoopReason`
      (the discriminated-union classifier shape to mirror), `isUnusablePid`, `END_OF_OPTIONS`, and
      the module header's single-import claim.
    - `packages/backend/src/kill-plan.test.ts` — the existing case style: one behaviour per case,
      value-level assertions, red input stated per case.
    - `.planning/phases/08-process-lifecycle/08-VERIFICATION.md` — gap 1's `reason`, which contains
      the exact defective expression and the reading that made it vacuous.
    - `packages/backend/src/platform.ts` — `Platform`, and the injected-platform-never-read rule.
    - `CLAUDE.md` § *The Result Pattern*, § *Pure Helpers Split for Testability*, § *Backend
      Constraints (QuickJS Runtime)*.
  </read_first>

  <files>packages/backend/src/kill-plan.ts, packages/backend/src/kill-plan.test.ts</files>

  <behavior>
    Write these cases FIRST, watch them fail against an absent implementation, then implement.

    `classifyLivenessObservation({ spawnThrew, exitCode, timedOut, stdout, pid })`:
    - spawn threw → `inconclusive`, reason names the unavailable enumerator. THE RED INPUT: an
      implementation that coalesces the unavailable case to "not alive" returns `dead` here.
    - timed out → `inconclusive`, reason names the timeout.
    - exit code `undefined` → `inconclusive`. exit code `null` → `inconclusive`.
    - exit code 0 and a row whose leading integer equals `pid`, state field not starting with `Z`
      → `alive`.
    - exit code 0 and a row whose leading integer equals `pid`, state field starting with `Z`
      → `dead`, reason names the zombie state.
    - exit code 1 and empty stdout → `dead`, reason names the successful non-match.
    - exit code 0 and empty stdout → `inconclusive`. An enumerator that reports success and prints
      nothing is contradicting itself; that is not evidence of absence.
    - exit code 0 and stdout carrying only a blank line → `inconclusive`.
    - ADJACENCY: `pid` 44284, stdout row for 4428 → `inconclusive`, not `alive` and not `dead`. Same
      for a row for 442840. THE RED INPUT: a substring or `includes()` comparison returns `alive`.
    - ORDERING: `pid` 44284, stdout whose first line is blank and whose second line is the matching
      row → the same verdict as when the matching row is first. Same for a stdout whose matching row
      is last after two noise lines. THE RED INPUT: an implementation reading only
      `stdout.split("\n")[0]` returns `inconclusive` for these.
    - EMPTY / SINGLE-ELEMENT: empty stdout, a single newline, a single space — all `inconclusive`
      unless the exit code positively establishes the non-match arm above.
    - TOTALITY: iterate the cartesian product of `spawnThrew ∈ {true,false}`,
      `timedOut ∈ {true,false}`, `exitCode ∈ {0,1,2,null,undefined}` and a small fixed set of stdout
      shapes, and assert that the returned verdict is `alive` or `dead` ONLY for the input shapes
      enumerated above and `inconclusive` for every other combination. THE RED INPUT: any
      implementation that reaches a definite verdict through a default or a coalescing operator
      fails this case for at least one combination.

    `buildLivenessProbePlan({ pid, platform })`:
    - `pid` undefined / 0 / -1 / 1.5 / NaN → refusal with the shared `no-pid` reason.
    - `platform` `win32` → refusal with `unsupported-platform`.
    - `platform` `darwin` and `pid` 44284 → a spawn plan whose argv contains the decimal pid as a
      positive operand and contains no `-` immediately preceding the pid. THE RED INPUT: a negative
      operand, or any process-group reference, fails.
    - `windowsVerbatimArguments` is `false`.

    `formatSpikeVerdict(verdict)`:
    - `alive` → the unfavourable string, which must contain the negation the 2026-08-27 reading used.
    - `dead` → the favourable string.
    - `inconclusive` → a string that contains neither the favourable word nor the unfavourable word,
      and that names the reason. THE RED INPUT: a formatter whose inconclusive branch falls through
      to either verdict's string fails the assertion that the three outputs are pairwise distinct
      and that the inconclusive one shares no verdict word with the other two.
  </behavior>

  <action>
    Add the three helpers to `packages/backend/src/kill-plan.ts`, at the foot of the file alongside
    the orphan-reap helpers, under an ASCII box header naming what they are for. Keep the module's
    single import (`./platform`) — add no second import, and say in the header that the constraint is
    unchanged.

    `LivenessVerdict` is the three-member union `alive | dead | inconclusive`.
    `LivenessObservation` is a discriminated result carrying `verdict` plus a `reason` string drawn
    from a closed union, in the same shape `OrphanScanOutcome` uses, so the reason vocabulary is
    enumerable rather than free prose.

    `classifyLivenessObservation` takes `{ spawnThrew, exitCode, timedOut, stdout, pid }` and returns
    a `LivenessObservation`. Order the arms the way `classifyOrphanScanOutcome` orders its own —
    unspawnable first, timeout second, then the arms that can reach a definite answer — because in
    this module branch order is the contract. Parse the enumerator's output by scanning every line
    for a leading decimal integer and comparing it to `pid` as a NUMBER, never as a substring; take
    the second whitespace-delimited field as the process state and treat a leading `Z` as not alive.
    A line that does not begin with a decimal integer is noise and is skipped, not treated as a
    negative answer.

    Write the reason for the design at the site, in the file's house voice, and make it the finding
    rather than a style note: the expression this replaces determined liveness with an optional call
    on a primitive that the same diagnostics run measured absent, then coalesced the absent case to
    "not alive". On that runtime the favourable string was emitted unconditionally and the red input
    did not exist. Name the rule the phase earned — an assertion whose red input requires hardware
    must state what a "cannot tell" answer looks like and must never coalesce it into either verdict.

    `buildLivenessProbePlan` returns the existing `KillTreePlan` union so it inherits the refusal
    vocabulary and every consumer keeps its shape. Refuse first on pid using the module's existing
    `isUnusablePid`, then on `win32`. The enumerator is the process-table lookup for a single pid,
    printing the pid and the process state with no header, spawned by bare name. State at the site
    that the bare name is a deliberate, bounded acceptance: this plan builder is consumed ONLY by an
    uncommitted local diagnostic build (plan 08-15's patch), never by a shipping path, and a hijacked
    binary on that path produces `inconclusive` rather than a wrong verdict because the classifier
    requires a pid-matching row and does not trust a bare exit code. Cross-reference threat T-08-55.

    `formatSpikeVerdict` maps a `LivenessObservation` to the exact string the probe writes into its
    diagnostics field. Keep the two existing verdict strings byte-identical to the ones the
    2026-08-27 run produced, so the new reading is comparable to the old one; add a third for
    `inconclusive` that names the reason and shares no verdict word with either.
    <!-- planner-discipline-allow: detached honoured -->

    Add the test cases from `<behavior>` to `packages/backend/src/kill-plan.test.ts` in that order,
    each with its red input stated in a comment at the case, in the style the file already uses.
  </action>

  <verify>
    <automated>cd /Users/six2dez/Tools/drift &amp;&amp; npx vitest run packages/backend/src/kill-plan.test.ts</automated>
    <automated>cd /Users/six2dez/Tools/drift &amp;&amp; pnpm -r typecheck</automated>
    <automated>cd /Users/six2dez/Tools/drift &amp;&amp; pnpm lint</automated>
  </verify>

  <acceptance_criteria>
    - `npx vitest run packages/backend/src/kill-plan.test.ts` passes with a strictly higher case count
      than at the plan's start; record both counts.
    - RED INPUT, DEMONSTRATED (the original defect): temporarily replace the classifier body with an
      implementation that coalesces the unspawnable case to `dead`; the spawn-threw case and the
      totality case both fail. Restore. Paste both outputs.
    - RED INPUT, DEMONSTRATED (adjacency): temporarily replace the pid comparison with a substring
      containment check; the 4428-vs-44284 case fails. Restore. Paste both outputs.
    - RED INPUT, DEMONSTRATED (ordering): temporarily replace the line scan with a read of the first
      line only; the leading-blank-line case fails. Restore. Paste both outputs.
    - RED INPUT, DEMONSTRATED (formatter's inconclusive branch): temporarily make
      `formatSpikeVerdict`'s `inconclusive` branch fall through to the favourable string; the
      pairwise-distinctness case and the shares-no-verdict-word case both fail. Restore. Paste both
      outputs. This is the fourth demonstration the `<verification>` section counts, and it is the
      one that matters most: the whole defect being corrected is an inconclusive answer wearing a
      verdict's clothes.
    - `classifyLivenessObservation` returns `inconclusive` for every combination in the totality case
      that is not one of the enumerated confident shapes — assert the count of `alive` plus `dead`
      results across the product and state it in the SUMMARY.
    - `buildLivenessProbePlan`'s argv, printed for pid 44284 on darwin, contains the string `44284`
      and contains no argument that is the pid with a leading minus sign.
    - Comment-stripped `grep -c` for the module's import statements in `kill-plan.ts` still shows
      exactly one import, and it is `./platform`.
    - `pnpm -r typecheck` and `pnpm lint` both exit 0.
    - The full `npx vitest run` count is greater than or equal to the 695/686/9 baseline in every
      column; state the new numbers.
  </acceptance_criteria>

  <done>
    The decision that produced gap 1 lives in a pure function whose "cannot tell" answer is a first
    class result, is proven by an executed totality case, and cannot be reached by a coalescing
    operator.
  </done>
</task>

<task type="auto">
  <name>Task 2: Correct the A1 verdict in kill-plan.ts's LIVE VERDICTS block</name>

  <read_first>
    - `packages/backend/src/kill-plan.ts` lines 55-135 — the whole marked-correction block, including
      the preserved 2026-08-24 quote above it and the four-reading paragraph below it.
    - `.planning/phases/08-process-lifecycle/08-VERIFICATION.md` — gap 1's first `artifacts` entry,
      which names the exact contradiction: the block states A1 closed favourably and, forty lines
      later, states that the kill primitive was absent in the same reading, and does not reconcile
      the two.
    - `.planning/phases/08-process-lifecycle/08-SPIKE.md` after plan 08-11 — the corrected verdict
      and prose this block must stay consistent with; the file says so itself.
  </read_first>

  <files>packages/backend/src/kill-plan.ts</files>

  <action>
    The block already contains both halves of the finding and never joins them: it records A1 as
    closed on a favourable reading, and forty lines further down records that the same run found the
    kill primitive absent — the very primitive the verdict line called. Join them, as a dated marked
    correction, preserving the 2026-08-27 text as a `// >` block quote exactly as that revision
    preserved the 2026-08-24 text.

    In the LIVE VERDICTS list, change A1's `Verdict` line to the retraction and add a one-line
    `Why retracted`. Keep the A1 `Reading` line — the instrument really printed that string, and
    deleting it would hide what the phase acted on. Leave the A6 entry, its pid table and its verdict
    completely alone.

    Rewrite the `WHAT EACH VERDICT CHANGES HERE` section's A1 paragraph. It currently argues that the
    POSIX arm no longer rests on source analysis alone and that the feared failure mode did not
    occur. With the reading withdrawn, the truthful statement is that the POSIX arm rests on source
    analysis of the pinned LLRT commit and on nothing executed on the shipping runtime, and that the
    feared failure mode is neither confirmed nor refuted because the instrument could not see. Say
    that plainly; do not hedge it into a third state.

    Correct OQ-2's stated justification, and state the direction of the correction because it matters
    to a reader deciding whether the rung is dead weight: the 2026-08-27 revision demoted the rung to
    defence against a FUTURE Caido that rebases its fork. With A1 open, it is defence against the
    PRESENT Caido as well. That revision UNDER-claimed the rung's necessity — an error toward more
    caution, not less. Keep the instruction not to delete the rung and keep the recorded decision
    reference.

    The `ONE MORE READING` paragraph at the foot of the block is CORRECT and must survive: the four
    readings really are one policy, the sandbox really does re-export a restricted process shim, and
    the module's refusal to import `process` really is right for the stronger reason. Extend it by
    one sentence naming the consumer nobody traced — the probe's own verdict line, in the same file
    the careful `isPidAlive` reasoning lives in — and point at `classifyLivenessObservation` as where
    the three-valued determination now lives.
  </action>

  <verify>
    <automated>cd /Users/six2dez/Tools/drift &amp;&amp; GATE_OUT="$(bash .planning/phases/08-process-lifecycle/verdict-gate.sh 2>&amp;1 || true)" &amp;&amp; printf '%s\n' "$GATE_OUT" | grep -c '^== ARM ' | grep -qx 3 &amp;&amp; printf '%s\n' "$GATE_OUT" | grep -Ec 'packages/backend/src/kill-plan\.ts' | grep -qx 0</automated>
    <automated>cd /Users/six2dez/Tools/drift &amp;&amp; pnpm -r typecheck</automated>
    <automated>cd /Users/six2dez/Tools/drift &amp;&amp; npx vitest run packages/backend/src/kill-plan.test.ts</automated>
  </verify>

  <acceptance_criteria>
    - `bash .planning/phases/08-process-lifecycle/verdict-gate.sh` no longer names
      `packages/backend/src/kill-plan.ts` in any arm. The absence is measured against a SINGLE captured gate run that ALSO carries all three
      `== ARM ` header lines, so a gate that syntax-errored, aborted early or matched nothing
      cannot satisfy this criterion with an empty match set. Paste the capture.
    - The file contains `RETRACTED` and contains `classifyLivenessObservation`.
    - The A6 entry's pid values 43921, 43752 and 44284 and the word `FALSIFIED` are all still present
      and their surrounding text is unchanged — show the A6 region has no diff hunk.
    - `git diff` for this task deletes no line carrying a reading value or a date; every superseded
      line appears as a moved-into-quote line. Account for each deleted line in the SUMMARY.
    - The block explicitly reconciles the two facts that sat five paragraphs apart: the verdict line
      called a primitive the same reading measured absent.
    - The OQ-2 correction states the direction of the 2026-08-27 error (under-claim, not relaxation).
    - Only comments change: `git diff -U0` for this file contains no line that is executable
      TypeScript. `pnpm -r typecheck` exits 0 and `kill-plan.test.ts` still passes with the same case
      count as at the end of task 1.
  </acceptance_criteria>

  <done>
    The module that carries the phase's POSIX termination contract states A1's actual verdict, joins
    the two facts it previously kept apart, and points a reader at the fixed determination.
  </done>
</task>

<task type="auto">
  <name>Task 3: Correct the A1 verdict in index.ts and kill-tree.posix.test.ts</name>

  <read_first>
    - `packages/backend/src/index.ts` around the single-pid rung inside `killTree` — the SUPERSEDED
      block quote, the 2026-08-27 CORRECTION paragraph, the `THE RUNG STAYS ANYWAY` paragraph and
      the A6 paragraph below it.
    - `packages/backend/src/index.ts` immediately above `getDiagnostics` — the three-line breadcrumb
      that still says no Wave-0 spike ran and that A1 and A6 stay open. It is stale in the OTHER
      direction: the probe DID run on 2026-08-27, it produced a real A6 measurement, and the A1
      reading is being withdrawn rather than never taken.
    - `packages/backend/src/kill-tree.posix.test.ts` header — the vehicle-caveat block, its
      preserved 2026-08-24 quote, and the paragraph beginning `THIS SUITE'S OWN REACH IS UNCHANGED`.
    - `packages/backend/src/kill-plan.ts` after task 2 — the corrected wording these two must match.
  </read_first>

  <files>packages/backend/src/index.ts, packages/backend/src/kill-tree.posix.test.ts</files>

  <action>
    In `index.ts`, at the single-pid rung: convert the 2026-08-27 CORRECTION paragraph into a dated
    block quote and write the retraction above it. Say what the rung's defence now is — it defends
    against a runtime whose honouring of the process-group spawn option is unmeasured, which is what
    it was originally written for, plus the future-rebase case the 2026-08-27 revision added. Keep
    the A6 paragraph and the win32-exclusion paragraph untouched; both are correct and neither
    depends on A1.

    At the `getDiagnostics` breadcrumb: it is now wrong in a subtle and dangerous way. It reads as
    though no measurement was ever attempted, which would let a later reader conclude the probe is
    untried rather than tried-and-found-blind. Rewrite it to state all three facts — the probe lived
    at this spot on the recoverable commit, it WAS executed on 2026-08-27 and produced the A6
    measurement, and its A1 verdict line was withdrawn because it determined liveness with a
    primitive the same run measured absent. Point at the fixed determination by name and at the
    committed patch file plan 08-15 produces, so the next person re-runs the fixed probe rather than
    rebuilding the broken one from the same commit.

    In `kill-tree.posix.test.ts`: convert the 2026-08-27 CORRECTION's A1 sentence into a dated block
    quote and write the retraction. Keep the A6 sentence, its pids and the word FALSIFIED exactly as
    they are. The paragraph beginning `THIS SUITE'S OWN REACH IS UNCHANGED` is the best sentence in
    the file and gets STRONGER, not weaker, under the retraction — it already says that closing A1
    elsewhere does not retroactively make this suite a proof of the shipping runtime. Extend it by
    one clause: with A1 withdrawn, this suite's Node-only reach is the phase's ONLY executed
    behavioural evidence for the POSIX group path, and it is evidence about Node.

    Change no executable line in either file. This is a comment-only task.
  </action>

  <verify>
    <automated>cd /Users/six2dez/Tools/drift &amp;&amp; GATE_OUT="$(bash .planning/phases/08-process-lifecycle/verdict-gate.sh 2>&amp;1 || true)" &amp;&amp; printf '%s\n' "$GATE_OUT" | grep -c '^== ARM ' | grep -qx 3 &amp;&amp; printf '%s\n' "$GATE_OUT" | grep -Ec 'packages/backend/src/(index\.ts|kill-tree\.posix\.test\.ts)' | grep -qx 0</automated>
    <automated>cd /Users/six2dez/Tools/drift &amp;&amp; npx vitest run</automated>
    <automated>cd /Users/six2dez/Tools/drift &amp;&amp; pnpm -r typecheck &amp;&amp; pnpm lint</automated>
  </verify>

  <acceptance_criteria>
    - `bash .planning/phases/08-process-lifecycle/verdict-gate.sh` names neither
      `packages/backend/src/index.ts` nor `packages/backend/src/kill-tree.posix.test.ts` in any arm. The absence is measured against a SINGLE captured gate run that ALSO carries all three
      `== ARM ` header lines, so a gate that syntax-errored, aborted early or matched nothing
      cannot satisfy this criterion with an empty match set. Paste the capture.
    - Combined with plan 08-11's and this plan's task 2, ARM A now names only carriers owned by plans
      08-13 and 08-14. List exactly what it names.
    - `git diff -U0` for both files contains no line that is executable TypeScript — every changed
      line begins with a comment marker. State the executable-line-change count as zero.
    - The `getDiagnostics` breadcrumb states all three facts (probe location, that it ran on
      2026-08-27, that its A1 verdict was withdrawn and why) and names both
      `classifyLivenessObservation` and the patch file path plan 08-15 produces.
    - `kill-tree.posix.test.ts` still contains `FALSIFIED`, the pids 43921 / 43752 / 44284, and its
      `THIS SUITE'S OWN REACH IS UNCHANGED` paragraph, now extended rather than softened.
    - `npx vitest run` reports a total at or above 695 with 0 failures; state all three columns.
    - `pnpm -r typecheck` exits 0, `pnpm lint` exits 0.
    - `git log --name-only` for this plan shows zero files under `packages/frontend` and zero under
      `packages/shared`.
  </acceptance_criteria>

  <verify>
    <human-check>
      Not required for this task — every criterion above is machine-checkable on this host. Recorded
      so the end-of-phase harvest does not invent one.
    </human-check>
  </verify>

  <done>
    Every source-file carrier of A1's withdrawn verdict states the retraction, the breadcrumb points
    at the fixed instrument rather than the broken one, and the phase's one executed POSIX behavioural
    proof is described accurately as evidence about Node.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| spawned enumerator → classifier | The liveness verdict is derived from an untrusted child process's stdout and exit code. |
| bare-name binary resolution → spawned process | `ps` and `kill` are spawned by bare name and resolved through a search order that includes the plugin host's working directory. |
| source comments → downstream phases | Phases 9-13 read `kill-plan.ts` and `index.ts` comments as the record of what was measured. |

## STRIDE Threat Register

*Threat-ID allocation for this gap-closure set (each ID unique across the set, and all above `08-SECURITY.md`'s maximum `T-08-50`, verified before allocating): **08-11** 51-54 · **08-12** 55, 57-59 · **08-13** 60-62 · **08-14** 63-66 · **08-15** 67, 68, 75, 76 · **08-16** 56, 69-71 · **08-17** 72-74, 77, 78.*

*Breadcrumb: `T-08-56` is deliberately absent from this plan's block — it is allocated to plan 08-16 (the recording of a hardware measurement). The hole is intentional, not a dropped threat.*

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-55 | Spoofing | `buildLivenessProbePlan` bare-name enumerator | medium | mitigate | Same class as T-08-03: a bare name on Windows resolves through the working directory, and on POSIX through whatever PATH the host supplies. Mitigated by scope — the builder is consumed only by an uncommitted local diagnostic build, never by a shipping path — and by design: `classifyLivenessObservation` requires a row whose parsed pid equals the requested pid, so a hijacked binary that exits 0 with unrelated or absent output yields `inconclusive`, not a wrong verdict. Recorded at the call site. |
| T-08-57 | Tampering | `classifyLivenessObservation` | high | mitigate | A classifier that reaches a definite verdict through a default or a coalescing operator reproduces gap 1 exactly. Mitigated by the executed totality case over the full input domain, which fails for at least one combination against any such implementation, and by three demonstrated red inputs (unspawnable, adjacency, ordering). |
| T-08-58 | Repudiation | source-comment marked corrections | medium | mitigate | Deleting the 2026-08-27 text would erase what the phase believed and acted on. Mitigated by the block-quote preservation convention and by an acceptance criterion that accounts for every deleted line in each diff. |
| T-08-59 | Information disclosure | `formatSpikeVerdict` output | low | accept | The verdict strings carry a state word and a reason token only — no pid, path, environment value or token. Accepted; the reason vocabulary is a closed union, so free prose cannot leak into it. |
</threat_model>

<verification>
- `npx vitest run` at or above the 695 / 686 / 9 baseline, zero failures, zero tests removed.
- `pnpm -r typecheck` exit 0, `pnpm lint` exit 0.
- `bash .planning/phases/08-process-lifecycle/verdict-gate.sh` names no file under `packages/`,
  measured against captures that each carry all three `== ARM ` header lines. Its overall exit code
  is NOT asserted by this plan: 08-13 and 08-14 run in the same wave and their carriers may still be
  named. Plan 08-15 owns the exit-0 assertion.
- `git log --name-only` for this plan: zero files under `packages/frontend`, zero under
  `packages/shared`.
- FOUR red-input demonstrations recorded with pasted output, one per `RED INPUT, DEMONSTRATED`
  criterion in task 1: the coalescing defect, the adjacency defect, the ordering defect, and the
  formatter's inconclusive branch.
</verification>

<success_criteria>
- The decision that produced gap 1 is a tested pure function whose "cannot tell" answer cannot be
  reached by accident.
- All three source carriers state A1's actual verdict, preserve their superseded text, and leave A6
  exactly as measured.
- The user-visible cancel and timeout surfaces are unchanged by construction: no frontend or shared
  file is touched.
</success_criteria>

<output>
Create `.planning/phases/08-process-lifecycle/08-12-SUMMARY.md` when done. It MUST include: the four
red-input demonstrations (all four) with pasted before/after output, the test-count ladder, the totality case's
count of definite verdicts across the input product, the accounting of deleted lines in all three
diffs, and the exact list of files `verdict-gate.sh` still names.
</output>
