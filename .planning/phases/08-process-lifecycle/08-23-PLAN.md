---
phase: 08-process-lifecycle
plan: 23
type: execute
wave: 16
gap_closure: true
depends_on: ["08-20", "08-21", "08-22"]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
  - .planning/WINDOWS.md
  - .planning/phases/08-process-lifecycle/08-VALIDATION.md
  - .planning/phases/08-process-lifecycle/08-UAT.md
  - .planning/phases/08-process-lifecycle/verdict-gate.sh
  - .planning/phases/08-process-lifecycle/threat-register-gate.sh
  - .planning/phases/08-process-lifecycle/threat-register-gate.test.sh
  - .planning/phases/08-process-lifecycle/08-SECURITY.md
autonomous: true
requirements: [LIF-01, LIF-02]

estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 3
  confidence: medium

must_haves:
  truths:
    - "The live LIF-02 traceability roll-up states that Plan 08-18 closed deferred item 10 and that the current A1 contract is eight carriers plus two live pointers, while LIF-02 remains unchecked for runtime/causality reasons."
    - "A standing executable pointer check fails if the obsolete single-carrier/item-10-pending meaning returns even when ARM B's marker counts remain green."
    - "Every living Windows evidence carrier states exactly two native kill-tree cases, records that e1ac837 removed the former dead-pid measurement for recycled-PID safety, and neither promises nor restores the deleted probe."
    - "The Windows datum is explicitly deferred/accepted until a safe native measurement is designed; the current 2/2 CI contract is not inflated into execution evidence."
    - "08-SECURITY.md audits the final package head after Plans 08-20..08-22, maps WR-01/02/03 to T-08-89..T-08-94 with executable evidence, and recomputes T-08-16/85/86/87, status, threats_open, totals, and accepted residuals honestly."
    - "The threat gate requires the contiguous T-08-01..T-08-94 range plus T-08-SC and proves new citations, stale audit baselines, missing mappings, and false aggregate closure red."
    - "LIF-01/LIF-02 remain unchecked and native-Windows/real-provider behavior gaps remain explicit."
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Current LIF-02 pointer and safe 2/2 Windows evidence contract"
    - path: ".planning/phases/08-process-lifecycle/verdict-gate.sh"
      provides: "Semantic traceability-row red input beyond marker counts"
    - path: ".planning/WINDOWS.md"
      provides: "Table/JSON entry 12 corrected together to the two-case safe suite"
    - path: ".planning/phases/08-process-lifecycle/08-SECURITY.md"
      provides: "Final-head Phase 08 security audit including WR-01/02/03"
    - path: ".planning/phases/08-process-lifecycle/threat-register-gate.sh"
      provides: "Contiguous through-T-08-94 citation/audit-integrity gate"
  key_links:
    - from: ".planning/phases/08-process-lifecycle/verdict-gate.sh"
      to: ".planning/REQUIREMENTS.md"
      via: "A focused traceability assertion checks Plan 08-18, item 10 closure, eight carriers, two pointers, and unchecked requirements"
      pattern: "08-18|item 10|eight|two"
    - from: ".planning/WINDOWS.md"
      to: ".github/workflows/ci.yml"
      via: "Both table and JSON descriptions mirror expectedTotal=2 and the pinned behavior test without claiming execution"
      pattern: "2/2|e1ac837|recycled"
    - from: ".planning/phases/08-process-lifecycle/08-SECURITY.md"
      to: "packages/backend/src/mcp-lifecycle.ts, packages/backend/src/owned-temp-file.ts, packages/backend/src/mcp-runtime-artifacts.ts"
      via: "T-08-89..T-08-94 current source plus executable tests"
      pattern: "T-08-89|T-08-94|WR-01|WR-02|WR-03"
  prohibitions:
    - statement: "Do not restore a test that sends taskkill to a numeric pid after its original process has exited; the pid may belong to an unrelated process."
    - statement: "Do not rewrite historical evidence silently; add marked current corrections while preserving superseded text."
    - statement: "Do not mark LIF-01/LIF-02 complete or describe static/Node/CI evidence as native-Windows or real-Caido causality proof."
---

<objective>
Converge the two stale evidence contracts and re-audit Phase 08 security at the final code head after closing WR-01/02/03.

Purpose: the code fixes alone leave REQUIREMENTS, Windows ledgers, and the security baseline observably false; the phase must preserve history while making every living contract current and mechanically falsifiable.
Output: corrected traceability/Windows records, strengthened standing gates, and a current T-08-01..T-08-94 security audit.
</objective>

<execution_context>
@/Users/six2dez/.codex/gsd-core/workflows/execute-plan.md
@/Users/six2dez/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/WINDOWS.md
@.github/workflows/ci.yml
@.planning/phases/08-process-lifecycle/08-VERIFICATION.md
@.planning/phases/08-process-lifecycle/08-REVIEW.md
@.planning/phases/08-process-lifecycle/08-SECURITY.md
@.planning/phases/08-process-lifecycle/08-VALIDATION.md
@.planning/phases/08-process-lifecycle/08-UAT.md
@.planning/phases/08-process-lifecycle/verdict-gate.sh
@.planning/phases/08-process-lifecycle/threat-register-gate.sh
@.planning/phases/08-process-lifecycle/threat-register-gate.test.sh
@packages/backend/src/kill-tree.win32.test.ts
@packages/backend/src/mcp-lifecycle.ts
@packages/backend/src/mcp-lifecycle.test.ts
@packages/backend/src/owned-temp-file.ts
@packages/backend/src/owned-temp-file.test.ts
@packages/backend/src/mcp-runtime-artifacts.ts
@packages/backend/src/mcp-runtime-artifacts.test.ts
</context>

## Planner assumptions and no-silent-drop backstops

- The current native suite deliberately has two cases. The removed dead-pid exit-code/stderr datum is not recreated; it is explicitly deferred until a design can prove the target process is still owned at signal time.
- The security audit begins only after Plans 08-20..08-22 are committed and uses their final package head as `audited_at_head`. Any later package fix invalidates the audit and requires this task to be rerun.
- `08-VERIFICATION.md` and `.planning/PROJECT.md` are dirty read-only inputs. Hash them before work and abort on change without restoring, staging, or committing them.
- T-08-89..T-08-94 are the complete new allocation. The threat gate's range and independent tests move together.
- No UI or release work is authorized; the 15 unresolved fallback edge rows remain open and represented by the unchanged LIF checkbox/evidence boundaries.

## Artifacts this phase produces

- Marked correction blocks for the LIF-02 traceability roll-up and every living Windows 3/3/dead-pid carrier.
- A semantic `validate_lif02_traceability` gate function or equivalent red-tested assertion.
- Updated table and JSON representations of broken-Windows entry 12.
- T-08-89..T-08-94 rows/evidence, updated audit frontmatter, and through-94 gate fixtures.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Make the stale LIF-02 traceability sentence a red input</name>
  <read_first>
    - .planning/REQUIREMENTS.md — LIF-02 row and the separate stale traceability roll-up
    - .planning/phases/08-process-lifecycle/verdict-gate.sh — ARM B pointer validation and self-test fixture model
    - .planning/phases/08-process-lifecycle/08-VERIFICATION.md — exact Plan 08-18/eight-carrier/two-pointer correction target
    - .planning/phases/08-process-lifecycle/deferred-items.md — item 10 closure record
  </read_first>
  <files>.planning/REQUIREMENTS.md, .planning/phases/08-process-lifecycle/verdict-gate.sh</files>
  <behavior>
    - "A traceability roll-up that says item 10 is pending or Spike is the single carrier is red even when marker records and checkbox counts are otherwise valid."
    - "The current record requires Plan 08-18, closed item 10, exactly eight mutable carriers, two live pointers, and open LIF-01/LIF-02 status."
    - "Empty, duplicate, separated, or contradictory traceability records are red."
  </behavior>
  <action>
Add a focused semantic assertion to the production verdict gate and invoke the same assertion from self-test fixtures. Require one bounded current traceability record naming Plan 08-18, item 10 closed, eight mutable carriers, two live pointers, and the runtime reasons LIF-02 remains open. Demonstrate the existing stale meaning red before editing REQUIREMENTS. Then append a dated marked correction to the live roll-up; preserve the pre-08-18 sentence as superseded history and leave both requirement boxes unchecked. Do not narrow ARM A/B/C discovery or edit the canonical Spike/summaries. Diagnostics identify only the record class/path, not matched content.
  </action>
  <verify>
    <automated>bash -n .planning/phases/08-process-lifecycle/verdict-gate.sh &amp;&amp; bash .planning/phases/08-process-lifecycle/verdict-gate.sh --self-test &amp;&amp; bash .planning/phases/08-process-lifecycle/verdict-gate.sh</automated>
    <fails_when>non-zero exit, any ARM/self-test red result after correction, or output lacking all three live ARM headers</fails_when>
  </verify>
  <acceptance_criteria>
    - The stale pre-propagation sentence is an executed red fixture, not merely a grep assertion.
    - REQUIREMENTS has one current marked correction with the exact Plan 08-18/eight/two/open meaning.
    - Both LIF boxes remain unchecked and all native/runtime blockers remain visible.
  </acceptance_criteria>
  <done>The separate LIF-02 roll-up can no longer contradict a green marker census silently.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Correct every living Windows carrier to the safe two-case contract</name>
  <read_first>
    - packages/backend/src/kill-tree.win32.test.ts — exact two native cases and their full names
    - .github/workflows/ci.yml — expectedTotal=2 and pinned behavioral assertion
    - .planning/REQUIREMENTS.md — live LIF-01 3/3 claim
    - .planning/phases/08-process-lifecycle/08-VALIDATION.md — T-08-10 and reserved-block narrative
    - .planning/phases/08-process-lifecycle/08-UAT.md — pending tests 4/5
    - .planning/WINDOWS.md — entry 12 table and JSON representations
    - .planning/STATE.md — live taskkill exit-code/reserved-block decisions
  </read_first>
  <files>.planning/REQUIREMENTS.md, .planning/phases/08-process-lifecycle/08-VALIDATION.md, .planning/phases/08-process-lifecycle/08-UAT.md, .planning/WINDOWS.md, .planning/STATE.md, .planning/phases/08-process-lifecycle/verdict-gate.sh</files>
  <behavior>
    - "Every current carrier says two native cases/2 total, not three."
    - "Every current carrier says e1ac837 removed the dead-pid measurement because a recycled pid could target an unrelated process."
    - "The missing exit-code/stderr datum is explicitly deferred/accepted and no reserved code block is claimed to exist."
    - "WINDOWS table and JSON entry 12 descriptions are semantically identical after parsing."
  </behavior>
  <action>
Measure the live suite and CI gate first. Append marked current corrections rather than deleting historical 3/3/dead-pid prose. Correct REQUIREMENTS LIF-01, VALIDATION T-08-10/compliance text, UAT native pending items, STATE live decisions, and both representations of WINDOWS entry 12 to: exactly two native kill-tree cases; both are still unexecuted locally; CI pins total 2 plus the behavioral full name; e1ac837 removed the former dead-pid vehicle for recycled-PID safety; no reserved block exists; the exit-code/stderr datum remains deferred until a safe owned-live-process measurement is designed. Do not add a process-signalling test or edit the native suite/CI. Extend `verdict-gate.sh` with `validate_windows_two_case_contract` (or equivalently named logic) that parses WINDOWS JSON, compares its entry-12 meaning with the table representation, requires the exact 2/2/e1ac837/recycled-pid/current-open contract across all five live carriers, and proves stale 3/3 plus promised-reserved-block fixtures red through `--self-test`.
  </action>
  <verify>
    <automated>pnpm exec vitest run packages/backend/src/kill-tree.win32.test.ts packages/backend/src/kill-tree.win32.gate.test.ts &amp;&amp; node /Users/six2dez/.codex/gsd-core/bin/gsd-tools.cjs windows status --raw &amp;&amp; bash .planning/phases/08-process-lifecycle/verdict-gate.sh --self-test &amp;&amp; bash .planning/phases/08-process-lifecycle/verdict-gate.sh</automated>
    <fails_when>non-zero exit, a native collection total other than two, a gate expectation other than two, table/JSON divergence, or a live record still promising the deleted reserved block</fails_when>
  </verify>
  <acceptance_criteria>
    - Current records across all five carriers use the exact 2/2 contract and preserve old text only as marked superseded history.
    - The reason for e1ac837 is explicit and no recycled-pid signal/test is restored.
    - Entry 12 remains open because no windows-latest/native run has been supplied.
    - LIF-01/LIF-02 remain unchecked.
  </acceptance_criteria>
  <done>The Windows record matches the safe suite and no longer advertises a deleted dangerous measurement vehicle.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Re-audit security at the final package head and register WR-01/02/03</name>
  <precondition>Plans 08-20, 08-21, and 08-22 are committed, their targeted suites/typecheck/lint are green, and no package-changing work is pending.</precondition>
  <read_first>
    - .planning/phases/08-process-lifecycle/08-SECURITY.md — current 318fe24 baseline, T-08-16/85/86/87, residual register, and aggregates
    - .planning/phases/08-process-lifecycle/threat-register-gate.sh — current through-88 parser/range/citation join
    - .planning/phases/08-process-lifecycle/threat-register-gate.test.sh — independent red matrix
    - .planning/phases/08-process-lifecycle/08-VERIFICATION.md — stale-audit measurements and WR mappings required
    - packages/backend/src/mcp-lifecycle.ts and packages/backend/src/mcp-lifecycle.test.ts — T-08-89/T-08-90 evidence
    - packages/backend/src/owned-temp-file.ts and packages/backend/src/owned-temp-file.test.ts — T-08-91/T-08-92 evidence
    - packages/backend/src/mcp-runtime-artifacts.ts and packages/backend/src/mcp-runtime-artifacts.test.ts — T-08-93/T-08-94 evidence
    - packages/backend/src/kill-tree.win32.test.ts — current two-case evidence replacing T-08-16's deleted assertion
  </read_first>
  <files>.planning/phases/08-process-lifecycle/08-SECURITY.md, .planning/phases/08-process-lifecycle/threat-register-gate.sh, .planning/phases/08-process-lifecycle/threat-register-gate.test.sh</files>
  <behavior>
    - "The main register is contiguous T-08-01 through T-08-94 plus T-08-SC with unique rows and current evidence."
    - "WR-01/02/03 map to T-08-89..T-08-94 with severity, disposition, owner, implementation and executable test evidence."
    - "T-08-16 cites the safe current two-case suite and records the removed datum as deferred, never as a deleted assertion still present."
    - "T-08-85/86/87 and frontmatter are recomputed from the measured final package head; any later package commit makes the gate red."
    - "Any missing high mitigation keeps status/threats_open non-secured rather than being hidden by aggregate prose."
  </behavior>
  <action>
Capture the final code HEAD and package tree hash after Plans 08-20..08-22. Re-audit every package-changing commit after the old `318fe24` boundary and the current source/tests, not summaries alone. Add contiguous T-08-89..T-08-94 rows plus owner/evidence ledger entries for WR-01/02/03. Correct T-08-16 to current two-case evidence and the explicit safe deferral. Recompute T-08-85, T-08-86, T-08-87, `audited_at_head`, package digest/delta, row/residual totals, `status`, and `threats_open`; set secured/0 only if all high mitigations have current executable evidence. Extend the production threat gate and independent tests from maximum 88 to 94, including a red next-ID fixture, exact-range/duplicate/empty checks, and a package-commit-after-audit red fixture. Keep diagnostics ID/count/relative-path only. Hash dirty read-only PROJECT and VERIFICATION before work; abort without restore/stage if either changes. Stage only the three owned security artifacts.
  </action>
  <verify>
    <automated>bash -n .planning/phases/08-process-lifecycle/threat-register-gate.sh &amp;&amp; bash -n .planning/phases/08-process-lifecycle/threat-register-gate.test.sh &amp;&amp; bash .planning/phases/08-process-lifecycle/threat-register-gate.test.sh &amp;&amp; bash .planning/phases/08-process-lifecycle/threat-register-gate.sh --self-scan &amp;&amp; bash .planning/phases/08-process-lifecycle/threat-register-gate.sh &amp;&amp; bash .planning/phases/08-process-lifecycle/verdict-gate.sh &amp;&amp; pnpm exec vitest run packages/backend/src/mcp-lifecycle.test.ts packages/backend/src/owned-temp-file.test.ts packages/backend/src/mcp-runtime-artifacts.test.ts packages/backend/src/index.source.test.ts &amp;&amp; pnpm -r typecheck &amp;&amp; pnpm lint</automated>
    <fails_when>non-zero exit, any gate/test/typecheck/lint red result, fewer or more than 94 numeric rows plus T-08-SC, any package commit after audited_at_head, or any open high mitigation paired with secured/zero</fails_when>
  </verify>
  <acceptance_criteria>
    - The audit baseline equals the final package code head and package drift after it is zero.
    - T-08-89..T-08-94 exist exactly once and each has executable evidence plus an accountable owner.
    - T-08-16/85/86/87 no longer assert deleted or stale evidence.
    - Both threat-gate scripts prove through-94 citation and stale-baseline red directions.
    - PROJECT/VERIFICATION dirty inputs and all pre-existing untracked paths remain untouched; no frontend/shared diff, release, push, or requirement completion occurs.
  </acceptance_criteria>
  <done>The security register is current to the final Phase 08 code, covers all three review defects, and can no longer stay green across citation or audit-baseline drift.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Historical record -> current contract | Superseded 3/3 and item-10-pending text must remain history without controlling current readers. |
| Numeric pid -> native Windows measurement | A pid observed dead can be recycled before taskkill, turning a measurement into action against an unrelated process. |
| Package head -> security sign-off | Any code commit after the audit invalidates the claimed mitigation/status baseline. |
| Live citations/evidence -> register aggregates | Complete IDs alone cannot prove current mitigation; source/tests and totals must agree. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-80 | Repudiation | LIF-02 live pointers | high | mitigate | Extend the current carrier/pointer gate to reject the stale item-10-pending/single-carrier traceability meaning. |
| T-08-63 | Tampering | WINDOWS table and JSON ledger | high | mitigate | Parse both representations, require the safe two-case contract, and red-test the former 3/3/reserved-block meaning. |
| T-08-85 | Repudiation | Audit baseline | high | mitigate | Recompute at final code HEAD and make any later package commit a standing gate failure. |
| T-08-87 | Tampering | Aggregate status | high | mitigate | Derive status/threats_open from parsed current rows; never preserve secured/0 across an open high mitigation. |
| T-08-16 | Denial of Service | Native dead-pid measurement | medium | mitigate | Preserve the never-branch-on-vendor-code rule and record that the unsafe recycled-pid measurement vehicle was removed; the missing datum remains explicitly deferred. |
</threat_model>

<verification>
- Run verdict and threat-register live/self-test/independent gates.
- Run all three new helper/lifecycle suites plus source structure, package typecheck, and lint.
- Parse Windows table/JSON and require current two-case semantics.
- Recompute audit head/digest/delta and require zero later package commits.
- Recheck dirty input hashes, no frontend/shared diff, unticked LIF boxes, no release/push.
</verification>

<success_criteria>
- The two record-integrity gaps are current and mechanically falsifiable.
- The final security audit covers WR-01/02/03 at the actual code head with honest aggregate state.
- Unsafe recycled-pid signalling is not restored, and absent Windows/real-provider evidence remains explicit.
</success_criteria>

## Multi-Source Coverage Audit

| Source type | Item | Coverage | Status |
|-------------|------|----------|--------|
| GOAL | Reliable bridge and no token-bearing orphan | Final record/security convergence after the three code repairs | COVERED |
| REQ | LIF-01 | Safe two-case contract, open checkbox, Phase 9 owner | COVERED (explicitly open) |
| REQ | LIF-02 | Current Plan 08-18 traceability plus WR mappings, open runtime blockers | COVERED (explicitly open) |
| RESEARCH | No fabricated runtime/platform evidence; token-safe cleanup | Marked corrections and current source/test audit | COVERED |
| CONTEXT | No 08-CONTEXT.md | Verifier's six-gap set is fully assigned across 08-20..08-23 | COVERED |
| VERIFICATION | Stale pointer, stale Windows contract, stale security audit | Tasks 1-3 | COVERED |
| UI-SPEC | No UI behavior | Frontend/shared excluded | COVERED |
| PROHIBITIONS | No recycled-pid signal, silent rewrite, or false LIF closure | Frontmatter, tasks and gates | COVERED (flagged-unverified fallback plus explicit tests where possible) |
