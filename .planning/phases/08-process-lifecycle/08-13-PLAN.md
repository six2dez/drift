---
phase: 08-process-lifecycle
plan: 13
type: execute
wave: 7
gap_closure: true
depends_on: ["08-11"]
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [LIF-01, LIF-02]

estimate:
  tokens: 54000
  raw_tokens: 54000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "ROADMAP SC-2 names BOTH mechanisms that serve the POSIX guarantee and states what each one covers: the process-group path for the case where the CLI's MCP child stays in the CLI's group, and the argv-marker orphan reap for the case where it does not."
    - "ROADMAP SC-2 states the boundary of the reap it now names — POSIX only, idle-gated, and dependent on the enumerator being spawnable — so the criterion cannot be read as claiming more than the mechanism delivers."
    - "No sentence in `.planning/ROADMAP.md` outside a block quote asserts that assumption A1 was measured, closed or corroborated by execution."
    - "No sentence in `.planning/REQUIREMENTS.md` outside a block quote asserts that assumption A1 was measured favourably on real hardware."
    - "REQUIREMENTS LIF-01 and LIF-02 remain unticked, and their explanatory prose agrees with `08-VERIFICATION.md`'s `gaps_found` verdict rather than with the superseded `human_needed` one."
    - "The requirement-coverage table rows for LIF-01 and LIF-02 name the owning phase for each item that is not closing in Phase 8: Phase 9 SC-4 for the Windows execution leg."
    - "Every amendment this plan writes moves SC-2 toward more precision or more difficulty; the SUMMARY states, for each edit, which of the two it is and why it is not a relaxation."
    - "A6's FALSIFIED verdict and its 2026-08-27 pids survive in ROADMAP and REQUIREMENTS exactly as measured."
  artifacts:
    - ".planning/ROADMAP.md — SC-2 amended in place to name both mechanisms; A1 retraction as a marked correction; gap-closure preamble corrected"
    - ".planning/REQUIREMENTS.md — LIF-01 and LIF-02 prose corrected; coverage table rows updated with owning phases"
  key_links:
    - "ROADMAP SC-2 → the argv-marker orphan reap shipped by plans 08-06/08-07, named in the criterion for the first time"
    - "ROADMAP SC-2 → recorded decision GD-01, which directed a mechanism independent of A6"
    - "REQUIREMENTS LIF-01 coverage row → Phase 9 SC-4, the named owner of the Windows execution leg"
  prohibitions:
    - statement: "No gate-output check is written as a bare `verdict-gate.sh | grep -c <file> | grep -qx 0`. That shape is satisfied by an EMPTY match set: a gate that syntax-errors, aborts early under `set -u`, or whose ERE matches nothing prints nothing, the count is 0, and the check goes green — the vacuous-gate defect this whole round exists to eliminate, in the plans that fix it. Every gate-output check in this plan captures the gate output ONCE and asserts, against that SAME capture, both LIVENESS (exactly three `== ARM ` header lines, which the gate prints unconditionally because `fail()` sets a flag rather than exiting) AND the target-file absence. Do not copy the weak form back in."
    - statement: "No success criterion is relaxed to match what shipped. Every amendment moves toward more precision or more difficulty, and the SUMMARY states which for each edit."
    - statement: "No artifact is rewritten to satisfy the verdict gate. Every correction preserves the superseded text as a dated block quote; no reading, pid, date or verdict cell is deleted."
    - statement: "A6's FALSIFIED verdict is not reverted, softened or bundled into the A1 retraction."
    - statement: "No requirement is ticked by this plan. LIF-01 and LIF-02 stay unticked; correcting the sentence beside a checkbox is not the same as earning the checkbox."
---

<objective>
Two gaps meet in these two files.

**Gap 1's record half.** ROADMAP SC-2's 2026-08-27 amendment re-justifies keeping the single-pid
rung on the grounds that "A1 was measured favourably on 2026-08-27". REQUIREMENTS LIF-02 states the
same. Both rest on the reading gap 1 retracts.

**Gap 2 in full.** SC-2 names ONE mechanism — `detached: true` plus a spawned process-group signal —
and that mechanism is *measured insufficient*: A6 was falsified by direct observation, so a group
signal aimed at the CLI's group cannot reach the token-bearing child for the one provider anyone has
measured, and the single-pid rung kept per OQ-2 signals the CLI rather than the child. What actually
serves the guarantee is the argv-marker orphan reap that plans 08-06/08-07 shipped, and SC-2 does not
name it. Two other criteria in this phase were amended in place for exactly this class of drift; the
amendment SC-2 actually needed did not happen.

`08-VERIFICATION.md` offers an alternative — accept the deviation with an override. That path is
**considered and not taken**, and the reason is recorded here rather than left implicit: an override
requires an `accepted_by` and an `accepted_at` that only the maintainer can supply, and the phase
already has an honest instrument for this exact situation which it has used three times, twice on
these very criteria. Amending is the path the record supports. If the maintainer prefers the override
after reading the amended criterion, the override block is quoted verbatim in
`08-VERIFICATION.md` § *Override suggestion* and remains available.

Purpose: gap 1 `missing` item 2 for these two files, and gap 2 `missing` item 1 in full.
Output: a success criterion that names the mechanism that actually serves the guarantee, and two
requirement rows whose prose matches their checkboxes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@CLAUDE.md

@.planning/phases/08-process-lifecycle/08-VERIFICATION.md
</context>

## Artifacts this phase produces

This plan creates no code symbols and no new files. It modifies prose in two existing planning
documents. New identifiers it introduces into the record: a dated SC-2 amendment note referencing the
argv-marker orphan reap by the names the shipped code already uses —
`buildSessionOrphanScanPlan`, `parseOrphanScanPids`, `buildOrphanKillPlan`,
`reapSessionOrphansIfIdle`, `reapMcpOrphans`, `shouldReapSessionOrphans` — all of which already exist
in `packages/backend/src/kill-plan.ts` and `packages/backend/src/index.ts`.

Symbols created by **sibling plans in this gap-closure set**, listed so a source-grounding pass does
not flag them: `STALE_VERDICT_ERE`, `READING_ERE`, `RETRACTION_TOKEN`, `SUMMARY_KNOWN_UNPINNED`
(08-11); `classifyLivenessObservation`, `buildLivenessProbePlan`, `formatSpikeVerdict`,
`LivenessVerdict`, `LivenessObservation` (08-12); `lastOrphanReap`, `recordOrphanReapOutcome`,
`a1-probe-fix.patch` (08-15).

<tasks>

<task type="auto">
  <name>Task 1: Amend ROADMAP SC-2 to name both mechanisms, and retract A1 inside it</name>

  <reversibility rating="costly">Amending a success criterion changes what every later phase
  verifies against. Undoing it means re-verifying Phase 8's POSIX guarantee under the old wording
  and reconciling three prior amendment notes that cross-reference it. Reversible in git, expensive
  in record consistency — flagged, not gated.</reversibility>

  <read_first>
    - `.planning/ROADMAP.md` § *Phase 8: Process Lifecycle*, criterion 2 in full — including BOTH its
      existing amendment notes (the 2026-08-27 stale-clause correction by plan 08-10, and the
      2026-08-24 negative-pid correction). Read them before writing a third; they establish the
      house form: what changed, when, why, on whose authority, and an explicit statement that the
      guarantee is unchanged.
    - `.planning/ROADMAP.md` Phase 5 SC-1 and Phase 7 SC-2 — the amend-in-place precedent both notes
      cite (05-CONTEXT.md D-02).
    - `.planning/phases/08-process-lifecycle/08-VERIFICATION.md` — gap 2 in full, the § *Override
      suggestion* block, and § *Are the amendments honest?* which grades all three prior amendments.
    - `packages/backend/src/kill-plan.ts` — `buildSessionOrphanScanPlan`, `buildOrphanKillPlan`,
      `shouldReapSessionOrphans` and the `OrphanScanNoopReason` union, so the criterion names the
      mechanism the way the code spells it.
    - `.planning/phases/08-process-lifecycle/08-SECURITY.md` — accepted residuals AR-04 (no Windows
      enumerator) and AR-07 (the multi-session idle-gate window), which bound what the reap covers.
  </read_first>

  <files>.planning/ROADMAP.md</files>

  <action>
    Write ONE new dated amendment note on criterion 2, in the same form as the two already there, and
    correct the A1 clause inside the existing 2026-08-27 note in the same edit.

    THE A1 CORRECTION, INSIDE THE EXISTING NOTE. That note's final argument reads that the rung was
    written as defence against an unmeasured A1, that A1 was measured favourably, and that the rung
    therefore now survives only as defence against a future Caido that rebases its LLRT fork. Convert
    that argument to a dated block quote and write the correction above it: the reading was withdrawn
    because the probe determined liveness with a primitive the same run measured absent, so with A1
    open the rung is defence against the PRESENT Caido as well. State the direction explicitly — the
    2026-08-27 note UNDER-claimed the rung's necessity, which is an error toward more caution. The
    rest of that note is correct and stays: the original "instead of today's single-pid ladder"
    clause really was stale, and correcting the criterion rather than the code really was the right
    call.

    THE NEW AMENDMENT. Rewrite the criterion body so it names both mechanisms and what each covers,
    and so a reader can tell which one is doing the work in which case:

      • The process-group path — `detached: true` at the provider spawn, plus a spawned group signal,
        in addition to the single-pid ladder kept per OQ-2 — covers the case where the CLI's MCP
        child stays in the CLI's process group. Keep every existing detail of this clause: the
        concrete argv spelling, the ban on the runtime's negative-pid form, and the fact that the
        single-pid rung is kept rather than replaced. Nothing here is weakened.
      • The argv-marker orphan reap — a scan for Drift's own session-unique temp-directory marker
        adjacent to the MCP server script, followed by a POSITIVE single-pid kill — covers the case
        where it does not. State why this is a stronger identity rather than a workaround: it
        identifies its target by the target's own command line, so it is independent of process
        groups by construction, and it is the only termination path in Drift that can reach an MCP
        child Drift did not spawn.

    STATE THE REAP'S BOUNDARY IN THE CRITERION ITSELF, so the amendment cannot be read as claiming
    more than the mechanism delivers, and so it is harder to satisfy rather than easier: POSIX only,
    with no equivalent enumerator on Windows (AR-04); idle-gated, so it fires when no session is
    active rather than continuously, leaving the multi-session cancel window open (AR-07); and
    dependent on the enumerator being spawnable in Caido's sandbox, which is unmeasured and fails
    closed into a no-op that removes no pre-existing termination path (ledger entries 14 and 15).

    WRITE THE AMENDMENT'S OWN JUSTIFICATION, because this is the third amendment to one criterion and
    the record must show it is not drift: the trigger is a MEASUREMENT — A6 falsified on 2026-08-27,
    codex pid 43921 in pgid 43752 with its own MCP child pid 44284 in pgid 44284 — and maintainer
    decision GD-01, which directed building a mechanism that does not depend on A6. The guarantee
    SC-2 exists for is unchanged; what changes is that the criterion now names the mechanism that
    actually delivers it. Cite the same amend-in-place precedent the other two notes cite.

    RECORD THE OVERRIDE PATH AS CONSIDERED AND NOT TAKEN, in one sentence inside the note, naming
    where the override wording lives so a maintainer who prefers it can still take it.
  </action>

  <verify>
    <automated>cd /Users/six2dez/Tools/drift &amp;&amp; GATE_OUT="$(bash .planning/phases/08-process-lifecycle/verdict-gate.sh 2>&amp;1 || true)" &amp;&amp; printf '%s\n' "$GATE_OUT" | grep -c '^== ARM ' | grep -qx 3 &amp;&amp; printf '%s\n' "$GATE_OUT" | grep -Ec '\.planning/ROADMAP\.md' | grep -qx 0</automated>
    <automated>cd /Users/six2dez/Tools/drift &amp;&amp; sed -E '/^[[:space:]]*&gt;/d' .planning/ROADMAP.md | grep -c 'orphan reap' | grep -qvx 0</automated>
  </verify>

  <acceptance_criteria>
    - `bash .planning/phases/08-process-lifecycle/verdict-gate.sh` no longer names `.planning/ROADMAP.md`. The absence is measured against a SINGLE captured gate run that ALSO carries all three
      `== ARM ` header lines, so a gate that syntax-errored, aborted early or matched nothing
      cannot satisfy this criterion with an empty match set. Paste the capture.
    - Criterion 2's body names both mechanisms, and for each one states the case it covers. Quote the
      new criterion text in full in the SUMMARY.
    - The criterion states all three of the reap's boundaries — POSIX only, idle-gated, enumerator
      spawnability unmeasured — and cross-references AR-04, AR-07 and the ledger entries by number.
    - The new amendment note carries: the date, the measurement that triggered it (A6's pids), the
      recorded decision that directed the response (GD-01), an explicit statement that the guarantee
      is unchanged, and the amend-in-place precedent citation.
    - The SUMMARY states, for each edit made to criterion 2, whether it moves the criterion toward
      more precision or more difficulty, and why it is neither a relaxation nor a retrofit. A reader
      must be able to check that claim against the diff.
    - The 2026-08-27 note's A1 argument is preserved verbatim as a block quote; `git diff` deletes no
      line carrying a reading value, a pid or a date. Account for every deleted line.
    - The 2026-08-24 negative-pid amendment note is byte-unchanged — it is correct and independent of
      A1's verdict. Show it has no diff hunk.
    - The override path is named in one sentence with a pointer to
      `08-VERIFICATION.md` § *Override suggestion*.
    - Only Phase 8's section of `ROADMAP.md` changes: `git diff` shows no hunk in any other phase's
      section, and the file's total heading count is unchanged.
  </acceptance_criteria>

  <done>
    SC-2 describes the mechanism that actually serves the POSIX guarantee, states what that mechanism
    does not cover, and no longer rests any part of its argument on a withdrawn reading.
  </done>
</task>

<task type="auto">
  <name>Task 2: Correct the Phase 8 gap-closure preamble and every other ROADMAP sentence that asserts A1 was measured</name>

  <read_first>
    - `.planning/ROADMAP.md` — the italic gap-closure preamble under Phase 8's plan list, which
      opens "UAT measured **A1 closed favourably** and **A6 FALSIFIED**".
    - `.planning/ROADMAP.md` — Phase 8's **Research flag** paragraph and the Phase 8 overview bullet
      near the top of the file, checked for the same claim.
    - The ARM A output from plan 08-11's SUMMARY — the authoritative list of what still carries the
      claim. Re-run the gate yourself; do not read the count out of any plan.
  </read_first>

  <files>.planning/ROADMAP.md</files>

  <action>
    Task 1 fixed criterion 2. This task sweeps the rest of the file, driven by the gate's own output
    rather than by a hand-written census — the carrier count in this phase has been wrong five times
    and every one of those was a hand-written list.

    Run the gate, take the ROADMAP hit list from its output, and correct each hit as a dated marked
    correction preserving the superseded text.

    For the gap-closure preamble specifically: it summarises the 2026-08-27 UAT round as having
    measured A1 closed favourably. Correct it to state what that round actually established — a real
    A6 falsification by direct observation, a token-bearing MCP orphan Drift never spawned, and an A1
    reading later withdrawn. Keep the maintainer decisions GD-01 and GD-02 exactly as they are; both
    were made on the A6 and G-04 findings, neither of which is withdrawn, so neither decision is
    affected by the retraction. Say so, so a reader does not wonder.

    Do not touch Phase 9, Phase 10 or Phases 11-13. If the gate names a hit inside one of them,
    report it rather than editing it — a stale A1 claim outside Phase 8 is a finding this plan does
    not own and must not silently absorb.
  </action>

  <verify>
    <automated>cd /Users/six2dez/Tools/drift &amp;&amp; GATE_OUT="$(bash .planning/phases/08-process-lifecycle/verdict-gate.sh 2>&amp;1 || true)" &amp;&amp; printf '%s\n' "$GATE_OUT" | grep -c '^== ARM ' | grep -qx 3 &amp;&amp; printf '%s\n' "$GATE_OUT" | grep -Ec '\.planning/ROADMAP\.md' | grep -qx 0</automated>
    <automated>cd /Users/six2dez/Tools/drift &amp;&amp; grep -c 'GD-01' .planning/ROADMAP.md | grep -qvx 0</automated>
  </verify>

  <acceptance_criteria>
    - The gate names no hit in `.planning/ROADMAP.md`, before this task's edits are counted as
      complete. Paste the gate output from before and after — each capture must carry all three
      `== ARM ` header lines, or the reading is of a silent gate and proves nothing.
    - The gap-closure preamble states A6's falsification and the G-04 orphan as measured findings and
      the A1 reading as withdrawn, and explicitly states that GD-01 and GD-02 are unaffected because
      neither was made on the A1 reading.
    - `GD-01` and `GD-02` still appear in the file with their original wording.
    - `git diff` for this task is confined to Phase 8's section. If the gate named a hit outside
      Phase 8, it is reported in the SUMMARY as an unowned finding and left unedited.
    - Every superseded sentence is preserved as a dated block quote; account for every deleted line.
  </acceptance_criteria>

  <done>
    `ROADMAP.md` carries no live claim that A1 was measured, and the decisions the 2026-08-27 round
    produced are visibly untouched by the retraction.
  </done>
</task>

<task type="auto">
  <name>Task 3: Correct LIF-01 and LIF-02 and their coverage rows in REQUIREMENTS.md</name>

  <read_first>
    - `.planning/REQUIREMENTS.md` — the LIF section (LIF-01 and LIF-02 with their full parenthetical
      prose) and the requirement-coverage table rows for both.
    - `.planning/phases/08-process-lifecycle/08-VERIFICATION.md` § *Requirements Coverage* — which
      rates both BLOCKED, states exactly what closes each, and notes that "the checkbox is honest;
      the sentence beside it is not".
    - `.planning/phases/08-process-lifecycle/08-SECURITY.md` — AR-01 and AR-04, the two residuals
      that bound LIF-01.
    - `.planning/ROADMAP.md` Phase 9 § *CI Hardening* SC-4 — the named owner of the Windows
      execution leg.
  </read_first>

  <files>.planning/REQUIREMENTS.md</files>

  <action>
    Both rows stay UNTICKED. Correcting the sentence beside a checkbox is not the same as earning the
    checkbox, and the verification confirms leaving both unticked was the right call.

    LIF-02's prose asserts that assumption A1 was measured favourably on real hardware on 2026-08-27.
    Convert that clause to a dated block quote and write the retraction above it, in one sentence
    naming the mechanism: the probe determined liveness with a primitive the same run measured
    absent, so the favourable string was emitted unconditionally. Everything else in LIF-02's prose
    is correct and survives — the mechanism is built, it is behaviourally proven under Node with a
    control, A6 was measured FALSE with those pids, the argv-marker reap is what closes the
    requirement independently of process groups, and that reap is itself covered by no executed
    assertion on the shipping runtime.

    LIF-01's prose does not mention A1 and is accurate; check it against the gate's output rather
    than assuming, and if the gate names nothing in it, change only what the next paragraph requires.
    Add to it the two residuals that bound it and are not currently named there: the tree-killer
    cannot reach a grandchild whose intermediate parent already exited (AR-01), and there is no
    Windows enumerator, so the foreign-parented orphan class the reap reaches on POSIX is
    unreachable on Windows (AR-04). Name Phase 9 SC-4 as the owner of the one thing that closes the
    row — a green Windows leg.

    Update both coverage-table rows. They currently cite the superseded `human_needed` verdict.
    Cite `gaps_found` instead, and for each row name the owning phase for every item not closing in
    Phase 8: the Windows execution leg is Phase 9 SC-4. For LIF-02 add the two items Phase 8 itself
    still owns and which plans 08-16 and 08-17 carry — the post-fix POSIX cancel and timeout
    measurements, and the A1 re-run with the fixed probe. Say plainly that nothing in the milestone
    owns the A1 re-run after Phase 8 closes, because that is the fact most likely to be lost.
  </action>

  <verify>
    <automated>cd /Users/six2dez/Tools/drift &amp;&amp; GATE_OUT="$(bash .planning/phases/08-process-lifecycle/verdict-gate.sh 2>&amp;1 || true)" &amp;&amp; printf '%s\n' "$GATE_OUT" | grep -c '^== ARM ' | grep -qx 3 &amp;&amp; printf '%s\n' "$GATE_OUT" | grep -Ec '\.planning/REQUIREMENTS\.md' | grep -qx 0</automated>
    <automated>cd /Users/six2dez/Tools/drift &amp;&amp; grep -cE '^- \[ \] \*\*LIF-0[12]\*\*' .planning/REQUIREMENTS.md | grep -qx 2</automated>
  </verify>

  <acceptance_criteria>
    - `bash .planning/phases/08-process-lifecycle/verdict-gate.sh` names no hit in
      `.planning/REQUIREMENTS.md`. The absence is measured against a SINGLE captured gate run that ALSO carries all three
      `== ARM ` header lines, so a gate that syntax-errored, aborted early or matched nothing
      cannot satisfy this criterion with an empty match set. Paste the capture.
    - Both LIF rows are still unticked — the grep for unticked LIF rows returns exactly 2.
    - LIF-02's prose contains the retraction and preserves the superseded clause as a dated block
      quote; LIF-02 still records A6's pids 43921 / 43752 / 44284 and the word FALSE or FALSIFIED.
    - LIF-01's prose names AR-01, AR-04 and Phase 9 SC-4.
    - Both coverage-table rows cite `gaps_found` rather than `human_needed`, and each names an owning
      phase for every item not closing in Phase 8.
    - The LIF-02 coverage row states that no later phase in this milestone owns the A1 re-run.
    - `git diff` for this task touches only the LIF section and the two coverage-table rows; the
      table's total row count is unchanged. Account for every deleted line.
  </acceptance_criteria>

  <done>
    The requirement rows say what is true: mechanism built, two measurements outstanding, one owned
    by Phase 9 and the rest owned by this phase's own remaining plans — with the ownerless item named
    as ownerless rather than left to be discovered.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ROADMAP success criteria → every later verification | A criterion is the contract a verifier checks against. A criterion that names the wrong mechanism produces a verdict about the wrong thing. |
| REQUIREMENTS coverage table → milestone completion | The table is what `/gsd-complete-milestone` and the audit commands read to decide whether a requirement is owned. An unowned item that reads as owned disappears. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-60 | Tampering | ROADMAP SC-2 amendment | high | mitigate | The failure mode of amending a criterion after the code shipped is retrofitting: relaxing the words until the code passes. Mitigated by an acceptance criterion requiring the SUMMARY to classify every edit as precision or difficulty and to justify it against the diff, by keeping every existing clause of the group-mechanism half intact, and by adding three explicit boundaries to the newly named mechanism so the amendment is strictly harder to satisfy than a bare "the reap closes it". |
| T-08-61 | Repudiation | superseded amendment text | medium | mitigate | Three amendment notes now stack on one criterion. Overwriting any of them erases why the criterion reads as it does. Mitigated by the dated block-quote convention and by an acceptance criterion accounting for every deleted line, plus a check that the independent 2026-08-24 note has no diff hunk at all. |
| T-08-62 | Denial of service | requirement ownership | medium | mitigate | An item that reads as owned but is not — the A1 re-run — silently disappears at milestone close. Mitigated by requiring the LIF-02 coverage row to state in plain words that no later phase owns it. |
</threat_model>

<verification>
- `bash .planning/phases/08-process-lifecycle/verdict-gate.sh` names neither `.planning/ROADMAP.md`
  nor `.planning/REQUIREMENTS.md`, measured against captures that each carry all three `== ARM `
  header lines. Its overall exit code is NOT asserted by this plan: 08-12 and 08-14 run in the same
  wave and their carriers may still be named. Plan 08-15 owns the exit-0 assertion.
- Both LIF requirement rows are still unticked.
- `git diff --stat` for this plan touches exactly two files.
- No source file is modified: `git diff --name-only` shows nothing under `packages/`.
- The SUMMARY classifies every SC-2 edit as precision or difficulty and defends the classification
  against the diff.
</verification>

<success_criteria>
- SC-2 names the mechanism that actually serves the POSIX guarantee and states its boundaries, so a
  future verifier checks the right thing.
- No planning document asserts a runtime fact nobody established.
- Every item neither requirement closes in Phase 8 has a named owner, and the one item with no owner
  is written down as having none.
</success_criteria>

<output>
Create `.planning/phases/08-process-lifecycle/08-13-SUMMARY.md` when done. It MUST include: the full
new text of criterion 2, the precision-or-difficulty classification of every SC-2 edit with its
justification, the before/after gate output for both files, the accounting of every deleted line in
both diffs, and any A1 hit the gate found outside Phase 8's section reported as an unowned finding.
</output>
