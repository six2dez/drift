---
phase: 08-process-lifecycle
plan: 20
type: execute
wave: 13
gap_closure: true
depends_on: ["08-19"]
files_modified:
  - packages/backend/src/mcp-lifecycle.ts
  - packages/backend/src/mcp-lifecycle.test.ts
  - packages/backend/src/index.ts
  - packages/backend/src/index.source.test.ts
autonomous: true
requirements: [LIF-01, LIF-02]

estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 2
  confidence: medium

must_haves:
  truths:
    - "A provider-start lease invalidated by teardown carries an explicit retired identity until the stale sender consumes it; path equality is never used as the retirement decision."
    - "If a paused sender recreates its captured runtime root while teardown still leaves that root in the global pointer, stale-send cleanup removes the recreated root and never commits a provider process."
    - "A current or committed provider-start lease is not classified as retired, so its live runtime root is not removed by stale-generation cleanup."
    - "The invalidation, recreation, stale cleanup, and teardown-completion order is executed in Vitest rather than inferred from source text."
    - "LIF-01/LIF-02 stay unchecked; this repair closes WR-01 but supplies neither native-Windows execution nor a non-Claude real-turn causality reading."
  artifacts:
    - path: "packages/backend/src/mcp-lifecycle.ts"
      provides: "Retired provider-start identity, one-shot release/consume decision, and injected-I/O retired-root cleanup"
    - path: "packages/backend/src/mcp-lifecycle.test.ts"
      provides: "Executable equal-pointer retired-root interleaving"
    - path: "packages/backend/src/index.ts"
      provides: "Stale-send cleanup wired to explicit lifecycle retirement"
    - path: "packages/backend/src/index.source.test.ts"
      provides: "Non-vacuous wiring census that rejects the former path-equality guard"
  key_links:
    - from: "packages/backend/src/mcp-lifecycle.ts"
      to: "packages/backend/src/index.ts"
      via: "releaseProviderStartLease returns whether the exact lease identity was retired"
      pattern: "retiredProviderStarts|releaseProviderStartLease"
    - from: "packages/backend/src/index.ts"
      to: "packages/backend/src/mcp-lifecycle.test.ts"
      via: "The exported cleanupRetiredProviderStartRoot consumes the explicit retired result and an injected rm callback even while mcpTempDir still equals the captured path"
      pattern: "cleanupRetiredProviderStartRoot|retired"
  prohibitions:
    - statement: "Do not replace object/generation identity with a pathname-only comparison; equal strings do not prove a lease is current."
    - statement: "Do not restore, signal, or otherwise act on a recycled process identifier while testing cleanup."
    - statement: "Do not convert Node/Vitest proof into a native-Windows or real-Caido completion claim."
  flagged_assumptions:
    - requirement_id: LIF-01
      category: specless-edge-set
      probe: "Boundary, adjacency, empty, ordering, precision, idempotency, and concurrency remain unresolved for native Windows execution."
      status: unresolved
      note: "The fallback edge probe produced seven LIF-01 rows; this plan changes only provider-start generation cleanup and preserves the Phase 9 owner."
    - requirement_id: LIF-02
      category: specless-edge-set
      probe: "Boundary, adjacency, empty, encoding, ordering, precision, idempotency, and concurrency remain unresolved for real-provider causality."
      status: unresolved
      note: "The fallback edge probe produced eight LIF-02 rows; this plan resolves the concrete WR-01 interleaving without silently upgrading the wider requirement."
---

<objective>
Replace WR-01's path-equality inference with an explicit retired provider-start identity and prove the exact stale-send/equal-pointer interleaving end to end.

Purpose: teardown currently clears pending leases but leaves no tombstone that later cleanup can consume, allowing a recreated retired root to survive when `mcpTempDir` still carries the same path.
Output: lifecycle state that remembers retired lease identities, wired stale-root cleanup, and executable interleaving coverage.
</objective>

<execution_context>
@/Users/six2dez/.codex/gsd-core/workflows/execute-plan.md
@/Users/six2dez/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-process-lifecycle/08-VERIFICATION.md
@.planning/phases/08-process-lifecycle/08-REVIEW.md
@.planning/phases/08-process-lifecycle/08-SECURITY.md
@.planning/phases/08-process-lifecycle/08-PATTERNS.md
@packages/backend/src/mcp-lifecycle.ts
@packages/backend/src/mcp-lifecycle.test.ts
@packages/backend/src/index.ts
@packages/backend/src/index.source.test.ts
</context>

## Planner assumptions and no-silent-drop backstops

- There is no `08-CONTEXT.md`; the authoritative current decisions are the six concrete gaps in `08-VERIFICATION.md`. The user requested continuation, so this plan does not reopen already executed product decisions.
- The explicit lifecycle identity is the exact `ProviderStartLease` object. A path can be equal across the teardown window; object identity plus epoch/id is the non-ambiguous carrier.
- No UI change is authorized. `08-UI-SPEC.md` is preserved and frontend/shared trees remain outside the plan.
- Threat IDs T-08-89 and T-08-90 are allocated here and rolled into the canonical register by dependent Plan 08-23.

## Artifacts this phase produces

- `McpLifecycleState.retiredProviderStarts` (or an equivalently named set keyed by exact `ProviderStartLease` identity).
- A one-shot retired result from `releaseProviderStartLease` or a dedicated `consumeRetiredProviderStartLease` helper.
- An import-free `cleanupRetiredProviderStartRoot({retired,tempDir,removeRoot})` helper (or equivalently named injected-I/O function) replacing `currentTempDir === leaseTempDir` inference.
- An executable lifecycle test reproducing invalidation, root recreation, stale cleanup, and teardown completion.

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Make retired provider-start identity executable</name>
  <read_first>
    - packages/backend/src/mcp-lifecycle.ts — current lease set, commit/release behavior, and teardown invalidation
    - packages/backend/src/mcp-lifecycle.test.ts — existing paused-start and synchronous-commit tests
    - .planning/phases/08-process-lifecycle/08-VERIFICATION.md — WR-01 interleaving and required equal-pointer reproduction
  </read_first>
  <files>packages/backend/src/mcp-lifecycle.ts, packages/backend/src/mcp-lifecycle.test.ts</files>
  <behavior>
    - "Teardown moves every pending exact lease identity into a retired set before clearing the active-start set and before its process pass."
    - "Releasing/consuming an invalidated lease returns retired exactly once; a second release is false and a current committed lease is false."
    - "A nested/defensive teardown does not revive or lose an already retired lease identity."
    - "The paused sender cannot commit after invalidation and leaves zero tracked provider processes."
  </behavior>
  <action>
Add explicit retired-provider-start state to `McpLifecycleState`. In `runMcpProviderTeardown`, copy each exact object from `providerStarts` into that retired state before clearing active leases and before invoking the teardown operation. Make the sender's release path obtain a one-shot boolean/result indicating whether that exact lease was retired; consuming it must remove only that exact identity. Add an import-free async cleanup helper that accepts `retired`, the captured `tempDir`, and an injected `removeRoot` callback; it calls the callback only for an explicit retired identity with a defined root. Keep `commitProviderStartLease` synchronous and retain its epoch, active-teardown, set-membership, and current-temp-dir guards. Write the tests first, including a held teardown promise so a test can observe invalidation before teardown completion and a real temporary-directory callback proving cleanup removes the recreated root. Cite T-08-89/T-08-90 in the identity and wrong-generation safety comments without logging paths or values.
  </action>
  <verify>
    <automated>pnpm exec vitest run packages/backend/src/mcp-lifecycle.test.ts</automated>
    <fails_when>non-zero exit, any failed test, or a summary reporting zero passed tests</fails_when>
  </verify>
  <acceptance_criteria>
    - `mcp-lifecycle.test.ts` executes a retired-identity case and a current-lease negative control.
    - The retired result is one-shot and object-identity based; no pathname comparison participates in retirement classification.
    - The existing provider-start tests remain green and no provider process can commit after teardown invalidation.
  </acceptance_criteria>
  <done>The pure lifecycle model can distinguish an invalidated lease from a current equal-path lease and exposes that distinction to the I/O owner.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Remove the recreated retired root even while the pointer is equal</name>
  <read_first>
    - packages/backend/src/index.ts — `sendCliMessage` finally ordering, `cleanupUncommittedProviderStart`, and `cleanupRetiredProviderStartRoot`
    - packages/backend/src/index.source.test.ts — existing WR-01 source assertion that currently pins the defective equality guard
    - packages/backend/src/mcp-lifecycle.ts — Task 1's exact release/consume contract
    - packages/backend/src/mcp-lifecycle.test.ts — Task 1 interleaving fixture and negative controls
  </read_first>
  <files>packages/backend/src/index.ts, packages/backend/src/index.source.test.ts, packages/backend/src/mcp-lifecycle.test.ts</files>
  <behavior>
    - "A stale sender cleans its per-session files, then removes its recreated retired root even when `mcpTempDir` still equals the captured root."
    - "A non-retired sender never removes the live root through this cleanup helper."
    - "Source coverage rejects reintroduction of `currentTempDir === leaseTempDir` as the retirement decision."
  </behavior>
  <action>
Capture Task 1's one-shot retired result before any release can discard it, then pass that result to the exported `cleanupRetiredProviderStartRoot` after `cleanupUncommittedProviderStart`, injecting `rm(root,{recursive:true,force:true})`. Delete the local path-comparison helper. Keep cleanup best-effort at the index adapter while preserving the explicit lifecycle decision separately from the filesystem effect. Replace the old source assertion with a wiring assertion that counts exactly one retired-result acquisition and exactly one explicit-retirement cleanup call, and asserts the former equality guard is absent. Extend the executable interleaving over a real temporary directory: invalidate the lease, recreate the captured root while the simulated installed pointer remains equal, run the production helper with the real remove callback, release teardown, and assert `stat` returns ENOENT.
  </action>
  <verify>
    <automated>pnpm exec vitest run packages/backend/src/mcp-lifecycle.test.ts packages/backend/src/index.source.test.ts &amp;&amp; pnpm -r typecheck &amp;&amp; pnpm lint</automated>
    <fails_when>non-zero exit, any failed test/typecheck/lint finding, or either Vitest file collecting zero tests</fails_when>
  </verify>
  <acceptance_criteria>
    - The exact verifier interleaving executes and finishes with the recreated retired root absent.
    - `cleanupRetiredProviderStartRoot` acts on explicit retirement and contains no equal-path early return.
    - Existing synchronous commit, teardown blocking, typecheck, and lint contracts remain green.
    - No frontend/shared/planning record file is modified by this plan.
  </acceptance_criteria>
  <done>WR-01 is closed in both the lifecycle model and production wiring, with the equal-pointer false-negative demonstrated red before the fix and green after it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Paused provider preparation -> teardown | A sender can resume after teardown invalidates the generation and removes its original files. |
| Lease identity -> filesystem cleanup | The lifecycle decision must cross into I/O without being weakened to a pathname comparison. |
| Retired root -> replacement generation | Cleanup must remove the retired root without deleting an independently current generation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-89 | Information Disclosure | Retired provider-start root | high | mitigate | Preserve exact retired lease identity through stale-send cleanup and recursively remove any root recreated by the invalidated sender. |
| T-08-90 | Denial of Service / Tampering | Wrong-generation cleanup | high | mitigate | Use exact object/epoch identity with a one-shot consume result and executable current-generation negative controls; never infer ownership from path equality alone. |
</threat_model>

<verification>
- Run lifecycle and source-structure suites, package typechecks, and lint.
- Require the executable equal-pointer reproduction to fail on the pre-fix behavior and pass after wiring explicit retirement.
- Confirm no requirement checkbox, frontend/shared file, release artifact, or native-Windows evidence record changed.
</verification>

<success_criteria>
- WR-01's invalidated-lease/equal-pointer root leak is no longer reproducible.
- Retirement is a lifecycle identity fact, not a pathname heuristic.
- The repair remains compatible with current macOS/Linux behavior and does not claim unavailable platform/runtime evidence.
</success_criteria>

## Multi-Source Coverage Audit

| Source type | Item | Coverage | Status |
|-------------|------|----------|--------|
| GOAL | No token-bearing orphan survives cancel/timeout | Retired preparation roots are removed after invalidation | COVERED |
| REQ | LIF-01 | Regression-only; native Windows execution remains Phase 9-owned | COVERED (explicitly open) |
| REQ | LIF-02 | WR-01 stale provider-start cleanup is implemented and tested | COVERED |
| RESEARCH | Kill/cleanup identity must survive awaits in constrained runtime | Pure lifecycle state plus index-owned I/O follows the existing module split | COVERED |
| CONTEXT | No 08-CONTEXT.md | Verifier WR-01 is the authoritative continuation scope | COVERED |
| VERIFICATION | Explicit tombstone plus equal-pointer interleaving | Tasks 1-2 | COVERED |
| UI-SPEC | No UI behavior change | Frontend/shared excluded | COVERED |
