---
phase: 08-process-lifecycle
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/backend/src/index.ts
  - .planning/phases/08-process-lifecycle/08-SPIKE.md
autonomous: false
requirements: [LIF-02]
user_setup: []

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A real Caido install on the maintainer's macOS host reports, in the Drift diagnostics block, whether a `detached: true` spawn made by Caido's own LLRT backend produces a process group whose kill reaches a grandchild (assumption A1)."
    - "The pgid of a real provider CLI's `mcp-server.mjs` child, read with `ps -eo pid,ppid,pgid,comm` during a live turn, is recorded against the pid of the CLI process Drift tracks (assumption A6)."
    - "The pre-fix control is recorded: with today's shipped code, `pgrep -f mcp-server.mjs` returns a non-zero count after the user clicks Stop — the LIF-02 defect reproduced on real hardware, not inferred."
    - statement: "Every temporary spike symbol added in this plan is gone at the plan's final commit — the tree carries no diagnostics scaffolding into wave 2."
      verification: backstop
  artifacts:
    - ".planning/phases/08-process-lifecycle/08-SPIKE.md — the three measurements, each with the Caido version and the date it was taken"
  key_links:
    - "getDiagnostics (index.ts:4908) → runLifecycleSpike() → the spike fields reaching SettingsView's Copy-diagnostics output"
    - "08-SPIKE.md's A1/A6 verdict lines → copied into kill-plan.ts's module header by plan 08-02 task T-08-04"
---

<objective>
Close assumptions **A1** (does the shipped Caido LLRT honour `detached: true`?) and **A6** (do the
provider CLIs put their MCP child in a different process group?) on the maintainer's own macOS Caido
install, before plan 08-02 bets the phase's POSIX mechanism on them.

This is maintainer decision **D-03**: the Wave 0 spike is in scope, planned as a real task, and its
removal is in the same phase. `08-RESEARCH.md` § *Assumptions Log* rates A1 as *"the phase's central
mechanism — if Caido ships an older fork without `detached`, LIF-02 has no POSIX mechanism at all and
the phase needs a redesign"* and A6 as *"medium-high, and untested"*. Neither is closable by CI: every
CI leg runs Node, never Caido's LLRT, and no CI leg executes a provider CLI binary
(`08-VALIDATION.md` § *Vehicle caveat* items 2 and 3).

Purpose: buy the phase's riskiest design decision one build and one chat turn on hardware the
maintainer already owns, instead of discovering it after five plans have shipped.
Output: `08-SPIKE.md` carrying three measurements; a tree with **zero** spike code left in it.

**This plan deliberately does not implement any part of LIF-01 or LIF-02.** It measures, records and
then deletes itself. If A1 comes back negative, stop and re-plan the phase rather than proceeding to
08-02.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-process-lifecycle/08-RESEARCH.md
@.planning/phases/08-process-lifecycle/08-VALIDATION.md
@CLAUDE.md
</context>

## Artifacts this phase produces

Every symbol and file this **phase** creates, so a source-grounding pass can tell a new symbol from
a drifted one. Symbols marked *(this plan, temporary)* are created **and deleted** inside plan 08-01.

| Kind | Name | Home | Created by |
|---|---|---|---|
| file | `packages/backend/src/kill-plan.ts` | new | 08-02 |
| file | `packages/backend/src/kill-plan.test.ts` | new | 08-02 |
| file | `packages/backend/src/kill-tree.posix.test.ts` | new | 08-02 |
| file | `packages/backend/src/kill-tree.win32.test.ts` | new | 08-04 |
| file | `packages/backend/src/kill-tree.win32.gate.test.ts` | new | 08-04 |
| file | `.planning/phases/08-process-lifecycle/08-SPIKE.md` | new | 08-01 |
| file | `.planning/phases/08-process-lifecycle/08-SECURITY.md` | new | 08-05 |
| type | `KillTreePlan` (arms `{ kind: "none"; reason: "no-pid" }`, `{ kind: "spawn"; file; args; windowsVerbatimArguments }`) | `kill-plan.ts` | 08-02 |
| type | `KillRung` (`"term" \| "kill"`) | `kill-plan.ts` | 08-02 |
| const | `DEFAULT_TASKKILL` | `kill-plan.ts` | 08-02 |
| fn | `buildKillTreePlan` | `kill-plan.ts` | 08-02 |
| fn | `shouldDetachProviderSpawn` | `kill-plan.ts` | 08-02 |
| fn | `killTree` | `index.ts` | 08-02 |
| fn | `isPidAlive` | `index.ts` | 08-02 |
| member | `detached: boolean` on `SpawnWithEnv` | `index.ts` | 08-02 |
| fn | `functionBody` (test helper) | `index.source.test.ts` | 08-03 |
| fn | `spawnFixtureTree`, `isAlive`, `settle` (test helpers) | `kill-tree.posix.test.ts` | 08-02 |
| fn | `runToCompletion`, `createDeadPid` (test helpers), const `SPAWN_TIMEOUT_MS` | `kill-tree.win32.test.ts` | 08-04 |
| CI step | `Gate: the win32 kill-tree suite actually ran` | `.github/workflows/ci.yml` | 08-04 |
| CI artifact | `$RUNNER_TEMP/win32-kill-tree-report.json` | `.github/workflows/ci.yml` | 08-04 |
| fn | `runLifecycleSpike` *(this plan, temporary)* | `index.ts` | 08-01 |
| type | `SpawnDetached` *(this plan, temporary)* | `index.ts` | 08-01 |
| const | `SPIKE_FIXTURE_SOURCE`, `SPIKE_GRANDCHILD_SOURCE` *(this plan, temporary)* | `index.ts` | 08-01 |
| diagnostics keys | `spikeProcessKillType`, `spikeDetachedGroupKill`, `spikeNote` *(this plan, temporary)* | `index.ts` | 08-01 |

<tasks>

<task type="auto">
  <name>T-08-01: Add the temporary A1 probe to the diagnostics block</name>
  <files>packages/backend/src/index.ts</files>
  <read_first>
    - `packages/backend/src/index.ts` lines 550-600 (`readParentEnv` and its rule that `process` is only ever reached through a guarded `globalThis` cast, never a bare `process.` reference — Caido's `@caido/quickjs-types` declares no `process` global)
    - `packages/backend/src/index.ts` lines 1985-2012 (`SpawnWithEnv` and the required-member reasoning — you are NOT editing this type in this task)
    - `packages/backend/src/index.ts` lines 2570-2683 (`spawnAndWait`: the never-rejects contract, the synchronous-throw `try`/`catch`, the `spawned` discriminator, and the fact that it has no timeout)
    - `packages/backend/src/index.ts` lines 4908-4990 (`getDiagnostics`, its `await getCachedNodeExecutable()` at ~4934, and the T-04-04 rule at ~4950: every field is a version string, a path Drift itself constructed, a boolean or an integer — never an enumerated environment value)
    - `.planning/phases/08-process-lifecycle/08-RESEARCH.md` § *Q1*, § *Q2*, § *Q5*, § *What is still genuinely unknown*, and the Assumptions Log rows A1 and A6
    - `CLAUDE.md` § *Backend Constraints (QuickJS Runtime)* and § *Anti-Patterns → RPC-during-await event loop starvation*
  </read_first>
  <action>
Add ONE temporary, clearly-fenced section to `packages/backend/src/index.ts`, delimited by the ASCII
box header `// ── TEMPORARY: Phase 8 Wave-0 spike (A1) — DELETED BY T-08-03 ──────` and closed by a
matching `// ── end TEMPORARY Phase 8 Wave-0 spike ─────────────────────────────` line, so T-08-03 can
remove it by locating exactly two markers.

Inside that section declare, and nothing outside it:

1. `type SpawnDetached` — a **local** cast of `spawn`, shaped like the existing `SpawnWithEnv` but with
   `detached: boolean` added. Do NOT touch the real `SpawnWithEnv` type; plan 08-02 owns that edit and
   making it required now would force three unrelated call sites to answer before the tracer exists.
2. `const SPIKE_GRANDCHILD_SOURCE` — the argv element for a `node -e` grandchild that idles for 30000 ms
   and exits on its own. The self-exit is the leak guard: if the group kill fails, nothing survives past
   30 s.
3. `const SPIKE_FIXTURE_SOURCE` — the argv element for a `node -e` fixture parent that spawns the
   grandchild with `require("child_process").spawn`, writes one line of JSON with the two pids under the
   keys `parent` and `grandchild` to stdout, then idles for 30000 ms and exits.
4. `async function runLifecycleSpike(): Promise<Record<string, string>>` returning exactly three keys:
   `spikeProcessKillType`, `spikeDetachedGroupKill`, `spikeNote`.

`runLifecycleSpike`'s behaviour, in order:

- If `host?.platform === "win32"`, return all three keys with the value `"n/a (win32 — POSIX-only probe)"`
  and do nothing else. The probe answers a POSIX question only.
- `spikeProcessKillType`: read `typeof kill` off the guarded `globalThis` cast in exactly the shape
  `readParentEnv` uses (a `globalThis as typeof globalThis & { process?: { kill?: unknown } }` narrowing
  inside `try`/`catch`), and render the resulting `typeof` word. Never a bare `process.` reference.
- Resolve the node binary with `await getCachedNodeExecutable()`. If it is `undefined`, set
  `spikeDetachedGroupKill` to `"skipped (node executable not resolved)"` and return.
- Spawn the fixture through `SpawnDetached` with `detached: true`, `stdio: ["pipe","pipe","pipe"]`,
  `windowsVerbatimArguments: false` and `env: buildSpawnEnv({ parentEnv: readParentEnv(), driftVars: {} })`,
  inside a `try`/`catch` around the spawn call itself — `spawn()` throws synchronously for an unspawnable
  path and a throw here must not escape into an RPC handler (`08-RESEARCH.md` Pitfall 9).
- Capture the two pids from the first `stdout` `data` chunk into closure variables. Do **not** await the
  child's `close`: the fixture is long-lived, and awaiting a long-lived child inside an RPC handler is
  the starvation class CLAUDE.md § *Anti-Patterns* names.
- Yield time by `await`ing `spawnAndWait(nodeExecutable, ["-e", <a node -e that idles ~600 ms and exits>])`.
  A short-lived awaited child is the only wait primitive this codebase has evidence for inside an RPC
  handler (`resolveCommand` and `unregisterMcpFromCli` both rely on it); a bare `setTimeout` promise does
  not have that evidence.
- If no pids arrived, set `spikeDetachedGroupKill` to `"inconclusive (no fixture output)"`, then still
  run the cleanup below and return.
- Issue the group signal by spawning the OS utility: `spawnAndWait("kill", ["-TERM", "--", <the negative
  group reference built with String() from the captured parent pid>])`. Do **not** use the runtime's own
  signalling primitive with a negative pid — `08-RESEARCH.md` § *Q2* proves it raises an `Underflow`
  conversion error under Caido's LLRT while working perfectly under Node. Building the argv by hand here
  is acceptable **only** because this whole section is deleted in T-08-03; plan 08-02 moves the rendering
  into `buildKillTreePlan` where a test can reach it.
- Yield ~600 ms again the same way, then probe the grandchild for liveness with the signal-`0` form of
  the runtime's kill (a positive pid, so the `u32` range check in § *Q2* does not apply), wrapped in
  `try`/`catch` — an `EPERM` still throws.
- Set `spikeDetachedGroupKill` to one of exactly three literals: `"grandchild-died (detached honoured)"`,
  `"grandchild-survived (detached NOT honoured)"`, or `"error: <the error's constructor name only>"`.
- Set `spikeNote` to the fixed string `"Phase 8 A1 probe — temporary, removed by T-08-03"`.
- Cleanup, unconditionally, before returning: signal both captured pids individually with the positive-pid
  form inside `try`/`catch`, so a failed experiment leaves nothing running.

Finally, spread `await runLifecycleSpike()` into `getDiagnostics`' returned record, immediately after
the existing `runtimeProbe` spread.

**T-04-04 compliance is not optional here.** All three values are fixed literals or a `typeof` word.
Never render a pid, a path, an environment value, or an error `message` — the constructor name only.
  </action>
  <verify>
    <automated>pnpm -r typecheck && pnpm lint && pnpm exec vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'TEMPORARY: Phase 8 Wave-0 spike (A1) — DELETED BY T-08-03' packages/backend/src/index.ts` returns `1`
    - `grep -c 'end TEMPORARY Phase 8 Wave-0 spike' packages/backend/src/index.ts` returns `1`
    - `grep -c 'runLifecycleSpike' packages/backend/src/index.ts` returns `2` (the declaration and the single `getDiagnostics` spread)
    - `grep -c 'spikeDetachedGroupKill' packages/backend/src/index.ts` returns at least `4` (the win32 arm, the two verdict literals, the inconclusive arm)
    - `grep -c 'detached: boolean' packages/backend/src/index.ts` returns `1` — the temporary `SpawnDetached` alias only; the real `SpawnWithEnv` is untouched in this plan
    - `git diff --stat HEAD -- packages/backend/src/index.ts` shows `index.ts` as the only changed file
    - `pnpm -r typecheck` exits 0, `pnpm lint` exits 0, `pnpm exec vitest run` reports **534 tests, 528 passed, 6 skipped** — unchanged from the `08-VALIDATION.md` baseline, because this task adds no test and must remove none
  </acceptance_criteria>
  <done>`getDiagnostics` returns three additional `spike*` fields on macOS/Linux; the typecheck, lint and full suite are all green; the baseline test count is unchanged.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>T-08-02: Run the spike on a real Caido install and report A1, A6 and the LIF-02 control</name>
  <files>none — this task produces readings, not edits</files>
  <read_first>
    - `.planning/phases/08-process-lifecycle/08-RESEARCH.md` Assumptions Log rows **A1** and **A6**, and § *Manual-Only Verifications*
    - `.planning/phases/08-process-lifecycle/08-VALIDATION.md` § *Wave 0 Requirements* item 5
  </read_first>
  <what-built>
T-08-01 added a temporary, fenced diagnostics probe to `packages/backend/src/index.ts` that reports three
fields through Drift's existing Copy-diagnostics action: `spikeProcessKillType`, `spikeDetachedGroupKill`
and `spikeNote`. The probe spawns a fixture parent with `detached: true` from Caido's own LLRT backend,
has that parent spawn a grandchild, signals the resulting process group by spawning the OS `kill` utility,
and then reports whether the grandchild died. Nothing else in the plugin changed and no test was added or
removed. The probe is deleted again in T-08-03.
  </what-built>
  <how-to-verify>
**Setup**
1. `pnpm build`
2. Install `dist/plugin_package.zip` into Caido (or point Caido at the dev build) and restart Caido.
3. Note the Caido version string from Caido's own About/Settings — it goes into `08-SPIKE.md` because
   assumption A1 is a claim about *the fork this specific Caido ships*.

**Reading 1 and 2 — A1 (does the shipped LLRT honour `detached`?)**
4. Open Drift → Settings → the Copy-diagnostics action, and paste the three `spike*` fields.
   - `spikeProcessKillType` — expected `"function"`. Anything else means the liveness probe in
     `08-RESEARCH.md` § *Q5* is not available and plan 08-03's deferred-rung guard needs a redesign.
   - `spikeDetachedGroupKill` — **this is A1.** `"grandchild-died (detached honoured)"` closes A1
     favourably and the phase proceeds as planned. `"grandchild-survived (detached NOT honoured)"`
     falsifies the phase's POSIX mechanism: STOP, do not run plan 08-02, and re-plan.
     `"inconclusive"` or `"error: …"` — report it verbatim and stop; an unclear A1 is not a pass.

**Reading 3 and 4 — A6 (is the CLI's MCP child in the CLI's process group?)**
5. Start a Claude Code chat turn in Drift with the MCP attached, and while it is still running, in a
   terminal: `ps -eo pid,ppid,pgid,comm | grep -E 'mcp-server|claude'`
6. Report two numbers: the **pgid** of the `node …mcp-server.mjs` row, and the **pid** of the `claude`
   row. A6 is closed favourably when they are equal — the MCP child sits in the CLI's group, so a group
   signal reaches it. If they differ, the CLI calls `setsid()` on its own child and LIF-02 is **not**
   closed by process groups alone: report it and stop.

**Reading 5 — the LIF-02 control, on today's shipped code**
7. With that same turn still running, run `pgrep -f mcp-server.mjs | wc -l` and note the count.
8. Click Stop in the Drift chat. Wait ~3 seconds. Run `pgrep -f mcp-server.mjs | wc -l` again.
9. Report both counts. A non-zero count **after** Stop is the LIF-02 defect reproduced on real hardware
   — the measurement plan 08-05's closing checkpoint will be compared against.
10. Clean up any survivor by pid before continuing.

Do not summarise, interpret or round any of the five readings. Record what the terminal said.
  </how-to-verify>
  <resume-signal>
Reply with the five readings and the Caido version string. Reply `approved` only when
`spikeDetachedGroupKill` reads `grandchild-died (detached honoured)` **and** the `mcp-server.mjs` pgid
equals the `claude` pid — those two together close A1 and A6 and release plan 08-02. Any other value,
including `inconclusive` or an `error:` verdict, is a **halt**: report it verbatim and the phase is
re-planned rather than continued.
  </resume-signal>
  <action>
Stop and hand control to the maintainer. This runs on the maintainer's own macOS Caido install — the
only vehicle in this project that executes Caido's LLRT, and the only one that executes a real provider
CLI binary. Do not proceed to plan 08-02 until all five readings are back and both assumptions are
closed favourably. Present the `<how-to-verify>` steps exactly as written and collect the readings
verbatim.
  </action>
  <verify>
    <human-check>All five readings are reported verbatim: `spikeProcessKillType`, `spikeDetachedGroupKill`, the `mcp-server.mjs` pgid, the `claude` pid, and the two `pgrep` counts (before and after Stop) — plus the Caido version string.</human-check>
    <automated>test -f .planning/phases/08-process-lifecycle/08-SPIKE.md || echo "PENDING — T-08-03 writes this file from the readings collected here"</automated>
  </verify>
  <acceptance_criteria>
    - Five readings plus the Caido version string are in hand, each as the literal value observed
    - `spikeDetachedGroupKill` is one of the three declared literals — an unclear or errored value halts the phase rather than being interpreted
    - If A1 is falsified (`grandchild-survived`) or A6 is falsified (pgid ≠ CLI pid), execution stops here and the phase is re-planned; the plan does NOT proceed to 08-02 on a negative reading
  </acceptance_criteria>
  <done>The maintainer has reported all five readings and the Caido version, and either A1 and A6 are both closed favourably (proceed) or the phase is halted for re-planning.</done>
</task>

<task type="auto">
  <name>T-08-03: Record the measurements in 08-SPIKE.md and delete every temporary symbol</name>
  <files>.planning/phases/08-process-lifecycle/08-SPIKE.md, packages/backend/src/index.ts</files>
  <read_first>
    - `packages/backend/src/index.ts` — the two `TEMPORARY: Phase 8 Wave-0 spike` marker lines added by T-08-01, and the `getDiagnostics` spread site
    - `packages/backend/src/spawn-plan.ts` lines 40-80 — the house measurement-recording style: the measured value, what was expected before it, and the run URL or host that produced it, kept in place because deleting the falsified expectation would hide the measurement
    - `.planning/phases/08-process-lifecycle/08-RESEARCH.md` Assumptions Log (the A1/A6 rows this file is answering)
  </read_first>
  <action>
Write `.planning/phases/08-process-lifecycle/08-SPIKE.md` with these sections, in this order:

- Front matter: `phase: 8`, `slug: process-lifecycle`, `measured: <the date>`, `vehicle: "Caido <version> on darwin, real install"`.
- `# Phase 8 Wave-0 spike — A1 and A6, measured on real hardware`
- A one-paragraph statement of why this file exists: A1 and A6 are the two highest-risk assumptions in
  `08-RESEARCH.md`, neither is closable by CI because every CI leg runs Node and none executes a provider
  CLI, and this was the cheapest available closure.
- `## A1 — does the shipped Caido LLRT honour `detached: true`?` with a table carrying the Caido version,
  `spikeProcessKillType`, `spikeDetachedGroupKill`, and a one-line verdict of `CLOSED — honoured`,
  `CLOSED — NOT honoured` or `OPEN — inconclusive`.
- `## A6 — is the CLI's MCP child in the CLI's process group?` with the observed `mcp-server.mjs` pgid,
  the `claude` pid, whether they matched, and the verdict line.
- `## Control — the LIF-02 defect, reproduced` with the two `pgrep -f mcp-server.mjs` counts (before Stop
  and after Stop) and the sentence naming the after-count as the number plan 08-05's closing
  human-verify must drive to zero.
- `## What this spike does NOT close` — carry forward, unsoftened: it measured **one** Caido build on
  **one** platform with **one** provider CLI; it says nothing about Windows, nothing about Gemini, Codex
  or Copilot, and nothing about a future Caido that rebases its LLRT fork. Name `08-VALIDATION.md`
  § *Vehicle caveat* as the standing list.

Then remove **all** spike code from `packages/backend/src/index.ts`: everything between and including
the two `TEMPORARY: Phase 8 Wave-0 spike` marker lines, plus the `await runLifecycleSpike()` spread in
`getDiagnostics`. Nothing else in that function changes. The `SpawnDetached` alias,
`SPIKE_FIXTURE_SOURCE`, `SPIKE_GRANDCHILD_SOURCE` and the three `spike*` diagnostics keys all go with it.

Leave a single one-line breadcrumb comment at the former section's location, of the form
`// Phase 8 Wave-0 spike (A1/A6) ran here and was removed; readings in 08-SPIKE.md.` so a future reader
finds the measurement rather than re-deriving it.
  </action>
  <verify>
    <automated>test "$(grep -c 'runLifecycleSpike\|SPIKE_FIXTURE_SOURCE\|SPIKE_GRANDCHILD_SOURCE\|spikeDetachedGroupKill\|spikeProcessKillType\|SpawnDetached' packages/backend/src/index.ts)" = "0" && test -f .planning/phases/08-process-lifecycle/08-SPIKE.md && pnpm -r typecheck && pnpm lint && pnpm exec vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'runLifecycleSpike' packages/backend/src/index.ts` returns `0`
    - `grep -c 'TEMPORARY: Phase 8 Wave-0 spike' packages/backend/src/index.ts` returns `0`
    - `grep -c 'detached' packages/backend/src/index.ts` returns `0` — `detached` re-enters the file only in plan 08-02
    - `grep -c 'Phase 8 Wave-0 spike (A1/A6) ran here' packages/backend/src/index.ts` returns `1`
    - `git diff --stat <the commit before T-08-01> -- packages/backend/src/index.ts` shows only the one breadcrumb line as a net change
    - `.planning/phases/08-process-lifecycle/08-SPIKE.md` contains the literal headings `## A1`, `## A6`, `## Control` and `## What this spike does NOT close`
    - `08-SPIKE.md` contains a Caido version string and two `pgrep` counts
    - `pnpm exec vitest run` reports 534 tests, 528 passed, 6 skipped — the baseline, restored exactly
  </acceptance_criteria>
  <done>`08-SPIKE.md` records all five readings with their vehicle and date; `packages/backend/src/index.ts` is byte-equivalent to its pre-plan state apart from one breadcrumb comment; typecheck, lint and the full suite are green at the baseline count.</done>
</task>

</tasks>

<threat_model>
ASVS L1. Blocking threshold: `high`. Source: `08-RESEARCH.md` § *Security Domain*.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Caido backend (LLRT) → spawned child | Drift hands a command and an environment block to the OS. The environment carries `CAIDO_TOKEN` on the production path; the spike's fixture deliberately carries no DRIFT vars. |
| Backend → `getDiagnostics` → clipboard | A user-triggered copy that leaves the machine in bug reports. Anything rendered here is effectively published. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-05 | Information Disclosure | `runLifecycleSpike` → `getDiagnostics` (index.ts) | medium | mitigate | Three fixed literals and one `typeof` word. No pid, no path, no environment value, no error `message` — constructor name only. Enforced by the T-04-04 rule already governing every other field in that record. |
| T-08-09 | Denial of Service | the spike's fixture parent + grandchild | low | mitigate | Both fixture processes self-exit after 30 s, and `runLifecycleSpike` signals both captured pids individually before returning, on every path including the inconclusive and error arms. A failed experiment leaks nothing. |
| T-08-10 | Tampering | temporary diagnostics code surviving into the shipped tree | medium | mitigate | Two ASCII marker lines fence the whole section; T-08-03's acceptance criteria grep for `0` occurrences of every temporary symbol and for a byte-equivalent `index.ts` diff. Removal is a gated task, not a note. |
| T-08-06 | Repudiation (silent control failure) | the spike's group-kill argv | high | mitigate | The spike spawns the OS `kill` utility rather than the runtime's own negative-pid signalling form, which `08-RESEARCH.md` § *Q2* proves raises an `Underflow` conversion error under Caido's LLRT while passing under Node. Using the broken spelling here would make the probe report a false negative for A1 — the exact inversion the probe exists to prevent. |
| T-08-SC | Tampering | npm/pip/cargo installs | n/a | accept | **This phase installs zero packages.** `08-RESEARCH.md` § *Package Legitimacy Audit* is present and empty ("audited, empty", not "skipped"). No install task exists in any Phase 8 plan, so no legitimacy checkpoint is required. If a later plan proposes a dependency, the gate must be run at that point. |
</threat_model>

<verification>
- `pnpm -r typecheck` — exits 0 after every task
- `pnpm lint` — exits 0 after every task (`eslint . --max-warnings 0`)
- `pnpm exec vitest run` — 534 tests, 528 passed, 6 skipped, both before and after this plan
- `git diff --stat 1b3fde6 -- packages/backend/src/index.ts` at plan close — one breadcrumb comment line
- `.planning/phases/08-process-lifecycle/08-SPIKE.md` exists and carries the A1, A6 and control readings
</verification>

<success_criteria>
- A1 is closed with a literal reading from a real Caido install, or the phase is halted
- A6 is closed with two real pgid/pid numbers from a live provider turn, or the phase is halted
- The LIF-02 defect is reproduced on real hardware and its `pgrep` count recorded
- Zero spike code remains in `packages/backend/src/index.ts`
- The test count is unchanged at 534 with zero tests removed (CMP-01 baseline preserved)
</success_criteria>

<output>
Create `.planning/phases/08-process-lifecycle/08-01-SUMMARY.md` when done, and carry the A1 and A6
verdict lines forward verbatim — plan 08-02 task T-08-04 copies them into `kill-plan.ts`'s module header.
</output>

