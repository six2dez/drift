---
phase: 08-process-lifecycle
plan: 14
type: execute
wave: 7
gap_closure: true
depends_on: ["08-11"]
files_modified:
  - .planning/WINDOWS.md
  - .planning/STATE.md
  - .planning/phases/08-process-lifecycle/08-UAT.md
  - .planning/phases/08-process-lifecycle/08-VALIDATION.md
  - .planning/phases/08-process-lifecycle/08-SECURITY.md
autonomous: true
requirements: [LIF-01, LIF-02]

estimate:
  tokens: 66000
  raw_tokens: 66000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Broken-windows ledger entry 11 states A1's verdict as withdrawn in BOTH of its representations — the markdown table row and the JSON object — and the two representations carry the same description text."
    - "Entry 11's superseded 2026-08-27 description is preserved verbatim in the ledger's `## Marked corrections` section, following the convention that section already establishes for a machine-readable register that cannot carry a block quote."
    - "Entry 11's status stays `open`: A1's retraction narrows nothing and closes nothing, and the entry's original concern — that the nine POSIX termination sites rest on unexecuted source analysis — is live again."
    - "A new ledger entry records the A1 probe's unfalsifiable verdict line as a distinct defect from the twelve vacuous gates already recorded, naming what made it different: it produced a reading rather than a green tick, which is why it propagated instead of being noticed."
    - "The ledger's counts (`open_count`, `fixed_count`, `total_count`) agree with the number of rows in each status after every edit, and the markdown table and the JSON array carry the same number of entries."
    - "`08-UAT.md` test 1 no longer reads `result: pass`; it records the reading that was taken, the verdict that was drawn from it, and the retraction, with the original result preserved."
    - "`08-UAT.md` test 2's A6 result, its pid table and its `severity: major` are byte-unchanged."
    - "`.planning/STATE.md`, `08-VALIDATION.md` and `08-SECURITY.md` carry no live claim that A1 was measured, closed or corroborated by execution."
    - "`08-SECURITY.md`'s threat register and its nine accepted residuals AR-01 through AR-09 keep every column populated; no residual loses its owner and no threat changes disposition as a side effect of the A1 correction."
    - "`08-VALIDATION.md`'s `nyquist_compliant: false` and its stated reasons are unchanged or strengthened, never relaxed."
  artifacts:
    - ".planning/WINDOWS.md — entry 11 corrected in both representations; a new entry for the unfalsifiable probe verdict"
    - ".planning/STATE.md — the A1 half of the Phase 8 decision record corrected"
    - ".planning/phases/08-process-lifecycle/08-UAT.md — test 1's result and evidence corrected"
    - ".planning/phases/08-process-lifecycle/08-VALIDATION.md — the A1 rows of the vehicle-caveat and validation tables corrected"
    - ".planning/phases/08-process-lifecycle/08-SECURITY.md — the A1 paragraph corrected; register and residuals untouched"
  key_links:
    - "ledger entry 11 markdown row ↔ ledger entry 11 JSON object — one description, two representations, which is the specific way this file goes silently wrong"
    - "the new ledger entry → the twelve vacuous gates already recorded in entries 16-19 and in the phase's SUMMARYs, as the same family with a different failure surface"
    - "`08-UAT.md` test 1 → `08-SPIKE.md`'s corrected A1 table — the UAT record and the spike record must agree"
  prohibitions:
    - statement: "No gate-output check is written as a bare `verdict-gate.sh | grep -c <file> | grep -qx 0`. That shape is satisfied by an EMPTY match set: a gate that syntax-errors, aborts early under `set -u`, or whose ERE matches nothing prints nothing, the count is 0, and the check goes green — the vacuous-gate defect this whole round exists to eliminate, in the plans that fix it. Every gate-output check in this plan captures the gate output ONCE and asserts, against that SAME capture, both LIVENESS (exactly three `== ARM ` header lines, which the gate prints unconditionally because `fail()` sets a flag rather than exiting) AND the target-file absence. Do not copy the weak form back in."
    - statement: "No artifact is rewritten to satisfy the verdict gate. Every correction preserves the superseded text as a dated block quote, or — for the machine-readable ledger, which cannot carry one — in the file's own `## Marked corrections` section."
    - statement: "A6's FALSIFIED verdict, its 2026-08-27 pids and `08-UAT.md` test 2 are not reverted, softened or bundled into the A1 retraction."
    - statement: "No threat in `08-SECURITY.md` changes disposition and no accepted residual loses a column as a side effect of a documentation correction. `threats_open` does not move."
    - statement: "No ledger entry is closed, waived or marked fixed by this plan. Correcting an entry's description is not the same as resolving it."
---

<objective>
Four planning documents and one machine-readable register still assert that A1 was measured.

The register is the interesting one. `.planning/WINDOWS.md` carries every entry TWICE — once as a
markdown table row and once inside a JSON array — and neither representation can hold a block quote,
which is why the file already grew a `## Marked corrections` section when plan 08-10 rewrote entry 11
on 2026-08-27. That section is the convention this plan follows. The failure mode specific to this
file is editing one representation and not the other, which produces a register that reads correctly
to a human and wrongly to a tool.

Entry 11's correction is not a narrowing. The entry originally feared a nine-site POSIX regression
if the shipped LLRT ignores the detached spawn; the 2026-08-27 rewrite declared that fear resolved.
With the reading withdrawn, that fear is live again, and the entry must say so rather than sitting in
a middle state that reads as partial progress.

Purpose: gap 1 `missing` item 2 for the ledger, the project state file, and the three phase
documents `verdict-gate.sh` will still be naming after plans 08-11, 08-12 and 08-13.
Output: a register whose two representations agree, and three phase documents whose A1 rows say what
the instrument could see.

WHERE THIS PLAN SITS. It is one of THREE wave-7 carrier plans (08-12, 08-13, 08-14) that run in
parallel behind 08-11. It therefore clears its OWN five files from the gate's ARM A output and
asserts exactly that — never that the gate exits 0, which it cannot know, because its siblings' files
may still be named when it finishes. Plan 08-15 depends on all four carrier plans and is where the
gate goes green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/WINDOWS.md
@CLAUDE.md

@.planning/phases/08-process-lifecycle/08-VERIFICATION.md
</context>

## Artifacts this phase produces

This plan creates no code symbols. It creates one new broken-windows ledger entry (id assigned by
`gsd-tools windows append`, expected 20) and adds marked-correction blocks to five existing planning
documents.

Symbols created by **sibling plans in this gap-closure set**, listed so a source-grounding pass does
not flag them: `STALE_VERDICT_ERE`, `READING_ERE`, `RETRACTION_TOKEN`, `SUMMARY_KNOWN_UNPINNED`,
`requires_retraction` (08-11, `verdict-gate.sh`); `classifyLivenessObservation`,
`buildLivenessProbePlan`, `formatSpikeVerdict`, `LivenessVerdict`, `LivenessObservation` (08-12,
`kill-plan.ts`); `lastOrphanReap`, `recordOrphanReapOutcome` (08-15, `index.ts`);
`.planning/phases/08-process-lifecycle/a1-probe-fix.patch` (08-15, new file).

<tasks>

<task type="auto">
  <name>Task 1: Correct ledger entry 11 in both representations, and record the unfalsifiable probe verdict as its own entry</name>

  <read_first>
    - `.planning/WINDOWS.md` in full — the frontmatter counts, the markdown table (19 rows), the
      fenced JSON array (19 objects), the hazard note, and the `## Marked corrections` section at the
      foot, whose entry-11 block is the exact convention this task extends.
    - Entries 12, 13, 14, 15, 16, 17, 18 and 19 specifically — 13/14/15 record the reap's unrun-verify
      residuals and 16-19 record four self-reported vacuous gates. The new entry must sit alongside
      16-19 as the same family, and must not duplicate 14 or 15.
    - `.planning/phases/08-process-lifecycle/08-VERIFICATION.md` — gap 1's `reason`, the
      § *Anti-Patterns Found* row for the probe, and § *On the twelve self-reported vacuous gates*.
  </read_first>

  <files>.planning/WINDOWS.md</files>

  <action>
    ENTRY 11, BOTH REPRESENTATIONS. Rewrite the `description` so it states A1's verdict as withdrawn
    and states the consequence in the entry's own terms: the nine-site POSIX concern this entry was
    opened for is live again, because whether the shipped runtime honours the detached spawn is
    unmeasured. Keep the A6 half of the description exactly as it is — its pids, its verdict word and
    its conclusion about the argv-marker reap are all unaffected. Keep the `WHAT KEEPS THIS ENTRY
    OPEN` clause and extend it with the A1 re-run. Update the `reason` column to match. Leave
    `status: open`, `recorded_at` and `resolved_at` untouched.

    Apply the identical text to the JSON object with the same id. The two representations are the
    specific way this file goes wrong: they are written by hand, they carry the same string twice,
    and a divergence is invisible to a reader of either one. Verify they match after the edit by
    extracting both and comparing them mechanically, not by eye.

    Append a new block to `## Marked corrections` preserving entry 11's 2026-08-27 `description` and
    `reason` verbatim, in the same nested-block-quote form the existing entry-11 block uses for the
    2026-08-24 text. Do not disturb the existing block; the file now carries two supersessions for
    one entry and both must survive.

    THE NEW ENTRY. Use `gsd-tools windows append` — never hand-edit the table, the JSON array and the
    counts, because that is three places to get wrong and the tool writes all three. Invoke the tool
    with no arguments first to have it enumerate its required flags, then append an entry of kind
    `deviation` for phase `08` against `packages/backend/src/index.ts`, whose description records:
    the probe at the recoverable commit decided A1's verdict from an optional call on a primitive the
    same diagnostics run measured absent, coalesced the absent case to "not alive", and therefore
    emitted the favourable string unconditionally; the red input did not exist. Say what makes this
    instance different from the twelve already recorded — it produced a READING rather than a green
    tick, which is why it propagated into eleven carriers, the ROADMAP, REQUIREMENTS.md and this
    ledger's own entry 11 instead of being caught. Name the fix that shipped
    (`classifyLivenessObservation` in `kill-plan.ts`, three-valued, plan 08-12) and name what still
    closes the entry (the re-run on real hardware, plan 08-17). Leave it `open`.

    Do not close, waive or mark fixed any entry. Entries 8, 11, 12, 13, 14, 15, 16, 17 and 19 stay
    open; entry 18 stays open. Correcting a description is not resolving a defect.
  </action>

  <verify>
    <automated>cd /Users/six2dez/Tools/drift &amp;&amp; GATE_OUT="$(bash .planning/phases/08-process-lifecycle/verdict-gate.sh 2>&amp;1 || true)" &amp;&amp; printf '%s\n' "$GATE_OUT" | grep -c '^== ARM ' | grep -qx 3 &amp;&amp; printf '%s\n' "$GATE_OUT" | grep -Ec '\.planning/WINDOWS\.md' | grep -qx 0</automated>
    <automated>cd /Users/six2dez/Tools/drift &amp;&amp; node "$HOME/.claude/gsd-core/bin/gsd-tools.cjs" windows status</automated>
    <automated>cd /Users/six2dez/Tools/drift &amp;&amp; test "$(grep -c '^| [0-9]* |' .planning/WINDOWS.md)" = "$(grep -c '\"id\":' .planning/WINDOWS.md)"</automated>
  </verify>

  <acceptance_criteria>
    - `bash .planning/phases/08-process-lifecycle/verdict-gate.sh` names no hit in `.planning/WINDOWS.md`.
    - The markdown table row count equals the JSON object count, and both are 20 after the append.
    - Entry 11's `description` string is BYTE-IDENTICAL between the markdown row and the JSON object.
      Prove it by extracting both programmatically and diffing them; paste the (empty) diff.
    - Entry 11 states that the nine-site POSIX concern is live again, keeps its A6 half unchanged
      including the pids 43921 / 43752 / 44284, and keeps `status: open`.
    - `## Marked corrections` now contains two preserved supersessions for entry 11 — the 2026-08-24
      one that was already there and the 2026-08-27 one this task adds — and neither is altered.
    - The new entry was created by `gsd-tools windows append`, not by hand. Paste the command and its
      output. The frontmatter `open_count`, `fixed_count` and `total_count` are consistent with the
      rows after the append; state all three before and after.
    - `gsd-tools windows status` runs without error and reports the new entry.
    - No entry's `status` changed from `open` to anything else. State the open-entry id list before
      and after; it must gain exactly one id and lose none.
  </acceptance_criteria>

  <done>
    The register's two representations agree, entry 11 states the live risk rather than a resolved
    one, and the defect that caused this whole gap-closure round is recorded as its own broken window
    with a named fix and a named closing condition.
  </done>
</task>

<task type="auto">
  <name>Task 2: Correct STATE.md's Phase 8 record and 08-UAT.md test 1</name>

  <read_first>
    - `.planning/STATE.md` — the Phase 8 decision/finding bullet that records the 2026-08-27
      measurement, plus the frontmatter `status` and `last_activity_desc` fields, and the
      `## Current Position` block which still cites the superseded `human_needed` verdict.
    - `.planning/phases/08-process-lifecycle/08-UAT.md` — the frontmatter, test 1 in full (its
      `expected`, `result`, `reported` and `evidence` fields), test 2 in full, the § *Summary* counts,
      and the § *The root cause behind G-01 and G-02* table, which already records
      `typeof process.kill` as `undefined` five lines from where test 1 draws its verdict.
    - `.planning/phases/08-process-lifecycle/08-VERIFICATION.md` — gap 1's `artifacts` entry for
      `08-UAT.md`.
  </read_first>

  <files>.planning/STATE.md, .planning/phases/08-process-lifecycle/08-UAT.md</files>

  <action>
    STATE.md. The Phase 8 bullet asserts A1 closed favourably and draws the conclusion that the
    nine-site POSIX regression cannot happen. Correct it as a dated marked correction preserving the
    2026-08-27 text as a block quote. Keep the A6 half — pids, verdict, the argv-marker conclusion —
    exactly as it is; keep the `STILL OPEN` clause and extend it with the A1 re-run. Update the
    `## Current Position` block and the frontmatter `status` / `last_activity_desc` to cite
    `08-VERIFICATION.md`'s current `gaps_found` verdict rather than the superseded `human_needed`
    one, and state the score the current report gives.

    08-UAT.md test 1. This is the record that first turned the reading into a verdict, and its own
    file contains the contradiction four sections above it: the § *root cause* table records
    `typeof process.kill` as `undefined` from the same probe run that test 1 reads as a pass.

    Change `result: pass` to `result: issue`, matching the vocabulary test 2 already uses for a
    finding rather than a failure. Keep the `reported` block byte-identical — those three field
    values are what the instrument printed and they are the evidence, not the error. Rewrite
    `evidence` to state the mechanism: the probe determined liveness with an optional call on the
    primitive the same run measured absent, coalesced the absent case to "not alive", and emitted the
    favourable string unconditionally, so the reading carries no information about whether the
    grandchild died. Preserve the 2026-08-27 `evidence` text verbatim as a block quote below the new
    one. Add a `severity: major` field matching test 2's form, and a pointer to where the fixed
    three-valued determination lives and which plan re-runs it.

    Add one sentence to the § *root cause* section noting that the same reading it tabulates —
    `typeof process.kill` absent — is what invalidates test 1's verdict, and that nobody joined the
    two facts at the time. That is the finding, and the file is where it happened.

    Update the § *Summary* counts to match the tests' actual states after this edit. Do not touch
    test 2, its pid table, its severity, its evidence, or the G-01/G-02/G-04/G-05 gap entries.
  </action>

  <verify>
    <automated>cd /Users/six2dez/Tools/drift &amp;&amp; GATE_OUT="$(bash .planning/phases/08-process-lifecycle/verdict-gate.sh 2>&amp;1 || true)" &amp;&amp; printf '%s\n' "$GATE_OUT" | grep -c '^== ARM ' | grep -qx 3 &amp;&amp; printf '%s\n' "$GATE_OUT" | grep -Ec '\.planning/STATE\.md|08-UAT\.md' | grep -qx 0</automated>
    <automated>cd /Users/six2dez/Tools/drift &amp;&amp; node "$HOME/.claude/gsd-core/bin/gsd-tools.cjs" query state.load &gt;/dev/null</automated>
  </verify>

  <acceptance_criteria>
    - `bash .planning/phases/08-process-lifecycle/verdict-gate.sh` names neither `.planning/STATE.md`
      nor `08-UAT.md`.
    - `gsd-tools query state.load` parses `STATE.md` without error after the edit.
    - `08-UAT.md` test 1's `reported` block is byte-identical to its pre-edit content — show it has no
      diff hunk. Its `result` is no longer `pass`.
    - The 2026-08-27 `evidence` text for test 1 is preserved verbatim as a block quote.
    - Test 2 is byte-unchanged: no diff hunk anywhere between its heading and the next heading.
    - The § *Summary* counts equal the actual per-test states; recount them from the file rather than
      adjusting the old numbers arithmetically, and show the recount.
    - `STATE.md`'s `## Current Position` and frontmatter cite `gaps_found` and the current score.
    - Every superseded sentence in both files is preserved as a dated block quote; account for every
      deleted line in both diffs.
  </acceptance_criteria>

  <done>
    The project state file and the UAT record agree with the verification report, and the UAT file
    names the contradiction it was carrying between two of its own sections.
  </done>
</task>

<task type="auto">
  <name>Task 3: Correct 08-VALIDATION.md and 08-SECURITY.md without disturbing the register or the residuals</name>

  <read_first>
    - `.planning/phases/08-process-lifecycle/08-VALIDATION.md` — the A1 bullet in its findings
      section, the row of the vehicle-caveat / assumption table whose status cell records the
      2026-08-27 run, the § *Vehicle caveat* list itself, and the frontmatter including
      `nyquist_compliant`.
    - `.planning/phases/08-process-lifecycle/08-SECURITY.md` — the A1 paragraph, § *G-02 — recorded,
      not fixed* (which traces the same absent primitive correctly and in detail, and is the contrast
      case worth preserving intact), the threat register's `threats_open` count, and the accepted
      residuals AR-01 through AR-09 with their seven columns.
    - `.planning/phases/08-process-lifecycle/08-VERIFICATION.md` — the § *Required Artifacts* rows
      for both files, which grade `08-SECURITY.md` as the strongest artifact in the phase.
  </read_first>

  <files>.planning/phases/08-process-lifecycle/08-VALIDATION.md, .planning/phases/08-process-lifecycle/08-SECURITY.md</files>

  <action>
    Drive both edits from the gate's output rather than from any count written down anywhere. Run it,
    take the hit list for these two files, correct each hit.

    08-VALIDATION.md. The A1 bullet and the assumption-table row both record the 2026-08-27 run as
    having closed A1. Correct both as dated marked corrections preserving the superseded cells — the
    table row already carries one preserved cell from 2026-08-24 in parentheses, so follow that exact
    form and add a second. In § *Vehicle caveat*, item 2 was recorded as partly discharged by the
    2026-08-27 run; restore it to fully undischarged and say why, keeping the note that the
    kill-primitive half is moot because the sandbox exposes no such function. Item 3 stays as a
    negative measurement — A6 was really measured and really came back false. `nyquist_compliant:
    false` and its stated reasons stay; if anything, add the A1 re-run to the reasons. Do not relax
    any of them.

    08-SECURITY.md. Correct only the A1 paragraph, as a dated marked correction with the superseded
    text preserved. Everything else in this file is graded as its strongest artifact and must come
    through untouched: the threat register, `threats_open`, the one accepted high with its named
    decider, and all nine residuals with all seven of their columns. § *G-02 — recorded, not fixed*
    is the passage that traced this same absent primitive correctly, and it is the contrast case the
    verification report singles out — do not edit it, and add one sentence beside the corrected A1
    paragraph pointing at it, noting that the analysis that would have caught this was already in the
    file.

    Consider whether the A1 retraction changes any threat's disposition. It does not — every threat
    in the register was written against a mechanism, not against A1's verdict — but check each
    disposition rather than assuming, and state in the SUMMARY that you checked and what you found.
    If a disposition genuinely does depend on A1, report it as a finding and do not change it
    unilaterally.
  </action>

  <verify>
    <automated>cd /Users/six2dez/Tools/drift &amp;&amp; GATE_OUT="$(bash .planning/phases/08-process-lifecycle/verdict-gate.sh 2>&amp;1 || true)" &amp;&amp; printf '%s\n' "$GATE_OUT" | grep -c '^== ARM ' | grep -qx 3 &amp;&amp; printf '%s\n' "$GATE_OUT" | grep -Ec '08-VALIDATION\.md|08-SECURITY\.md' | grep -qx 0</automated>
    <automated>cd /Users/six2dez/Tools/drift &amp;&amp; grep -c 'threats_open: 0' .planning/phases/08-process-lifecycle/08-SECURITY.md | grep -qvx 0</automated>
    <automated>cd /Users/six2dez/Tools/drift &amp;&amp; grep -cE 'AR-0[1-9]' .planning/phases/08-process-lifecycle/08-SECURITY.md | grep -qvx 0</automated>
  </verify>

  <acceptance_criteria>
    - `bash .planning/phases/08-process-lifecycle/verdict-gate.sh` names neither `08-VALIDATION.md`
      nor `08-SECURITY.md`, measured against a SINGLE captured run that also carries all three
      `== ARM ` header lines. Paste the full captured output. THIS TASK DOES NOT ASSERT THE GATE
      EXITS 0 and must not be rewritten to: this plan runs in wave 7 ALONGSIDE 08-12 and 08-13, so
      when it finishes their carriers (`packages/backend/src/`, `.planning/ROADMAP.md`,
      `.planning/REQUIREMENTS.md`) may still be named by ARM A. A non-zero overall exit here is
      EXPECTED and is not a failure of this task. Plan 08-15, which depends on all four carrier
      plans, is where the gate goes green and where the green is re-demonstrated against a red input.
    - Report the residual ARM A hit list from the captured run in the SUMMARY, so 08-15 inherits a
      measured starting point rather than an assumption. Every remaining name must belong to 08-12 or
      08-13; a name outside those two plans' `files_modified` is a finding and stops the task.
    - `08-SECURITY.md`'s `threats_open` value is unchanged, all nine residuals AR-01 through AR-09 are
      present, and every one still has a non-empty owner. State the count of residuals and the count
      of non-empty owner cells.
    - § *G-02 — recorded, not fixed* has no diff hunk.
    - `08-VALIDATION.md` still carries `nyquist_compliant: false` with its reasons intact or extended;
      no reason is removed. List the reasons before and after.
    - § *Vehicle caveat* item 2 is restored to fully undischarged; item 3 still records A6 as a
      negative measurement.
    - The SUMMARY states explicitly that every threat disposition was checked against the A1
      retraction and reports the result.
    - Every superseded cell and paragraph is preserved; account for every deleted line in both diffs.
  </acceptance_criteria>

  <done>
    Every living artifact THIS PLAN owns states A1's actual verdict, the gate no longer names either
    of them in a run proven to have executed all three arms, and the two documents this task touched
    kept everything that made them good.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ledger markdown row ↔ ledger JSON object | One description, two hand-maintained representations. A divergence is invisible to a reader of either. |
| `STATE.md` → every GSD command | `state.load` is read by the orchestrators. A malformed edit breaks the workflow rather than the record. |
| `08-SECURITY.md` register → `/gsd-secure-phase` and the seal gate | `threats_open` and the residual columns are read as machine state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-63 | Tampering | `.planning/WINDOWS.md` dual representation | high | mitigate | Editing one representation and not the other yields a register that reads correctly to a human and wrongly to a tool, with no gate covering it today. Mitigated by a byte-identity check between the two entry-11 descriptions extracted programmatically, a row-count equality check between table and JSON, and by requiring the new entry to go through `gsd-tools windows append` rather than three hand edits. |
| T-08-64 | Tampering | `08-SECURITY.md` register and residuals | high | mitigate | A prose correction inside the phase's strongest security artifact is the cheapest way to silently drop a residual owner or flip a disposition. Mitigated by explicit acceptance criteria on `threats_open`, on the residual count and on the non-empty-owner count, by a no-diff-hunk requirement on § *G-02*, and by a written check of every disposition against the retraction. |
| T-08-65 | Repudiation | ledger entry 11's two supersessions | medium | mitigate | The register cannot carry a block quote, so a supersession lives in a separate section and is easy to overwrite with the next one. Mitigated by requiring both the 2026-08-24 and the 2026-08-27 preserved blocks to survive and by accounting for every deleted line. |
| T-08-66 | Denial of service | `.planning/STATE.md` | medium | mitigate | A malformed edit to STATE.md breaks `state.load` and with it every GSD orchestrator. Mitigated by running `state.load` as an automated verify after the edit. |
</threat_model>

<verification>
- `bash .planning/phases/08-process-lifecycle/verdict-gate.sh`, captured once per check, prints all
  three `== ARM ` headers (liveness) and names none of this plan's five files. Its overall exit code
  is NOT asserted here — 08-12 and 08-13 run in the same wave and their carriers may still be named.
  Plan 08-15 owns the exit-0 assertion and the red-input re-demonstration at green.
- `gsd-tools query state.load` parses `STATE.md`.
- `gsd-tools windows status` runs and reports the new entry; table and JSON entry counts are equal.
- `08-UAT.md` test 2 and `08-SECURITY.md` § *G-02* have no diff hunks.
- `git diff --name-only` shows nothing under `packages/`.

<verify>
  <human-check>
    None required. Every criterion in this plan is machine-checkable on this host; recorded so the
    end-of-phase harvest does not invent one.
  </human-check>
</verify>
</verification>

<success_criteria>
- None of this plan's five files presents A1 as measured, proven against a captured gate run that is
  demonstrably live (all three `== ARM ` headers present) rather than silent. The repo-wide exit-0
  claim and the re-demonstration of the gate's red input at green belong to plan 08-15.
- The broken-windows register's two representations agree, and the defect that caused this round is
  itself a recorded broken window with a named fix and a named closing condition.
- Everything that made `08-SECURITY.md` the phase's strongest artifact survives the edit intact.
</success_criteria>

<output>
Create `.planning/phases/08-process-lifecycle/08-14-SUMMARY.md` when done. It MUST include: the
byte-identity diff for entry 11's two representations, the `windows append` command and output, the
open-entry id list before and after, the full captured gate output with its three `== ARM ` headers
and the residual ARM A hit list attributed to 08-12 or 08-13, the disposition check result for every
threat, and the accounting of deleted lines in all five diffs.
</output>
