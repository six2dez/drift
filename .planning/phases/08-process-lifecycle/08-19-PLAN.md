---
phase: 08-process-lifecycle
plan: 19
type: execute
wave: 12
gap_closure: true
depends_on: ["08-18"]
files_modified:
  - .planning/phases/08-process-lifecycle/threat-register-gate.sh
  - .planning/phases/08-process-lifecycle/threat-register-gate.test.sh
  - .planning/phases/08-process-lifecycle/08-SECURITY.md
autonomous: true
requirements: [LIF-01, LIF-02]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "08-SECURITY.md contains exactly one main-register row for every numeric threat T-08-01 through T-08-88 plus T-08-SC, including the complete T-08-51..T-08-78 allocation from Plans 08-11..08-17."
    - "A separate compact T-08-51..T-08-88 roll-up ledger preserves the canonical seven-column main-register header while giving every row a severity, disposition, and non-empty controlled Owner value with a named responsibility."
    - "Every mitigated row added in this roll-up is supported by current source, an executable test/gate, or both; plan prose and historical SUMMARY prose are allocation inputs rather than closure evidence."
    - "The security frontmatter's audited_at_head is the measured current audit baseline, no package-changing commit exists after it, and status/threats_open reflect the re-audited high-severity rows rather than the former d2d502b snapshot."
    - "All nine pre-existing accepted residuals remain visible, the plan-allocated accepts T-08-54/T-08-59/T-08-68/T-08-73 receive explicit residual records, and the existing accepted-high exception is not hidden by a zero-open count."
    - "The standing citation-integrity gate NUL-safely discovers quoted adversarial package/support paths, rejects newline-bearing paths explicitly, and fails on an unregistered live citation, empty discovery, duplicate row, malformed ID, or prose outside the exact `## Threat Register` table."
    - "The production gate and its test remain inside support-artifact discovery, contain no literal intentionally missing/malformed citation, and pass a final self-scan proving runtime-assembled red fixtures cannot trip the live gate by existing in the scripts themselves."
    - "No package, frontend, or shared file is modified; LIF-01/LIF-02 runtime evidence and checkbox state remain unchanged."
  artifacts:
    - path: ".planning/phases/08-process-lifecycle/threat-register-gate.sh"
      provides: "Standing fail-closed census from live T-08 citations to the bounded main security register"
    - path: ".planning/phases/08-process-lifecycle/threat-register-gate.test.sh"
      provides: "Non-vacuous green/red fixture matrix using the production gate implementation"
    - path: ".planning/phases/08-process-lifecycle/08-SECURITY.md"
      provides: "Current-HEAD Phase 08 threat register, evidence ledger, residual register, and recomputed sign-off"
  key_links:
    - from: ".planning/phases/08-process-lifecycle/threat-register-gate.sh"
      to: "packages/"
      via: "Recursive `find -print0` discovery, quoted operands, explicit newline-path rejection, only generated/dependency directories pruned, and hard liveness sentinels for T-08-55/T-08-59/T-08-75"
      pattern: "T-08-[0-9]{2}"
    - from: ".planning/phases/08-process-lifecycle/threat-register-gate.sh"
      to: ".planning/phases/08-process-lifecycle/verify-a1-patch.sh"
      via: "Dynamic Phase 08 support-artifact discovery and T-08-67 liveness sentinel"
      pattern: "T-08-67"
    - from: ".planning/phases/08-process-lifecycle/threat-register-gate.sh"
      to: ".planning/phases/08-process-lifecycle/a1-probe-fix.patch"
      via: "Applied-patch discovery and T-08-76 liveness sentinel"
      pattern: "T-08-76"
    - from: ".planning/phases/08-process-lifecycle/threat-register-gate.sh"
      to: ".planning/phases/08-process-lifecycle/08-SECURITY.md"
      via: "Parsing anchored at the existing exact `## Threat Register` heading and seven-column header through the next level-2 heading, followed by uniqueness/range checks and the citation-to-row join"
      pattern: "T-08-01|T-08-88|T-08-SC"
    - from: ".planning/phases/08-process-lifecycle/08-SECURITY.md"
      to: "packages/backend/src/kill-plan.ts"
      via: "Current source/test evidence for production-cited T-08-55, T-08-59, and T-08-75"
      pattern: "T-08-55|T-08-59|T-08-75"
    - from: ".planning/phases/08-process-lifecycle/08-SECURITY.md"
      to: ".planning/phases/08-process-lifecycle/verify-a1-patch.sh"
      via: "Current executable proof that the T-08-67/T-08-76 probe patch still applies at 68199fa, type-checks, builds, cleans its scratch worktree, and leaves the main package tree unchanged"
      pattern: "verify-a1-patch.sh|T-08-67|T-08-76"
---

<objective>
Re-audit Phase 08 security at the current package head, roll every allocated threat from Plans 08-11..08-19 into the canonical register, and install a non-vacuous standing citation-integrity gate.

Purpose: Close authoritative verification gap 2 without deriving threat closure from plan prose or erasing accepted residual risk.
Output: A current 89-row main register (T-08-01..T-08-88 plus T-08-SC), an owner-bearing T-08-51..T-08-88 evidence ledger, current evidence/disposition totals, and a tested citation-to-register gate.
</objective>

<execution_context>
@/Users/six2dez/.codex/gsd-core/workflows/execute-plan.md
@/Users/six2dez/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-process-lifecycle/08-VERIFICATION.md
@.planning/phases/08-process-lifecycle/08-SECURITY.md
@.planning/phases/08-process-lifecycle/08-SPIKE.md
@.planning/phases/08-process-lifecycle/08-VALIDATION.md
@.planning/phases/08-process-lifecycle/08-UI-SPEC.md
@.planning/phases/08-process-lifecycle/08-PATTERNS.md
@.planning/phases/08-process-lifecycle/a1-probe-fix.patch
@.planning/phases/08-process-lifecycle/verify-a1-patch.sh
@.planning/phases/08-process-lifecycle/verdict-gate.sh
@.planning/phases/08-process-lifecycle/08-11-PLAN.md
@.planning/phases/08-process-lifecycle/08-12-PLAN.md
@.planning/phases/08-process-lifecycle/08-13-PLAN.md
@.planning/phases/08-process-lifecycle/08-14-PLAN.md
@.planning/phases/08-process-lifecycle/08-15-PLAN.md
@.planning/phases/08-process-lifecycle/08-16-PLAN.md
@.planning/phases/08-process-lifecycle/08-17-PLAN.md
@.planning/phases/08-process-lifecycle/08-18-PLAN.md
@.planning/phases/08-process-lifecycle/08-11-SUMMARY.md
@.planning/phases/08-process-lifecycle/08-12-SUMMARY.md
@.planning/phases/08-process-lifecycle/08-13-SUMMARY.md
@.planning/phases/08-process-lifecycle/08-14-SUMMARY.md
@.planning/phases/08-process-lifecycle/08-15-SUMMARY.md
@.planning/phases/08-process-lifecycle/08-16-SUMMARY.md
@.planning/phases/08-process-lifecycle/08-17-SUMMARY.md
@.planning/phases/08-process-lifecycle/08-18-SUMMARY.md
@packages/backend/src/kill-plan.ts
@packages/backend/src/index.ts
@packages/backend/src/index.source.test.ts
@packages/backend/src/kill-plan.test.ts
@packages/backend/src/kill-tree.posix.test.ts
</context>

## Planner assumptions and no-silent-drop backstops

- **LIF-01 / unclassified:** the security audit must retain the native-Windows execution limitation and must not tick LIF-01.
- **LIF-02 / adjacency:** the Plan 08-18 A1 correction remains one current-plus-retraction record; this plan audits it but does not split or reinterpret it.
- **LIF-02 / empty:** citation discovery requires measured package and support-artifact sentinels, NUL-safe pathname enumeration, and explicit newline-path refusal; main-register parsing is anchored to the existing exact `## Threat Register` region and requires the contiguous numeric range plus T-08-SC. An empty or mis-anchored side cannot pass.
- **LIF-02 / ordering:** install and red-demonstrate the citation gate against the stale register first, then populate/re-audit the register, then run the independent fixture matrix and both standing gates green.
- The fallback `single-pid` cue is recorded only as a security/evidence-boundary breadcrumb. It does not authorize another process-lifecycle implementation change.
- `08-UI-SPEC.md` remains unchanged: there are zero authorized edits under `packages/frontend/**` or `packages/shared/**` and no new diagnostic UI.

## Required threat allocation census

The original plan row supplies category, severity, and initial disposition; its matching SUMMARY supplies implementation pointers. The current source/tests/gates determine whether a mitigation is actually closed.

| Allocating plan | IDs that must be rolled up | Current mechanism/evidence family |
|-----------------|----------------------------|-----------------------------------|
| 08-11 | T-08-51..T-08-54 | verdict spelling/discovery red inputs, marked Spike correction, ARM C summary immutability, bounded gate output |
| 08-12 | T-08-55, T-08-57..T-08-59 | bare-name liveness census, total closed classifier, marked source correction, closed diagnostic reason union |
| 08-13 | T-08-60..T-08-62 | ROADMAP criterion amendment, supersession blocks, explicit requirement ownership |
| 08-14 | T-08-63..T-08-66 | WINDOWS dual representation, SECURITY consistency, ledger supersessions, STATE boundedness |
| 08-15 | T-08-67, T-08-68, T-08-75, T-08-76 | patch verification, accepted temporary live-Caido elevation, scalar-only diagnostics, no-HEAD/no-leak probe patch |
| 08-16 | T-08-56, T-08-69..T-08-71 | measured hardware result, redacted process evidence, survivor boundedness, evidence-versus-interpretation separation |
| 08-17 | T-08-72..T-08-74, T-08-77, T-08-78 | unchanged verdict gate until propagation, accepted unsigned local build, explicit owner list, restored build, three-valued A1 recording |
| 08-18 | T-08-79..T-08-83 | current-truth gate, inseparable retraction, exact carrier census, value-free output, ARM C/Spike immutability |
| 08-19 | T-08-84..T-08-88 | complete ID census, current audit head, evidence-backed closure, residual/status integrity, non-vacuous citation gate |

## Artifacts this phase produces

- A standalone `threat-register-gate.sh` that joins live citations only to the bounded table under the existing exact `## Threat Register` heading and exposes a fixture-root/self-test interface without weakening its default live-tree behavior.
- A standalone `threat-register-gate.test.sh` whose red cases use the production discovery/parser functions and never mutate the real repository.
- A current `08-SECURITY.md` roll-up covering T-08-51..T-08-88, all prior rows, T-08-SC, the complete residual ledger, and an auditable package-head measurement.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace one live citation through a fail-closed register gate</name>
  <precondition>Plan 08-18's default verdict gate is green and `08-SECURITY.md` still lacks at least one measured live citation row from the authoritative gap report.</precondition>
  <reversibility rating="costly">The scanner becomes a standing security-record contract; changing its inclusion/exclusion rules later requires coordinated register and fixture updates.</reversibility>
  <read_first>
    - .planning/phases/08-process-lifecycle/08-VERIFICATION.md — authoritative missing-ID measurements and current-head staleness proof
    - .planning/phases/08-process-lifecycle/08-SECURITY.md — bounded main-register format and existing frontmatter/residual structure
    - packages/backend/src/kill-plan.ts — live T-08-55/T-08-59/T-08-75 production citations
    - packages/backend/src/index.ts — live T-08-75 production citation
    - .planning/phases/08-process-lifecycle/verify-a1-patch.sh — live T-08-67 executable support citation
    - .planning/phases/08-process-lifecycle/a1-probe-fix.patch — live T-08-76 applied-patch citation
    - .planning/phases/08-process-lifecycle/verdict-gate.sh — shell fail-closed/output conventions
  </read_first>
  <files>.planning/phases/08-process-lifecycle/threat-register-gate.sh</files>
  <behavior>
    - "A live package citation whose numeric ID has no main-register row fails and reports only the ID plus relative path."
    - "A live Phase 08 shell/executable/applied-patch citation whose ID has no main-register row fails under the same join."
    - "A citation mentioned outside the bounded table under the exact `## Threat Register` heading does not satisfy the register-row requirement."
    - "Before roll-up, the tracer proves it parsed exactly 50 numeric rows plus T-08-SC from the existing `## Threat Register` table before it reports missing live IDs; zero rows or the wrong heading/header is a parser-liveness failure."
    - "No package files, no support artifacts, no parsed register rows, duplicate rows, non-canonical numeric IDs, or a gap in T-08-01..T-08-88 each fail closed."
    - "Package/support filenames containing spaces, tabs, quotes, or glob characters are discovered without splitting or expansion; a filename containing a newline is detected and rejected without printing the unsafe name."
    - "Every intentionally missing or malformed fixture citation is assembled from fragments at runtime; the production gate contains no such literal, and a runtime-created peer support fixture proves the future second script will be discovered without requiring that live Task 3 artifact."
  </behavior>
  <action>
Create a Bash gate that resolves the repository root from its own location and defaults to the real tree. Discover text-bearing regular files recursively under `packages/`, pruning exactly the path components `node_modules`, `dist`, and `coverage` and no other subtree; separately discover Phase 08 depth-one support artifacts when executable or ending in `.sh`/`.patch`, including `verdict-gate.sh`, `verify-a1-patch.sh`, `a1-probe-fix.patch`, and this gate. Both families must use `find ... -print0` consumed by `while IFS= read -r -d ''`, quote every pathname operand, avoid command-substitution/line-splitting `for` loops and unsafe glob expansion, and explicitly reject a discovered pathname containing a newline. For a newline rejection emit only a constant record label/count, never the unsafe path; for ordinary failures retain relative paths only. Do not handpick source/test files. In `--self-test`, create a temporary peer `.sh` fixture beneath the fixture root and prove the dynamic support rule discovers it; do not create, require, or inspect Task 3's live peer artifact.

Extract every canonical numeric citation, reject malformed numeric spellings, normalize/sort IDs, and retain only IDs plus safe relative paths. Anchor register parsing to the existing literal heading `## Threat Register`, require the immediately associated exact header `| Threat ID | Category | Component | Sev | Disposition | Mitigation (verified) | Status |` and its seven-column delimiter row, and stop at the next line beginning `## `; do not anchor on the plan-only `STRIDE Threat Register` wording or consume `###` correction sections/accepted-risk prose as rows. Reject duplicate IDs and, in final mode, require exactly T-08-01..T-08-88 plus T-08-SC. Before any `08-SECURITY.md` edit, run the tracer against the live stale file and require its capture to report `50` numeric rows and one T-08-SC from that exact region before listing the independently measured missing live IDs. Any other pre-roll-up count is a parser-liveness failure, not successful gap detection.

Add hard liveness mappings for package citations T-08-55/T-08-59/T-08-75 and support citations T-08-67/T-08-76 so empty or narrowed discovery is red per T-08-84/T-08-88. Provide a fixture-root/self-test interface calling the same discovery/parser/join functions as default mode. Construct every deliberately missing or malformed fixture token from separately stored prefix/numeric fragments at runtime; this production script may carry no literal invalid citation. Use the runtime-created peer fixture to prove a future support script follows the same discovery and literal-citation checks, without depending on a live peer file. Bound diagnostics to IDs, safe relative paths, or the constant newline-path label and never emit matching source lines/nearby content. Capture the expected pre-roll-up red set only after the 50-plus-SC parser liveness assertion proves the tracer reached the real canonical table. Task 3 owns all final live two-script validation.
  </action>
  <verify>
    <automated>bash -n .planning/phases/08-process-lifecycle/threat-register-gate.sh &amp;&amp; bash .planning/phases/08-process-lifecycle/threat-register-gate.sh --self-test</automated>
  </verify>
  <acceptance_criteria>
    - The gate's self-test is green while its first default run is red on the stale canonical register and names live missing IDs from both scan families.
    - The pre-roll-up capture records exactly 50 numeric rows plus T-08-SC under `## Threat Register` before any missing-ID diagnostic; a zero-row parse cannot satisfy the tracer.
    - The default scan is recursive/dynamic, has exactly documented pruning, and hard-fails if any required sentinel disappears.
    - Only main-register table rows satisfy a citation; prose, residual rows, PLAN files, and SUMMARY files do not.
    - Spaces, tabs, single/double quotes, and glob characters are NUL-safely discovered in both scan families; newline paths fail closed with a constant label and no unsafe-name echo.
    - Output contains IDs and safe relative paths only and cannot echo a canary secret placed next to a citation; the production gate's self-test validates a runtime-created peer support fixture without requiring or creating Task 3's live peer artifact.
  </acceptance_criteria>
  <done>A real package/support citation reaches a production-quality gate, fails against the stale register for the right reason, and the same implementation passes its falsifying self-tests.</done>
</task>

<task type="auto">
  <name>Task 2: Re-audit and roll the complete T-08-51..T-08-88 register</name>
  <reversibility rating="costly">The canonical security ledger is consumed by milestone verification; corrections must preserve prior residual and evidence history.</reversibility>
  <read_first>
    - .planning/phases/08-process-lifecycle/08-SECURITY.md — rows T-08-01..T-08-50/T-08-SC, nine accepted residuals, and stale d2d502b audit baseline
    - .planning/phases/08-process-lifecycle/08-11-PLAN.md through 08-17-PLAN.md — canonical allocated category/severity/disposition rows T-08-51..T-08-78
    - .planning/phases/08-process-lifecycle/08-11-SUMMARY.md through 08-17-SUMMARY.md — implementation pointers, not sufficient closure evidence
    - .planning/phases/08-process-lifecycle/08-18-PLAN.md and 08-18-SUMMARY.md — T-08-79..T-08-83 allocation and executed gate/carrier evidence
    - .planning/phases/08-process-lifecycle/08-19-PLAN.md — T-08-84..T-08-88 allocation
    - packages/backend/src/kill-plan.ts, packages/backend/src/index.ts, packages/backend/src/index.source.test.ts, packages/backend/src/kill-plan.test.ts, packages/backend/src/kill-tree.posix.test.ts — current production/test mechanisms
    - .planning/phases/08-process-lifecycle/verdict-gate.sh, verify-a1-patch.sh, a1-probe-fix.patch, threat-register-gate.sh — executable/current record mechanisms
  </read_first>
  <files>.planning/phases/08-process-lifecycle/08-SECURITY.md</files>
  <action>
Capture `AUDIT_HEAD=$(git rev-parse HEAD)` immediately before the audit, capture the current package-tree digest, and recompute the number of package-changing commits from the former `d2d502b` baseline. Build a complete ID census from the Required threat allocation table: transcribe each original plan's category, severity, and disposition for T-08-51..T-08-78, include T-08-79..T-08-83 from Plan 08-18 and T-08-84..T-08-88 from this plan, and require an unbroken T-08-01..T-08-88 sequence plus T-08-SC. For every new row, inspect the cited current source/test/gate mechanism and run its executable evidence; a PLAN or SUMMARY may locate the mechanism but cannot close it per T-08-86. In particular, run `verify-a1-patch.sh` as current executable evidence for T-08-67/T-08-76, record its apply/typecheck/build result, compare the pre/post `git worktree list` count, and compare pre/post package-tree status so its detached scratch worktree is removed and the main package tree is unchanged. Keep the canonical main table's exact seven-column header `| Threat ID | Category | Component | Sev | Disposition | Mitigation (verified) | Status |` unchanged. Under the exact separate heading `## T-08-51..T-08-88 Roll-up Evidence Ledger`, add the compact header `| Threat ID | Severity | Disposition | Owner | Evidence | Outcome |` and exactly one row for every T-08-51..T-08-88. Use plain severity values `critical`, `high`, `medium`, or `low`; plain dispositions `mitigate`, `accept`, or `transfer`; current artifact/test/gate evidence; and the re-audited outcome. Every Owner cell is mandatory and must use exactly one accountable vocabulary value: `six2dez:implementation` owns local source/test/gate/documentation mitigation and reruns, `six2dez:risk-acceptance` owns the explicit accept decision plus residual review, and `six2dez:evidence-coordination` owns obtaining and recording hardware/runtime proof unavailable locally without converting missing evidence into closure. Do not use blanks, `TBD`, an unnamed team, or an external reporter as the accountable owner. This covers all 38 rows and specifically T-08-55, T-08-59, T-08-75, and T-08-76 with mechanically checkable severity, disposition, and ownership. Preserve the meaning, severity, and rationale of all nine existing accepted residuals. Add explicit residual entries for the allocated accepts T-08-54, T-08-59, T-08-68, and T-08-73, yielding thirteen visible residuals unless current evidence requires a marked correction rather than a silent disposition change. Recompute all frontmatter/table aggregates from parsed rows: the expected main register is 88 numeric rows plus T-08-SC, accepted residuals are explicit, and the existing accepted-high T-08-47 exception remains separately counted. Set `audited_at_head` to the captured current audit baseline and document that package changes after it are zero. Set `status: secured` and `threats_open: 0` only after every high-severity mitigation has current executable/source evidence and both standing gates pass; otherwise retain the exact open IDs and stop completion rather than lowering severity or erasing residuals per T-08-85/T-08-87. Preserve Plan 08-18's corrected A1 sign-off and the native-Windows/real-turn caveats.
  </action>
  <verify>
    <automated>drift_wt_before=$(git worktree list --porcelain | grep -c '^worktree ') &amp;&amp; drift_pkg_before=$(git status --porcelain=v1 --untracked-files=all -- packages) &amp;&amp; bash .planning/phases/08-process-lifecycle/verify-a1-patch.sh &amp;&amp; drift_wt_after=$(git worktree list --porcelain | grep -c '^worktree ') &amp;&amp; drift_pkg_after=$(git status --porcelain=v1 --untracked-files=all -- packages) &amp;&amp; test "$drift_wt_after" -eq "$drift_wt_before" &amp;&amp; test "$drift_pkg_after" = "$drift_pkg_before" &amp;&amp; awk -F'|' '/^## T-08-51\.\.T-08-88 Roll-up Evidence Ledger$/ { in_ledger=1; next } in_ledger &amp;&amp; /^## / { in_ledger=0 } in_ledger &amp;&amp; $2 ~ /^[[:space:]]*T-08-[0-9][0-9][[:space:]]*$/ { id=$2; severity=$3; disposition=$4; owner=$5; gsub(/[[:space:]]/, "", id); gsub(/^[[:space:]]+|[[:space:]]+$/, "", severity); gsub(/^[[:space:]]+|[[:space:]]+$/, "", disposition); gsub(/^[[:space:]]+|[[:space:]]+$/, "", owner); if (id ~ /^T-08-(5[1-9]|6[0-9]|7[0-9]|8[0-8])$/) { if (++seen[id] != 1 || severity !~ /^(critical|high|medium|low)$/ || disposition !~ /^(mitigate|accept|transfer)$/ || owner !~ /^six2dez:(implementation|risk-acceptance|evidence-coordination)$/) exit 2; count++ } } END { for (i=51; i!=89; i++) { id=sprintf("T-08-%02d", i); if (seen[id] != 1) exit 3 } if (count != 38) exit 4 }' .planning/phases/08-process-lifecycle/08-SECURITY.md &amp;&amp; bash .planning/phases/08-process-lifecycle/threat-register-gate.sh &amp;&amp; bash .planning/phases/08-process-lifecycle/verdict-gate.sh &amp;&amp; pnpm exec vitest run packages/backend/src/kill-plan.test.ts packages/backend/src/kill-tree.posix.test.ts packages/backend/src/index.source.test.ts &amp;&amp; pnpm -r typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Main register has exactly one row for every T-08-01..T-08-88 plus one T-08-SC row; all T-08-51..T-08-78 allocations are present, not merely the seven formerly missing IDs.
    - The separate roll-up ledger has exactly one T-08-51..T-08-88 row with allowed severity/disposition values and one non-empty controlled Owner value per row; T-08-55/T-08-59/T-08-75/T-08-76 are explicitly covered, while the canonical main-register header remains byte-for-byte unchanged.
    - Every added mitigation cites verified current source/test/gate evidence; accepted rows cite explicit residual rationale and are not mislabeled as mitigated.
    - `verify-a1-patch.sh` currently applies, type-checks, and builds; its scratch worktree count and main package-tree status exactly match their pre-run snapshots.
    - All nine old residuals plus T-08-54/T-08-59/T-08-68/T-08-73 remain visible, and accepted-high accounting still exposes T-08-47.
    - `audited_at_head` equals the captured audit baseline and `git rev-list --count &lt;audited_at_head&gt;..HEAD -- packages/` is zero.
    - The register is `secured` with zero open threats only if every high mitigation and standing gate is green; no row is closed solely from plan/SUMMARY prose.
  </acceptance_criteria>
  <done>The canonical register and separate owner-bearing ledger cover the whole post-T-08-50 round through T-08-88 at a current package baseline, with honest dispositions, preserved residuals, and evidence-backed aggregate status.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Lock citation integrity with independent red-input demonstrations</name>
  <read_first>
    - .planning/phases/08-process-lifecycle/threat-register-gate.sh — production discovery/parser/join implementation
    - .planning/phases/08-process-lifecycle/08-SECURITY.md — now-green canonical main table and aggregate state
    - .planning/phases/08-process-lifecycle/verdict-gate.sh — independent A1/summary standing gate
    - packages/backend/src/kill-plan.ts and packages/backend/src/index.ts — live package sentinels
    - .planning/phases/08-process-lifecycle/verify-a1-patch.sh and a1-probe-fix.patch — live support sentinels
  </read_first>
  <files>.planning/phases/08-process-lifecycle/threat-register-gate.sh, .planning/phases/08-process-lifecycle/threat-register-gate.test.sh, .planning/phases/08-process-lifecycle/08-SECURITY.md</files>
  <behavior>
    - "The unchanged real repository is green and reports both discovery families plus 89 unique main-register rows."
    - "A runtime-fragment-assembled next numeric ID in a scratch package file is red, and the same assembled ID in a scratch Phase 08 support script/patch is independently red."
    - "Removing a live sentinel row, duplicating a main row, placing the row only in prose, runtime-assembled malformed numeric spelling, and empty package/support discovery are independently red."
    - "Package and support fixtures with spaces, tabs, single/double quotes, and glob metacharacters reach the join; a newline-bearing path is rejected before citation processing."
    - "A final self-scan proves `threat-register-gate.sh` and `threat-register-gate.test.sh` contain no literal intentionally missing/malformed citation and are not excluded from support discovery."
    - "A canary secret adjacent to a failing citation never appears in stdout/stderr; only its generated ID and relative path may appear."
  </behavior>
  <action>
Create an executable Bash test that builds an isolated temporary repository fixture, installs cleanup traps, and invokes the production gate through its supported root override; do not modify live package files or the live security register to manufacture failures. Derive the next unregistered ID from the parsed maximum and build every intentionally missing/malformed token by concatenating prefix and numeric fragments only at runtime, including strings written into fixtures; the test source itself must carry no literal invalid citation because it is a live support artifact. Exercise every behavior separately and assert nonzero status plus the expected safe ID/path where applicable. Include PLAN/SUMMARY-only and outside-register-table cases to prove excluded allocation/prose cannot satisfy the join.

Build an adversarial pathname matrix in both package and support families: regular files whose basenames contain a space, a tab, a single quote, a double quote, and glob characters (`*`, `?`, `[`), first with registered sentinel citations to prove discovery and then with runtime-assembled missing citations to prove the join. Create a newline-bearing pathname separately and require a fail-closed constant diagnostic before citation parsing; do not compare or print that unsafe pathname. All fixture creation/invocation operands are quoted. Assert a high-entropy canary adjacent to a failing citation never appears in output. Make the test fail if either discovery family or fixed sentinel is absent.

Task 3 now owns both live shell files: add the final production self-scan over them, accepting canonical registered literals while rejecting any literal intentionally missing/malformed token, and require both scripts to remain inside support discovery. This is the first task allowed to require the real `threat-register-gate.test.sh`; do not move that live two-script acceptance earlier. If tests reveal a gate defect, harden production and update the registered T-08-84/T-08-88 mechanism text in SECURITY; do not narrow scope or exclude a newly failing path. Finish by running the independent test, production self-scan, live threat-register gate, verdict gate, and `verify-a1-patch.sh`; bracket the patch verification with worktree-count and package-status snapshots, capture both post-run measurements in fail-propagating assignments, and require exact equality after they succeed. Then recheck register totals, the 38-row owner ledger, `audited_at_head` package drift, protected residual counts, and zero diffs under packages/frontend/shared. No command may print environment values, secret-bearing source lines, or an unsafe newline path.
  </action>
  <verify>
    <automated>drift_wt_before=$(git worktree list --porcelain | grep -c '^worktree ') &amp;&amp; drift_pkg_before=$(git status --porcelain=v1 --untracked-files=all -- packages) &amp;&amp; bash -n .planning/phases/08-process-lifecycle/threat-register-gate.sh &amp;&amp; bash -n .planning/phases/08-process-lifecycle/threat-register-gate.test.sh &amp;&amp; bash .planning/phases/08-process-lifecycle/threat-register-gate.test.sh &amp;&amp; bash .planning/phases/08-process-lifecycle/threat-register-gate.sh --self-scan &amp;&amp; bash .planning/phases/08-process-lifecycle/threat-register-gate.sh &amp;&amp; bash .planning/phases/08-process-lifecycle/verdict-gate.sh &amp;&amp; bash .planning/phases/08-process-lifecycle/verify-a1-patch.sh &amp;&amp; drift_wt_after=$(git worktree list --porcelain | grep -c '^worktree ') &amp;&amp; drift_pkg_after=$(git status --porcelain=v1 --untracked-files=all -- packages) &amp;&amp; test "$drift_wt_after" -eq "$drift_wt_before" &amp;&amp; test "$drift_pkg_after" = "$drift_pkg_before" &amp;&amp; git diff --exit-code -- packages packages/frontend packages/shared</automated>
  </verify>
  <acceptance_criteria>
    - Independent test script proves the parser/join red matrix plus both-family adversarial pathname cases using production code and leaves no temporary live-tree file behind.
    - Default gate exits zero with both liveness families, exactly 88 numeric register rows plus T-08-SC, no duplicate/malformed rows, and no missing live citation.
    - Both shell files remain in support discovery and pass the self-scan with every intentionally invalid token assembled only at runtime.
    - Spaces, tabs, quotes, and glob characters are discovered without splitting/expansion; the newline-path case fails with a constant label and no raw unsafe name.
    - Red diagnostics contain only generated IDs, safe relative paths, or the constant newline-path label; the canary secret and raw newline pathname are absent.
    - Both standing gates and `verify-a1-patch.sh` are green, its scratch worktree is removed, the main package status is unchanged, package drift after `audited_at_head` is zero, and existing residuals/caveats remain visible.
    - Packages, frontend, and shared trees are unchanged; no release was cut and nothing was pushed.
  </acceptance_criteria>
  <done>The standing citation-integrity control is independently proven non-vacuous, the current register and A1 gate are green together, and verification gap 2 is closed without hiding accepted or hardware-bound risk.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Live package/support text -> citation census | Source and executable support artifacts can introduce security IDs that must resolve to a canonical row without leaking surrounding content. |
| Historical plan/SUMMARY allocation -> current audit | Planning prose identifies intended threats but is untrusted as proof that a mitigation exists or passes now. |
| Git package history -> `audited_at_head` | A stale or conveniently chosen SHA can make an old audit look current. |
| Main register -> aggregate status/residual ledger | Row counts, accepted risk, open-high state, and sign-off must agree mechanically. |
| Fixture input -> shell gate output | Adversarial filenames/text cross NUL-safe discovery; newline paths must fail closed, and no path may cause command injection, scope narrowing, or secret-bearing output. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-84 | Tampering | Live citation and allocation census | high | mitigate | Require the contiguous T-08-01..T-08-88 range, explicit plan allocations, dynamic package/support discovery, and fixed cross-family liveness sentinels. |
| T-08-85 | Repudiation | `audited_at_head` and package-history comparison | high | mitigate | Capture HEAD at audit start, hash the package tree, record the former-baseline delta, and require zero package-changing commits after the captured audit SHA. |
| T-08-86 | Repudiation | Threat disposition and mitigation closure | high | mitigate | Treat plans/SUMMARYs only as locators; require current source plus executable test/gate evidence for every mitigated row and retain exact open IDs on failure. |
| T-08-87 | Tampering | `status`, `threats_open`, totals, and accepted residuals | high | mitigate | Recompute from parsed rows, preserve nine existing residuals, append all allocated accepts, expose accepted-high T-08-47, and block secured/zero on any unmet high mitigation. |
| T-08-88 | Tampering | `threat-register-gate.sh` discovery/parser/output | high | mitigate | Use one implementation for live/fixture roots, NUL-safe enumeration with quoted operands and newline refusal, runtime-fragment invalid fixtures, both-script self-scan, and ID/safe-path-only diagnostics with a secret canary. |
</threat_model>

<verification>
- Run the threat-register gate's self-test, independent shell test, and default live-tree audit; all must exit zero after the register roll-up.
- Run the independent verdict gate to ensure the shared `08-SECURITY.md` edit preserves Plan 08-18's corrected A1 contract.
- Run `drift_wt_before=$(git worktree list --porcelain | grep -c '^worktree ') &amp;&amp; drift_pkg_before=$(git status --porcelain=v1 --untracked-files=all -- packages) &amp;&amp; bash .planning/phases/08-process-lifecycle/verify-a1-patch.sh &amp;&amp; drift_wt_after=$(git worktree list --porcelain | grep -c '^worktree ') &amp;&amp; drift_pkg_after=$(git status --porcelain=v1 --untracked-files=all -- packages) &amp;&amp; test "$drift_wt_after" -eq "$drift_wt_before" &amp;&amp; test "$drift_pkg_after" = "$drift_pkg_before"` in Task 2 and again in final verification; both post-run assignments must succeed before either comparison.
- Run kill-plan, POSIX-tree, and source-structure suites plus all package typechecks as current mechanism evidence.
- Confirm the exact `## Threat Register` parser first measured the stale 50-numeric-plus-SC baseline, then the final 88-numeric-plus-SC table; zero-row/wrong-heading parses are red.
- Confirm both gate scripts are scanned, every invalid fixture citation is runtime-assembled, adversarial quoted path cases pass, and the newline case fails closed without printing the raw name.
- Mechanically confirm 88 unique numeric main rows plus T-08-SC, no duplicates/gaps, thirteen explicit accepted residual records, separately visible accepted-high T-08-47, and exactly 38 T-08-51..T-08-88 roll-up rows whose severity, disposition, and Owner values pass Task 2's `awk` gate.
- Confirm `git rev-list --count <audited_at_head>..HEAD -- packages/` is zero and packages/frontend/shared have no plan diff.
- Treat any missing high-severity evidence as open; never alter severity, scope, or exclusions to manufacture a secured result.
</verification>

<success_criteria>
- Authoritative verification gap 2 is closed with the complete T-08-51..T-08-78 roll-up, not a seven-row patch, and the register also includes this round's T-08-79..T-08-88 controls.
- Canonical security state is measured at the current package baseline, aggregate fields agree with parsed rows/residuals, and every mitigation has current evidence.
- The separate roll-up ledger gives every T-08-51..T-08-88 row a mechanically valid severity, disposition, and concrete six2dez responsibility owner without changing the canonical seven-column main-register header.
- Any new live T-08-NN citation under packages or executable/applied Phase 08 support artifacts fails until the main register gains exactly one row.
- The fixture suite proves parser anchoring, false-green resistance, NUL-safe adversarial-path handling, script self-scan, and output confidentiality; both standing gates and the current A1 patch verifier pass together.
- Runtime/UI code, LIF checkbox state, accepted residuals, native-Windows limitations, and release/push state remain intact.
</success_criteria>

## Multi-Source Coverage Audit

| Source type | Item | Coverage | Status |
|-------------|------|----------|--------|
| GOAL | Cancel/timeout must leave no token-bearing orphan | Existing implementation is re-audited through current source/tests; this plan makes no new runtime claim | COVERED (non-regression) |
| REQ | LIF-01 | Native-Windows limitation, open checkbox, and security residual ownership remain explicit | COVERED |
| REQ | LIF-02 | Current POSIX evidence, A1 record integrity, and process-lifecycle threat mechanisms are audited without upgrading real-turn causality | COVERED |
| RESEARCH | Zero packages, QuickJS evidence boundary, value-free diagnostics, kill-before-sweep | Task 2 verifies current mechanisms; no dependency/runtime/API addition | COVERED |
| CONTEXT | No `08-CONTEXT.md`; explicit user instruction is to close only the two verifier gaps | This dependent plan owns verifier gap 2 and leaves runtime/UI/release work untouched | COVERED |
| VERIFICATION | Gap 2: stale/incomplete register and absent standing citation-integrity control | Tasks 1-3 provide ordered red tracer, full roll-up, and independent green/red gate proof | COVERED |
| SECURITY | T-08-51..T-08-78 plus new round controls, current HEAD, severities, dispositions, owners, residuals, status | Required allocation census and Task 2's mechanically checked owner-bearing evidence ledger cover every row | COVERED |
| UI-SPEC | No frontend behavior in this round | Zero-diff backstop in truths, tasks, and verification | COVERED |

Exclusions are accounted for, not missing: PLAN/SUMMARY prose is an allocation locator rather than a live-citation source; generated dependency/build outputs are pruned explicitly; the `single-pid` cue does not create runtime scope. No source-audit item is unplanned.

<output>
Create `.planning/phases/08-process-lifecycle/08-19-SUMMARY.md` when done.
</output>
