---
phase: 08-process-lifecycle
plan: 17
type: execute
wave: 10
gap_closure: true
depends_on: ["08-15", "08-16"]
files_modified:
  - .planning/phases/08-process-lifecycle/08-SPIKE.md
  - .planning/WINDOWS.md
  - .planning/phases/08-process-lifecycle/deferred-items.md
autonomous: false
requirements: [LIF-01, LIF-02]

estimate:
  tokens: 62000
  raw_tokens: 62000
  tasks: 3
  confidence: low

user_setup:
  - service: "the maintainer's own macOS Caido install"
    why: "A1 is a claim about the LLRT fork a specific Caido ships. No CI leg executes that runtime and none ever will. Nothing in this milestone owns this re-run after Phase 8 closes."
    dashboard_config:
      - task: "Install the temporary probe build into Caido and restart it"
        location: "Caido → Plugins → install from file"
      - task: "Reinstall the HEAD build afterwards — the probe build spawns fixture processes on every diagnostics call and must not be left installed"
        location: "Caido → Plugins → install from file"

must_haves:
  truths:
    - "A1 is re-run on real hardware with the fixed probe, and its result is recorded as exactly one of three values — alive, dead, or inconclusive — where inconclusive is a first-class recorded outcome and never folded into either verdict."
    - "The pre-fix Control is taken on the same machine and in the same session: a `pgrep -f mcp-server.mjs | wc -l` count after Stop against a build that predates the argv-marker reap, making the post-fix count from plan 08-16 half of a before/after pair rather than an isolated reading."
    - "The probe build is never committed, never signed and never released, and Caido is returned to the HEAD build at the end of the session — because the probe spawns fixture processes on every diagnostics call."
    - "The fixed probe's fixture cleanup is observed: after the diagnostics call, the number of surviving fixture node processes is recorded, so the leak the original probe had is confirmed fixed rather than assumed fixed."
    - "Any reading not taken is written as a dated, marked abstention naming what was attempted and what stopped it — never a favourable default, never an inference."
    - "If A1 returns a definite verdict, `verdict-gate.sh` going red on `08-SPIKE.md` is recorded as the EXPECTED and CORRECT consequence, and the gate is not weakened to hide it; the propagation to every other carrier is written down as a named follow-up with the gate as the instrument that finds them."
    - "The Windows execution leg is re-deferred to Phase 9 SC-4 explicitly and in writing, with its closing condition restated, rather than dropped."
    - "Every item this phase leaves open has a named owner, and every item with no owner in the remaining milestone is written down as ownerless with the phases that were checked and excluded."
  artifacts:
    - ".planning/phases/08-process-lifecycle/08-SPIKE.md — the A1 re-run result and the Control, or their dated abstentions"
    - ".planning/WINDOWS.md — entry 11 and the probe-defect entry updated with the re-run outcome"
    - ".planning/phases/08-process-lifecycle/deferred-items.md — the explicit re-deferral of the Windows leg and the ownerless-item record"
  key_links:
    - "`a1-probe-fix.patch` + `verify-a1-patch.sh` (plan 08-15) → the probe build this plan installs"
    - "the Control count → plan 08-16's post-fix cancel count, forming the before/after pair the T-08-14 attestation has lacked since 2026-08-24"
    - "a definite A1 verdict → `verdict-gate.sh` ARM A going red on `08-SPIKE.md`, which is the discovery mechanism for the propagation follow-up"
    - "the Windows leg → Phase 9 SC-4, its named owner"
  prohibitions:
    - statement: "No measurement is recorded that was not taken. An untaken reading is a dated marked abstention naming what was attempted and what stopped it."
    - statement: "No cannot-tell answer is coalesced into either verdict. An inconclusive A1 is recorded verbatim as inconclusive and is not retried into a definite answer."
    - statement: "The probe is not committed to HEAD, not signed and not released. Caido is returned to the HEAD build before the session is declared complete."
    - statement: "The verdict gate is not weakened, narrowed or excluded-from to accommodate a new A1 verdict. If it goes red because `08-SPIKE.md` now states a definite verdict the other carriers do not, that red is the correct signal and the carriers are what change."
    - statement: "No open item is dropped. Every one is closed by a reading, re-deferred to a named owner, or written down as ownerless."
---

<objective>
The one measurement nothing in this milestone owns after Phase 8 closes, and the one Control that
would make this phase's POSIX evidence a before/after pair rather than an isolated zero.

`08-VERIFICATION.md` ranks the A1 re-run first among the human items and states the ownership
problem in plain words: *"NO LATER PHASE OWNS THIS: Phase 9 is CI (Node only), Phase 10 SC-5/SC-6 are
Windows and Gemini, Phases 11-13 are the plugin bridge. If Phase 8 closes without it, A1 is
ownerless AND currently recorded as closed."* Plans 08-11 through 08-14 fixed the second half of that
sentence. This plan attempts the first.

The Control is the phase's `insufficient_spec` item — `08-01` truth 3 — abstained on 2026-08-24,
still unmet, and now doubly load-bearing: with A1's reading retracted it is the only remaining route
to a trustworthy POSIX reading. It needs the pre-fix build, and the probe build IS a pre-fix build,
so one install serves both readings.

Plan 08-15 turned the probe fix into a committed patch and proved it applies, type-checks and builds.
This plan turns it into an installed build and asks for the reading.

Purpose: gap 1 `missing` item 4, the `insufficient_spec` item, gap 3 `missing` items 1 and 4, and
`human_verification` items 1 and 5.
Output: two readings on real hardware or two dated abstentions, and a written account of what this
phase leaves open and who owns each piece.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md

@.planning/phases/08-process-lifecycle/08-VERIFICATION.md
@.planning/phases/08-process-lifecycle/08-SPIKE.md
@.planning/phases/08-process-lifecycle/deferred-items.md
</context>

## Artifacts this phase produces

This plan creates no code symbols and no source files. It produces one throwaway build artifact
outside version control, adds a dated results section to `08-SPIKE.md`, updates two ledger entries,
and adds two records to `deferred-items.md`.

Symbols created by **sibling plans in this gap-closure set**, listed so a source-grounding pass does
not flag them: `STALE_VERDICT_ERE`, `READING_ERE`, `RETRACTION_TOKEN`, `SUMMARY_KNOWN_UNPINNED`
(08-11); `classifyLivenessObservation`, `buildLivenessProbePlan`, `formatSpikeVerdict`,
`LivenessVerdict`, `LivenessObservation` (08-12); `formatOrphanReapRecord`, `OrphanReapRecord`,
`lastOrphanReap`, `recordOrphanReapOutcome`, `a1-probe-fix.patch`, `verify-a1-patch.sh` (08-15).

<tasks>

<task type="auto">
  <name>Task 1: Produce the patched probe build and stage the A1 and Control tables</name>

  <read_first>
    - `.planning/phases/08-process-lifecycle/verify-a1-patch.sh` — including its optional output-path
      argument, which exists so this task reuses the recipe rather than reimplementing it.
    - `.planning/phases/08-process-lifecycle/a1-probe-fix.patch` — read the whole patch before
      building from it, so the SUMMARY can state what the installed build actually does.
    - `.planning/phases/08-process-lifecycle/08-SPIKE.md` — the corrected A1 table, the Control table
      (still an honest absence), and § *How to run this spike later* as rewritten by plan 08-15,
      including its three-outcome list and its step 5.
    - `.planning/phases/08-process-lifecycle/08-VERIFICATION.md` — `human_verification` item 1 and the
      `insufficient_spec_items` entry.
  </read_first>

  <files>.planning/phases/08-process-lifecycle/08-SPIKE.md</files>

  <action>
    Run `verify-a1-patch.sh` with an output path outside the repository's tracked tree, so it produces
    the probe package as a side effect of the check it already performs. Record the exit code, the
    output path, the package size and the commit the worktree was created at. Confirm afterwards that
    `git worktree list` shows only the main worktree and that `git status --porcelain packages/` is
    empty — the probe must not reach the working tree, let alone a commit.

    Then add a dated section to `08-SPIKE.md` — leaving the existing A1, A6 and Control tables alone —
    with two results tables, every value cell empty and visibly marked as not yet taken, following the
    Control table's honest-absence form.

    Table 1, the A1 re-run: the Caido version string; the three probe diagnostics fields as the
    patched probe reports them; the fixed probe's three-valued verdict; the reason token that
    accompanies an inconclusive result; and the count of surviving fixture processes after the
    diagnostics call, which is how the original probe's fixture leak is confirmed fixed rather than
    assumed fixed.

    Table 2, the Control: the count during a live turn on the pre-fix build, and the count roughly
    five seconds after Stop on that same build. Add a cell for the build identity, so the pairing with
    plan 08-16's post-fix counts is explicit rather than implied.

    Write the three-outcome rule directly above table 1, in the section's own words: the fixed probe
    reports alive, dead, or inconclusive; inconclusive is a result and is recorded verbatim; it is not
    retried into a definite answer and it is not read as either verdict. Write the abstention rule
    beneath both tables in the same form plan 08-16 established.

    Write one paragraph, before the tables, stating what a definite favourable A1 verdict would mean
    for the verdict gate — `08-SPIKE.md` would then carry a live favourable claim that no other
    carrier does, ARM A would go red naming this file, and that red is the correct signal to propagate
    the new verdict rather than a reason to touch the gate. State it in advance so it is not
    discovered as a surprise mid-session and resolved the wrong way.
  </action>

  <verify>
    <automated>cd /Users/six2dez/Tools/drift &amp;&amp; git worktree list | grep -c . | grep -qx 1</automated>
    <automated>cd /Users/six2dez/Tools/drift &amp;&amp; git status --porcelain packages/ | grep -c . | grep -qx 0</automated>
    <automated>cd /Users/six2dez/Tools/drift &amp;&amp; bash .planning/phases/08-process-lifecycle/verdict-gate.sh</automated>
    <automated>cd /Users/six2dez/Tools/drift &amp;&amp; npx vitest run</automated>
  </verify>

  <acceptance_criteria>
    - `verify-a1-patch.sh` exits 0 and produces a package at the named output path. Record the exit
      code, the path, the size and the source commit.
    - The package path is outside the tracked tree: `git status --porcelain` does not list it.
    - `git worktree list` shows exactly one worktree and `git status --porcelain packages/` is empty.
    - `08-SPIKE.md` gains two results tables with every value cell empty and marked as not yet taken,
      plus the three-outcome rule, the abstention rule, and the gate-consequence paragraph.
    - The existing A1, A6 and Control tables have no diff hunks.
    - `bash .planning/phases/08-process-lifecycle/verdict-gate.sh` STILL exits 0 and `npx vitest run`
      is green at or above the running baseline. A regression check on the green plan 08-15
      established, not a claim to have turned it green. NOTE the one exception this plan carries: if
      the A1 re-run returns a FAVOURABLE verdict, ARM A going red on `08-SPIKE.md` is the EXPECTED
      and correct consequence (threat T-08-72) — record it, do not weaken the gate, and do not edit
      a carrier to make it green.
    - The SUMMARY states, from reading the patch, what the installed build does differently from HEAD
      — in particular that it spawns two fixture processes on every diagnostics call, which is why it
      must be uninstalled afterwards.
  </acceptance_criteria>

  <done>
    A probe package that exists outside version control, a repository whose working tree never held
    it, and two results tables whose empty cells read as an absence.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: Re-run A1 with the fixed probe and take the pre-fix Control, then restore the HEAD build</name>
  <action>Install the temporary probe build, take the A1 reading and the pre-fix Control, then put the HEAD build back</action>
  <instructions>
    I built a temporary probe package from the historical commit with the fixed probe patch applied,
    and staged two empty results tables. Everything automatable is done.

    READ THIS FIRST. The build you are about to install SPAWNS TWO FIXTURE PROCESSES EVERY TIME THE
    DIAGNOSTICS PANEL IS OPENED. That is why it was deleted from the shipping code in the first place.
    Open the diagnostics panel ONCE, and reinstall the HEAD build when you are done. I will ask you to
    confirm you did.

    This gate cannot be auto-approved in any mode. If you cannot take a reading, say so — an
    abstention is a valid outcome and I will record it as one. A guessed value is not.

    SETUP
    1. Install the probe package I built (path in my previous message) into Caido and restart it.
    2. From Caido's own About/Settings screen, note the Caido version string. This has been recorded
       as "not recorded" since 2026-08-27 and A1 is a claim about the fork a specific Caido ships.

    READING 1 — A1, with the fixed probe
    3. Open Drift → Settings → Copy diagnostics ONCE. Paste the three probe fields verbatim — the
       kill-primitive type, the detached-group-kill verdict, and the probe note.
       The verdict field now reports exactly one of three things. All three are results:
         • the grandchild died — the runtime honours the detached spawn;
         • the grandchild survived — it does not;
         • inconclusive, with a reason — the probe could not determine liveness, most likely because
           it could not spawn its enumerator. This is the answer the old probe was structurally unable
           to give, and it is the reason this re-run exists. Paste it verbatim; do not retry it.
    4. Immediately after that diagnostics call, run:
         pgrep -f 'node -e' | wc -l
       and tell me the number, plus anything in `ps` that looks like a leftover fixture. The old probe
       could not clean up after itself on this runtime; this tells us whether the fix works.

    READING 2 — the Control, on this same pre-fix build
    5. This build predates the orphan-reap mechanism entirely, which is exactly what makes it the
       Control. Start a Claude Code chat turn in Drift with the MCP attached, and while it is still
       streaming:
         pgrep -f mcp-server.mjs | wc -l
    6. Click Stop. Wait about five seconds, then:
         pgrep -f mcp-server.mjs | wc -l
       Record both counts. A NON-ZERO count after Stop is the reported defect reproduced on real
       hardware, and it is the number plan 08-16's post-fix zero needs to be compared against. A zero
       here is also a result — it would mean the defect does not reproduce on this machine and that
       the post-fix zero proves less than it appears to.
    7. Clean up any survivor by pid, and tell me whether there was one.

    TEARDOWN — not optional
    8. Reinstall the HEAD build into Caido and restart it. Confirm you have done this.

    Paste the raw terminal output and the raw diagnostics values. Do not summarise, round or
    interpret. If a step could not be completed, tell me which one and what stopped it.
  </instructions>
  <verification>
    I will record every value verbatim, record a dated marked abstention for anything not taken, and
    cross-check the Control against plan 08-16's post-fix counts to state plainly whether they form a
    before/after pair or whether the comparison is still confounded. I will also check that the
    reported A1 verdict is one of the three permitted values and flag it if it is not.
  </verification>
  <resume-signal>
    Paste the readings, and confirm the HEAD build is reinstalled. If you could not take some or all
    of them, say which and what stopped you.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 3: Record the readings, and write down who owns everything this phase leaves open</name>

  <read_first>
    - `.planning/phases/08-process-lifecycle/08-SPIKE.md` — the staged tables from task 1, the
      corrected A1 table from plan 08-11, and the Control table.
    - `.planning/WINDOWS.md` — entry 11 as corrected by plan 08-14, and the probe-defect entry plan
      08-14 appended.
    - `.planning/phases/08-process-lifecycle/deferred-items.md` — the existing three records and their
      form: what was found, where, why it was not fixed here, and a suggested resolution.
    - `.planning/phases/08-process-lifecycle/08-VERIFICATION.md` — the `deferred` block naming Phase 9
      as the Windows leg's owner, and the full `human_verification` list.
    - `.planning/ROADMAP.md` Phase 9 SC-4 and Phase 10 SC-5/SC-6 — so the ownership claims made here
      are checked against what those phases actually say rather than assumed.
  </read_first>

  <files>.planning/phases/08-process-lifecycle/08-SPIKE.md, .planning/WINDOWS.md, .planning/phases/08-process-lifecycle/deferred-items.md</files>

  <action>
    TRANSCRIBE FIRST. Every pasted value into its cell, byte for byte. Every unfilled cell gets a
    dated marked abstention naming what was attempted and what stopped it.

    THEN HANDLE THE A1 OUTCOME, whichever of the four it is, and handle it in the open:

      • INCONCLUSIVE. Record it verbatim with its reason token. Update the A1 verdict in `08-SPIKE.md`
        to state that the re-run was taken and returned inconclusive, and say what that establishes:
        the phase now knows the question cannot be answered by this instrument on this runtime, which
        is strictly more than it knew before. Leave A1 open. Do not retry.
      • THE GRANDCHILD SURVIVED. This falsifies A1 and it is the outcome with the largest blast
        radius — the POSIX group mechanism would not work on the shipping runtime at all. Record it,
        do not soften it, and raise it in the SUMMARY's first paragraph as a finding that needs a
        decision rather than a documentation update.
      • THE GRANDCHILD DIED. A1 closes favourably, this time on an instrument that could have said
        otherwise. Record it — and expect `verdict-gate.sh` to go RED on `08-SPIKE.md`, because this
        file will then carry a live favourable claim the other carriers do not. That red is correct.
        Do NOT touch the gate, do not add an exclusion, and do not weaken the pattern. Record the
        propagation as a named follow-up, with the gate's own ARM A output as the list of carriers to
        update, and say in the SUMMARY that the gate is expected red until that follow-up lands.
      • NOT TAKEN. A dated abstention, and A1 goes into the ownerless list below.

    THE CONTROL. Pair it explicitly with plan 08-16's post-fix counts and state the conclusion
    plainly, in whichever direction it falls. A non-zero pre-fix after-Stop count beside a zero
    post-fix count is the before/after pair this phase has wanted since 2026-08-24, and it is what
    finally excludes the confounder — say so. A zero pre-fix count means the defect did not reproduce
    on this machine and the post-fix zero proves less than it appears to — say that just as plainly.
    Update `08-UAT.md` test 3 out of pending only if a reading exists.

    UPDATE THE LEDGER. Entry 11 and the probe-defect entry both close on this re-run. Close an entry
    only if the reading actually closes it, and only with `gsd-tools windows fixed`. An inconclusive
    result does not close entry 11 — it narrows it, and a narrowed entry stays open with its
    description updated. Keep the markdown row and the JSON object byte-identical.

    WRITE THE OWNERSHIP RECORD into `deferred-items.md`, in the file's existing form. Two records:

      1. The Windows execution leg, RE-DEFERRED EXPLICITLY to Phase 9 SC-4 rather than dropped. State
        its closing condition verbatim from Phase 9's own text — one green matrix run executes the
        win32 kill-tree suite and fills the reserved measurement block — and name the ledger entry it
        corresponds to. Check the claim against Phase 9's actual wording rather than restating it from
        memory.
      2. THE OWNERLESS LIST. Every item this phase leaves open that no remaining phase owns. Name each
        one, say which phases were checked and why each was excluded, and state what would close it.
        If A1 was not measured, it is the first entry, and the verification report's own sentence
        about its ownerlessness should be quoted rather than paraphrased. Suggest a resolution in the
        file's house style — an entry point, not an assignment, because assigning it would imply a
        decision that has not been made.

    Do not touch `08-VERIFICATION.md`. The next verification round consumes these artifacts.
  </action>

  <verify>
    <automated>cd /Users/six2dez/Tools/drift &amp;&amp; node "$HOME/.claude/gsd-core/bin/gsd-tools.cjs" windows status</automated>
    <automated>cd /Users/six2dez/Tools/drift &amp;&amp; test "$(grep -c '^| [0-9]* |' .planning/WINDOWS.md)" = "$(grep -c '\"id\":' .planning/WINDOWS.md)"</automated>
    <automated>cd /Users/six2dez/Tools/drift &amp;&amp; npx vitest run</automated>
  </verify>

  <acceptance_criteria>
    - Every cell in both staged tables is either a verbatim reading or a dated marked abstention
      naming what was attempted and what stopped it. State the count of each; there are no unexplained
      blanks.
    - The maintainer's pasted block is quoted in the SUMMARY beside the resulting tables so the
      transcription is checkable byte for byte.
    - The A1 verdict recorded is one of exactly three permitted values. If the pasted value is
      anything else, that is reported as an anomaly and not normalised into one of the three.
    - The fixture-survivor count after the diagnostics call is recorded, and the SUMMARY states
      whether the original probe's leak is confirmed fixed, confirmed still present, or unmeasured.
    - The Control is explicitly paired with plan 08-16's post-fix counts, with the conclusion stated
      in whichever direction it falls, including the direction where the post-fix zero proves less
      than it appears to.
    - If A1 returned a favourable verdict: `verdict-gate.sh` is RED naming `08-SPIKE.md`, the SUMMARY
      says so in its first paragraph, the gate is unmodified (`git diff` on it is empty), and the
      propagation follow-up is recorded with the gate's ARM A output as its worklist.
    - If A1 returned inconclusive or was not taken: `verdict-gate.sh` exits 0 and ledger entry 11
      remains open with an updated description.
    - Any entry closed was closed with `gsd-tools windows fixed`; paste the command and output. State
      the open-entry id list before and after. Ledger descriptions are byte-identical across the
      markdown row and the JSON object — prove it with a programmatic diff.
    - `deferred-items.md` gains exactly two records: the Windows leg re-deferred to Phase 9 SC-4 with
      its closing condition quoted from Phase 9's own text, and the ownerless list naming every item,
      the phases checked, and why each was excluded.
    - The ownerless list quotes `08-VERIFICATION.md`'s own sentence about A1's ownerlessness verbatim
      rather than paraphrasing it, if A1 remains unmeasured.
    - `08-VERIFICATION.md` has no diff. `08-UAT.md` test 3 advances out of pending only if a Control
      reading exists.
    - `npx vitest run` is green at or above the running baseline; `git status --porcelain packages/`
      is empty; `git worktree list` shows one worktree.
  </acceptance_criteria>

  <verify>
    <human-check>
      Every reading the maintainer abstained on is a `human_needed` item and must reach the
      end-of-phase harvest carrying its stated blocker, rather than being closed by this plan. Carry
      each one forward verbatim. The A1 re-run in particular must not be marked resolved by the
      existence of this plan — only by a reading.
    </human-check>
  </verify>

  <done>
    The two readings only this hardware can produce are taken or honestly abstained, the pre-fix
    Control is paired with the post-fix count in whichever direction it falls, and every open item
    this phase leaves behind has a named owner or is written down as having none.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| unsigned probe build → the maintainer's live Caido | An uncommitted build carrying a process-spawning probe is installed into an application holding a live Caido session token. |
| maintainer's pasted output → the phase record | The only channel by which a runtime fact about the shipping LLRT enters this project. |
| a definite A1 verdict → every carrier, the roadmap, the requirements and the ledger | A new verdict must propagate everywhere or the repository becomes inconsistent in the opposite direction. |

## STRIDE Threat Register

*Threat-ID allocation for this gap-closure set (each ID unique across the set, and all above `08-SECURITY.md`'s maximum `T-08-50`, verified before allocating): **08-11** 51-54 · **08-12** 55, 57-59 · **08-13** 60-62 · **08-14** 63-66 · **08-15** 67, 68, 75, 76 · **08-16** 56, 69-71 · **08-17** 72-74, 77, 78.*

*Breadcrumb: `T-08-77` and `T-08-78` were originally drafted as 54/56 and collided with plan 08-11's `T-08-54` and plan 08-16's `T-08-56`. Renumbered for the same reason as 08-15's.*

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-77 | Denial of service | the probe build left installed | high | mitigate | The probe spawns two fixture processes per diagnostics call and the original could not clean them up. Mitigated by: the patch fixing the cleanup to use a spawned killer; an explicit open-the-panel-once instruction; a mandatory teardown step reinstalling the HEAD build; a fixture-survivor count taken as a reading so the fix is confirmed rather than assumed; and a resume-signal that asks the maintainer to confirm the teardown. |
| T-08-78 | Tampering | the recording of the A1 re-run | high | mitigate | Recording a favourable A1 that was not measured is the exact defect this whole gap-closure round exists to correct, and doing it twice would be unrecoverable. Mitigated by `gate="blocking-human"`, by requiring the recorded verdict to be one of exactly three permitted values with anomalies reported rather than normalised, by verbatim transcription checkable against the pasted block, and by a dated abstention path that is a first-class outcome. |
| T-08-72 | Tampering | `verdict-gate.sh`, under a favourable re-run | high | mitigate | A favourable A1 turns the gate red on `08-SPIKE.md`, and the cheapest way to make it green would be to weaken the gate — recreating the original defect from the other direction. Mitigated by stating the expected red in advance in `08-SPIKE.md` itself, by a prohibition against modifying the gate, and by an acceptance criterion requiring `git diff` on the gate to be empty in that branch. |
| T-08-73 | Elevation of privilege | unsigned build installed into Caido | medium | accept | The probe build is unsigned and installed by hand, bypassing the Ed25519 release pipeline. Accepted: it is built by the maintainer from a committed patch against a committed commit, on their own machine, verified to apply and build by a committed control, installed once and uninstalled. It is never published and the store pipeline is untouched. |
| T-08-74 | Repudiation | the ownerless list | medium | mitigate | An open item with no owner disappears at milestone close, which is how this phase inherited A1 in the first place. Mitigated by requiring the list to name the phases checked and the reason each was excluded, and to quote the verification report's own ownerlessness sentence rather than paraphrase it. |
</threat_model>

<verification>
- `gsd-tools windows status` runs; ledger table and JSON entry counts are equal; entry descriptions
  are byte-identical across both representations.
- `npx vitest run` green at or above the running baseline; `git status --porcelain packages/` empty;
  `git worktree list` shows one worktree.
- `08-VERIFICATION.md` has no diff.
- `verdict-gate.sh` is unmodified by this plan (`git diff` on it is empty) regardless of the A1
  outcome.
- Every table cell is a verbatim reading or a dated abstention; the two counts sum to the cell count.
- `deferred-items.md` carries the Windows re-deferral and the ownerless list.
</verification>

<success_criteria>
- A1 has a reading taken with an instrument that could have said otherwise, or a dated abstention that
  names what stopped it — and either way the phase's record matches what is known.
- The pre-fix Control exists, so this phase's POSIX evidence is a pair rather than an isolated zero,
  or its absence is recorded with a named blocker.
- Nothing this phase leaves open is unowned without being written down as unowned.
</success_criteria>

<output>
Create `.planning/phases/08-process-lifecycle/08-17-SUMMARY.md` when done. It MUST include: the
probe-build provenance (source commit, patch, package path and size), the maintainer's pasted block
quoted in full beside the resulting tables, the count of verbatim cells and abstained cells, the A1
outcome and its consequence for the verdict gate stated in the first paragraph, the Control/post-fix
pairing and its conclusion in whichever direction it falls, the ledger before/after open-entry id
lists, the confirmation that the HEAD build was reinstalled, and the full ownerless list.
</output>
