---
phase: 08-process-lifecycle
plan: 05
type: execute
wave: 4
depends_on: ["08-03", "08-04"]
files_modified:
  - .planning/ROADMAP.md
  - CLAUDE.md
  - .planning/codebase/CONVENTIONS.md
  - .planning/phases/08-process-lifecycle/08-VALIDATION.md
  - .planning/phases/08-process-lifecycle/08-SECURITY.md
autonomous: false
requirements: [LIF-01, LIF-02]
user_setup: []

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "ROADMAP SC-2 names the mechanism that actually shipped — `detached: true` at the provider spawn plus a spawned `kill` process-group signal — and states the LLRT reason the canonical spelling was unusable, so a later reader cannot 'fix' the code back to the broken form."
    - "CLAUDE.md's Pure Helpers Split table carries `kill-plan.ts` as its sixth row, and so does its generation source `.planning/codebase/CONVENTIONS.md`, so the next docs regeneration does not silently drop it."
    - "`08-VALIDATION.md`'s Per-Task Verification Map has zero `{pending}` task IDs: every row names the task that owns it, and every seed that turned out to be wrong is marked as a correction rather than silently overwritten."
    - "`08-SECURITY.md` exists and records the three residuals this phase accepts rather than mitigates, each with the reason it is not mitigable here and the phase or command that owns it next."
    - "On the maintainer's own macOS Caido, a real cancel leaves zero `mcp-server.mjs` processes — compared directly against the non-zero count plan 08-01 recorded on the same machine before any fix existed."
  artifacts:
    - ".planning/phases/08-process-lifecycle/08-SECURITY.md — the threat register roll-up and the accepted residuals"
    - ".planning/ROADMAP.md — SC-2 amended in place with the mechanism and the LLRT reason"
    - "CLAUDE.md + .planning/codebase/CONVENTIONS.md — the sixth pure-helper row"
  key_links:
    - "08-VALIDATION.md Per-Task Verification Map → the T-08-NN task IDs emitted by plans 08-01 through 08-04"
    - "08-SPIKE.md's control pgrep count → the closing human-verify's after-count"
    - "08-SECURITY.md residual (b) → OQ-3, deferred to its own discuss-phase"
    - "08-SECURITY.md residual (a) → ROADMAP Phase 10 SC-5's real-machine Windows confirmation"
---

<!-- planner-discipline-allow: process.kill(- -->
<!-- planner-discipline-allow: process\.kill(-pid, sig) -->

<objective>
Close the phase: amend the criterion whose stated mechanism research proved unusable, document the sixth
pure-helper module, fill the validation contract's task-ID column from the tasks that were actually
emitted, record what this phase accepts rather than fixes, and confirm on real hardware that the defect
the phase exists to close is gone.

Purpose: three of these are the difference between "the code is right" and "the next reader can tell the
code is right". SC-2 as written names a mechanism that throws under Caido's runtime; left unamended it is
an open invitation to a future "cleanup" that reintroduces the bug with a green suite. The
`{pending}` task-ID column is the seam between the validation contract and the plans that satisfy it. And
the residuals are the difference between an accepted risk and an unnoticed one.
Output: a phase whose artifacts agree with its code, and one measured confirmation from real hardware.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-process-lifecycle/08-RESEARCH.md
@.planning/phases/08-process-lifecycle/08-VALIDATION.md
@.planning/phases/08-process-lifecycle/08-SPIKE.md
@.planning/phases/08-process-lifecycle/08-03-SUMMARY.md
@.planning/phases/08-process-lifecycle/08-04-SUMMARY.md
@.planning/phases/07-provider-spawn-registration/07-SECURITY.md
@CLAUDE.md
</context>

## Artifacts this phase produces

See the phase-level table in `08-01-PLAN.md` § *Artifacts this phase produces*. The artifact **this plan**
creates is `.planning/phases/08-process-lifecycle/08-SECURITY.md`. Everything else this plan touches is an
edit to a file that already exists.

## Flagged assumptions — spec-less probe fallback accounting

This phase has **no SPEC**, so neither `## Edge Coverage` nor `## Prohibitions` was supplied and the
deterministic edge probe was run in their place. Accounting, so nothing was dropped silently: the probe
surfaced **4** items across LIF-01 and LIF-02; **3** were resolved with explicit acceptance criteria and
authored into `must_haves.truths` (LIF-02 *empty* → the refusal-arm criterion in plan 08-02; LIF-02
*ordering* → the kill-before-`rm` positional criterion in plan 08-03; LIF-02 *adjacency* → the
already-dead-pid measurement criterion in plan 08-04); and **1** remains flagged below. 3 authored + 1
flagged = 4 surfaced. Nothing was auto-dismissed.

> **⚠ FLAGGED ASSUMPTION — LIF-01, `unclassified`, `unresolved`.**
> The edge probe could not classify LIF-01 (*"On Windows, cancelling or timing out a turn terminates the
> whole process tree, leaving no orphaned token-bearing process"*) into any of its shape categories, and
> an unclassified row is never auto-resolved with a backstop. It is surfaced here rather than converted
> into a criterion the probe did not actually derive.
>
> What this means concretely: LIF-01's edge behaviour on Windows — which pids `/T` reaches when the tree
> is three levels deep, what happens when the intermediate `cmd.exe` has already exited, and what the
> vendor's exit codes mean — is covered in this phase only by what plan 08-04 could measure on a runner.
> The residual is real and is recorded as `08-SECURITY.md` residual (a). A verifier must **not** read the
> three resolved LIF-02 edges as though they also covered LIF-01.

<tasks>

<task type="auto">
  <name>T-08-12: Amend ROADMAP SC-2 in place, and add the sixth pure-helper row</name>
  <files>.planning/ROADMAP.md, CLAUDE.md, .planning/codebase/CONVENTIONS.md</files>
  <read_first>
    - `.planning/ROADMAP.md` lines 303-328 (the Phase 8 block: goal, depends-on, requirements, the five success criteria and the plans line) and lines 285-287 (Phase 7's SC-2, which is the **amend-in-place house format** to copy — the amended clause first, then a parenthetical `*(Amended <date> during Phase N planning, per `NN-RESEARCH.md` … — the original wording named X, which is not sufficient because Y … Same amend-in-place precedent as Phase 5 SC-1 per 05-CONTEXT.md D-02.)*`)
    - `.planning/ROADMAP.md` lines 190-233 (Phase 5, for the SC-1 amendment this precedent originates from)
    - `.planning/phases/08-process-lifecycle/08-RESEARCH.md` § *Q1* and § *Q2* — the exact source citations (repository, branch, commit and file:line) the amendment must carry
    - `CLAUDE.md` lines 94 and 125-139 — the `<!-- GSD:conventions-start source:CONVENTIONS.md -->` block marker, the Pure Helpers Split table, and the `<!-- GSD:conventions-end -->` marker
    - `.planning/codebase/CONVENTIONS.md` lines 134-146 — the generation **source** of that block
    - `.planning/phases/08-process-lifecycle/08-RESEARCH.md` § *The module split* — the row text for `kill-plan.ts` and the four modules the shipped table is already missing
  </read_first>
  <action>
**1. Amend ROADMAP SC-2 in place (maintainer decision D-01).** Use a scoped `Edit` on the SC-2 bullet
only — never rewrite `.planning/ROADMAP.md` wholesale; every other phase entry in that file must be
byte-identical afterwards.

Rewrite the mechanism clause so it names what actually shipped: `detached: true` at the provider spawn
**plus a spawned `kill` process-group signal**, rather than the bare phrase "process-group signalling".
Then append a parenthetical amendment note in the Phase 7 SC-2 format, carrying all of:

- the date and that it was amended during Phase 8 planning, per `08-RESEARCH.md` § *Q2*
- what the original wording implied and why it is not usable: the canonical spelling of a group signal is
  `process.kill(-pid, sig)`, and Caido's LLRT types `process.kill`'s `pid` parameter as a Rust `u32` while
  rquickjs converts through `f64` with an explicit range check, so a negative pid raises an `Underflow`
  conversion error before `libc::kill` is reached
- the reason this is dangerous rather than merely inconvenient: it works perfectly under Node, which is
  the only vehicle any CI leg in this repository runs, so the broken spelling would be green on all five
  legs and broken on every real Caido install — Phase 5 finding L-4 recurring verbatim
- that `detached: true` itself **is** honoured, source-verified at `caido/dependency-llrt` branch `caido`
  commit `a5b021c`, `modules/llrt_child_process/src/lib.rs:512-521` (`command.process_group(0)`, i.e.
  `setpgid(0,0)`, so pgid equals the child pid)
- that a comment-stripped static gate over `packages/backend/src` keeps the broken spelling out, and that
  it is comment-stripped **deliberately**, because the module and orchestrator comments quote the banned
  form by house rule so a future reader does not reintroduce it
- the precedent line: same amend-in-place tradition as Phase 5 SC-1 and Phase 7 SC-2, per
  `05-CONTEXT.md` D-02

Do not touch SC-1, SC-3, SC-4 or SC-5, and do not touch the Phase 8 goal line.

**2. Add the `kill-plan.ts` row to both copies of the Pure Helpers Split table.** Edit
`.planning/codebase/CONVENTIONS.md` (the generation source, lines 138-144) **and** `CLAUDE.md`'s rendered
block (lines 126-132) with the same row: module `packages/backend/src/kill-plan.ts`; what it owns — the
platform-branched termination argv, the refusal cases and the `detached` decision; what `index.ts` owns —
spawning the killer, ordering kill-before-sweep, and reading `activeProcesses`.

Editing only `CLAUDE.md` would be silently reverted the next time the conventions block is regenerated
from its source, which is exactly how a documentation row disappears without anyone noticing.

Add one line beneath the table, in both files, recording that the table is **known incomplete**: it omits
`platform.ts`, `spawn-plan.ts`, `mcp-server-spec.ts` and `fs-retry.ts`, which `08-RESEARCH.md`
§ *The module split* lists, and a full refresh belongs to `/gsd-docs-update` rather than to this phase.
Do **not** add those four rows here — expanding a generated block beyond the phase's own scope is how
these files drift from their sources.
  </action>
  <verify>
    <automated>grep -c 'kill-plan.ts' CLAUDE.md .planning/codebase/CONVENTIONS.md && grep -c 'Amended 2026-08-24 during Phase 8 planning' .planning/ROADMAP.md && test "$(grep -c '^### Phase' .planning/ROADMAP.md)" = "24"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^### Phase' .planning/ROADMAP.md` returns `24` — the same count as before the edit, proving no phase entry was destroyed by a whole-file write
    - The SC-2 bullet itself names the shipped mechanism: `sed -n '/^### Phase 8:/,/^### Phase 9:/p' .planning/ROADMAP.md | grep -c 'detached: true' ` returns at least `1` and the same slice greps non-zero for `kill` as a spawned utility
    - `grep -c 'Amended 2026-08-24 during Phase 8 planning' .planning/ROADMAP.md` returns `1` — pre-edit baseline is `0`
    - `grep -c 'Underflow' .planning/ROADMAP.md` returns `2` — pre-edit baseline is `1` (the Research-flag line already carries one); the amendment adds the second
    - `grep -c 'a5b021c' .planning/ROADMAP.md` returns at least `1` — pre-edit baseline is `0`; the amendment cites the pinned commit rather than asserting the behaviour
    - `grep -c '05-CONTEXT.md' .planning/ROADMAP.md` returns `5` — pre-edit baseline is `4`; the new Phase 8 amendment note cites the same precedent Phase 5 SC-1 and Phase 7 SC-2 do
    - `grep -c 'kill-plan.ts' CLAUDE.md` returns `1` and `grep -c 'kill-plan.ts' .planning/codebase/CONVENTIONS.md` returns `1`
    - `grep -c 'fs-retry.ts' CLAUDE.md` returns `1` — the known-incomplete note names the four omitted modules, and `grep -c '| \`packages/backend/src/fs-retry.ts\` |' CLAUDE.md` returns `0`, proving the note was added rather than the rows
    - `git diff --stat -- .planning/ROADMAP.md` shows a single-digit changed-line count confined to the Phase 8 block
  </acceptance_criteria>
  <done>SC-2 names the shipped mechanism and cites the source-verified reason the original spelling was unusable; both copies of the pure-helper table carry `kill-plan.ts` and a note that they are otherwise stale; no other ROADMAP phase entry changed.</done>
</task>

<task type="auto">
  <name>T-08-13: Fill the validation contract's task IDs and write 08-SECURITY.md</name>
  <files>.planning/phases/08-process-lifecycle/08-VALIDATION.md, .planning/phases/08-process-lifecycle/08-SECURITY.md</files>
  <read_first>
    - `.planning/phases/08-process-lifecycle/08-VALIDATION.md` in full — the frontmatter flags (`status`, `nyquist_compliant`, `wave_0_complete`), the note at the top that task IDs are `{pending}` until the planner emits them and that a wrong seed must be marked as a **correction** rather than silently overwritten (the `07-VALIDATION.md` convention), the fifteen-row Per-Task Verification Map, and the Validation Sign-Off checklist
    - `.planning/phases/07-provider-spawn-registration/07-VALIDATION.md` § *Rows the seed did not anticipate* — the house format for recording a seed correction
    - `.planning/phases/07-provider-spawn-registration/07-SECURITY.md` in full — the frontmatter shape (`status`, `threats_open`, `asvs_level`, `block_on`, `audited_at_head`, `register_authored_at_plan_time`), the "what this phase changed about credential exposure" opener, the trust-boundary table, the threat register and the Accepted Risks section with its `AR-NN` identifiers
    - All five Phase 8 `<threat_model>` blocks (`08-01` through `08-05` PLAN.md) — the register this document rolls up
    - `.planning/phases/08-process-lifecycle/08-RESEARCH.md` § *Security Domain*, § *Open Questions* OQ-3, and Pitfall 5
    - The four SUMMARY files from plans 08-01 through 08-04 — the measured counts and the Windows run URL this document cites
  </read_first>
  <action>
**1. Fill `08-VALIDATION.md`'s Per-Task Verification Map.** Replace every `{pending}` in the Task ID and
Plan columns with the task IDs and plan numbers actually emitted:

| Row (by its Secure Behavior text) | Task | Plan |
|---|---|---|
| `buildKillTreePlan` win32 argv + SystemRoot handling | T-08-05 | 02 |
| bad-pid refusal arms | T-08-05 | 02 |
| POSIX arm + rungs + `undefined` platform | T-08-05 | 02 |
| `shouldDetachProviderSpawn` | T-08-05 | 02 |
| POSIX behavioural control + proof | T-08-04 | 02 |
| win32 integration: resolve, run, record the dead-pid exit code | T-08-10 | 04 |
| the `--reporter=json` CI gate step | T-08-11 | 04 |
| the every-platform gate guard | T-08-11 | 04 |
| kill precedes every `rm` at the three sites | T-08-09 | 03 |
| `cleanupMcpRuntime` kills at all | T-08-08 | 03 |
| all in-scope sites route through `killTree` | T-08-09 | 03 |
| the LLRT-trap static gate | T-08-06 | 02 |
| `shell: true` appears nowhere | T-08-09 | 03 |
| full-suite regression + grown test count | T-08-14 | 05 |
| the frontend cancel-race tripwire | T-08-14 | 05 |

Set every row's Status from the plan SUMMARYs — ✅ where its command is green, ⚠️ or ❌ otherwise — and
correct the File Exists column now that the files exist.

Then add a `## Corrections to the seed` section, in the `07-VALIDATION.md` format, recording **three**
corrections as corrections rather than overwriting them silently:

- **The LLRT-trap gate's command was wrong as seeded.** The seed specified a raw
  `grep -rc 'process\.kill(-' packages/backend/src`. That spelling goes red against the module and
  orchestrator **comments**, which quote the banned form verbatim by house rule precisely so a future
  reader does not reintroduce it. The shipped gate is the comment-stripped vitest assertion in
  `index.source.test.ts` (T-08-06), reusing the existing whole-line-only `stripCommentLines`; the shell
  form kept in the plans adds `| grep -vE ':[0-9]+:[[:space:]]*(//|\*)'`. Note also that the seed's
  alternative `sed 's://.*::'` strips `//` inside string literals, which `stripCommentLines` explicitly
  avoids and `index.ts` — carrying URLs and Windows-shaped paths — would be corrupted by.
- **The ordering gate's vehicle changed** from the seeded `awk`+`sed` shell pipeline to an in-test
  `functionBody(code, name)` helper (D-P1, plan 08-03), for the same string-literal reason plus the fact
  that a vitest assertion runs on all five CI legs unprompted. The shell form is retained as an
  independent second read, not as the evidence.
- **The `killTree(` call-site count is not 5.** The seed said *"a `killTree(` call-site count of 5 (or the
  count the reviewer measures)"*. The measured count is the one plan 08-03's SUMMARY records — one
  declaration plus eight call sites, because `requestGracefulShutdown` and `cancelCliMessage` each carry
  two rungs. Cite the measured numbers, not the seed's.

Finally set the frontmatter: `wave_0_complete: true`, `status: validated`, and `nyquist_compliant` to
`true` only if every row's Status is ✅ — otherwise leave it `false` and say which row is not, because a
`validated` document with a false compliance flag is how `audit-milestone` distinguishes PARTIAL from
NOT-VALIDATED. Tick the Sign-Off checklist items that are genuinely true and leave the rest unticked.

**2. Write `.planning/phases/08-process-lifecycle/08-SECURITY.md`**, modelled on `07-SECURITY.md`:
frontmatter (`phase: 8`, `slug: process-lifecycle`, `status: secured`, `asvs_level: 1`, `block_on: high`,
`threats_open`, `audited_at_head`, `register_authored_at_plan_time: true`); an opening section titled
*"What this phase changed about credential exposure"* stating the phase's defining security fact — before
Phase 8, a cancel left the MCP child holding a live Caido bearer token in its environment on **both**
platforms, and the temp sweep deleted the child's context file while the child lived, destroying the
forensic trail without removing the capability; a trust-boundary table; the rolled-up threat register
from all five `<threat_model>` blocks with each row's final disposition; and an **Accepted Risks** section
carrying these three, each with an `AR-NN` identifier, the reason it is not mitigable in this phase, and
who owns it next:

- **`/T` cannot reach a grandchild whose intermediate parent has already exited** (Pitfall 5, assumption
  A3). Not mitigable without a Windows Job Object with `JOB_OBJECT_LIMIT_KILL_ON_JOB_CLOSE`, which
  requires Win32 API calls neither Caido's LLRT nor Node exposes without a native addon — banned by the
  QuickJS runtime constraint. Mitigating facts: `cmd.exe /c` normally waits for its child, so the window
  is narrow, and Phase 6 already prefers `.exe` over `.cmd` (RES-02), which removes the level entirely
  wherever a native install exists. Owner: ROADMAP Phase 10 SC-5's real-machine confirmation is the only
  place this could be observed.
- **A hard-killed Caido leaves an MCP orphan that no start-up sweep touches** (OQ-3).
  `sweepOrphanedMcpTempDirs` removes residue **directories**, never processes, so a token-bearing process
  from a previous run survives indefinitely with its context file deleted underneath it. Deliberately
  **out of scope** for Phase 8: identifying "a Drift MCP process from a previous run" requires image-name
  matching that could terminate an unrelated `node`, and a blast-radius decision of that kind needs its
  own `/gsd-discuss-phase`. Recorded as a residual, not planned as a task.
- **Real revocation is not available to Drift.** Killing the process removes the capability from that
  process; it does not invalidate the token, which would require rotating the Caido session credential —
  something Drift cannot do. Any orphan that escaped every mechanism in this phase still holds a valid
  bearer token until the user's Caido session ends.

Add a short *"What this phase's evidence does not cover"* section that reproduces
`08-VALIDATION.md` § *Vehicle caveat*'s five items by reference rather than restating them softer, and
name the flagged LIF-01 edge-probe assumption from this plan's `## Flagged assumptions` section so a
security reader sees it alongside the residuals.
  </action>
  <verify>
    <automated>test "$(grep -c '{pending}' .planning/phases/08-process-lifecycle/08-VALIDATION.md)" = "0" && test -f .planning/phases/08-process-lifecycle/08-SECURITY.md && grep -q 'block_on: high' .planning/phases/08-process-lifecycle/08-SECURITY.md && grep -q 'AR-01' .planning/phases/08-process-lifecycle/08-SECURITY.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '{pending}' .planning/phases/08-process-lifecycle/08-VALIDATION.md` returns `0`
    - `grep -c 'T-08-' .planning/phases/08-process-lifecycle/08-VALIDATION.md` returns at least `15` — every row in the Per-Task Verification Map names a task
    - `grep -c '## Corrections to the seed' .planning/phases/08-process-lifecycle/08-VALIDATION.md` returns `1`, and that section names all three corrections (the LLRT-trap command, the ordering vehicle, and the call-site count)
    - `grep -c 'wave_0_complete: true' .planning/phases/08-process-lifecycle/08-VALIDATION.md` returns `1` and `grep -c 'status: validated' .planning/phases/08-process-lifecycle/08-VALIDATION.md` returns `1`
    - `.planning/phases/08-process-lifecycle/08-SECURITY.md` exists and its frontmatter carries `asvs_level: 1` and `block_on: high`
    - `grep -c 'AR-0' .planning/phases/08-process-lifecycle/08-SECURITY.md` returns at least `3` — the three accepted residuals each have an identifier
    - `grep -c 'T-08-' .planning/phases/08-process-lifecycle/08-SECURITY.md` returns at least `10` — the register is a roll-up of the five plan blocks, not a fresh shorter list
    - `grep -c 'OQ-3' .planning/phases/08-process-lifecycle/08-SECURITY.md` returns at least `1` — the start-up orphan-process sweep is recorded as a deferred residual, in the security artifact, exactly where the maintainer's decision placed it
    - Every threat rated `high` in any Phase 8 `<threat_model>` block appears in `08-SECURITY.md` with disposition `mitigate` and a non-empty mitigation; `threats_open` counts only rows at or above `high` that are still open
  </acceptance_criteria>
  <done>The validation contract names an owning task for every row, records its three seed corrections as corrections, and declares its own compliance honestly; `08-SECURITY.md` rolls up the register and records three identified accepted residuals including OQ-3.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>T-08-14: Confirm on real hardware that a cancel leaves no token-bearing orphan</name>
  <files>none — this task produces a confirmation, not edits</files>
  <read_first>
    - `.planning/phases/08-process-lifecycle/08-SPIKE.md` § *Control* — the two `pgrep -f mcp-server.mjs` counts recorded on this same machine **before** any fix existed
    - `.planning/phases/08-process-lifecycle/08-VALIDATION.md` § *Manual-Only Verifications* row 1 — the phase-gating manual check
    - `.planning/ROADMAP.md` Phase 10 SC-5 — the Windows equivalent of this check, which is **not** this phase's and is not satisfied by anything here
  </read_first>
  <what-built>
Phase 8 is code-complete. `packages/backend/src/kill-plan.ts` decides what to spawn to terminate a whole
process tree on either platform; `killTree` in `index.ts` performs it fire-and-forget at all eight
in-scope cancel, timeout, close and cleanup sites; the provider spawn now carries
`detached: shouldDetachProviderSpawn(host?.platform)`; and every file removal at `cleanupMcpRuntime`,
`closeCliSession` and `deleteChat` now happens **after** the termination statement. Proven automatically:
the POSIX mechanism behaviourally with a falsifying control, the Windows mechanism behaviourally on the
`windows-latest` runner, the full argv contract by unit test, and the statement ordering by two
independent source scanners.

What remains is the one thing no runner can produce: a real cancel, in real Caido, on real hardware.
  </what-built>
  <how-to-verify>
Before handing over, the executor runs the full automated close and reports its results, so the human step
is the only thing outstanding:

- `pnpm exec vitest run` — must be green, with the total **grown** from the 534 baseline and **zero** tests
  removed as obsolete. Report the total and the delta; CMP-01 is a count comparison, not a vibe.
- `pnpm -r typecheck` and `pnpm lint` — must exit 0
- `git diff --stat 1b3fde6 -- packages/frontend/src/views/ChatView.cancel.test.ts packages/frontend/src/views/ChatView.vue` — must be **empty**. This is SC-5's tripwire: the user-visible `[Cancelled]` semantics are byte-unchanged, which is what "cancellation semantics visible to the user are unchanged" means operationally.
- the comment-filtered LLRT-trap scan and the repo-wide `shell: *true` scan — both `0`
- the `windows-latest` run URL and per-step conclusions from plan 08-04's SUMMARY, including the kill-tree
  gate step's executed-count log line

Then the maintainer performs these steps and reports the readings:

1. `pnpm build`, install the built plugin in Caido, restart Caido.
2. Start a Claude Code chat turn with the Drift MCP attached.
3. While the turn is running: `pgrep -f mcp-server.mjs | wc -l` — record the count. It must be non-zero,
   or the test is not exercising anything.
4. Click Stop in the Drift chat. Wait ~5 seconds.
5. `pgrep -f mcp-server.mjs | wc -l` — record the count. **Expected: zero.**
6. Repeat once for the timeout path if practical: start a turn and let `processTimeoutSeconds` elapse
   rather than clicking Stop, then re-run the `pgrep`. Report the count, or report that the path was not
   exercised — do not infer it from the cancel result.
7. Report all counts against the numbers `08-SPIKE.md` recorded on this same machine before the fix, so
   the confirmation is a **comparison**, not an isolated zero.

If the after-count is non-zero, the phase is not closed: report the surviving pid's `ps -o pid,ppid,pgid,comm`
row, which distinguishes "the group signal missed it" (a different pgid — assumption A6 wrong) from "the
kill never ran" (same pgid, still alive).

State plainly, in the report, what this confirmation does **not** establish: nothing about Windows. The
Windows behavioural half is ROADMAP Phase 10 SC-5's, whose text already guards the substitution trap —
another green CI run does not satisfy it. Per maintainer decision **D-02**, this phase does not
pre-declare SC-3 PARTIAL; it presents exactly what evidence each half has and lets `/gsd-verify-work` rule.
  </how-to-verify>
  <resume-signal>
Reply with the before-Stop and after-Stop `pgrep -f mcp-server.mjs` counts, the timeout-path result (or an
explicit "not exercised"), and the automated close figures. Reply `approved` when the after-Stop count is
**zero** against a non-zero before-Stop count. A non-zero after-count is a **halt**: reply with the
surviving pid's `ps -o pid,ppid,pgid,comm` row instead, which distinguishes a missed group from a kill
that never ran, and the phase is not closed.
  </resume-signal>
  <action>
Stop and hand control to the maintainer. `08-VALIDATION.md` marks this as the one manual verification that
**gates this phase**: cheap, decisive, and on hardware the maintainer already owns. Run the automated close
first and report it, then present the `<how-to-verify>` steps and collect the readings verbatim, comparing
each against the pre-fix counts `08-SPIKE.md` recorded on the same machine.
  </action>
  <verify>
    <human-check>On the maintainer's macOS Caido: `pgrep -f mcp-server.mjs | wc -l` is non-zero during a live turn and **zero** ~5 s after clicking Stop, reported alongside the pre-fix counts from 08-SPIKE.md.</human-check>
    <automated>pnpm exec vitest run && pnpm -r typecheck && pnpm lint && test -z "$(git diff --stat 1b3fde6 -- packages/frontend/src/views/ChatView.cancel.test.ts packages/frontend/src/views/ChatView.vue)" && test "$(grep -rn 'process\.kill(-' packages/backend/src --include='*.ts' | grep -vE ':[0-9]+:[[:space:]]*(//|\*)' | wc -l | tr -d ' ')" = "0" && test "$(grep -rn 'shell: *true' packages/ --include='*.ts' | wc -l | tr -d ' ')" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm exec vitest run` green; total strictly greater than 534; zero previously-existing tests removed — both numbers reported, not asserted from memory
    - `pnpm -r typecheck` and `pnpm lint` exit 0
    - `git diff --stat 1b3fde6 -- packages/frontend/src/views/ChatView.cancel.test.ts packages/frontend/src/views/ChatView.vue` is empty (SC-5 tripwire)
    - The comment-filtered LLRT-trap scan returns `0` and the repo-wide `shell: *true` scan returns `0`
    - The maintainer reports a non-zero `pgrep` count during a live turn and a zero count after Stop, both compared against `08-SPIKE.md`'s pre-fix counts
    - The timeout path is either exercised and reported, or explicitly reported as not exercised — never inferred from the cancel result
    - A non-zero after-count halts the phase and produces the surviving pid's `ps -o pid,ppid,pgid,comm` row rather than a retry
  </acceptance_criteria>
  <done>The full automated close is green with a grown, nothing-removed test count and an empty frontend tripwire; and the maintainer has confirmed on real hardware that a cancel that previously left a token-bearing MCP child now leaves none.</done>
</task>

</tasks>

<threat_model>
ASVS L1. Blocking threshold: `high`. Source: `08-RESEARCH.md` § *Security Domain*.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the phase's artifacts → the next reader | A criterion that names an unusable mechanism is an instruction to reintroduce a defect. Documentation is a control here, not decoration. |
| user consent withdrawal → the live agent | The closing confirmation is the only end-to-end observation, on real hardware, that consent withdrawal now actually withdraws capability. |
| generated documentation block → its generation source | A row added only to the rendered copy is silently reverted on the next regeneration. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-06 | Repudiation (a security control that fails green) | ROADMAP SC-2's unamended mechanism clause | high | mitigate | T-08-12 amends SC-2 in place with the mechanism, the source-verified LLRT reason and the pinned commit. An unamended criterion is a standing instruction to a future reader to "fix" the code back to a spelling that throws under Caido's runtime while passing on every CI leg. The static gate stops the code; the amendment stops the intent. |
| T-08-01 | Information Disclosure → Elevation of Privilege | the closing confirmation on real hardware | high | mitigate | T-08-14 measures the orphan count directly, against a pre-fix baseline taken on the same machine in plan 08-01. Without the baseline a zero is unfalsifiable — it could mean the fix works or that the turn never spawned an MCP child. |
| T-08-18 | Repudiation | a documentation row added only to `CLAUDE.md` | low | mitigate | T-08-12 edits both `CLAUDE.md` and its generation source `.planning/codebase/CONVENTIONS.md`, with a grep for each as an acceptance criterion. |
| T-08-19 | Repudiation | a `{pending}` validation row silently overwritten with a wrong seed | medium | mitigate | The three seed corrections are recorded as **corrections** in a named section, per the `07-VALIDATION.md` convention, rather than overwritten. `nyquist_compliant` is set from the actual row statuses, so a `validated` document with unmet rows is distinguishable from a compliant one. |
| T-08-07 | Information Disclosure | `/T`'s dead-intermediate-parent residual | low | accept | `08-SECURITY.md` AR-01. Not mitigable without a Windows Job Object, which the runtime does not expose. |
| T-08-08 | Information Disclosure | a hard-killed Caido's surviving MCP orphan (OQ-3) | medium | accept | `08-SECURITY.md` AR-02, recorded as a residual with its reason: identifying such a process requires image-name matching that could terminate an unrelated `node`, and that blast-radius decision needs its own discuss-phase. Deliberately not planned as a task in this phase. |
| T-08-20 | Information Disclosure | no real revocation available | medium | accept | `08-SECURITY.md` AR-03. Killing the process removes the capability from that process; invalidating the credential would require rotating the Caido session token, which Drift cannot do. |
| T-08-SC | Tampering | npm/pip/cargo installs | n/a | accept | This phase installs zero packages; `08-RESEARCH.md` § *Package Legitimacy Audit* is present and empty. |
</threat_model>

<verification>
- `grep -c '{pending}' .planning/phases/08-process-lifecycle/08-VALIDATION.md` — `0`
- `test -f .planning/phases/08-process-lifecycle/08-SECURITY.md` — exists, with `asvs_level: 1` and `block_on: high`
- `grep -c '^### Phase' .planning/ROADMAP.md` — `24`, unchanged
- `grep -c 'kill-plan.ts' CLAUDE.md .planning/codebase/CONVENTIONS.md` — `1` each
- `pnpm exec vitest run` — green, total > 534, zero tests removed (CMP-01)
- `pnpm -r typecheck` and `pnpm lint` — exit 0
- `git diff --stat 1b3fde6 -- packages/frontend/src/views/ChatView.cancel.test.ts packages/frontend/src/views/ChatView.vue` — empty (SC-5 tripwire)
- `grep -rn 'process\.kill(-' packages/backend/src --include='*.ts' | grep -vE ':[0-9]+:[[:space:]]*(//|\*)' | wc -l` — `0`
- `test "$(grep -rn 'shell: *true' packages/ --include='*.ts' | wc -l | tr -d ' ')" = "0"`
- The maintainer's `pgrep -f mcp-server.mjs` after-count — zero, against the pre-fix count in `08-SPIKE.md`
</verification>

<success_criteria>
- SC-2 names the mechanism that shipped and the source-verified reason the original spelling could not
- The pure-helper table carries its sixth row in both the rendered and the source copy
- Every validation row names an owning task, and every wrong seed is marked as a correction
- The three accepted residuals are identified, reasoned and assigned an owner
- A real cancel on real hardware leaves zero token-bearing MCP processes, measured against a pre-fix baseline
</success_criteria>

<output>
Create `.planning/phases/08-process-lifecycle/08-05-SUMMARY.md` when done. Record the final test total and
its delta from 534, the `windows-latest` run URL, the maintainer's before/after `pgrep` counts against
`08-SPIKE.md`'s pre-fix numbers, and — explicitly — that SC-3's Windows behavioural half is evidenced only
as a mechanism proof on a CI runner and is owned for real-hardware confirmation by ROADMAP Phase 10 SC-5.
Do not pre-declare a verdict for it (maintainer decision D-02): present the evidence and let
`/gsd-verify-work` rule.
</output>
