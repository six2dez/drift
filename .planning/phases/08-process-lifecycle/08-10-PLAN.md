---
phase: 08-process-lifecycle
plan: 10
type: execute
wave: 5
depends_on: ["08-09"]
gap_closure: true
autonomous: true
requirements: [LIF-01, LIF-02]
files_modified:
  - .planning/phases/08-process-lifecycle/08-SPIKE.md
  - .planning/phases/08-process-lifecycle/08-VALIDATION.md
  - .planning/phases/08-process-lifecycle/08-SECURITY.md
  - .planning/phases/08-process-lifecycle/08-VERIFICATION.md
  - .planning/phases/08-process-lifecycle/08-RESEARCH.md
  - .planning/phases/08-process-lifecycle/08-01-PLAN.md
  - .planning/phases/08-process-lifecycle/08-05-PLAN.md
  - .planning/phases/08-process-lifecycle/deferred-items.md
  - .planning/phases/08-process-lifecycle/COVERAGE.md
  - .planning/phases/08-process-lifecycle/verdict-gate.sh
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/WINDOWS.md
  - .planning/STATE.md

estimate:
  tokens: 62000
  raw_tokens: 62000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "No LIVING artifact presents A1 or A6 as unmeasured. A1 reads closed favourably and A6 reads falsified, both dated 2026-08-27, in all NINE carriers: `08-SPIKE.md`, `08-VALIDATION.md`, `08-SECURITY.md`, `08-VERIFICATION.md`, `.planning/WINDOWS.md`, `.planning/STATE.md`, `kill-plan.ts`, `index.ts` and `kill-tree.posix.test.ts`. Measured at HEAD 2026-08-27 with the three-spelling ERE: 15 hits total — 5 source hits across 3 files and 10 document hits across 6 files. This census was wrong twice: an early draft named four carriers, a later one named seven, and BOTH would have shipped this truth false. The count is no longer the gate's contract — `verdict-gate.sh` scans `.planning/` and `packages/` repo-wide and excludes a named historical-record class, so a tenth carrier fails CLOSED instead of going undetected. The enumeration above is calibration; the scan is the contract."
    - "Dated SUMMARY files are historical records and are NOT rewritten — a SUMMARY dated 2026-08-24 stating the verdicts were open was correct on its date. The rule that separates a living reference from a historical record is written down, and the gate asserts the SUMMARY class is unmodified rather than leaving its exclusion implicit."
    - "Every superseded verdict remains visible beside its replacement inside a marked-correction block; nothing was silently overwritten."
    - "The unrunnable A6 command survives nowhere as an instruction: corrected in the two living reference documents, annotated as superseded in the two executed plans, with the rule that decides which treatment applies written down."
    - "ROADMAP SC-2 no longer claims the group signal replaces the single-pid ladder, and REQUIREMENTS.md no longer ticks LIF-01 or LIF-02 as complete."
    - "G-02 is recorded as degraded-as-designed with no code change, and the stale 08-01 checkpoint prediction that preceded the CR-02 fix is retired."
    - "The SC-4 completion-order question, the win32 orphan class and the multi-session cancel gap are each explicitly decided and declared as named residuals with a named decider, not left implicit."
    - "Every accepted residual at or above the `block_on: high` severity carries an `Accepted By` cell naming the maintainer and citing the recorded decision — an accepted high with only an owner is a silent carry past the phase's own blocking gate."
    - "Broken-windows ledger entry 11 reflects the measurement; entry 12 is untouched."
  artifacts:
    - ".planning/phases/08-process-lifecycle/COVERAGE.md — the reasoned no-external-API declaration"
    - ".planning/phases/08-process-lifecycle/08-SECURITY.md — re-rated threats and residuals AR-04, AR-05, AR-06, AR-07, each with a named decider in the Accepted By column"
    - ".planning/WINDOWS.md — ledger entry 11 rewritten to the measured state, in both the table and the JSON block"
  key_links:
    - "08-SPIKE.md frontmatter verdicts -> the 2026-08-27 readings recorded in 08-UAT.md tests 1 and 2"
    - "08-SECURITY.md AR-02 -> the reap mechanism plans 08-06 and 08-07 shipped"
    - "REQUIREMENTS.md LIF status -> 08-VERIFICATION.md's human_needed verdict"
  prohibitions:
    - "No verdict written that cannot be cited to a reading recorded in 08-UAT.md. The Control measurement was NOT taken and must continue to read as not taken."
    - "No executed plan rewritten. 08-01-PLAN.md and 08-05-PLAN.md receive dated annotations only."
    - "No superseded text deleted. Marked corrections preserve it inside a block quote."
    - "No source file under packages/ modified by this plan."
    - "No change to broken-windows ledger entry 12."
---

<objective>
Bring every contract document in line with what was measured on 2026-08-27, and decide in
writing the three questions the verifier and the UAT left open.

Four kinds of work, all records rather than code:

1. **A1 and A6 are now MEASURED.** `08-SPIKE.md` frontmatter still reads `status: not-run`
   with `A1: OPEN` and `A6: OPEN`, and its three result tables still say "not recorded — spike
   not run". `08-VALIDATION.md` (lines 254, 273, 298, 301, 318) and `08-SECURITY.md` (lines
   244-264, 287-288, 344) repeat it. A1 is closed favourably —
   `spikeDetachedGroupKill: grandchild-died (detached honoured)` on darwin 25.6.0, probe build
   68199fa. A6 is FALSIFIED — codex pid 43921 in pgid 43752, its `mcp-server.mjs` child pid
   44284 in pgid 44284.

   **There are NINE carriers, and this census was wrong TWICE before this draft** — first four,
   then seven; each time the must_haves truth would have shipped false. Measured at HEAD
   2026-08-27, 15 hits across 9 files: three source files (`kill-plan.ts` ×2,
   `kill-tree.posix.test.ts` ×2, `index.ts` ×1 — the last inside `killTree`'s single-pid-rung
   comment, i.e. production source at the phase's central mechanism) and six documents
   (`08-SPIKE.md` ×2, `08-VALIDATION.md` ×1, `08-SECURITY.md` ×2, `08-VERIFICATION.md` ×2,
   **`.planning/WINDOWS.md` ×2** and **`.planning/STATE.md` ×1**). Plan 08-06 corrects the three
   source carriers; this plan corrects the six documents (Task 1 the four phase documents, Task
   3 the two project-level ones) and gates all nine together.

   **THREE spellings, and the gate must know all three.** Measured 2026-08-27: eleven hits
   across seven files write `OPEN — not measured` with an EM DASH; `08-SECURITY.md:247` and
   `:250` write `A1 — OPEN, not measured` with a COMMA; and `.planning/WINDOWS.md:28` and `:159`
   — ledger entry 11, in the markdown row and the JSON block — write `OPEN - not measured` with
   a plain **ASCII HYPHEN**. The hyphen form is the one no previous draft named at all, and
   under a fixed-string em-dash pattern `WINDOWS.md` scores `0` and disappears from the census
   entirely. A gate greping only the em-dash form returns `0` for `08-SECURITY.md` AND for
   `WINDOWS.md` vacuously — green while both stale claims sit untouched. That is the same
   pass-by-accident shape as the `grep -c … | grep -v` gate this gap set removed from plan
   08-09, and it is why `verdict-gate.sh` uses the character-class ERE
   `OPEN[[:space:]]*[,—-][[:space:]]*not measured` rather than a fixed string. The hyphen sits
   last inside `[,—-]` so it is a literal and not a range endpoint; verify all three forms match
   before committing the script.

   **The gate is a repo-wide scan, not a list of carriers.** Two censuses in a row were wrong,
   which is the evidence that an enumerated gate cannot be trusted to stay complete: an
   inclusion-list loop can only ever check files it already names, so a carrier missing from the
   list is invisible to it BY CONSTRUCTION rather than by accident. `verdict-gate.sh` therefore
   inverts — it scans `.planning/` and `packages/` and skips a short, commented EXCLUSION list —
   so the default for any new or forgotten carrier is FAIL, not silence.

   **The historical-record class is NOT carriers, and the exclusion list is exactly that class.**
   `08-01-` through `08-05-SUMMARY.md` contain the stale verdict and they keep it: a SUMMARY
   dated 2026-08-24 recording an open verdict was correct on its date, and rewriting it would
   falsify the record rather than correct it. This is the same living-reference /
   historical-record rule item 2 below applies to the unrunnable command, applied to the
   verdicts. Because the gate now scans repo-wide, that class must be named explicitly, and
   measured at HEAD 2026-08-27 it has **four members**, each verified to carry the string rather
   than assumed to:

   - `*-SUMMARY.md` — 6 hits across the five dated 08-0x summaries.
   - `*-PLAN.md` — 08-06-PLAN.md ×5 and 08-10-PLAN.md (this file) quote the stale string as
     their SUBJECT. Without this exclusion the gate is red on the very plan that defines it.
   - `08-REVIEW-FIX.md` ×1 — a dated review record, same rule as a SUMMARY.
   - `verdict-gate.sh` itself — the script DEFINES the pattern, so a repo-wide scan that did not
     skip it would match its own source and be permanently, unfixably red. This one is not
     optional and is easy to miss.

   The exclusion list is deliberately short and every entry carries a written reason, because
   each entry is a hole in the scan. The gate additionally asserts the SUMMARY class is
   UNMODIFIED, so that exclusion is checked in both directions rather than merely trusted.

2. **G-03 — the unrunnable A6 command.** `ps -eo comm` prints the executable name, so an
   alternation on `mcp-server` could never match a process whose comm is `node`. It survived
   research, plan, plan-check, execution and verification because no gate ever executed it.
   Already corrected in `08-SPIKE.md` and `08-UAT.md`; five stale copies remain.

3. **Two stale contract statements.** ROADMAP SC-2 says the group signal replaces "today's
   single-pid ladder" when the shipped `killTree` keeps that rung *in addition* on POSIX
   (recorded decision OQ-2). The code does strictly more than the criterion, so this is a
   stale clause and not a relaxation, and it must be amended in place for the same reason SC-1
   and SC-2 were amended before: the gate stops the code, the amendment stops the intent. And
   `REQUIREMENTS.md` ticks LIF-01 and LIF-02 complete, which read alone overstates what every
   other artifact qualifies and what `08-VERIFICATION.md` ruled `human_needed`.

4. **Three decisions that must be recorded rather than implied** — G-02 (record, do not fix),
   the SC-4 completion-order race, and the win32 orphan class the reaper cannot reach.

## The correction convention, and the gate that enforces it

Every correction follows the `07-VALIDATION.md` style already used at `08-SPIKE.md` § *Step 3*:
the superseded text stays visible, dated, with its replacement beside it. Nothing is silently
overwritten — that is how the phase can still tell measured ground from assumed ground.

To make the convention machine-checkable, **superseded text is preserved inside a markdown
block quote, every line beginning with `>`.** The active statement sits outside the quote. A
gate then strips the quote layer and asserts the stale verdict is absent from the active text
— the same technique `index.source.test.ts` already applies to comments in source. Put a stale
verdict back outside a block quote and the gate goes red.

## The G-03 treatment rule, written down because the gap asks for the reasoning

**Corrected in place — `08-VERIFICATION.md` and `08-RESEARCH.md`.** Both are living reference
documents that later work reads: the verifier's human-test list is what a re-run consults, and
RESEARCH's Validation Architecture is inherited by Phases 9 and 10. A document that will be
*followed* must be correct.

**Annotated as superseded, never rewritten — `08-01-PLAN.md` and `08-05-PLAN.md`.** An executed
plan is the record of the instructions an executor actually received. Rewriting it would make
the record disagree with what was run and would destroy the audit trail this gap depends on:
the finding is precisely that a wrong command travelled unchanged through five gates, and that
finding is only legible while the plans still show what they said. They get a dated pointer to
the corrected procedure and nothing else.

## The two open decisions, decided here

**SC-4's completion-order clause — DECLARED as a residual, and the verifier's alternative was
considered and rejected.** SC-4's statement ORDER is enforced by code and independently
verified at all three sites, but its "so token-bearing processes die before their env-source
files are removed" clause is not: `killTree` is fire-and-forget by design and the removals are
unawaited. The verifier offered two options — declare it, or await the killer in
`cleanupMcpRuntime` where the call site is already async. **Awaiting is rejected**, and not for
convenience: awaiting a spawned killer inside an RPC handler suspends that handler while
waiting on a child-process callback and a timer that Caido's runtime does not reliably deliver
during an await. That is the event-loop starvation CLAUDE.md names as an anti-pattern and the
reason `killTree` is fire-and-forget in the first place (OQ-4, T-08-13). So the clause is
declared as **AR-05**, with the one thing that genuinely improved recorded alongside it: the
reaper plans 08-06 and 08-07 shipped identifies its target by the target's own argv rather than
by anything on disk, so unlike `killTree` it is INSENSITIVE to losing that race. Statement
order is still enforced, and now enforced for the reap as well as for the kill.

**The win32 orphan class — DECLARED as AR-04.** `taskkill /t` walks `ParentProcessId` and is
indifferent to process groups, so A6 was never a Windows question and LIF-01's mechanism is
unaffected. What Windows does not get is the G-04 class: an MCP server launched by a CLI
instance Drift did not spawn. Reaching it needs a command-line enumerator, and the three
candidates were each rejected with a reason — `tasklist` does not print command lines, `wmic`
is removed from current Windows, and spawning PowerShell from the plugin opens a surface this
phase will not open. Recorded with an owner rather than left as an absence.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-process-lifecycle/08-UAT.md
@.planning/phases/08-process-lifecycle/08-VERIFICATION.md
@.planning/phases/08-process-lifecycle/08-06-SUMMARY.md
@.planning/phases/08-process-lifecycle/08-07-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: A1 closed, A6 falsified — marked corrections in the phase-document carriers, gated repo-wide across all nine</name>
  <read_first>
    - `.planning/phases/08-process-lifecycle/08-UAT.md` tests 1 and 2 in full, including `reported`, `evidence`, `scope_and_caveats`, `attempt_1` and `correction`. These are the only readings this task may cite.
    - `.planning/phases/08-process-lifecycle/08-SPIKE.md` in full: frontmatter, the three result tables, § *How to run this spike later*, § *What this spike does NOT close* and § *The consequence, plainly*.
    - `.planning/phases/08-process-lifecycle/08-VALIDATION.md` lines 250-320 — the Wave-0 partial-closure paragraph, § *Vehicle caveat* items 2 and 3, and the A1 manual-only row.
    - `.planning/phases/08-process-lifecycle/08-SECURITY.md` lines 240-300 and line 344 — the A1/A6 paragraphs, § *What this phase's evidence does not cover*, and the closing-checklist item.
    - `packages/backend/src/kill-plan.ts` header as corrected by plan 08-06 — the wording to stay consistent with, not to duplicate.
    - `.planning/phases/08-process-lifecycle/08-SPIKE.md` § *Step 3* CORRECTION block — the format every correction in this plan reuses.
  </read_first>
  <files>.planning/phases/08-process-lifecycle/08-SPIKE.md, .planning/phases/08-process-lifecycle/08-VALIDATION.md, .planning/phases/08-process-lifecycle/08-SECURITY.md, .planning/phases/08-process-lifecycle/08-VERIFICATION.md, .planning/phases/08-process-lifecycle/verdict-gate.sh</files>
  <action>
Correct FOUR documents; the three source carriers were corrected by plan 08-06 and are only
gated here. `08-VERIFICATION.md` is corrected in this task for its A1/A6 VERDICT text at lines
21 and 76 — Task 2 separately corrects its unrunnable command at line 31, and the two edits
must not be reordered, because this task's gate runs over the file and would fail if its
verdict text were left to Task 2.

**08-SPIKE.md.** Move the frontmatter to the measured state: a `status` naming a partial
measurement rather than not-run, a `measured` field naming the date and which readings were
taken, a `vehicle` field naming the probe build installed on real hardware, and an
`assumptions` block recording A1 as closed favourably and A6 as falsified. Fill the A1 table
and the A6 table with the values quoted verbatim from the UAT — the platform string, the two
spike fields, and the two process rows' pid, ppid and pgid. Leave the Control table exactly as
it is: the pre-fix after-Stop count was never taken and must continue to read as not taken.
Rewrite the opening paragraph, which declares that every result field reads open and that the
file closes nothing at all; that is now false for two of the three readings and still true for
the third, and the replacement must say which is which. Rewrite § *What this spike does NOT
close* to the same standard: it closes A1, it closes A6 in the negative for one provider, and
it still says nothing about Claude Code, nothing about Windows, nothing about Gemini or
Copilot, and nothing about a future Caido that rebases its LLRT fork.

Add a short section recording that the spike's own conclusion inverted. The file previously
stated that OQ-2's single-pid rung was the phase's only protection against a negative A1. A1
came back positive; the failure landed at A6 instead, which that rung does not address because
it signals the CLI rather than the child. Name the mechanism that does address it — the
argv-marker reap plans 08-06 and 08-07 shipped — and state that it is independent of process
groups by construction.

**08-VALIDATION.md.** Correct the four A1/A6 statements and the A1 manual-only row. Vehicle
caveat item 2 is now partly false and must say exactly which part: the detached spawn option
IS proven on the shipped runtime by measurement; the kill primitive's parameter typing is not,
and is doubly moot because the sandbox exposes no such function at all. Item 3 is now a
measurement rather than an absence, and the measurement is negative. Update the test-count
baseline paragraph to the totals the 08-06 through 08-09 SUMMARYs report, keeping the
grown-with-none-removed rule visible.

**08-VERIFICATION.md — VERDICT TEXT ONLY in this task.** Two sites carry the stale verdict:
the `why_human` frontmatter field (line 21) and the SC-2 row (line 76), both reading "A1 reads
OPEN — not measured". Correct both to A1 closed favourably, dated, with the superseded line
preserved as a `> ` block quote. **Do not soften what these two sites are saying.** Their point
is that the behavioural proof runs under Node while the shipping runtime is LLRT; A1's closure
narrows that gap but does not remove it, because `kill-tree.posix.test.ts` still executes no
line of `index.ts` and still runs under Node. The `human_needed` verdict and the three
`behavior_unverified_items` stand. Line 31's unrunnable command belongs to Task 2 and is not
touched here.

**08-SECURITY.md.** Correct the A1 and A6 paragraphs — note they use the COMMA spelling
(`A1 — OPEN, not measured` at `:247` and `:250`), which is the spelling the earlier gate would
have missed — the § *What this phase's evidence does not cover* item, and the closing-checklist
line that still reads as open. Add the sandbox
finding as its own short subsection: four readings from one real install establish that
Caido's plugin sandbox re-exports a restricted process shim rather than the runtime's own
module, so the research that source-read the runtime was answering a different question than
the one that mattered. Record the consequence the UAT names — SC-2's stated conversion-error
reason is incomplete though its conclusion holds, because on this build the call would fail as
a missing function first. A marked correction, not a scramble; the static gate still earns its
place.

Apply the block-quote convention throughout: superseded sentences stay, quoted and dated,
with the replacement outside the quote.
  </action>
  <verify>
    <automated>S='OPEN[[:space:]]*[,—-][[:space:]]*not measured'; t=0; for f in .planning/phases/08-process-lifecycle/08-SPIKE.md .planning/phases/08-process-lifecycle/08-VALIDATION.md .planning/phases/08-process-lifecycle/08-SECURITY.md .planning/phases/08-process-lifecycle/08-VERIFICATION.md packages/backend/src/kill-plan.ts packages/backend/src/index.ts packages/backend/src/kill-tree.posix.test.ts; do n=$(sed -E '/^[[:space:]]*(\/\/[[:space:]]*)?>/d' "$f" | grep -cE "$S"); t=$((t+n)); done; test "$t" = "0" && test -x .planning/phases/08-process-lifecycle/verdict-gate.sh</automated>
  </verify>
  <acceptance_criteria>
    - **Why this task's verify is SCOPED while the gate it writes is repo-wide.** `verdict-gate.sh` ARM A covers all nine carriers, but `.planning/WINDOWS.md` and `.planning/STATE.md` are corrected by **Task 3 of this plan**, which has not run yet. Running the full gate here would fail on work this task does not own. So this task's verify asserts `0` stale hits across exactly the seven carriers that ARE corrected by now — the four phase documents from this task, plus the three source files from prior-wave plan 08-06 — using the identical three-spelling ERE and identical block-quote strip. Verified 2026-08-27: that scoped command returns `12` against the uncorrected tree and must return `0` after this task. **The full nine-carrier gate runs in Task 3's verify and in the plan's `<verification>` section.** This is a staging split, not a weakening: no carrier escapes, because Task 3 cannot pass without the unscoped run.
    - Write `.planning/phases/08-process-lifecycle/verdict-gate.sh` as part of this task, and `chmod +x` it. It is a **repo-wide scan with an exclusion list, NOT a list of carriers** — see ARM A below for why that inversion is the whole point. It has three arms and exits non-zero naming the offending file and the failing arm.
    - **ARM A — discovery (fail-closed).** Scan `.planning/` and `packages/` for the stale ERE with `grep -rlE`, pruning `node_modules`, `dist` and `.git`. Skip any path matching the EXCLUSION list — `*-SUMMARY.md`, `*-PLAN.md`, `*08-REVIEW-FIX.md`, `*/verdict-gate.sh` — with the one-line reason for each written as a comment beside it. For every surviving file, strip block-quote lines (`^[[:space:]]*(//[[:space:]]*)?>` covers both the markdown `> ` and the source `// > ` forms) and assert `0` occurrences of the stale ERE in what remains. **This arm is what makes the census irrelevant to correctness:** an inclusion-list loop cannot detect a carrier absent from its own list, so under the previous draft a forgotten `WINDOWS.md` or `STATE.md` shipped GREEN. Verified 2026-08-27 against the uncorrected tree: this arm names all nine carriers with per-file counts `STATE.md 1, WINDOWS.md 2, 08-VALIDATION.md 1, 08-VERIFICATION.md 2, 08-SECURITY.md 2, 08-SPIKE.md 2, kill-plan.ts 2, kill-tree.posix.test.ts 2, index.ts 1` — and names no excluded file.
    - **ARM B — positive content on the nine known carriers.** For each of `08-SPIKE.md`, `08-VALIDATION.md`, `08-SECURITY.md`, `08-VERIFICATION.md`, `.planning/WINDOWS.md`, `.planning/STATE.md`, `packages/backend/src/kill-plan.ts`, `packages/backend/src/index.ts`, `packages/backend/src/kill-tree.posix.test.ts`: assert at least one occurrence of `2026-08-27`, and — for the carriers that state an A6 verdict — at least one occurrence of `FALSIFIED`. The three source carriers are corrected by plan 08-06 and only GATED here; `WINDOWS.md` and `STATE.md` are corrected by Task 3 of this plan; the other four by Task 1 and Task 2. This arm IS an explicit list, and that is sound only because ARM A already covers the hard direction: a carrier forgotten entirely still trips ARM A, so ARM B's list can only ever be too short in the harmless direction. State that reasoning in the script's header comment so a later reader does not "simplify" ARM A back into ARM B.
    - **ARM C — historical-record immutability.** `git diff --stat HEAD -- '.planning/phases/08-process-lifecycle/*-SUMMARY.md'` is empty.
    - **The stale pattern is an ERE covering all THREE spellings, and this is not optional.** Measured at HEAD 2026-08-27, 15 hits across the nine carriers: **11 hits across 7 files** spell it `OPEN — not measured` (em dash); `08-SECURITY.md:247` and `:250` spell it **`A1 — OPEN, not measured`** (comma); `.planning/WINDOWS.md:28` and `:159` spell it **`OPEN - not measured`** (plain ASCII hyphen). A gate greping only the em-dash form returns `0` for `08-SECURITY.md` and `0` for `WINDOWS.md` **vacuously** — passing while both stale claims sit untouched. Use `OPEN[[:space:]]*[,—-][[:space:]]*not measured`, keeping the hyphen LAST inside the bracket so it is a literal rather than a range endpoint, and verify it matches all THREE forms before committing the script. Red input, verified 2026-08-27: revert the pattern to the fixed string `OPEN — not measured` and re-run — `08-SECURITY.md` and `WINDOWS.md` both pass, which is the bug. For calibration, that fixed string finds **10** of the 15 hits across the seven previously-listed carriers and **11** across all nine; the substantive delta it misses is the two comma hits and the two hyphen hits.
    - The script asserts the **historical-record class is untouched**: `git diff --stat HEAD -- '.planning/phases/08-process-lifecycle/*-SUMMARY.md'` is empty. A SUMMARY dated 2026-08-24 recording the verdicts as open was correct on its date, and rewriting one would be falsifying the record rather than correcting it. Red input: edit any SUMMARY and the gate fails.
    - **Red inputs for the whole script, each verified against a real violating input on 2026-08-27 rather than asserted.** (1) Move any stale verdict outside its block quote and ARM A goes red — verified: a file containing only `> Superseded 2026-08-24: A1 OPEN — not measured` plus a dated replacement scores `0`; append the same sentence unquoted and it scores `1`. Both the markdown `> ` and the source `// > ` forms strip correctly. (2) Add a **tenth carrier** — a file no list anywhere names — and ARM A goes red: verified by creating `08-NEWCARRIER-test.md` carrying one stale hit, which the gate named and failed on. (3) Edit any SUMMARY and ARM C goes red.
    - **A previous draft of this criterion claimed: "add an eighth carrier without adding it to the list, and the script exits non-zero." That red input was STRUCTURALLY IMPOSSIBLE, not merely mis-worded.** An inclusion-list loop opens only the files it names; a carrier absent from the list is never read, so NO input could satisfy that claimed red. This is recorded rather than quietly reworded because it is the same vacuous-gate shape as the em-dash-only pattern and the `grep -c … | grep -v` gate this gap set already removed — and it shipped inside the plan whose own thesis is that miscounted censuses ship false truths. The inversion to ARM A is what makes red input (2) achievable at all; do not reinstate the enumerated loop.
    - `08-SPIKE.md` frontmatter contains neither `A1: OPEN` nor `A6: OPEN` and contains no `status: not-run`.
    - The Control table in `08-SPIKE.md` still records that no reading was taken. Red input: filling it with any number contradicts `08-UAT.md`, where test 3 is still `[pending]`.
    - Each of the three documents contains the measured pids `43921` and `44284` at least once, so the A6 verdict cites evidence rather than a paraphrase.
    - `grep -c 'grandchild-died (detached honoured)' .planning/phases/08-process-lifecycle/08-SPIKE.md` is at least `1`.
    - Every correction is a block quote plus a dated replacement — verified by `git diff` showing the superseded lines re-quoted rather than removed.
  </acceptance_criteria>
  <done>All nine A1/A6 carriers record the measured verdicts as dated marked corrections with their superseded text preserved, and one script proves it — by scanning `.planning/` and `packages/` repo-wide against all three spellings of the stale verdict, skipping only a named and commented historical-record class, and asserting that class is itself untouched. A tenth carrier introduced anywhere fails the gate rather than escaping it.</done>
</task>

<task type="auto">
  <name>Task 2: The unrunnable command, SC-2's stale clause, and the LIF requirement status</name>
  <read_first>
    - `.planning/phases/08-process-lifecycle/08-UAT.md` gap G-03 in full, especially its `propagation` list — the five stale sites, each already labelled living-reference or historical-record.
    - `.planning/phases/08-process-lifecycle/08-SPIKE.md` § *Step 3* as corrected — the canonical runnable command and the reason `comm` could never match.
    - `.planning/phases/08-process-lifecycle/08-VERIFICATION.md` line 31 and `.planning/phases/08-process-lifecycle/08-RESEARCH.md` line 1120 — the two living-reference sites.
    - `.planning/phases/08-process-lifecycle/08-01-PLAN.md` lines 20-30 and 220-230, and `08-05-PLAN.md` lines 330-370 — the two historical-record sites.
    - `.planning/ROADMAP.md` Phase 8 SC-2 (line 312) in full, including its existing amendment note and the 05-CONTEXT.md D-02 precedent it cites.
    - `.planning/REQUIREMENTS.md` lines 69-70 and 174-175, and the file's italic-qualifier style at SIG-01, which is the house form for a qualified requirement.
    - `.planning/phases/08-process-lifecycle/08-VERIFICATION.md` frontmatter — the `human_needed` verdict and its three `behavior_unverified_items`, which are what the requirement status must agree with.
  </read_first>
  <files>.planning/phases/08-process-lifecycle/08-VERIFICATION.md, .planning/phases/08-process-lifecycle/08-RESEARCH.md, .planning/phases/08-process-lifecycle/08-01-PLAN.md, .planning/phases/08-process-lifecycle/08-05-PLAN.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <action>
**The unrunnable command, five sites, two treatments.** Apply the rule stated in this plan's
objective and restate it in one sentence at each site so a later reader sees the reasoning
rather than an inconsistency.

Correct `08-VERIFICATION.md` line 31 in place: replace the broken specifier with the corrected
command from `08-SPIKE.md` § *Step 3* and add a one-line dated note that its stated A6 test is
superseded by that section. Correct `08-RESEARCH.md` § *Validation Architecture*, Wave 0 item 5,
in place the same way, and add a marked correction to the § *Assumptions Log* row for **A6**
recording that the assumption is now measured FALSE for one provider — the row currently rates
it "medium-high, and untested".

Annotate `08-01-PLAN.md` at both sites and `08-05-PLAN.md` at its three: a dated superseded
marker naming `08-SPIKE.md` § *Step 3* as the corrected procedure, and nothing else. Do not
touch the surrounding instruction text, the acceptance criterion at `08-01-PLAN.md:225`, or
any halt procedure. Add one sentence at the top of each annotation stating that an executed
plan is a record of instructions received and is therefore annotated rather than rewritten.

**ROADMAP SC-2.** Amend the "instead of today's single-pid SIGTERM to SIGKILL ladder" clause in
place, in the same dated parenthetical form the criterion already carries. State that the
shipped `killTree` keeps that rung on POSIX in ADDITION to the group spawn, that this is
recorded decision OQ-2, that the code therefore does strictly more than the criterion asked,
and that the clause was stale rather than the code relaxed. Add one sentence recording what
changed the rung's justification: it was written as defence against an unmeasured A1, A1 is
now measured favourably, and the rung survives as defence against a future Caido that rebases
its LLRT fork. Do not weaken the criterion in any other respect and do not touch SC-1, SC-3,
SC-4 or SC-5.

**REQUIREMENTS.md.** Reconcile LIF-01 and LIF-02 with the verification verdict. Untick both
checkboxes and append an italic qualifier to each in the file's existing style, dated, naming
concretely what ships and what has no execution behind it: for LIF-01, the Windows mechanism
is built and unit-proven and no `windows-latest` run has ever executed it; for LIF-02, the
POSIX group mechanism is built and behaviourally proven under Node, assumption A6 is measured
FALSE for one provider, and the argv-marker reap this gap set adds is what closes it
independently of process groups. Change both status-table rows from complete to a qualified
value that points at `08-VERIFICATION.md`. Do not touch any other requirement.
  </action>
  <verify>
    <automated>test "$(grep -c 'pid,ppid,pgid,comm' .planning/phases/08-process-lifecycle/08-VERIFICATION.md)" = "0" && test "$(grep -c 'pid,ppid,pgid,comm' .planning/phases/08-process-lifecycle/08-RESEARCH.md)" = "0" && test "$(grep -c 'SUPERSEDED' .planning/phases/08-process-lifecycle/08-01-PLAN.md)" -ge 2 && test "$(grep -c 'SUPERSEDED' .planning/phases/08-process-lifecycle/08-05-PLAN.md)" -ge 3 && test "$(grep -c '\[x\] \*\*LIF-0' .planning/REQUIREMENTS.md)" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'pid,ppid,pgid,comm'` is `0` in both `08-VERIFICATION.md` and `08-RESEARCH.md`. Red input: leave either uncorrected and the gate fails.
    - `grep -c 'pid,ppid,pgid,args'` is at least `1` in each of those two files — the corrected spelling is present, not merely the broken one absent.
    - `grep -c 'SUPERSEDED'` is at least `2` in `08-01-PLAN.md` and at least `3` in `08-05-PLAN.md`, and `git diff` shows those two files gained annotation lines only, with no instruction text altered.
    - `grep -c '\[x\] \*\*LIF-0' .planning/REQUIREMENTS.md` is `0`, and each LIF line carries a dated italic qualifier. Red input: re-tick either box and the gate fails.
    - The two status-table rows for LIF-01 and LIF-02 no longer read complete and each names `08-VERIFICATION.md`.
    - ROADMAP SC-2 contains the word `addition` in its amended clause and no longer presents the group signal as replacing the single-pid ladder; SC-1, SC-3, SC-4 and SC-5 are byte-identical, verified by `git diff`.
  </acceptance_criteria>
  <done>The unrunnable command exists nowhere as an instruction, the two living-reference documents carry the corrected spelling, the two executed plans carry dated superseded markers with their text intact, SC-2 states what the code actually does, and the LIF requirement status agrees with the verification verdict.</done>
</task>

<task type="auto">
  <name>Task 3: The security register, the three recorded decisions, the ledger, and COVERAGE.md</name>
  <read_first>
    - `.planning/phases/08-process-lifecycle/08-SECURITY.md` — the threat register table, § *Accepted Residuals* (AR-01, AR-02, AR-03) and the closing checklist. The residual row format is the template for AR-04, AR-05 and AR-06.
    - `.planning/phases/08-process-lifecycle/08-UAT.md` gaps G-01, G-02, G-04 and G-05 in full, plus § *Observations* and the operational note about the failed Gemini registration removals.
    - `.planning/phases/08-process-lifecycle/08-01-PLAN.md` — the Wave-0 checkpoint's prediction that a value other than a function for the kill primitive would mean the deferred-rung guard "needs a redesign". That prediction predates the CR-02 fix and must be retired.
    - `packages/backend/src/index.ts` lines 4745-4775 — the comment that documents the identity-only fallback in advance, and the accepted residual stated immediately below it. This is what makes G-02 a record rather than a defect.
    - `.planning/WINDOWS.md` — both the markdown table row 11 and the JSON object with `"id": 11`; they must stay in sync.
    - `.planning/STATE.md` lines 220-226 — the two bullets asserting A1/A6 remain unmeasured.
    - `.planning/phases/08-process-lifecycle/08-UAT.md` frontmatter `gate_overrides` — the recorded api-coverage false positive whose reasoning `COVERAGE.md` restates.
    - `.planning/phases/08-process-lifecycle/deferred-items.md` — the existing entry's format.
    - The 08-06 through 08-09 SUMMARYs, for the shipped shape of the reaper and the three-consumer fallback.
  </read_first>
  <files>.planning/phases/08-process-lifecycle/08-SECURITY.md, .planning/phases/08-process-lifecycle/08-01-PLAN.md, .planning/phases/08-process-lifecycle/COVERAGE.md, .planning/phases/08-process-lifecycle/deferred-items.md, .planning/WINDOWS.md, .planning/STATE.md</files>
  <action>
**Threat register updates in 08-SECURITY.md.** Re-rate **T-08-03** from medium to high and
record why in the mitigation cell: the empty-environment condition is measured, not
hypothetical, which made the bare-name arm the expected Windows path rather than a remote
contingency; and record the mitigation plan 08-08 shipped — a derived system root between the
environment read and the bare name, applied identically to the command interpreter and the
path-search binary. Add rows for the new threats the shipped mechanism creates and closes,
taking their identifiers and severities from the `<threat_model>` blocks of plans 08-06
through 08-09 rather than inventing new ones; every row needs a disposition and a severity.
Add a note to **T-08-04** recording that on Caido the deferred-rung guard runs on handle
identity alone, because the sandbox exposes no kill primitive so the liveness probe cannot
run, with the accepted residual already stated in the source.

**Re-examine AR-02** rather than deleting it. It was deferred on the blast-radius grounds that
identifying a Drift MCP process required image-name matching. Record that the residual now has
a measured instance rather than a hypothetical one, that the objection is answered by the
two-anchor adjacency pattern, and that plan 08-07's start-up reap closes the case AR-02
describes. State precisely what remains: the reap is POSIX-only, and it fires at start-up and
at teardown rather than continuously.

**Four new residuals.** **AR-07** — **the multi-session cancel residual, which currently exists
in no artifact a verifier or executor reads.** Plan 08-07's idle gate means that while a second
session is live, cancelling one leaves its token-bearing MCP child to the group path that UAT
measured FALSE (A6); the orphan survives, holding a valid `CAIDO_TOKEN`, until the last session
closes and the idle reap fires. Record it with: the bound (the window is the multi-session
period, not indefinite, because the reap does fire at the last close); the reason it is not
mitigated in Phase 8 (a session-precise reap needs a per-chat argv marker, which Drift CAN
author for `claude-cli` and `copilot-cli` — it writes their `args` array via
`buildMcpServerSpec` → `toMcpConfigDocument` → the per-chat config at `index.ts:3960`/`:4009` —
but CANNOT for `gemini-cli`/`codex-cli`, which share one `mcp add drift` registration, so
closing it means shipping two mechanisms, not one); and the explicit statement that this is a
**scope decision, not a constraint**. Do not write that argv cannot distinguish sessions — that
claim is false for two of the four providers, and one of them is the maintainer's active
provider. Cross-reference threat **T-08-50** in plan 08-07. It carries the same seven columns as
AR-01/02/03, including **Accepted By**.

**AR-04** — the win32 orphan class, with the three rejected
enumerator candidates and their reasons, and an owner. **AR-05** — SC-4's completion-order
clause, with the rejected await option and the runtime reason it was rejected, and the note
that the reaper is insensitive to losing that race because it identifies its target by argv.
**AR-06** — registration hygiene: Drift's `mcp add drift` registration persists in the CLI's
own configuration and any instance of that CLI on the machine can launch Drift's MCP server on
its own initiative with a live token. GD-02 puts this explicitly OUT of scope for Phase 8;
record it pointing at a future registration-focused phase entered through a discussion, and
record the same-family evidence in the same diagnostics — the Gemini `mcp remove` failures at
user and project scope, both exit 127, which left a token-bearing registration Drift could not
withdraw. Mirror the Windows ACL acceptance plan 08-09 recorded at the source as a residual row
here too, so the register is the single place a reader can enumerate them.

**G-02, recorded and not fixed.** Add a short subsection stating the reading, the mechanism,
and why no code change follows: the guard reduces on Caido to its identity half alone, the
source documents exactly that fallback in advance, and every unknown resolves toward alive so
neither check can ever ADD a kill. Then retire the stale prediction: annotate the 08-01
checkpoint's claim that a non-function value would mean the guard needs a redesign as
superseded by the CR-02 fix that introduced the identity check, dated, using the same
annotate-do-not-rewrite rule Task 2 applies to executed plans.

**Ledger entry 11 in WINDOWS.md.** Rewrite its description to the measured state in BOTH the
markdown table row and the JSON object with matching id — the two representations must agree.
The entry does not simply close: A1 is measured favourably, A6 is measured FALSE for one
provider, and Claude Code, the active provider, remains unmeasured. Set the status and the
`reason` field to reflect that narrowing honestly, naming the argv-marker reap as the phase's
answer to the falsification and naming a Drift-spawned Claude Code turn as what would settle
the remaining half. Do not touch entry 12 in either representation.

**STATE.md.** Correct only the two bullets asserting that A1 and A6 remain unmeasured, as
dated marked corrections in the same block-quote convention. Change nothing else in the file.

**COVERAGE.md** (new, in the phase directory). Write the reasoned no-external-API declaration
the seal-time gate accepts in place of a matrix, restating the reasoning already recorded in
`08-UAT.md`'s `gate_overrides` block: the detector matched prose stating that a Win32 API is
NOT available and NOT used; this phase integrates no external API, it terminates processes;
there is no capability surface to enumerate. One reasoned line in the accepted form, plus the
detector-output citation beneath it.

**deferred-items.md.** Append the two out-of-scope discoveries this gap set produced, in the
existing entry format with the same found-during, not-fixed-here and suggested-resolution
structure: the registration-hygiene question that AR-06 names, and the Gemini removal failures
that are its live instance.
  </action>
  <verify>
    <automated>test -f .planning/phases/08-process-lifecycle/COVERAGE.md && test "$(grep -c 'AR-04' .planning/phases/08-process-lifecycle/08-SECURITY.md)" -ge 1 && test "$(grep -c 'AR-05' .planning/phases/08-process-lifecycle/08-SECURITY.md)" -ge 1 && test "$(grep -c 'AR-06' .planning/phases/08-process-lifecycle/08-SECURITY.md)" -ge 1 && test "$(grep -c 'AR-07' .planning/phases/08-process-lifecycle/08-SECURITY.md)" -ge 1 && test "$(grep -c 'recorded decision GD-02' .planning/phases/08-process-lifecycle/08-SECURITY.md)" -ge 1 && test "$(grep -c '| 12 | 08 | unrun-verify' .planning/WINDOWS.md)" = "1" && python3 -c "import json,re,sys; s=open('.planning/WINDOWS.md').read(); b=re.search(r'\`\`\`\`json\n(.*?)\n\`\`\`\`', s, re.S); json.loads(b.group(1)); print('JSON-OK')" && bash .planning/phases/08-process-lifecycle/verdict-gate.sh</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/08-process-lifecycle/COVERAGE.md` exists and carries a reasoned no-external-API declaration with a one-line reason, matching the form the seal-time gate accepts.
    - `08-SECURITY.md` contains rows for AR-04, AR-05, AR-06 and AR-07, each carrying **all seven** columns of the AR-01/AR-02/AR-03 template: `Risk ID | Threat Ref | Rationale | Not mitigable here because | Owner next | Accepted By | Date`. **The `Accepted By` cell is not optional and is not the same as `Owner next`.** An owner is who picks the work up; a decider is who accepted the risk. Red input: leave `Accepted By` empty on any row and the criterion fails.
    - **AR-06 in particular names the maintainer as decider and cites GD-02.** `08-SECURITY.md` has `block_on: high` and AR-06 records an ACCEPTED `high` (T-08-47). An accepted high with no named decider reads as a silent carry past the phase's own blocking gate. The cell must read `six2dez (recorded decision GD-02)`, matching the form AR-02 already uses (`six2dez (recorded decision OQ-3)`). Red input: write only an owner and the criterion fails.
    - `grep -c 'AR-07' .planning/phases/08-process-lifecycle/08-SECURITY.md` is at least `1`, and its row cites threat `T-08-50`. Red input: leave the multi-session cancel residual in 08-07's prose only — where it is today — and this fails.
    - T-08-03's severity cell reads high and its mitigation cell names the derived system root. Red input: leave it at medium and the re-rating the UAT calls for has not happened.
    - Every threat row added carries both a severity and a disposition; `threats_open` in the frontmatter is recomputed and stated.
    - AR-02 is re-examined in place, not deleted, and states what remains after plan 08-07.
    - The WINDOWS.md JSON block still parses and its entry 11 object's description matches the table row's description. Red input: edit one representation only and the descriptions diverge — the executor must diff them and report the comparison.
    - `grep -c '| 12 | 08 | unrun-verify' .planning/WINDOWS.md` is exactly `1` and `git diff` shows entry 12 unchanged in both representations.
    - `STATE.md` shows exactly two corrected bullets and no other change, verified by `git diff`.
    - **This task carries the plan's FULL nine-carrier gate.** `bash .planning/phases/08-process-lifecycle/verdict-gate.sh` exits `0` here — the first point in the plan where every carrier is corrected, because this task is the one that corrects `.planning/WINDOWS.md` and `.planning/STATE.md`. Task 1 wrote the script and ran a scoped subset; this is the unscoped run. Red input: skip or partially apply either the WINDOWS.md or the STATE.md correction and ARM A names the file and fails — the precise failure the previous inclusion-list gate could not detect, since neither file appeared in its list at all.
    - `deferred-items.md` gained two entries in the existing format.
  </acceptance_criteria>
  <done>The security register carries every re-rating and all three new residuals with owners, G-02 is recorded as decided-not-fixed with its stale prediction retired, ledger entry 11 reflects the measurement in both representations with entry 12 untouched, and COVERAGE.md closes the api-coverage gate with a reason rather than an override.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Planning record → future executor | A stale instruction in a document that is followed becomes a wrong action. |
| Requirement status → release decision | A ticked requirement with no execution behind it is a false green at the milestone level. |
| Threat register → residual ownership | An unrecorded residual has no owner and is silently inherited. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-44 | Repudiation | a stale verdict surviving in a document a later phase reads | **high** | mitigate | The four-carrier verdict gate, block-quote-stripped so a preserved superseded quote cannot satisfy it and a re-introduced active claim cannot hide in one. Script committed, so it is re-runnable rather than a one-time check. |
| T-08-45 | Repudiation | an unrunnable command surviving as an instruction | medium | mitigate | Corrected in the two living-reference documents and gated at `0`; annotated in the two executed plans and gated at a minimum annotation count, so the audit trail survives while no follower can be misled. |
| T-08-46 | Repudiation | a requirement ticked complete without execution behind it | **high** | mitigate | Both LIF boxes unticked with dated qualifiers naming exactly what has no execution, gated by a negative grep on the ticked form. |
| T-08-47 | Information Disclosure | a token-bearing MCP registration Drift cannot withdraw | **high** | **accept** | AR-06. Explicitly out of scope per GD-02; recorded with the Gemini exit-127 removal failures as its live instance and pointed at a registration-focused phase. The user-facing remediation is recorded alongside it. |
| T-08-48 | Repudiation | the two WINDOWS.md representations diverging | medium | mitigate | Both edited together, the JSON block re-parsed by the gate, and the two descriptions diffed and the comparison reported. |
| T-08-49 | Repudiation | fabricating the Control reading to complete a table | **high** | mitigate | Prohibited in frontmatter and asserted in the acceptance criteria: the Control table must continue to record that no reading was taken, because `08-UAT.md` test 3 is still pending. A fabricated confirmation is the precise failure the spike existed to prevent. |
| T-08-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package-manager install task in this plan; no dependency added and no source file under `packages/` touched. |
</threat_model>

<verification>
- `bash .planning/phases/08-process-lifecycle/verdict-gate.sh` exits 0. It is proven red three ways before being trusted: reverting its pattern to the em-dash-only fixed string (`08-SECURITY.md` and `WINDOWS.md` then pass vacuously); introducing a stale verdict in a file named by no list (ARM A fails closed); and editing any dated SUMMARY (ARM C).
- `grep -c 'pid,ppid,pgid,comm'` is `0` in `08-VERIFICATION.md` and in `08-RESEARCH.md`.
- `grep -c '\[x\] \*\*LIF-0' .planning/REQUIREMENTS.md` is `0`.
- `.planning/WINDOWS.md`'s JSON block parses and entry 12 is byte-identical in both representations.
- `.planning/phases/08-process-lifecycle/COVERAGE.md` exists with a reasoned declaration.
- `test -z "$(git diff --stat HEAD -- packages/)"` — this plan modifies no source.
- `pnpm exec vitest run` still green at the total 08-09 reported, zero removed (no code changed, so this is a regression check on the working tree rather than a new measurement).
</verification>

<success_criteria>
- A1 reads closed favourably and A6 reads falsified in all NINE carriers — the four phase documents, `.planning/WINDOWS.md`, `.planning/STATE.md` and the three source files — dated, with superseded text preserved as block quotes; the dated SUMMARY files are unmodified.
- The Control reading still reads as not taken.
- No document presents the unrunnable command as an instruction; both executed plans keep their text with dated superseded markers.
- SC-2 states what the code does; LIF-01 and LIF-02 state what has and has not executed.
- G-02, the SC-4 race and the win32 orphan class are each explicitly decided with an owner.
- Ledger entry 11 reflects the measurement in both representations; entry 12 is untouched.
- `COVERAGE.md` exists, so the api-coverage seal gate passes on a reason rather than on an override.
</success_criteria>

## Artifacts this phase produces

| Artifact | Kind | Path |
|---|---|---|
| No-external-API declaration | new document | `.planning/phases/08-process-lifecycle/COVERAGE.md` |
| Repo-wide verdict gate (exclusion-list, fail-closed), three-spelling ERE, historical-record immutability arm | new script | `.planning/phases/08-process-lifecycle/verdict-gate.sh` |
| AR-04 (win32 orphan class), AR-05 (SC-4 completion order), AR-06 (registration hygiene), AR-07 (multi-session cancel) | residual records | `.planning/phases/08-process-lifecycle/08-SECURITY.md` |
| T-08-03 re-rating and T-08-04 identity-only note | register updates | `.planning/phases/08-process-lifecycle/08-SECURITY.md` |
| Measured A1/A6 verdicts | corrections | `08-SPIKE.md`, `08-VALIDATION.md`, `08-SECURITY.md` |
| Corrected A6 procedure | corrections | `08-VERIFICATION.md`, `08-RESEARCH.md` |
| Dated superseded markers | annotations | `08-01-PLAN.md`, `08-05-PLAN.md` |
| SC-2 amended clause | contract change | `.planning/ROADMAP.md` |
| LIF-01 / LIF-02 reconciled status | contract change | `.planning/REQUIREMENTS.md` |
| Ledger entry 11 rewritten | record | `.planning/WINDOWS.md` |
| Registration-hygiene and Gemini-removal entries | records | `.planning/phases/08-process-lifecycle/deferred-items.md` |

<output>
Create `.planning/phases/08-process-lifecycle/08-10-SUMMARY.md` when done.
</output>
