---
phase: 08-process-lifecycle
plan: 11
type: execute
wave: 6
gap_closure: true
depends_on: ["08-10"]
files_modified:
  - .planning/phases/08-process-lifecycle/verdict-gate.sh
  - .planning/phases/08-process-lifecycle/08-SPIKE.md
autonomous: true
requirements: [LIF-01, LIF-02]

estimate:
  tokens: 62000
  raw_tokens: 62000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`bash .planning/phases/08-process-lifecycle/verdict-gate.sh` names `08-SPIKE.md` in its ARM A output before task 1's document edit and does not name it after; the count of files named by ARM A strictly decreases across that edit. Both readings come from captures that each carry all three `== ARM ` header lines, so a silent gate cannot produce the 'after' reading."
    - "ARM A goes RED when the favourable A1 verdict is injected as a live (non-block-quoted) line into a non-excluded file under `.planning/` or `packages/`, in every spelling the executor's own pre-edit scan measured — demonstrated on a scratch fixture, not asserted."
    - "ARM A goes RED when a file carries the raw 2026-08-27 A1 reading string outside a block quote but does not also carry the retraction word — demonstrated on a scratch fixture."
    - "ARM C goes RED when an eighth `*-SUMMARY.md` appears in the phase directory that is neither in the pin list nor in the known-unpinned list — demonstrated on a scratch fixture."
    - "`08-SPIKE.md` frontmatter records A1 as retracted rather than closed favourably, and `A6: FALSIFIED` is byte-unchanged."
    - "Every superseded A1 cell, verdict line and paragraph in `08-SPIKE.md` is preserved as block-quoted text carrying its own date; no reading value, no pid, no date is deleted from the file."
    - "`08-SPIKE.md` still records the A6 pids 43921 / 43752 / 44284 and the 2026-08-27 date, unchanged by this plan."
    - "The Control table in `08-SPIKE.md` is byte-unchanged — it was already an honest absence and this plan does not touch it."
  artifacts:
    - ".planning/phases/08-process-lifecycle/verdict-gate.sh — re-pointed at A1's actual verdict, ARM A's fail-closed discovery shape kept"
    - ".planning/phases/08-process-lifecycle/08-SPIKE.md — A1 retracted as a marked correction"
  key_links:
    - "verdict-gate.sh ARM A stale-verdict ERE → every spelling of the favourable A1 claim that the executor's own scan measures in the tree"
    - "verdict-gate.sh ARM A reading-context scan → the retraction word, required to travel with the raw reading string"
    - "verdict-gate.sh ARM B → the retraction word on every enumerated A1-verdict carrier; A6's FALSIFIED requirement unchanged"
    - "verdict-gate.sh ARM C known-unpinned list → the seven gap-round SUMMARY filenames 08-11 … 08-17"
  prohibitions:
    - statement: "No gate-output check is written as a bare `verdict-gate.sh | grep -c <file> | grep -qx 0`. That shape is satisfied by an EMPTY match set: a gate that syntax-errors, aborts early under `set -u`, or whose ERE matches nothing prints nothing, the count is 0, and the check goes green — the vacuous-gate defect this whole round exists to eliminate, in the plans that fix it. Every gate-output check in this plan captures the gate output ONCE and asserts, against that SAME capture, both LIVENESS (exactly three `== ARM ` header lines, which the gate prints unconditionally because `fail()` sets a flag rather than exiting) AND the target-file absence. Do not copy the weak form back in."
    - statement: "No artifact is rewritten to satisfy this gate. Every A1 correction preserves the superseded text as a dated, block-quoted supersession; no reading value, verdict cell or date is deleted."
    - statement: "`08-VERIFICATION.md` is not edited, reworded or re-dated to make the gate green. The gate is re-pointed; the report that diagnosed the defect stands as written."
    - statement: "A6's FALSIFIED verdict is not reverted, softened, hedged or bundled into the A1 retraction. The two assumptions were measured by different instruments and only one reading is being withdrawn."
    - statement: "No dated `*-SUMMARY.md` is modified. A summary that recorded the verdicts as open, or as closed favourably, was correct on its date."
---

<objective>
Fix the instrument before any carrier, because today the instrument forbids the correction.

`verdict-gate.sh` hardcodes A1's favourable verdict as ground truth in both arms: ARM A treats
`OPEN — not measured` as the stale string wherever it appears outside a block quote, and ARM B
requires nine named carriers to carry the favourable verdict word. Writing `08-VERIFICATION.md`
turned the gate RED purely because the report retracts A1 — measured again at this plan's start,
still red, still on the report's own text. A gate that cannot be satisfied by a correction is a
gate that forbids one.

This plan re-points the gate at whatever A1's verdict actually is, keeps ARM A's fail-closed
discovery shape (the best idea in the script, learned the hard way after the carrier census was
wrong three times), and then proves the whole path end-to-end by landing the retraction in the one
carrier that is the phase's living spike record: `08-SPIKE.md`.

Purpose: gap 1's `missing` item 1 — "Update `verdict-gate.sh` FIRST — it must be fixed before any
carrier can be." Every other plan in this gap-closure set depends on this one.

Output: a re-pointed three-arm gate whose red input is demonstrated rather than asserted, and a
spike record whose A1 rows say what the instrument could actually see.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md

@.planning/phases/08-process-lifecycle/08-VERIFICATION.md
@.planning/phases/08-process-lifecycle/verdict-gate.sh
@.planning/phases/08-process-lifecycle/08-SPIKE.md
</context>

## Artifacts this phase produces

Symbols, files and identifiers created or renamed by **this plan**:

- `.planning/phases/08-process-lifecycle/verdict-gate.sh` — shell variables `STALE_VERDICT_ERE`,
  `READING_ERE`, `RETRACTION_TOKEN`, `SUMMARY_KNOWN_UNPINNED`; functions `is_excluded` (widened),
  `requires_retraction`.
- No new source symbols under `packages/`. No new files.

Symbols created by **sibling plans in this gap-closure set**, listed here so a source-grounding
pass does not flag them as drift: `classifyLivenessObservation`, `buildLivenessProbePlan`,
`formatSpikeVerdict`, `LivenessVerdict`, `LivenessObservation` (08-12, `kill-plan.ts`);
`lastOrphanReap`, `recordOrphanReapOutcome` (08-15, `index.ts`);
`.planning/phases/08-process-lifecycle/a1-probe-fix.patch` (08-15, new file).

<tasks>

<task type="tracer">
  <name>Task 1: Re-point ARM A and ARM B at A1's actual verdict, then land the retraction in 08-SPIKE.md and watch the carrier drop out</name>

  <read_first>
    - `.planning/phases/08-process-lifecycle/verdict-gate.sh` — the whole file. Read ARM A's header
      argument (lines 13-27 and 108-147) before changing a byte of it: it explains why the arm is a
      repo-wide exclusion-list scan rather than an inclusion list, why `-Z` was rejected, and why the
      newline-in-filename case is detected rather than ignored. That reasoning is sound and survives.
    - `.planning/phases/08-process-lifecycle/08-SPIKE.md` — frontmatter, the A1 table (§ *A1 — does
      the shipped Caido LLRT honour `detached: true`?*), and the § *Control* table.
    - `.planning/phases/08-process-lifecycle/08-VERIFICATION.md` — gap 1's `reason` and `missing`
      arrays, and the `verdict-gate.sh` row of § *Required Artifacts*.
    - `.planning/phases/07-*/07-VALIDATION.md` (or the marked-correction block quotes already inside
      `08-SPIKE.md`) — the house marked-correction convention this edit must follow.
  </read_first>

  <files>.planning/phases/08-process-lifecycle/verdict-gate.sh, .planning/phases/08-process-lifecycle/08-SPIKE.md</files>

  <action>
    This is the tracer slice: the instrument, one carrier, and the gate's own output, wired
    end-to-end and proven by a before/after run. Do the gate first, the document second, in that
    order, in one task, because the ordering is the finding.

    STEP 1 — MEASURE THE SPELLINGS BEFORE INVENTING A PATTERN. Do not trust any count written in
    this plan or in any prior plan; the carrier census in this phase has been wrong five times and
    that is why the gate consumes no count. Run your own scan over `.planning/` and `packages/`
    (pruning `node_modules`, `dist`, `.git`) for every way the tree currently states A1's favourable
    verdict, case-insensitively, and write the resulting spelling list into the script's header as a
    measured fact with the date you measured it. At minimum the scan must reach the spellings
    already known to differ in shape: the hyphenated frontmatter form, the spaced prose form, the
    lowercase prose form, and the "measured favourably" form used in `ROADMAP.md` and
    `REQUIREMENTS.md`. Verify each alternative you write actually matches at least one line in the
    tree BEFORE committing the pattern — an alternative that matches nothing is a vacuous branch and
    this phase has shipped thirteen of those.
    <!-- planner-discipline-allow: CLOSED FAVOURABLY -->
    <!-- planner-discipline-allow: measured favourably -->
    <!-- planner-discipline-allow: grandchild-died (detached honoured) -->

    STEP 2 — INVERT ARM A INTO TWO SCANS, KEEPING ITS SHAPE. Replace the `STALE_ERE` variable with
    `STALE_VERDICT_ERE` (the favourable-A1-verdict alternation from step 1) and add a second
    variable `READING_ERE` holding the raw instrument output string the 2026-08-27 run printed —
    `grandchild-died (detached honoured)`. Keep `QUOTE_STRIP` exactly as it is, both dialects,
    stripping first. Keep the `find … -exec grep -lIE {} +` discovery, the newline-in-path detector,
    and the whole `-Z`/`ugrep` commentary verbatim. The two scans are:

      • Scan A1-STALE: any live (non-block-quoted) occurrence of `STALE_VERDICT_ERE` in a
        non-excluded file is a FAIL naming the file and the count.
      • Scan A1-READING: a file may quote the raw reading — it is a fact and deleting it would
        falsify the record — but only if the retraction travels with it. So: any file with a live
        occurrence of `READING_ERE` that does NOT also contain `RETRACTION_TOKEN` anywhere is a
        FAIL. Define `RETRACTION_TOKEN` as the single word `RETRACTED` and put it in a variable so
        both arms read the same string.

    STEP 3 — WIDEN THE EXCLUSION LIST BY ONE CLASS, WITH ITS COUNTERWEIGHT. Add `*-VERIFICATION.md`
    to `is_excluded`, with a written reason in the same style as the existing four entries: a
    verification report is a dated historical record, and `08-VERIFICATION.md` is the document that
    retracted the reading — its prose necessarily quotes the retracted claim in order to describe
    it, and `08-VERIFICATION.md` states explicitly that it must not be reworded to make this gate
    green. Every entry in that list is a hole, so close this one in the other direction exactly as
    ARM C closes the SUMMARY hole: add a positive check that every `*-VERIFICATION.md` under
    `.planning/phases/` which mentions `A1` at all must contain `RETRACTION_TOKEN`. Verify by
    inspection that today's `08-VERIFICATION.md` satisfies it, and record that you checked.

    STEP 4 — FLIP ARM B's A1 REQUIREMENT. Keep the arm's inclusion-list-is-sound-only-because-ARM-A-
    covers-discovery argument verbatim. Keep the A6 requirement unchanged — every listed carrier that
    states an A6 verdict must still carry `FALSIFIED`, and the 2026-08-27 date requirement stays,
    because A6 was measured that day and that measurement is not being withdrawn. Change only the A1
    half: `packages/backend/src/index.ts` must now carry `RETRACTION_TOKEN` instead of the favourable
    verdict word, and add the same requirement to every other carrier in `CARRIERS_A6` that states an
    A1 verdict. Add a second date requirement — the retraction date — so a carrier cannot satisfy the
    arm by carrying only the old date. Use the date on which this task runs; write it once into a
    variable so every arm reads the same value.

    STEP 5 — RUN THE GATE AND RECORD WHAT IT NAMES. Before touching `08-SPIKE.md`, run the rewritten
    gate and capture the full list of files ARM A names. Save that list; it is the before half of the
    tracer's proof and it is also the working list every sibling plan in this set consumes.

    STEP 6 — CORRECT `08-SPIKE.md`'s A1 VERDICT, AS A MARKED CORRECTION. In the frontmatter, change
    `A1: CLOSED-FAVOURABLY` to `A1: RETRACTED — reading withdrawn, verdict OPEN` and change
    `status: partially-measured` to a value that states what is actually true of the file now
    (A6 measured, A1 withdrawn, Control never taken). Leave `A6: FALSIFIED`, `probe_commit`,
    `vehicle` and the A6 half of `measured` alone. In the § *A1* table, replace the `Verdict` cell
    with the retraction and its date, and add a `Why retracted` row stating the mechanism in one
    sentence: the probe determined liveness with an optional call on a primitive the same run
    measured absent, coalesced the absent case to "not alive", and therefore emitted the favourable
    string unconditionally — the red input did not exist. Preserve the superseded 2026-08-27 cells
    exactly as the file already preserves its 2026-08-24 cells: a dated block quote directly under
    the table, nothing deleted. The `spikeProcessKillType` and `spikeDetachedGroupKill` reading rows
    STAY — they are what the instrument printed, and the whole point is that the reading is real and
    the verdict drawn from it was not.

    Do not touch the § *A6* table. Do not touch the § *Control* table — it was already an honest
    absence, is defended as such in the file's own prose, and this plan's edits must leave it byte
    for byte as it is. Do not touch § *How to run this spike later*; plan 08-15 owns that section.
  </action>

  <verify>
    <automated>cd /Users/six2dez/Tools/drift &amp;&amp; GATE_OUT="$(bash .planning/phases/08-process-lifecycle/verdict-gate.sh 2>&amp;1 || true)" &amp;&amp; printf '%s\n' "$GATE_OUT" | grep -c '^== ARM ' | grep -qx 3 &amp;&amp; printf '%s\n' "$GATE_OUT" | grep -Ec '08-SPIKE\.md' | grep -qx 0</automated>
    <automated>cd /Users/six2dez/Tools/drift &amp;&amp; git diff --stat -- .planning/phases/08-process-lifecycle/08-VERIFICATION.md | wc -l | grep -qx 0</automated>
    <automated>cd /Users/six2dez/Tools/drift &amp;&amp; sed -E '/^[[:space:]]*(\/\/[[:space:]]*)?&gt;/d' .planning/phases/08-process-lifecycle/08-SPIKE.md | grep -c 'FALSIFIED' | grep -qvx 0</automated>
  </verify>

  <acceptance_criteria>
    - The before/after gate runs are both recorded in the SUMMARY. The before run names
      `08-SPIKE.md`; the after run does not; the number of distinct files ARM A names is strictly
      smaller after the document edit than before. Both raw outputs are pasted, not paraphrased.
    - `bash .planning/phases/08-process-lifecycle/verdict-gate.sh` exits non-zero at the end of this
      task and names ONLY carriers owned by sibling plans 08-12, 08-13 and 08-14. That non-zero exit
      is the correct state at this point and the SUMMARY says so.
    - RED INPUT, DEMONSTRATED (scan A1-STALE): create a scratch file under `.planning/` containing
      one live line in each measured spelling, run the gate, confirm it names that file with the
      right count, delete the file, confirm the count returns to its prior value. Paste both outputs.
    - RED INPUT, DEMONSTRATED (scan A1-READING): create a scratch file under `.planning/` containing
      one live occurrence of the raw reading string and no retraction word; the gate names it. Add
      the retraction word to the same file; the gate stops naming it. Delete the scratch file. Paste
      both outputs.
    - RED INPUT, DEMONSTRATED (block-quote strip still bidirectional): the same injected line, moved
      inside a `>` block quote, does NOT trip either scan — proving the strip still works and that a
      preserved supersession does not fail the gate.
    - `git diff` for `08-SPIKE.md` shows ZERO deleted lines carrying a reading value, a pid, a date
      or a verdict cell — every superseded cell appears in the diff as a moved-into-block-quote line,
      not a removed one. State the deleted-line count and what each deleted line was.
    - `git diff` touches exactly two files: `verdict-gate.sh` and `08-SPIKE.md`.
    - The § *Control* table in `08-SPIKE.md` is byte-unchanged: `git diff` shows no hunk inside it.
    - `A6: FALSIFIED` is present in `08-SPIKE.md` frontmatter and the A6 pid values 43921, 43752 and
      44284 all still appear in the file.
    - The script's header states, as a dated measured fact, the spelling list from step 1 and the
      fact that each alternative was verified to match at least one line before the pattern shipped.
  </acceptance_criteria>

  <done>
    The gate asserts A1's actual verdict instead of a withdrawn one, both of its scans have a
    demonstrated red input, `08-SPIKE.md` no longer presents A1 as measured, and the gate's ARM A
    output is a strictly shorter, correct worklist for the rest of this gap-closure set.
  </done>
</task>

<task type="auto">
  <name>Task 2: Make ARM C survive this gap-closure round without going permanently red, and give all three arms a written red input</name>

  <read_first>
    - `.planning/phases/08-process-lifecycle/verdict-gate.sh` — ARM C in full (its header explains
      why `git diff --stat HEAD` asserted nothing at a commit boundary, and why a pinned blob hash
      beats a commit count under a shallow clone). That reasoning survives unchanged.
    - `.planning/phases/08-process-lifecycle/` directory listing — the ten existing `*-SUMMARY.md`
      files ARM C pins.
  </read_first>

  <files>.planning/phases/08-process-lifecycle/verdict-gate.sh</files>

  <action>
    ARM C's fail-closed half requires the count of `*-SUMMARY.md` on disk to equal the count of
    pinned blobs. This gap-closure round will write seven new summaries, so that equality breaks the
    moment plan 08-11's own SUMMARY lands, and it cannot be repaired by pinning: a plan cannot know
    the blob hash of a file that is written after it finishes. Left alone, ARM C goes red for the
    rest of the phase and stays red — which is the same "a gate that forbids the correct next step"
    defect this whole plan exists to remove, relocated one arm to the right.

    Introduce `SUMMARY_KNOWN_UNPINNED` — a newline-separated list of summary filenames that are
    expected to appear during this gap-closure round and are not yet pinned. Populate it with the
    seven names this set will produce: `08-11-SUMMARY.md` through `08-17-SUMMARY.md`. Replace the
    strict count equality with a membership rule: every `*-SUMMARY.md` on disk must be EITHER in the
    pin list OR in the known-unpinned list; anything else is a FAIL naming the file. That keeps the
    arm fail-closed against a summary nobody expected while letting the expected ones land.

    Keep the pinned-blob comparison for the ten historical summaries exactly as it is, keep the
    working-tree diff check exactly as it is, and keep the deliberate-friction paragraph — it still
    applies to the pinned class.

    Write the promotion step into the script header as an explicit outstanding action rather than
    leaving it implicit: once this gap-closure round closes, each name in `SUMMARY_KNOWN_UNPINNED`
    must be promoted to a pinned blob hash, and the list must return to empty. Record the same item
    in the SUMMARY's follow-ups so it is not lost.

    Then rewrite the script's top-of-file `WHAT IT ASSERTS` block. Today it states the premise being
    withdrawn. It must now state what the gate actually asserts: that no living artifact presents A1
    as measured or closed favourably; that any artifact quoting the raw 2026-08-27 reading carries
    the retraction with it; that A6 remains FALSIFIED and every carrier says so; and that the dated
    historical-record class is unmodified. Under each arm, write ONE sentence naming its red input —
    the concrete condition under which that arm fails — because a gate whose red input is not stated
    is the exact defect this phase has now shipped thirteen times.
  </action>

  <verify>
    <automated>cd /Users/six2dez/Tools/drift &amp;&amp; GATE_OUT="$(bash .planning/phases/08-process-lifecycle/verdict-gate.sh 2>&amp;1 || true)" &amp;&amp; printf '%s\n' "$GATE_OUT" | grep -c '^== ARM ' | grep -qx 3 &amp;&amp; printf '%s\n' "$GATE_OUT" | grep -Fc 'FAIL [ARM C]' | grep -qx 0</automated>
  </verify>

  <acceptance_criteria>
    - ARM C passes at the end of this task with the ten historical summaries still pinned by blob
      hash and the seven expected names listed as known-unpinned.
    - RED INPUT, DEMONSTRATED: `touch` a scratch `08-99-SUMMARY.md` in the phase directory that is in
      neither list; the gate fails naming that exact file. Delete it; ARM C passes again. Paste both
      outputs.
    - RED INPUT, DEMONSTRATED (pin half still live): append one byte to a pinned summary in the
      working tree; ARM C fails on the working-tree diff check. Restore with `git checkout --` and
      confirm it passes. Paste both outputs. The pinned summary must be byte-identical to HEAD when
      the task ends.
    - `grep -c 'RETRACTED' verdict-gate.sh` is non-zero and the header's `WHAT IT ASSERTS` block no
      longer states that A1 was closed favourably.
    - Each of the three arms has exactly one sentence in the header naming its red input, and each of
      those sentences describes a condition an executor could construct. Quote all three in the
      SUMMARY.
    - The follow-up "promote the seven known-unpinned summaries to pinned blob hashes when this round
      closes" is recorded in the SUMMARY.
  </acceptance_criteria>

  <done>
    All three arms pass or fail for stated, demonstrated reasons; ARM C tolerates this round's new
    summaries without losing its fail-closed property; and the script's header describes the gate
    that exists rather than the one that was built on a withdrawn reading.
  </done>
</task>

<task type="auto">
  <name>Task 3: Correct the three 08-SPIKE.md prose sections that argue from A1's closure</name>

  <read_first>
    - `.planning/phases/08-process-lifecycle/08-SPIKE.md` — § *What this spike does and does NOT
      close*, § *The spike's own conclusion INVERTED*, and § *The consequence, plainly*. All three
      reason forward from A1 having been closed favourably; all three are now arguing from a
      withdrawn premise.
    - `.planning/phases/08-process-lifecycle/08-VERIFICATION.md` — gap 1 and gap 2, and the
      § *Are the amendments honest?* discussion of OQ-2's rung.
    - `packages/backend/src/kill-plan.ts` lines 60-135 — the LIVE VERDICTS block that carries the
      same argument and is corrected by sibling plan 08-12. Keep the two consistent; the file itself
      says to.
  </read_first>

  <files>.planning/phases/08-process-lifecycle/08-SPIKE.md</files>

  <action>
    Task 1 fixed the verdict cells. This task fixes the three sections that reason from them, because
    a corrected table above three paragraphs of the old argument is a half-correction that reads as a
    contradiction.

    In § *What this spike does and does NOT close*: move the A1 bullet from the CLOSES list to the
    does-NOT-close list, as a marked correction with today's date and the 2026-08-27 text preserved
    in a block quote. State the reason in the bullet itself, in one sentence, so the section is
    readable without following a cross-reference. Leave the A6 bullet in the CLOSES list untouched —
    it is a real reading taken by direct observation, which is exactly why it survives the scrutiny
    A1 did not. Reinstate the item that the nine `killTree` sites are backed by source analysis of a
    pinned commit and not by execution on the shipping runtime; that sentence was correct before
    2026-08-27 and is correct again.

    In § *The spike's own conclusion INVERTED*: numbered point 1 asserts "A1 came back positive" and
    everything after it inherits that. Correct point 1 in place — the failure mode the section
    predicted is neither confirmed nor refuted, because the instrument could not see. Points 2 and 3
    survive unchanged and must be preserved: the failure DID land at A6, and OQ-2's rung genuinely
    does not address A6. Add the consequence the verification report spells out and note its
    direction explicitly: with A1 open, OQ-2's single-pid rung is defence against the PRESENT Caido
    and not only against a future one, so the 2026-08-27 re-justification UNDER-claimed the rung's
    necessity. That is an error toward more caution, not less, and saying so is part of the record.

    In § *The consequence, plainly*: the paragraph asserts that "the nine-site regression this
    paragraph feared did not occur and is not the live risk". Restore it as an open risk, again as a
    marked correction preserving the 2026-08-27 text. The live risk paragraph must now name BOTH
    risks rather than trading one for the other: A6 is falsified, so the group signal cannot reach
    the token-bearing child for the one provider measured; and A1 is unmeasured, so whether the group
    reference names a group that exists at all is unknown on the shipping runtime. Keep the closing
    instruction that OQ-2's rung must not be deleted, and update its stated justification to match.

    Add one short section recording what the phase learned from this, in the file's own voice, and
    state it as the rule rather than as an anecdote: an assertion whose red input requires hardware
    must state, in the same breath, what a "cannot tell" answer looks like, and must never coalesce
    it into either verdict. Name where the fixed three-valued classifier lives so a later reader can
    find it — sibling plan 08-12 puts it in `kill-plan.ts` as `classifyLivenessObservation`.
  </action>

  <verify>
    <automated>cd /Users/six2dez/Tools/drift &amp;&amp; GATE_OUT="$(bash .planning/phases/08-process-lifecycle/verdict-gate.sh 2>&amp;1 || true)" &amp;&amp; printf '%s\n' "$GATE_OUT" | grep -c '^== ARM ' | grep -qx 3 &amp;&amp; printf '%s\n' "$GATE_OUT" | grep -Ec '08-SPIKE\.md' | grep -qx 0</automated>
    <automated>cd /Users/six2dez/Tools/drift &amp;&amp; grep -c 'classifyLivenessObservation' .planning/phases/08-process-lifecycle/08-SPIKE.md | grep -qvx 0</automated>
  </verify>

  <acceptance_criteria>
    - All three named sections read consistently with the corrected A1 verdict cells; no sentence in
      the file asserts, outside a block quote, that A1 was closed, measured or corroborated by
      execution.
    - Every superseded paragraph from the 2026-08-27 revision is preserved as a dated block quote.
      `git diff` for this task shows deleted lines ONLY where text moved into a quote; state the
      count and account for each one.
    - § *The spike's own conclusion INVERTED* still contains its points 2 and 3 (the failure landed
      at A6; OQ-2's rung does not address A6) with their original force, and now also records that
      the 2026-08-27 re-justification under-claimed the rung's necessity.
    - The § *A6* table and the § *Control* table are byte-unchanged across this task.
    - `08-SPIKE.md` names `classifyLivenessObservation` as the place the three-valued liveness
      determination now lives.
    - `bash verdict-gate.sh` does not name `08-SPIKE.md` in any arm. The absence is measured against a SINGLE captured gate run that ALSO carries all three
      `== ARM ` header lines, so a gate that syntax-errored, aborted early or matched nothing
      cannot satisfy this criterion with an empty match set. Paste the capture.
  </acceptance_criteria>

  <done>
    `08-SPIKE.md` is internally consistent: its A1 verdict, its reading rows, its does-not-close list
    and its three argument sections all say the same thing, and every superseded claim is preserved
    rather than erased.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository record → downstream phases | Phases 9-13 read this phase's recorded runtime facts as established. A wrong fact here is inherited silently. |
| gate script → CI / future maintainers | `verdict-gate.sh` is a standing control. Whatever it asserts becomes the definition of "consistent" for anyone who runs it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-51 | Tampering | `verdict-gate.sh` ARM A pattern | high | mitigate | A gate rewritten to be satisfiable becomes a gate satisfied by anything. Mitigated by: spellings enumerated from a measured scan rather than invented; each alternative verified to match a real line before shipping; both scans given a demonstrated red input on a scratch fixture; ARM A's fail-closed exclusion-list shape kept, and the one new exclusion (`*-VERIFICATION.md`) closed in the other direction by a positive retraction requirement. |
| T-08-52 | Repudiation | `08-SPIKE.md` A1 correction | medium | mitigate | A correction that deletes rather than supersedes destroys the record of what the phase believed and when. Mitigated by the marked-correction convention, an acceptance criterion that accounts for every deleted line in the diff, and ARM C's immutability of the dated SUMMARY class. |
| T-08-53 | Tampering | dated `*-SUMMARY.md` class | medium | mitigate | The cheapest way to make ARM A green would be to rewrite a dated summary. ARM C's pinned blob hashes plus the working-tree diff check make that a hard failure, and this plan keeps both while widening only the membership rule. |
| T-08-54 | Information disclosure | gate output | low | accept | `verdict-gate.sh` prints repository paths and match counts only. No token, pid or environment value can reach its output. Accepted with no mitigation. |
</threat_model>

<verification>
- `bash .planning/phases/08-process-lifecycle/verdict-gate.sh` runs to completion, ARM B and ARM C
  pass, and ARM A names only carriers owned by plans 08-12, 08-13 and 08-14.
- FIVE red-input demonstrations are recorded with pasted before/after output — count them against
  the `RED INPUT, DEMONSTRATED` criteria in the tasks, which is the list this line must reconcile
  with: (1) the stale-verdict scan, (2) the reading-context scan, (3) the block-quote strip still
  being bidirectional, (4) ARM C's unlisted-summary case, and (5) ARM C's pin half, via a modified
  pinned summary in the working tree.
- `git diff --stat` for this plan touches exactly `verdict-gate.sh` and `08-SPIKE.md`.
- `08-VERIFICATION.md` is byte-unchanged.
- `pnpm -r typecheck` and `pnpm lint` are unaffected (no source file is touched) — run both once to
  confirm the baseline has not moved.
</verification>

<success_criteria>
- The gate can be satisfied by a correct correction and cannot be satisfied by a stale claim, and
  both directions are demonstrated rather than asserted.
- `08-SPIKE.md` states A1's verdict as withdrawn, preserves every superseded cell and paragraph, and
  leaves A6 and the Control exactly as they were.
- The remaining carriers are enumerated by the gate itself rather than by a hand-written census, so
  the sixth wrong count in this phase cannot happen.
</success_criteria>

<output>
Create `.planning/phases/08-process-lifecycle/08-11-SUMMARY.md` when done. It MUST include: the
before/after ARM A file lists in full, all five red-input demonstrations with pasted output, the
measured spelling list, the accounting of every deleted line in both diffs, and the outstanding
follow-up to promote the seven known-unpinned summaries to pinned blob hashes.
</output>
