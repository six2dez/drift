---
phase: 08-process-lifecycle
plan: 21
type: execute
wave: 14
gap_closure: true
depends_on: ["08-20"]
files_modified:
  - packages/backend/src/owned-temp-file.ts
  - packages/backend/src/owned-temp-file.test.ts
  - packages/backend/src/index.ts
  - packages/backend/src/index.source.test.ts
autonomous: true
requirements: [LIF-01, LIF-02]

estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 2
  confidence: medium

must_haves:
  truths:
    - "Every token-bearing Claude/Copilot config final path and staging path is owned before the first filesystem write can yield or fail."
    - "Config publication is atomic: the complete 0o600 staging file is renamed to the final path; callers never receive a partial final document."
    - "Cleanup removes an ownership entry only after `rm(..., {force:true})` resolves; an EACCES/EPERM unlink failure leaves the exact path owned for a later retry."
    - "Real temporary-filesystem tests exercise partial-write failure, successful atomic publication, failed unlink retention, and later successful cleanup."
    - "Both Claude and Copilot use the same provider-neutral owner and cleanup funnel."
  artifacts:
    - path: "packages/backend/src/owned-temp-file.ts"
      provides: "Injected-I/O atomic owned-file state machine compatible with the constrained backend"
    - path: "packages/backend/src/owned-temp-file.test.ts"
      provides: "Real-filesystem and injected unlink-failure coverage"
    - path: "packages/backend/src/index.ts"
      provides: "Claude/Copilot config writer and finalizer wired to ownership-before-write"
    - path: "packages/backend/src/index.source.test.ts"
      provides: "Provider/call-site census rejecting add-after-write and clear-before-unlink"
  key_links:
    - from: "packages/backend/src/index.ts"
      to: "packages/backend/src/owned-temp-file.ts"
      via: "writeChatMcpConfig supplies final/staging paths, owner set, fs-retry write, rename, and rm callbacks"
      pattern: "writeOwnedTempFile|cleanupOwnedPaths"
    - from: "packages/backend/src/owned-temp-file.ts"
      to: "packages/backend/src/owned-temp-file.test.ts"
      via: "The same state machine runs against real mkdtemp paths and deterministic failing callbacks"
      pattern: "owners|rename|remove"
  prohibitions:
    - statement: "Do not clear the ownership set before unlink results are known, and do not swallow an unlink failure by forgetting its path."
    - statement: "Do not write the token-bearing final config in place or expose token/config contents in diagnostics."
    - statement: "Do not introduce a dependency or Node API that is unavailable to Caido's existing fs/promises surface."
---

<objective>
Make token-bearing per-turn MCP configs owned before write, atomically published, and retained as owned until deletion actually succeeds.

Purpose: WR-02 currently loses partial files when write fails and loses successfully written files when unlink fails because ownership starts late and is cleared early.
Output: a small testable owned-file state machine, real temporary-filesystem tests, and provider-neutral production wiring.
</objective>

<execution_context>
@/Users/six2dez/.codex/gsd-core/workflows/execute-plan.md
@/Users/six2dez/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/08-process-lifecycle/08-VERIFICATION.md
@.planning/phases/08-process-lifecycle/08-REVIEW.md
@.planning/phases/08-process-lifecycle/08-PATTERNS.md
@packages/backend/src/fs-retry.ts
@packages/backend/src/fs-retry.test.ts
@packages/backend/src/index.ts
@packages/backend/src/index.source.test.ts
</context>

## Planner assumptions and no-silent-drop backstops

- `fs/promises` methods already imported by `index.ts` are the runtime boundary. The new helper receives I/O callbacks rather than adding an unverified runtime import.
- A staging filename is unique for the turn (final path plus a generated suffix), lives in the same private runtime directory, and is itself ownership-tracked because it can contain the token.
- T-08-91 and T-08-92 are allocated here and registered/audited by Plan 08-23.
- The unresolved fallback edge sets listed in Plan 08-20 remain open; WR-02 specifically strengthens LIF-02 cleanup idempotency and failure precision without claiming the whole requirement.

## Artifacts this phase produces

- `owned-temp-file.ts` with an atomic-write function and failed-unlink-retaining cleanup function.
- `owned-temp-file.test.ts` with real temp directory coverage and deterministic write/remove failure injection.
- A `writeChatMcpConfig` ownership parameter or equivalent single owner-aware write entry point.
- Source tests proving exactly two provider config call sites use the owner-aware entry point and no add-after-write/clear-before-rm pattern remains.

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Build the owned atomic-file state machine against a real temp filesystem</name>
  <read_first>
    - packages/backend/src/index.ts — current `writeTemp`, `writeChatMcpConfig`, and `cleanupOwnedMcpConfigPaths`
    - packages/backend/src/fs-retry.ts — injected dependency and never-throw retry conventions
    - packages/backend/src/fs-retry.test.ts — Vitest injection style and no-conditional-expect discipline
    - .planning/phases/08-process-lifecycle/08-VERIFICATION.md — WR-02 partial-write and EACCES reproductions
  </read_first>
  <files>packages/backend/src/owned-temp-file.ts, packages/backend/src/owned-temp-file.test.ts</files>
  <behavior>
    - "Final and staging paths enter the owner set synchronously before `writeStaged` is called."
    - "A successful stage+rename removes only staging ownership and retains final ownership."
    - "A write or rename error never returns a final path; any staging/final path not confirmed removed remains owned."
    - "Cleanup deletes one owner only after remove resolves; a failed remove retains it and a later cleanup can delete it."
    - "Two overlapping cleanup calls remain safe and cannot make a failed path ownerless."
  </behavior>
  <action>
Create an import-free helper whose filesystem operations are injected callbacks so production can use the already-proven `fs/promises` surface and tests can use both real files and deterministic failures. The atomic writer must synchronously add both the unique staging path and final path to the shared `Set<string>` before its first await, write only the staging path, rename within the same directory, remove staging ownership only after rename resolves, and return the final path. On any error, attempt best-effort removal but delete ownership only for paths whose removal resolves; rethrow/return an error without exposing content. The cleanup function iterates a snapshot but never calls `clear`; it deletes each set member only after the supplied remove callback resolves. Test real on-disk content/rename behavior under `mkdtemp`, plus injected partial-write and EACCES removal paths. Avoid timers and runtime-specific permission assertions on Windows.
  </action>
  <verify>
    <automated>pnpm exec vitest run packages/backend/src/owned-temp-file.test.ts</automated>
    <fails_when>non-zero exit, any failed test, or a summary reporting zero passed tests</fails_when>
  </verify>
  <acceptance_criteria>
    - Tests observe owner-set contents before the first injected write and after every success/failure transition.
    - A real final file becomes visible only after rename and contains the complete expected JSON bytes.
    - An injected unlink error leaves the path in the set; a subsequent successful cleanup removes both file and owner.
    - The helper imports no filesystem/runtime module and emits no diagnostics or secret values.
  </acceptance_criteria>
  <done>The ownership and atomic-publication rules are executable independently of `index.ts`, including the exact failure directions the verifier reproduced.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Route both provider configs through ownership-before-write</name>
  <read_first>
    - packages/backend/src/owned-temp-file.ts — Task 1 contract
    - packages/backend/src/index.ts — config serialization, retry ladder, Claude/Copilot call sites, finalize and outer-finally cleanup
    - packages/backend/src/index.source.test.ts — current add-after-write and clear-before-unlink assertions
    - packages/backend/src/mcp-server-spec.ts — shared provider config projection and token/value boundary
  </read_first>
  <files>packages/backend/src/index.ts, packages/backend/src/index.source.test.ts</files>
  <behavior>
    - "Claude and Copilot each call the same owner-aware writer exactly once and do not add ownership after it returns."
    - "The existing bounded fs retry wraps directory creation plus staging write/promotion and preserves `lastTempWriteAttempts`."
    - "Finalize and outer finally both call the same cleanup helper; a failed removal remains visible to the second caller."
    - "No config content, token, environment value, or matched secret-bearing line is logged on error."
  </behavior>
  <action>
Import Task 1's helper and change `writeChatMcpConfig` to accept the shared `ownedMcpConfigPaths`. Compute a unique same-directory staging path, establish ownership through the helper before invoking the existing `withFsRetry` operation, keep `mkdir(...0o700)` and `writeFile(...0o600)`, add `rename` to the existing named `fs/promises` import, and promote with that already-supported module surface. Preserve the `findExpandableEnvKeys` refusal before any file is created. Remove both `ownedMcpConfigPaths.add(cfgFile)` call sites. Replace `cleanupOwnedMcpConfigPaths` with Task 1's remove-after-success contract using `rm(path,{force:true})`; log only the existing bounded path label and a scalar cleanup status, never contents or environment values. Rewrite the source test to require exactly two owner-aware config calls, zero add-after-write sites, zero `configPaths.clear()` in cleanup, and delete-after-await ownership behavior. Cite T-08-91/T-08-92 at the production seam.
  </action>
  <verify>
    <automated>pnpm exec vitest run packages/backend/src/owned-temp-file.test.ts packages/backend/src/index.source.test.ts packages/backend/src/mcp-server-spec.test.ts &amp;&amp; pnpm -r typecheck &amp;&amp; pnpm lint</automated>
    <fails_when>non-zero exit, any failed test/typecheck/lint finding, or a targeted Vitest file collecting zero tests</fails_when>
  </verify>
  <acceptance_criteria>
    - Both provider branches use one owner-aware writer and no post-write `Set.add` remains.
    - Cleanup contains no eager set clear and a remove rejection cannot reduce the owner count.
    - Owner-only modes, retry-attempt diagnostics, shared config projection, and expandable-value refusal remain intact.
    - No frontend/shared/planning record file changes and no new dependency.
  </acceptance_criteria>
  <done>WR-02 is closed: no partial or unlink-failed token config becomes ownerless, and both providers share the same tested lifecycle.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Token-bearing JSON -> filesystem | A partial write can leave credential material even when the caller receives an error. |
| Owner set -> unlink | An unlink failure must not be translated into ownership success. |
| Staging path -> final provider config | Providers must see only complete atomically promoted documents. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-91 | Information Disclosure | Per-turn MCP config partial/staging files | high | mitigate | Own staging/final paths before write, keep 0o600, atomically rename, and retain ownership for every path not confirmed removed. |
| T-08-92 | Repudiation / Information Disclosure | Config cleanup owner set | high | mitigate | Never clear eagerly; delete an owner only after remove resolves and test EACCES retention plus later retry. |
</threat_model>

<verification>
- Run real-filesystem owned-file tests, source wiring tests, config projection tests, typecheck, and lint.
- Reproduce an unlink rejection and require both file survival and owner retention, then retry cleanup successfully.
- Confirm no values/tokens enter diagnostic output and no dependency or frontend/shared change is introduced.
</verification>

<success_criteria>
- WR-02's partial-write and clear-before-unlink failures are both falsified by production-helper tests.
- Provider config content is atomically visible and remains owned until deletion succeeds.
- Existing QuickJS-compatible I/O and POSIX mode behavior are preserved.
</success_criteria>

## Multi-Source Coverage Audit

| Source type | Item | Coverage | Status |
|-------------|------|----------|--------|
| GOAL | No token-bearing artifacts survive unowned | Atomic write and retained cleanup ownership | COVERED |
| REQ | LIF-01 | No platform claim; existing Windows-safe I/O surface retained | COVERED (explicitly open) |
| REQ | LIF-02 | Token config cleanup failure paths are implemented/tested | COVERED |
| RESEARCH | QuickJS and security-conscious temp modes | Injected I/O plus existing fs/promises callbacks and modes | COVERED |
| CONTEXT | No 08-CONTEXT.md | Verifier WR-02 defines scope | COVERED |
| VERIFICATION | Own before write; delete owner after unlink | Tasks 1-2 | COVERED |
| UI-SPEC | No UI work | Frontend/shared excluded | COVERED |
