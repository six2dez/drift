---
phase: 06-windows-command-resolution
plan: 07
type: execute
wave: 6
depends_on: ["06-06"]
files_modified:
  - packages/frontend/src/views/HelpView.vue
  - README.md
autonomous: true
requirements: [UX-02]
tags: [ux-copy, frontend, phase-gate, evidence]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The frontend help panel renders its install-command list from the shared table, so it cannot drift from the backend error banner."
    - "The archived Copilot extension command appears in no source file under any package and in no user-facing document, while the historical changelog entry that records what version 0.1.0 shipped is untouched."
    - "All four in-repo comments this phase makes false are corrected in place, verified by search rather than by assertion."
    - "The full test suite, the workspace typecheck and the lint gate are green on both continuous-integration legs before the phase is claimed complete."
    - "Every claim this phase does NOT make is recorded: that a resolved shim is launchable, that any Windows path was exercised on a real machine, and that the Windows search timeout is a measurement."
  artifacts:
    - packages/frontend/src/views/HelpView.vue
    - README.md
  key_links:
    - "shared cli-providers PROVIDER_INSTALL_COMMANDS -> HelpView list. The second of the two live surfaces D-15 unifies; the first is the backend error banner."
    - "shared cli-providers CLI_PROVIDER_DISPLAY_NAMES -> the list's bold labels, so the list needs no second literal."
  prohibitions:
    - "The historical changelog entry is not edited."
    - "No install-command string is hardcoded in the frontend view."
    - "No Windows install or prerequisite documentation section is added to the project readme."
    - "No shell-configuration or execution-policy note is added to any user-facing surface."
    - "No claim that a resolved shim path is launchable appears in any artifact this phase produces."
---

<objective>
Close UX-02's second live surface — the frontend help panel — and close the phase on evidence:
every stale comment corrected, every non-claim recorded, every gate green on both continuous-
integration legs.

Purpose: D-15 unifies the two surfaces that can lie to a user mid-session. The backend error banner
was unified in plan 06-06; the help panel is the other one. And this repository's discipline is that
a phase closes on checked evidence, not on a claim — 03-FINDINGS cites run identifiers, not
recollection.

Output: a help panel that renders from one source of truth, and a recorded, checked closure.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-windows-command-resolution/06-CONTEXT.md
@.planning/phases/06-windows-command-resolution/06-RESEARCH.md
@.planning/phases/06-windows-command-resolution/06-VALIDATION.md
@packages/shared/src/cli-providers.ts
@packages/frontend/src/views/HelpView.vue
</context>

## Artifacts this plan produces

No new exported symbols. This plan wires an existing shared export into an existing view and closes
the phase on checked evidence.

## Planner decisions recorded for this plan

- **The readme's Copilot edit is a no-op and is recorded as one.** The deferred item asked for the
  readme prose naming the deprecated Copilot command to be updated in place. The research searched
  the readme for that command and for every related token and found ZERO hits — so there is nothing
  to update, and the deferred item is closed as a no-op rather than left looking undone.
- **The readme's probed-locations sentence IS updated, and only that sentence.** It names the
  install locations Drift searches and becomes incomplete on Windows the moment this phase lands.
  One sentence of prose accuracy is in scope; a Windows install or prerequisite SECTION is not —
  that is a later phase's requirement, and D-13 already declined to pull its copy forward.
- **The help panel renders the macOS/Linux arm plus the Windows arm, labelled.** The panel is
  static help rather than a live error banner, so it has no platform value to branch on and no
  reason to guess. Showing both labelled arms is the same union-when-unknown rule this phase
  applies at four other sites, and it makes the panel useful to a reader on either platform.

<tasks>

<task type="auto">
  <name>T-06-19: The help panel renders from the shared install-command table (D-15)</name>
  <files>packages/frontend/src/views/HelpView.vue</files>
  <reversibility rating="costly">Completes D-15, recorded `costly` because it crosses the package boundary — unwinding it means re-inlining the table in two packages. Not `one-way`, so no decision checkpoint is earned.</reversibility>
  <read_first>
    - `packages/frontend/src/views/HelpView.vue` — the whole file, and in particular the block listing the four install commands as static items and the surrounding explanatory prose. The component currently imports one component and nothing else, so this adds its first shared-package import.
    - `packages/frontend/src/components/chat/CliStatus.vue` and `packages/frontend/src/stores/settings.ts` — the frontend's shared-import convention: a bare package specifier, not a relative path, and a type-only import where only types are needed.
    - `packages/shared/src/cli-providers.ts` — the table added in plan 06-06, the provider union constant, and the display-name record that supplies the list's bold labels.
  </read_first>
  <behavior>
    - The install-command list is generated by iterating the shared provider union rather than written out item by item.
    - Each item's bold label comes from the shared display-name record; the list contains no second hardcoded product-name list.
    - Each item shows the macOS/Linux command and the Windows command, each labelled with its platform, and shows a single command when both arms are equal.
    - Adding a fifth provider to the shared union makes it appear in this list with no edit to this file.
    - No install command string appears as a literal anywhere in the view.
    - The surrounding explanatory prose, the card structure and the styling classes are unchanged.
  </behavior>
  <action>
    Replace the static install-command list in `packages/frontend/src/views/HelpView.vue` with a
    loop over the shared table.

    Import the provider union constant, the display-name record and the install-command table from
    the shared package using the bare package specifier the other frontend modules already use.
    Build the list by iterating the union's values so a provider added to the shared package appears
    here automatically — that automatic propagation is the point of the change, and hand-listing the
    four keys would preserve exactly the drift the table exists to remove.

    Render each item with the display name as the bold label, exactly as the existing items do, and
    the commands in the same monospace styling the existing items use. Where the two platform arms
    differ, show both with a short platform label on each; where they are equal, show one command
    with no label. Keep every existing class name, the card structure and the surrounding prose
    untouched — this is a data-source change, not a redesign.

    Add a brief comment above the loop stating that the commands come from the shared table so this
    panel and the backend error banner cannot disagree, and that these are the two live surfaces
    that can mislead a user mid-session.

    Do not add a platform-detection branch to this component. The panel is static help with no
    platform value available to it, and showing both labelled arms is both the honest answer and
    the same union-when-unknown rule this phase applies elsewhere.
  </action>
  <verify>
    <automated>pnpm -r typecheck && pnpm lint && pnpm exec vitest run && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm -r typecheck` exits 0, `pnpm lint` exits 0, `pnpm exec vitest run` exits 0, `pnpm build` exits 0.
    - The view imports from the shared package with a bare specifier: `grep -Ec 'from "shared"' packages/frontend/src/views/HelpView.vue` returns at least `1`.
    - No install command is hardcoded in the view: `grep -Ec 'npm install -g|claude.ai/install|install.ps1' packages/frontend/src/views/HelpView.vue` returns `0`.
    - No second product-name list exists in the view's install block: the bold labels come from the shared display-name record, verified by `grep -Ec 'CLI_PROVIDER_DISPLAY_NAMES' packages/frontend/src/views/HelpView.vue` returning at least `1`.
    - The list is generated rather than enumerated: `grep -Ec 'v-for' packages/frontend/src/views/HelpView.vue` returns at least `1`.
    - The deprecated command is absent from the whole frontend package: `grep -rc 'gh extension install' packages/frontend/src` reports `0` for every file.
      <!-- planner-discipline-allow: gh extension install -->
    - The surrounding card structure survives: the explanatory sentence directing the user to the settings panel's command field is still present in the file.
  </acceptance_criteria>
  <done>A user reading the in-app help sees the same install commands the error banner would give them, on both platforms, and a future provider addition reaches both surfaces from one edit.</done>
</task>

<task type="auto">
  <name>T-06-20: Readme prose accuracy and the phase evidence gate</name>
  <files>README.md</files>
  <precondition>The blocking Windows verification job exists in the repository's continuous-integration workflow, landed by Phase 5. Confirm by searching `.github/workflows/ci.yml` for a job whose name identifies the Windows verification leg and whose runner is a Windows image; halt and report if it is absent, because this task's second gate cannot be satisfied without it.</precondition>
  <read_first>
    - `README.md` — the configuration section's sentence naming the install locations Drift probes. Read the whole paragraph before editing; the sentence sits inside guidance about pinning an absolute binary path, which stays.
    - `.planning/phases/06-windows-command-resolution/06-PATTERNS.md` § *Recorded non-claims and to-do hand-offs* — the table of the four in-repo comments this phase makes false, with their file and line references. All four are corrected by earlier plans; this task verifies the correction landed.
    - `.planning/phases/06-windows-command-resolution/06-VALIDATION.md` — the per-task verification map and the manual-only table, so the closure records match the contract.
    - `.github/workflows/ci.yml` — both verification jobs, so the run evidence is cited by job name.
  </read_first>
  <action>
    **1. Fix the one readme sentence that this phase makes inaccurate.** The configuration section
    tells the reader that Drift probes a named set of common install locations. That list is
    POSIX-shaped and becomes incomplete the moment this phase lands. Extend the sentence to say that
    on Windows the probed set is the Windows equivalent — the roaming application-data package
    prefix, the user profile's local binary directory, and the version-manager and package-manager
    locations — without enumerating every path, because the enumeration lives in the source with its
    citations and duplicating it here would create the drift surface this phase spent a plan
    removing. Change nothing else in the paragraph: the guidance to pin an absolute path stays, and
    the provider status table stays.
    Do NOT add a Windows install or prerequisite section, and do NOT add a shell-configuration
    caveat. Both belong to a later phase's requirement and were deliberately not pulled forward.

    **2. Record that the deferred readme Copilot edit is a no-op.** The deferred item expected the
    readme prose naming the deprecated Copilot command to be corrected. Searching the readme for
    that command and its related tokens returns nothing — the readme never named it. Verify that by
    search, and record the no-op in this plan's summary so the deferred item is closed with a reason
    rather than left looking undone.

    **3. Run the phase-wide comment-correction gate.** Four in-repo comments claimed either that
    Phase 6 would do something or that Phase 6 would not touch something; all four are now false and
    all four were corrected by earlier plans in this phase. Verify by search that none of the stale
    claims survives, in the candidate-resolution module, the platform module twice, and the
    runtime-probe module. Any survivor is a defect to fix here, not to note.

    **4. Run the phase-wide evidence gates and record the results.** Execute the full test suite,
    the workspace typecheck, the lint gate at its zero-warning setting, and the build. Then confirm
    both continuous-integration legs are green — the multi-version verification job and the blocking
    Windows verification job — and cite them by job name and run identifier in the summary. Cite
    committed evidence rather than artifacts: this project's own discipline is that artifacts expire
    and committed records do not.

    **5. Record the phase's non-claims in the summary, explicitly.** Three of them, each stated as a
    non-claim rather than implied by silence: no Windows path in this phase was exercised against a
    real Windows install, because the maintainer has no such machine, so the catalogue's spelling is
    proven and its existence is not; nothing in this phase makes a resolved shim launchable, which
    remains a later phase's requirement; and the Windows search timeout is a headroom estimate
    bounded by two numbers already in this codebase, not a measurement. These continue the recorded
    non-claim practice the previous phase established and the verifier expects.
  </action>
  <verify>
    <automated>pnpm exec vitest run && pnpm -r typecheck && pnpm lint && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm exec vitest run`, `pnpm -r typecheck`, `pnpm lint` and `pnpm build` all exit 0.
    - The readme sentence names the Windows probed set: `grep -Eci 'windows' README.md` returns at least `1` within the configuration section's provider paragraph.
    - No Windows install or prerequisite heading was added: the readme's heading count is unchanged from before this task.
    - No execution-policy note was added: `grep -Eci 'ExecutionPolicy' README.md` returns `0`.
    - The historical changelog entry is untouched: `git diff --name-only CHANGELOG.md` produces no output, and `grep -c 'gh extension install' CHANGELOG.md` still returns `1`.
      <!-- planner-discipline-allow: gh extension install -->
    - The deprecated command survives nowhere else: `grep -rl 'gh extension install' --include='*.ts' --include='*.vue' --include='*.mjs' --include='*.md' . | grep -v node_modules | grep -v '^./.planning'` outputs only the changelog path.
    - All four stale comments are corrected — each of these returns `0`: `grep -c 'belongs to Phase 6' packages/backend/src/command-resolution.ts`; `grep -c 'NOT implemented here' packages/backend/src/platform.ts`; `grep -c 'which this phase does not touch' packages/backend/src/platform.ts`; `grep -c 'Phase 6 compares exactly those' packages/backend/src/runtime-probe.ts`.
    - The preserved export still exists and is still uncalled by production code: `grep -Ec 'export async function normalizePathForCompare' packages/backend/src/runtime-probe.ts` returns `1` and `grep -rc 'normalizePathForCompare' packages/backend/src/index.ts` returns `0`.
    - The zero-import property of the platform module survives the whole phase: `grep -c '^import' packages/backend/src/platform.ts` returns `0`.
    - Both continuous-integration legs are green and are cited in the summary by job name and run identifier.
    - The summary records the three non-claims verbatim as non-claims.
  </acceptance_criteria>
  <done>The phase closes the way this project closes phases: every gate run rather than assumed, every stale claim searched for rather than presumed corrected, and every limit of what was proven written down rather than left for a later reader to discover.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Drift's help copy → the user's shell | Commands shown in the help panel are commands a user will paste into a terminal with their own privileges. |
| shared package → browser frontend | The shared module is loaded into the browser context as well as the backend's constrained runtime, so it must stay data-only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-SC | Tampering | package names in help-panel copy | high | mitigate | This plan installs no packages and adds no new command string: it renders the table whose provenance plan 06-06 established, with three of four provider commands verified against first-party documentation and the fourth carrying an explicit assumption tag on an unchanged string. Removing the hardcoded list is itself the mitigation — a hand-copied command cannot be corrected in one place. 06-RESEARCH § *Package Legitimacy Audit* records zero slop and zero suspicious verdicts, so no legitimacy checkpoint is warranted. |
| T-06-T26 | Spoofing | a stale command surviving in one surface | medium | mitigate | A command corrected in the error banner but left stale in the help panel would still send a user to an archived repository. Wiring both surfaces to one table removes the possibility. Gate: the repository-wide absence search in T-06-20. |
| T-06-T27 | Tampering | shared package gaining browser-hostile content | low | mitigate | The table stays data-only with no imports and no input/output, so it loads in both the browser and the constrained backend runtime. Gate: the build succeeding, which exercises the frontend bundle. |
| T-06-T28 | Repudiation | phase closed without evidence | medium | mitigate | A phase claimed complete without run evidence cannot be audited later. Both continuous-integration legs are cited by job name and run identifier, and committed records are cited rather than artifacts, which expire. |
</threat_model>

<verification>
- `pnpm exec vitest run`, `pnpm -r typecheck`, `pnpm lint` and `pnpm build` all exit 0.
- Both continuous-integration legs green, cited by job name and run identifier.
- The deprecated command appears in exactly one file, the historical changelog, which is untouched.
- All four stale in-repo comments are corrected, verified by search.
- The three phase non-claims are recorded in the summary.
</verification>

<success_criteria>
- ROADMAP SC-4's second surface is closed: the in-app help panel and the error banner render the same install commands from one table.
- ROADMAP SC-5 is closed: the extended candidate-resolution unit tests cover the Windows cases and run green on the Linux runner — and, as a free second data point, on the Windows runner too.
- The phase's limits are recorded as explicitly as its results.
</success_criteria>

<output>
Create `.planning/phases/06-windows-command-resolution/06-07-SUMMARY.md` when done
</output>
