---
phase: 04-platform-foundation
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/backend/src/bounded-buffer.ts
  - packages/backend/src/bounded-buffer.test.ts
autonomous: true
requirements: [PERF-04]

must_haves:
  truths:
    - "appendBounded returns the accumulated text unchanged when it is below the cap — the zero-overhead common case"
    - "Above the cap, both-ends retention keeps a head and a tail joined by a truncation marker that carries the dropped byte count, so a reader can see what was lost instead of a silent hole"
    - "Four per-site caps ship as exported module constants so they are tunable on evidence rather than inline literals"
    - "Drift truncates and keeps reading rather than killing the child — Node's child_process.exec maxBuffer precedent of killing on overflow would convert a cosmetic problem into a lost turn"
  artifacts:
    - path: "packages/backend/src/bounded-buffer.ts"
      provides: "PERF-04 site A: bounded stdout/stderr accumulation with marked truncation"
      exports: ["BoundedBuffer", "BoundedRetention", "TRUNCATION_MARKER_PREFIX", "CLI_STDOUT_MAX_CHARS", "CLI_STDERR_MAX_CHARS", "SPAWN_STDOUT_MAX_CHARS", "SPAWN_STDERR_MAX_CHARS", "createBoundedBuffer", "appendBounded", "renderBoundedBuffer"]
      min_lines: 70
    - path: "packages/backend/src/bounded-buffer.test.ts"
      provides: "Unit proof of each retention policy and of the marker's byte count"
      min_lines: 90
  key_links:
    - from: "packages/backend/src/bounded-buffer.test.ts"
      to: "./bounded-buffer"
      via: "sibling relative import"
      pattern: "from \"./bounded-buffer\""
---

<objective>
Create `packages/backend/src/bounded-buffer.ts` — PERF-04 **site A**, the `stdout`/`stderr`
accumulators — plus its sibling `bounded-buffer.test.ts`.

Purpose: `spawnAndWait` (`index.ts:1522-1523`) and `sendCliMessage` (`index.ts:2205-2206`,
`:2484`, `:2573`) accumulate child output into unbounded `let stdout = ""` / `let stderr = ""`
strings. A runaway CLI grows those without limit inside the Caido backend's heap. The hazard at this
site is **total volume**; PERF-04's other site (`claude-print.ts`'s line buffer, plan 04-06) has a
genuinely different hazard and gets a genuinely different policy. Treating them with one policy is
the mistake to avoid.

**Claude's Discretion decisions recorded here** (CONTEXT.md left cap size, retention and marker text
to the planner): four per-site caps, three retention policies, and one greppable marker carrying the
dropped count — all as exported constants so a later phase can widen them on evidence.

Output: `packages/backend/src/bounded-buffer.ts`, `packages/backend/src/bounded-buffer.test.ts`.

Validation rows owned (04-VALIDATION.md): PERF-04 `-t "below the cap"`, `-t "both ends"`,
`-t "marker"`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-platform-foundation/04-CONTEXT.md
@.planning/phases/04-platform-foundation/04-PATTERNS.md
@packages/backend/src/mcp-runtime.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write bounded-buffer.ts — three retention policies, four caps, one marker</name>

  <read_first>
    - `packages/backend/src/index.ts` lines 335-347 — `summarizeDebugChunk`, the repo's only existing
      truncate-with-marker helper, and `DEBUG_CHUNK_PREVIEW_CHARS` at `index.ts:88`. Copy the
      `if (x.length <= cap) return x;` early return and the module-level cap constant style.
    - `packages/backend/src/index.ts` lines 1519-1529 (`spawnAndWait`'s accumulators), lines
      2205-2206 (the `sendCliMessage` declarations), lines 2482-2493 and 2571-2581 (the two `data`
      handlers). Plan 04-09 converts these; this plan only supplies the helper. Note the existing
      `text.slice(0, 400)` at `:2492`/`:2580` is a *log preview*, not the accumulator — leave it.
    - `packages/backend/src/provider-launch.ts` lines 1-6 — the "why this module is pure" header.
    - `.planning/phases/04-platform-foundation/04-RESEARCH.md` § *PERF-04: Bounded Buffer Design*
      → *Site A* (the cap table and the marker convention) and the note on Node's
      `child_process.exec` `maxBuffer` precedent.
  </read_first>

  <files>packages/backend/src/bounded-buffer.ts</files>

  <action>
Create `packages/backend/src/bounded-buffer.ts` with a 4-6 line header comment stating that the
truncation arithmetic is pure so it is unit-testable, that `index.ts` calls it inside the child
process `data` handlers, and that this covers PERF-04 **site A** only (total volume) — site B, the
Claude line buffer, has a different hazard and a different policy.

The module imports nothing.

Exports:

1. `export type BoundedRetention = "head" | "tail" | "both";`

2. `export type BoundedBuffer = { head: string; tail: string; droppedChars: number; maxChars: number; retention: BoundedRetention };`
   A state object rather than a bare string, because the marker must carry a **cumulative** dropped
   count and re-parsing a previously-embedded marker out of a string on every append would be
   fragile. This matches the backend's proven `createXState(): PlainObject` + free-functions shape
   (`claude-print.ts:103-117`, `:140`).

3. `export function createBoundedBuffer(options: { maxChars: number; retention: BoundedRetention }): BoundedBuffer`
   — initialises `head: ""`, `tail: ""`, `droppedChars: 0`.

4. `export function appendBounded(buffer: BoundedBuffer, chunk: string): BoundedBuffer` — returns a
   **new** state object, never mutates. Policies:
   - `"head"`: fill `head` up to `maxChars`; any excess increments `droppedChars` and is discarded.
     `tail` stays `""`.
   - `"tail"`: append to `tail`, then drop from the **front** until `tail.length <= maxChars`,
     incrementing `droppedChars` by the number of characters dropped. `head` stays `""`.
   - `"both"`: `head` fills to `Math.floor(maxChars / 2)`; everything after that goes into `tail`,
     which is then trimmed from the front to `maxChars - head.length`, incrementing `droppedChars`.
   Below the cap, all three policies must leave `droppedChars` at `0` and produce
   `head + tail === previousText + chunk`.

5. `export const TRUNCATION_MARKER_PREFIX = "\n…[drift: truncated ";` and a helper
   `export function buildTruncationMarker(droppedChars: number): string` producing exactly
   `` `\n…[drift: truncated ${droppedChars} bytes]…\n` `` — one greppable string with the count
   embedded so a reader knows what they lost. Comment that the count is UTF-16 code units (JS string
   `.length`) while the marker says "bytes" — the wording is kept for greppability and the
   discrepancy is documented rather than silently wrong.

6. `export function renderBoundedBuffer(buffer: BoundedBuffer): string` — returns `head + tail` when
   `droppedChars === 0`, otherwise `head + buildTruncationMarker(droppedChars) + tail`.

7. The four per-site caps as exported constants, each with a one-line rationale comment:
   - `export const CLI_STDOUT_MAX_CHARS = 2 * 1024 * 1024;` — `sendCliMessage` stdout, retention
     `"both"`. For gemini/codex/copilot this string *is* the chat answer (`index.ts:2332`, `:2340`);
     the opening of an answer matters to a reader and so does the conclusion, so a middle-drop
     preserves both. 2 MiB is roughly 20x a long answer while still bounding the heap.
   - `export const CLI_STDERR_MAX_CHARS = 256 * 1024;` — `sendCliMessage` stderr, retention `"tail"`.
     stderr is diagnostics; the *last* error is the actionable one and warnings pile up ahead of it.
   - `export const SPAWN_STDOUT_MAX_CHARS = 1024 * 1024;` — `spawnAndWait` stdout, retention
     `"head"`. Consumers read line 1 (`which` output, `node --version`) or a short `mcp add`
     acknowledgement.
   - `export const SPAWN_STDERR_MAX_CHARS = 256 * 1024;` — `spawnAndWait` stderr, retention `"tail"`.

Add a comment recording the deliberate divergence from Node's own precedent: `child_process.exec`'s
`maxBuffer` (default 1 MiB) **kills the child** on overflow. Drift must not — killing the CLI
mid-answer converts a cosmetic problem into a lost turn. Truncate and keep reading.
  </action>

  <verify>
    <automated>pnpm typecheck && pnpm exec eslint packages/backend/src/bounded-buffer.ts --max-warnings 0</automated>
  </verify>

  <acceptance_criteria>
    - `packages/backend/src/bounded-buffer.ts` has zero import statements — `grep -c '^import' packages/backend/src/bounded-buffer.ts` returns `0`.
    - It exports `BoundedBuffer`, `BoundedRetention`, `TRUNCATION_MARKER_PREFIX`, `buildTruncationMarker`, `createBoundedBuffer`, `appendBounded`, `renderBoundedBuffer`, `CLI_STDOUT_MAX_CHARS`, `CLI_STDERR_MAX_CHARS`, `SPAWN_STDOUT_MAX_CHARS`, `SPAWN_STDERR_MAX_CHARS`.
    - The four caps are exactly `2097152`, `262144`, `1048576`, `262144`.
    - `buildTruncationMarker(4194304)` returns the literal `"\n…[drift: truncated 4194304 bytes]…\n"`.
    - `appendBounded` never mutates its input — the returned object is not reference-equal to the input.
    - `grep -c "kill\|SIGTERM\|SIGKILL" packages/backend/src/bounded-buffer.ts` returns `0` — the module never terminates a child.
    - `pnpm typecheck` exits 0; `pnpm exec eslint packages/backend/src/bounded-buffer.ts --max-warnings 0` exits 0.
  </acceptance_criteria>

  <done>`bounded-buffer.ts` compiles and lints clean, exports the eleven symbols, and implements all three retention policies without mutation.</done>
</task>

<task type="auto">
  <name>Task 2: Write bounded-buffer.test.ts — below-cap passthrough, both-ends bound, marker count</name>

  <read_first>
    - `packages/backend/src/bounded-buffer.ts` — the module just written.
    - `packages/backend/src/mcp-runtime.test.ts` lines 1-13 — the named analog for structure.
    - `.planning/phases/04-platform-foundation/04-VALIDATION.md` lines 94-96 — the three `-t`
      selectors this file must satisfy.
  </read_first>

  <files>packages/backend/src/bounded-buffer.test.ts</files>

  <action>
Create `packages/backend/src/bounded-buffer.test.ts` with `import { describe, expect, it } from "vitest"`
and a sibling relative import from `"./bounded-buffer"`.

`describe("appendBounded")` with these `it` titles:
- `it("returns the input unchanged below the cap")` — the `-t "below the cap"` selector. For each of
  the three retentions, append several chunks totalling well under `maxChars`; assert
  `renderBoundedBuffer(result)` equals the exact concatenation of the chunks and `droppedChars` is
  `0` and the rendered string contains no `TRUNCATION_MARKER_PREFIX`.
- `it("keeps both ends and drops the middle when the cap is exceeded")` — the `-t "both ends"`
  selector. Use a small `maxChars` (e.g. `100`) so the assertion is legible. Append a long,
  positionally-identifiable string (e.g. a repeated numbered pattern). Assert: the rendered output
  starts with the original opening, ends with the original closing, contains the marker between them,
  and `rendered.length <= maxChars + buildTruncationMarker(result.droppedChars).length`.
- `it("keeps the head only under head retention")` and
  `it("keeps the tail only under tail retention")` — assert the surviving end and that the other end
  is gone.
- `it("embeds the dropped byte count in the marker")` — the `-t "marker"` selector. Assert the
  rendered string contains the literal `result.droppedChars` value and that `droppedChars` equals
  `totalAppendedLength - (head.length + tail.length)`.
- `it("accumulates the dropped count across several appends past the cap")` — three successive
  over-cap appends; assert `droppedChars` is monotonically increasing and the final marker carries
  the cumulative total, not the last append's contribution.
- `it("does not mutate the buffer it is given")` — capture the input state, append, assert the input
  object's `head`, `tail` and `droppedChars` are unchanged.
- `it("handles an empty chunk and an empty buffer")` — edge cases return a well-formed state.

`describe("cap constants")`:
- `it("exports the four site caps as tunable constants")` — assert the four exported values so a
  silent change to any of them fails a test rather than shipping.

Do not create `.snap` files. Do not use `vi.useFakeTimers()`.
  </action>

  <verify>
    <automated>pnpm exec vitest run packages/backend/src/bounded-buffer.test.ts</automated>
  </verify>

  <acceptance_criteria>
    - `pnpm exec vitest run packages/backend/src/bounded-buffer.test.ts` exits 0 with 0 failures.
    - Each of these matches at least one passing test: `-t "below the cap"`, `-t "both ends"`, `-t "marker"`.
    - The `-t "both ends"` test asserts an upper bound on total rendered length (`<= maxChars + marker.length`), not merely that truncation occurred.
    - The `-t "marker"` test asserts the numeric dropped count appears in the rendered string.
    - `grep -c "describe.only\|it.only" packages/backend/src/bounded-buffer.test.ts` returns `0`.
    - `pnpm exec eslint packages/backend/src/bounded-buffer.test.ts --max-warnings 0` exits 0.
  </acceptance_criteria>

  <done>All three `-t` selectors from 04-VALIDATION.md resolve to passing tests, and the total-length bound is asserted numerically.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Spawned CLI / MCP child process → backend `stdout`/`stderr` pipes | Unbounded, attacker-influenceable volume crosses here into the Caido backend's heap on a single-threaded runtime |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05 | Denial of Service | `appendBounded` at all four accumulator sites | mitigate | Every accumulator is capped by an exported constant (2 MiB / 256 KiB / 1 MiB / 256 KiB). A runaway CLI cannot exhaust the Caido backend's memory. Proven by the `-t "both ends"` upper-bound assertion |
| T-04-17 | Denial of Service | overflow handling policy | mitigate | Drift truncates and keeps reading rather than killing the child, deliberately diverging from Node's `child_process.exec` `maxBuffer` precedent. Killing mid-answer would convert a resource-bound into a lost turn — a self-inflicted availability failure |
| T-04-18 | Repudiation | silent truncation | mitigate | Truncation is **marked**, not silent: `buildTruncationMarker` embeds the dropped count so a user reading a short answer can tell it was cut rather than assuming the model stopped |
| T-04-SC | Tampering | npm/pnpm installs | mitigate | Phase 4 installs zero packages; this module has no imports at all |
</threat_model>

<verification>
- `pnpm typecheck` exits 0.
- `pnpm lint` exits 0 at `--max-warnings 0`.
- `pnpm exec vitest run packages/backend/src/bounded-buffer.test.ts` is green.
- `pnpm exec vitest run` — the pre-existing suite stays green.
</verification>

<success_criteria>
- ROADMAP SC-7's site-A half is satisfied in pure form: `stdout`/`stderr` accumulation is bounded with marked truncation, unit-tested per retention policy.
- The four caps and the marker text ship as exported constants, so the Claude's-Discretion tuning decisions are reviewable and changeable without touching call sites.
- No child process is ever killed by the bounding mechanism.
</success_criteria>

<output>
Create `.planning/phases/04-platform-foundation/04-05-SUMMARY.md` when done.
</output>
