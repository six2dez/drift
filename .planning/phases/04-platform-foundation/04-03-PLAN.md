---
phase: 04-platform-foundation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/backend/src/runtime-probe.ts
  - packages/backend/src/runtime-probe.test.ts
autonomous: true
requirements: [RUN-05]

must_haves:
  truths:
    - "D-06: the gate rule is encoded as data — os.platform() and os.tmpdir() are marked gating; realpath availability and the Windows env-var presence are marked reported-only, and every result lands in the report whether it gates or not"
    - "D-08: the failure message carries a best-effort version block whose every field renders 'unavailable' when its source throws, sourced from process.version/process.versions, os.platform()+os.release(), and Drift's own manifest version"
    - "D-04: normalizePathForCompare is IMPURE and lives OUTSIDE platform.ts — it lands in runtime-probe.ts with its fs rungs injected, and its ladder is realpathSync.native -> fs/promises.realpath -> path.resolve"
    - "The ladder reports which rung was actually reached, because realpath does not exist anywhere in caido/dependency-llrt's fs module and the ladder will always land on path.resolve under Caido"
    - "buildProbeReport accepts an optional realpathRungNote appended to the realpath detail, so plan 04-08 can report rung 1 as `not probed` — a different claim from `absent` — without importing the unverified bare \"fs\" specifier"
    - "normalizePathForCompare has NO Phase 4 caller by design — Phase 6 is its first — and must not be deleted to satisfy the linter"
    - "The probe report and failure message carry version strings, paths, booleans and integers only — never a process.env enumeration and never the Caido token"
  artifacts:
    - path: "packages/backend/src/runtime-probe.ts"
      provides: "Pure probe report + failure-message formatting, plus the D-04 realpath ladder"
      exports: ["PROBE_CAPABILITIES", "ProbeCapabilityResult", "ProbeReport", "buildProbeReport", "formatProbeFailure", "formatProbeReportFields", "RealpathRung", "normalizePathForCompare"]
      min_lines: 110
    - path: "packages/backend/src/runtime-probe.test.ts"
      provides: "Unit proof of gating classification, the 'unavailable' fallbacks, and the ladder fall-through"
      min_lines: 110
  key_links:
    - from: "packages/backend/src/runtime-probe.ts"
      to: "path"
      via: "the only import — no fs, no os"
      pattern: "^import path from \"path\";$"
    - from: "packages/backend/src/runtime-probe.test.ts"
      to: "normalizePathForCompare"
      via: "injected fake fs rungs proving the fall-through"
      pattern: "normalizePathForCompare"
---

<objective>
Create `packages/backend/src/runtime-probe.ts` — the RUN-05 capability report, the D-08 version
block, the user-facing failure message, and the **D-04** `normalizePathForCompare` ladder — plus its
sibling `runtime-probe.test.ts`.

Purpose: RUN-05's whole value is a message a Windows bug reporter can paste into an issue. That
message is what a verifier grades and what a user reads at 2 a.m. when MCP will not start, so it is
formatted by a pure function with a real unit test rather than assembled inline in the untested
3,004-line `index.ts`. **D-06**'s gate rule ("a primitive with a working fallback REPORTS; one
without GATES") is encoded here as *data* — a fixed capability list — so the classification is
assertable rather than implied by control flow.

**Deliberate choice recorded:** this module imports **nothing** from `./platform`. It deals in raw
strings and booleans, which keeps it independent (so it builds in the same wave as `platform.ts`) and
keeps a formatter free of a domain union it does not need. Plan 04-08 passes both the raw
`os.platform()` value and the normalised one.

**Deliberate choice recorded:** `normalizePathForCompare` lands here, not in `platform.ts`. D-04 only
requires it *outside* `platform.ts`; `04-VALIDATION.md` § *Per-Requirement Verification Map*'s
SC-10 realpath-ladder row pins its test to `runtime-probe.test.ts -t "ladder"` with injected fake fs
functions, so this is where it belongs.

Output: `packages/backend/src/runtime-probe.ts`, `packages/backend/src/runtime-probe.test.ts`.

Validation rows owned (04-VALIDATION.md): RUN-05 `-t "unavailable"`, `-t "gating"`;
SC-10 `-t "ladder"`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-platform-foundation/04-CONTEXT.md
@.planning/phases/04-platform-foundation/04-PATTERNS.md
@.planning/phases/03-ci-spike-prove-llrt-basics-on-windows/03-FINDINGS.md
@packages/backend/src/mcp-runtime.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write runtime-probe.ts — capability report, D-08 version block, failure message, D-04 ladder</name>

  <read_first>
    - `packages/backend/src/mcp-runtime.ts` lines 21-25 (`MCP_SELF_TEST_CHECKS` — the named-check-list
      constant that fixes render order and lets tests iterate it), lines 177-200
      (`parseMcpRuntimeContext` — never throw, always return a well-formed shape: exactly D-08's
      `"unavailable"` semantics), lines 251-276 (`buildSelfTestResult` — the
      `build*(input: {…}): Shape` formatter convention with derived fields computed inside).
    - `packages/backend/src/provider-launch.ts` lines 1-6 — the "why this module is pure" header.
    - `.planning/phases/04-platform-foundation/04-CONTEXT.md` § D-04, § D-06 (the gate table),
      § D-08.
    - `.planning/phases/04-platform-foundation/04-RESEARCH.md` § *LLRT Surface Risk* summary in the
      Summary section, § *Open Questions* 2 (the realpath rung is "the highest-value single field in
      the probe report"), § *MAX_PATH Budgeting* → *The cheap guard-rail worth adding*, and
      § *Security Domain* (V7 — the report must never carry `CAIDO_TOKEN` and must never enumerate
      `process.env`).
    - `.planning/phases/04-platform-foundation/04-PATTERNS.md`
      § `packages/backend/src/runtime-probe.ts` — including the warning to prefer explicit
      `toContain` assertions over `toMatchInlineSnapshot` because this repo has no `.snap` files.
  </read_first>

  <files>packages/backend/src/runtime-probe.ts</files>

  <action>
Create `packages/backend/src/runtime-probe.ts` with a 4-6 line header comment stating that the
formatter is pure so the message a bug reporter pastes is unit-tested, that `index.ts` collects the
facts and this module only shapes them, and that the module never touches `sdk.console`. Import
**only** `path`. Do not import `os` (D-02) and do not import `fs` — `normalizePathForCompare` takes
its fs rungs as injected parameters precisely because that surface is unproven under LLRT.

Exports:

1. `export const PROBE_CAPABILITIES = [...] as const;` — a fixed-order list encoding **D-06**'s gate
   table as data. Four entries, each `{ name, label, gating }`:
   - `{ name: "os.platform", label: "OS platform detection", gating: true }`
   - `{ name: "os.tmpdir", label: "Temp directory", gating: true }`
   - `{ name: "realpath", label: "Path canonicalisation", gating: false }`
   - `{ name: "windowsEnv", label: "Windows profile environment variables", gating: false }`
   Comment each `gating` value with D-06's reason: nothing downstream branches correctly without a
   recognised platform (and an unrecognised value silently takes the POSIX arm, which on Windows *is*
   the reported bug reintroduced one layer up); every runtime file depends on the temp dir; the
   realpath ladder already degrades to `path.resolve`; the Windows env vars are Phase 6's inputs with
   no Phase 4 consumer.

2. `export type ProbeCapabilityResult = { name: string; label: string; gating: boolean; ok: boolean; detail: string };`
   and `export type ProbeReport = { ok: boolean; capabilities: ProbeCapabilityResult[]; version: Record<string, string>; metrics: Record<string, string> };`

3. `export function buildProbeReport(input: { rawPlatform: string | undefined; normalizedPlatform: string | undefined; tmpdir: string | undefined; tempRoot: string | undefined; realpathRung: RealpathRung | undefined; realpathRungNote?: string; windowsEnvPresent: Record<string, boolean> | undefined; version: Record<string, string> }): ProbeReport`
   - Iterates `PROBE_CAPABILITIES` in order so the report always renders identically.
   - `os.platform`: `ok` when `normalizedPlatform` is a non-empty string. `detail` names the raw value
     observed and, when it did not normalise, says so explicitly (e.g. `os.platform() returned
     "freebsd", which Drift does not recognise`). Never print `undefined` as the word `undefined`
     without context — use `not available`.
   - `os.tmpdir`: `ok` when `tmpdir` is a non-empty string. `detail` includes the resolved
     `tempRoot`.
   - `realpath`: **never gates**. `ok` is `true` whenever a rung was reached at all (the ladder always
     terminates at `path.resolve`). `detail` names the rung reached, and when `input.realpathRungNote`
     is a non-empty string it is appended to `detail` verbatim after a `; ` separator. Comment:
     `realpath` / `realpathSync` do **not** exist anywhere in `caido/dependency-llrt@main`'s `fs`
     module, so under Caido this will read `path.resolve` — that is the expected answer, not a
     failure, and it is the single highest-value field in the report for Phase 6's design.
     The `realpathRungNote` hook exists for one specific honesty requirement: plan 04-08's runtime
     detector cannot probe rung 1 (`realpathSync.native`) at all, because doing so would need a
     module-scope import of the bare `"fs"` specifier, which is not source-verified for Caido's LLRT
     and would kill the whole plugin at load if it failed to resolve. The note carries
     **`not probed`**, which is a different claim from **`absent`**. A report whose purpose is to be
     pasted into a public bug report must never assert a measurement Drift did not make.
   - `windowsEnv`: only meaningful when `normalizedPlatform === "win32"`; otherwise `ok: true` with
     `detail` `not applicable on <platform>`. On win32, `ok` is `true` when `USERPROFILE` is present;
     `detail` lists **presence booleans by variable name**, e.g.
     `USERPROFILE=present APPDATA=present LOCALAPPDATA=missing`. **Never emit the values** — they
     contain the real account name and the report is pasted into public bug reports.
   - `ok` on the report is `true` when every capability with `gating: true` has `ok: true`.
   - `version` is passed straight through (the caller builds it — see D-08 below).
   - `metrics`: at minimum `tempRootLength` (character count of `tempRoot`, `"unavailable"` when
     absent) and `projectedWorstCasePathLength` (`tempRootLength` + `31` for the shortened
     `\drift-mcp-<20 hex>` directory component + `41` for the longest leaf filename
     `copilot-mcp-chat-<13 digits>-<4 chars>.json`). Comment the arithmetic and the 259-character
     `MAX_PATH` budget (260 including the terminating NUL), and that long-path opt-in requires both
     `LongPathsEnabled` and a `longPathAware` manifest, neither of which Drift controls because the
     process is Caido's. This converts an unknowable into two integers a bug reporter pastes.

4. `export function formatProbeReportFields(report: ProbeReport): Record<string, string>` — flattens
   the report into the flat `Record<string, string>` shape `getDiagnostics` already uses
   (`index.ts:2771`+). Keys prefixed `runtime` for capabilities (e.g. `runtimeOsPlatform`), the
   version block keys unprefixed, and the metric keys as-is.

5. `export function formatProbeFailure(report: ProbeReport, context?: { firstWriteError?: string; firstWriteAttempts?: number }): string`
   — the actionable message. It must name, in this order: (a) **which** primitive is missing and its
   `detail`; (b) **what Drift needed it for** (e.g. "Drift needs a temp directory to stage
   mcp-server.mjs and the token-bearing MCP wrapper"); (c) **the remedy** ("Update Caido, then reopen
   this panel and press Start MCP. If it still fails, open an issue at the Drift repository and paste
   the block below."); (d) the **version block** rendered as `key: value` lines; (e) the reported
   (non-gating) capabilities, so an LLRT gap is visible even when it did not block; (f) the metrics.
   When `context.firstWriteError` is supplied, the message leads with the write failure and includes
   `firstWriteAttempts`, because that is the actual failure a user hits (a read-only or
   Defender-locked temp dir passes any `stat()`-only check and then dies at the copy). Keep the whole
   thing plain text with newlines — no markdown fences, no ANSI.

6. `export type RealpathRung = "realpathSync.native" | "fs.realpath" | "path.resolve";`

7. `export async function normalizePathForCompare(input: { value: string; deps?: { realpathSyncNative?: (p: string) => string; realpath?: (p: string) => Promise<string>; resolve?: (p: string) => string } }): Promise<{ path: string; rung: RealpathRung }>`
   — the **D-04** ladder. Try `deps.realpathSyncNative` first inside `try`/`catch`; on absence or
   throw, try `deps.realpath`; on absence or throw, fall through to `deps.resolve ?? path.resolve`.
   Always returns; never throws. Report the rung actually reached.
   Add a comment block, immediately above the export, that states verbatim in substance: **this
   export has no Phase 4 caller by design — CONTEXT.md D-04 makes Phase 6 its first caller. Do not
   delete it to satisfy `--max-warnings 0`.** If `pnpm lint` flags the unused export, add an
   `eslint-disable` line whose comment cites D-04 — never a deletion (mandated by
   `04-VALIDATION.md` § *Lint note*).
   Also record why the ladder exists: Phase 3 measured `os.tmpdir()` returning the 8.3 short form
   `C:\Users\RUNNER~1\AppData\Local\Temp` while `USERPROFILE` returned the long form
   `C:\Users\runneradmin` — both valid, both on disk, **not string-comparable**
   (`03-FINDINGS.md` § `P0-TMP`).
  </action>

  <verify>
    <automated>pnpm typecheck && pnpm exec eslint packages/backend/src/runtime-probe.ts --max-warnings 0</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c '^import' packages/backend/src/runtime-probe.ts` returns `1` **and** `grep -c '^import path from "path";$' packages/backend/src/runtime-probe.ts` returns `1`. The pair stays exact-and-unchanged because the exact number **is** the property (D-02: this module reads exactly one built-in and it is `path`). Together they subsume the two narrower gates an earlier draft carried — `^import.*from "os"\|…"fs"\|…"node:fs"` returns `0`, and `^import.*from "./platform"` returns `0` — which are **deleted as duplicative**: if the file has exactly one import statement and that statement is byte-for-byte `import path from "path";`, no `os`, `fs` or `./platform` import can exist. Both anchors are `^import` because this task's action mandates header comments explaining why `os`, `fs` and `./platform` are deliberately absent, and a comment line begins with `/`.
    - `PROBE_CAPABILITIES` has exactly 4 entries; `os.platform` and `os.tmpdir` carry `gating: true`; `realpath` and `windowsEnv` carry `gating: false` (D-06).
    - `grep -c "D-04" packages/backend/src/runtime-probe.ts` is **at least** `1`, and reading the match confirms it is the do-not-delete comment immediately above `normalizePathForCompare`. An existence claim, so "at least" rather than "exactly" — the executor may legitimately cite D-04 more than once.
    - The module never reaches the environment: `grep -cE 'process\.env[.[]' packages/backend/src/runtime-probe.ts` returns `0` **and** `grep -c 'globalThis' packages/backend/src/runtime-probe.ts` returns `0`. Both are **re-anchored**, not comment-stripped. The old `grep -v '^[[:space:]]*[/*]' | grep -c "process\.env"` form removed only lines whose first non-space character was `/` or `*`, so the V7 security rationale this task mandates — which names `process.env` — survived as a trailing comment and failed the gate. The replacements require an actual **member access** (`process.env.` or `process.env[`) or the `globalThis` guard that is the only other route to the environment from a module whose sole import is `path`; prose naming `process.env` bare, or writing `Object.keys(process.env)`, matches neither.
    - `pnpm typecheck` exits 0; `pnpm exec eslint packages/backend/src/runtime-probe.ts --max-warnings 0` exits 0.
  </acceptance_criteria>

  <done>`runtime-probe.ts` compiles and lints clean, imports only `path`, encodes D-06's gate table as data, and ships `normalizePathForCompare` with its D-04 comment intact.</done>
</task>

<task type="auto">
  <name>Task 2: Write runtime-probe.test.ts — gating classification, "unavailable" fallbacks, ladder fall-through</name>

  <read_first>
    - `packages/backend/src/runtime-probe.ts` — the module just written.
    - `packages/backend/src/mcp-runtime.test.ts` lines 1-13 and 116-164 — the analog: one `describe`
      for the module, `it` per behaviour, `toMatchObject` for partial shape and `toEqual` for exact
      round-trips.
    - `.planning/phases/04-platform-foundation/04-VALIDATION.md` § *Per-Requirement Verification
      Map* — the two RUN-05 rows (`-t "unavailable"`, `-t "gating"`) and the SC-10 realpath-ladder
      row (`-t "ladder"`). Cite rows by requirement id and selector, never by line number: that file
      is edited during the phase and its line numbers rot.
  </read_first>

  <files>packages/backend/src/runtime-probe.test.ts</files>

  <action>
Create `packages/backend/src/runtime-probe.test.ts` with `import { describe, expect, it, vi } from "vitest"`
and a sibling relative import from `"./runtime-probe"`.

`describe("buildProbeReport")` — with at least these `it` titles:
- `it("marks os.platform and os.tmpdir as gating and realpath and windowsEnv as reported only")` —
  the `-t "gating"` selector. Assert by iterating the returned `capabilities` array: exactly two
  entries have `gating: true` and their `name`s are `os.platform` and `os.tmpdir`; exactly two have
  `gating: false`. Then assert the load-bearing consequence: a report where `realpath` is unavailable
  but both gating primitives are fine still has `ok: true`, and a report with an unrecognised
  platform has `ok: false`.
- `it("keeps ok true when only reported capabilities fail")`.
- `it("records the realpath rung reached in the capability detail")` — assert the detail string
  contains `path.resolve` when that rung was reached.
- `it("appends the realpath rung note verbatim and distinguishes not probed from absent")` — pass
  `realpathRungNote: "realpathSync.native: not probed — …"` and assert the `realpath` capability's
  `detail` contains the literal `not probed` and does **not** contain the word `absent`; then assert
  that omitting `realpathRungNote` leaves `detail` unchanged apart from the note. This is the
  guard against a later reader upgrading "we did not look" into "it is not there".
- `it("reports Windows env vars as presence booleans and never their values")` — pass
  `windowsEnvPresent: { USERPROFILE: true, APPDATA: true, LOCALAPPDATA: false }` and assert the
  detail contains `USERPROFILE=present` and `LOCALAPPDATA=missing` and does **not** contain any
  `C:\` substring.
- `it("computes tempRootLength and projectedWorstCasePathLength")` — a 36-character temp root yields
  `projectedWorstCasePathLength` of `108`.

`describe("formatProbeFailure")`:
- `it("renders every version-block field, using unavailable for each source that throws")` — the
  `-t "unavailable"` selector. Build a version block where `driftVersion` is a real string and
  `processVersion`, `versionsNode`, `versionsLlrt`, `osPlatform`, `osRelease` are all
  `"unavailable"`, and assert the rendered message contains each key and the literal `unavailable`
  for each failing source (use `toContain`, not a snapshot).
- `it("names the missing primitive, what Drift needed it for, and the remedy")` — assert the message
  contains the failing capability's `label`, a phrase describing the need, and the word
  `Start MCP`.
- `it("leads with the first-write failure when one is supplied")` — pass
  `{ firstWriteError: "EPERM: operation not permitted", firstWriteAttempts: 6 }` and assert both the
  code and the attempt count appear.
- `it("never contains a CAIDO_TOKEN value")` — build a report and assert the rendered string does not
  contain the literal `CAIDO_TOKEN` and does not contain a token-shaped fixture value passed nowhere
  into it. This is the T-04-04 regression tripwire.

`describe("normalizePathForCompare")`:
- `it("uses the realpathSync.native ladder rung when it is available")` — inject a `vi.fn()` returning
  a canonical path; assert `rung` is `"realpathSync.native"`.
- `it("falls back to fs.realpath when realpathSync.native is absent")` — omit rung 1; assert `rung`
  is `"fs.realpath"`.
- `it("falls through the ladder to path.resolve when both upper rungs are absent")` — the
  `-t "ladder"` selector. Inject **neither** upper rung; assert `rung` is `"path.resolve"` and the
  returned path is the `path.resolve` of the input. Add a comment in the test citing 04-RESEARCH:
  this is the rung Caido will actually reach, because `realpath` does not exist in
  `caido/dependency-llrt@main`'s `fs` module.
- `it("falls through to path.resolve when an upper rung throws")` — inject a rung-1 fake that throws;
  assert `rung` is `"fs.realpath"` when rung 2 is present, and `"path.resolve"` when it is not.
- `it("never throws")` — every injected rung throws; assert it still resolves.

Prefer explicit `toContain` / `toEqual` assertions over `toMatchInlineSnapshot`. Do not create
`.snap` files.
  </action>

  <verify>
    <automated>pnpm exec vitest run packages/backend/src/runtime-probe.test.ts</automated>
  </verify>

  <acceptance_criteria>
    - `pnpm exec vitest run packages/backend/src/runtime-probe.test.ts` exits 0 with 0 failures.
    - Each of these matches at least one passing test: `-t "gating"`, `-t "unavailable"`, `-t "ladder"`.
    - `-t "ladder"` proves the fall-through: with both upper rungs absent the returned `rung` is `"path.resolve"`.
    - A test asserts the rendered failure message contains no `CAIDO_TOKEN` literal and no `C:\`-prefixed environment value.
    - `grep -cE '\.toMatch(Inline)?Snapshot\(' packages/backend/src/runtime-probe.test.ts` returns `0`, and `ls packages/backend/src/__snapshots__ 2>/dev/null` finds nothing. **Re-anchored to the method-call form** (leading `.`, trailing `(`) rather than comment-stripped: this task's action and 04-PATTERNS.md both name the prefer-`toContain` rule, and the old `grep -v '^[[:space:]]*[/*]'` filter does not remove a trailing comment, so the rationale note could fail its own gate.
    - `grep -cE '(describe|it)\.only\(' packages/backend/src/runtime-probe.test.ts` returns `0` — anchored to the call form; vitest exits `0` with a `.only` present, so this gate carries signal the test run does not.
    - `pnpm exec eslint packages/backend/src/runtime-probe.test.ts --max-warnings 0` exits 0.
  </acceptance_criteria>

  <done>All three `-t` selectors from 04-VALIDATION.md resolve to passing tests, and the no-secrets property of the failure message is asserted rather than assumed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Caido backend → user-visible MCP status message / `sdk.console` / `getDiagnostics` | The probe report crosses here and is expected to be pasted into a **public** bug report |
| LLRT runtime → Drift's assumptions about `fs`/`os` availability | An absent primitive must surface as a legible report line, not a silent wrong answer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | Information Disclosure | `formatProbeFailure`, `buildProbeReport` | mitigate | Version strings, paths, booleans and integers only. The module never reaches the environment at all, gated by two prose-proof anchors rather than a comment filter: `grep -cE 'process\.env[.[]' … ` returns `0` (an actual member access, which prose naming `process.env` bare does not match) and `grep -c 'globalThis' … ` returns `0` (the only other route to the environment from a module whose sole import is `path`). Windows env vars render as **presence booleans by name**, never values. A dedicated test asserts the rendered message contains no `CAIDO_TOKEN` literal. Note `redactDebugText` (`index.ts:349`) covers the debug log — the probe report is a **different** surface and needs its own discipline |
| T-04-12 | Spoofing / Tampering | `normalizePathForCompare` | mitigate | The ladder never throws and always reports the rung reached, so a Phase 6 caller can never silently believe it got a canonical path when it got `path.resolve`. Under Caido it will always be `path.resolve` — surfacing that is the point |
| T-04-13 | Repudiation | probe report contents | mitigate | The D-08 version block (`process.version`, `process.versions.node`/`.llrt`, `os.platform()`+`os.release()`, Drift's manifest version) makes a bug report self-identifying. `process.versions.node === "0.0.0"` is a free LLRT-vs-Node discriminator |
| T-04-SC | Tampering | npm/pnpm installs | mitigate | Phase 4 installs zero packages; this module imports only `path` |
</threat_model>

<verification>
- `pnpm typecheck` exits 0.
- `pnpm lint` exits 0 at `--max-warnings 0` — and if the unused `normalizePathForCompare` export trips a rule, the fix is an `eslint-disable` citing D-04, **not** a deletion.
- `pnpm exec vitest run packages/backend/src/runtime-probe.test.ts` is green.
- `pnpm exec vitest run` — the pre-existing suite stays green.
</verification>

<success_criteria>
- ROADMAP SC-4's message half is satisfied in pure form: a fail-loud, actionable message including a best-effort version block, unit-tested field by field.
- ROADMAP SC-10's ladder is shipped and its fall-through to `path.resolve` is asserted with injected fakes.
- D-06's gate rule is encoded as assertable data, not as control flow.
- `normalizePathForCompare` exists, is uncalled, and is documented as deliberately uncalled.
</success_criteria>

<output>
Create `.planning/phases/04-platform-foundation/04-03-SUMMARY.md` when done.
</output>
