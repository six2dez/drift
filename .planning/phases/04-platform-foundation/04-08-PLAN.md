---
phase: 04-platform-foundation
plan: 08
type: execute
wave: 2
depends_on: [04-01, 04-02, 04-03]
files_modified:
  - packages/backend/src/index.ts
autonomous: true
requirements: [RUN-03, RUN-04, RUN-05, CMP-02]

must_haves:
  truths:
    - "D-02: os is touched in exactly ONE place — probeRuntime() at MCP start — inside try/catch, cached in a module-level `let host`, and every downstream site reads the cache and never os. A module-level const would kill the whole plugin during module evaluation if Caido's LLRT lacks os, and RUN-05 would never get to speak"
    - "D-05: the probe hard-fails on EVERY OS. There is no POSIX degrade-to-/tmp arm — one code path, one message"
    - "D-06: os.platform() and os.tmpdir() gate MCP start; the realpath rung and the Windows env-var presence are reported only, and every result lands in both the probe message and getDiagnostics"
    - "The backend never imports the bare \"fs\" specifier: only \"os\" (measured by Phase 3's P2-OS) and \"fs/promises\" (already imported by shipped 0.1.0 at index.ts:2) are added, because an unresolvable module-scope ESM specifier cannot be caught and would kill the whole plugin at load on every platform"
    - "The realpath probe reports rung 1 (realpathSync.native) as `not probed` rather than `absent` — Drift did not measure it, and a report meant to be pasted into a bug report must not assert a measurement it did not make"
    - "D-07: the probe WRAPS THE REAL FIRST WRITE — the existing mkdir(0o700) + writeFile(mcp-server.mjs) at index.ts:1717-1720. There is no separate canary file, and RUN-04's retry ladder and RUN-05's assertion are one mechanism"
    - "D-08: the failure message carries a best-effort version block, each field individually guarded and rendering 'unavailable' on failure, reusing the existing detectPluginVersion() at index.ts:455 rather than a new manifest reader"
    - "All three hardcoded /tmp sites resolve through the cached os.tmpdir(): mcpTempDir (index.ts:1716), the orphan sweep (index.ts:1681/1685) and getSessionDebugLogPath (index.ts:337)"
    - "The macOS orphan sweep still finds drift-mcp-* under /var/folders/… AND still sweeps the legacy /tmp directories the shipped 0.1.0 left behind (CMP-02)"
    - "The probe runs BEFORE the sweep and before mcpTempDir is assigned, because the sweep now needs host.tmpdir"
    - "Temp paths are always built with path.join, never template concatenation, because LLRT's os.tmpdir() can return a trailing separator that Node strips"
  artifacts:
    - path: "packages/backend/src/index.ts"
      provides: "The single os read, the cached host facts, the probe-wrapped first write, the tmpdir-rooted temp paths, and the D-06/D-08 diagnostics fields"
      contains: "probeRuntime"
  key_links:
    - from: "packages/backend/src/index.ts"
      to: "os"
      via: "the only os.* call site in the backend, inside probeRuntime's try/catch"
      pattern: "os\\.(platform|tmpdir|release)\\(\\)"
    - from: "packages/backend/src/index.ts startMcpServer"
      to: "./fs-retry withFsRetry"
      via: "wrapping the real mkdir + writeFile first write"
      pattern: "withFsRetry"
    - from: "packages/backend/src/index.ts"
      to: "./platform getTempRoot / getSweepRoots"
      via: "temp path construction and the orphan sweep root list"
      pattern: "getTempRoot|getSweepRoots"
---

<objective>
Wire the runtime probe into `packages/backend/src/index.ts` and migrate all three hardcoded `/tmp`
sites onto the cached `os.tmpdir()`.

Purpose: this is the plan RUN-03, RUN-04, RUN-05 and CMP-02 actually land in. It introduces the
**first `import … from "os"` in the backend source** — Phase 3's `P2-OS` proved the bare `"os"`
specifier resolves, but on a Node vehicle, with LLRT closed only by source analysis of
`caido/dependency-llrt@main`'s module resolver. That residual risk is exactly why **D-02** reads `os`
lazily inside the probe rather than at module scope, and why **D-05** makes the failure loud on every
OS instead of quietly degrading POSIX to `/tmp`.

Output: modified `packages/backend/src/index.ts`.

Validation rows owned (04-VALIDATION.md): RUN-03 static "no hardcoded `/tmp` remains"; RUN-05 static
"the probe runs before the sweep and before `mcpTempDir` assignment"; and the wiring behind the
CMP-02 and RUN-04 unit rows already proven in plans 04-01 and 04-02.

**Vehicle caveat carried from Phase 3 and required in the summary:** the Windows *platform* was
measured on a real `windows-latest` host; the Caido *runtime* is inferred. Cite
`.planning/phases/03-ci-spike-prove-llrt-basics-on-windows/03-FINDINGS.md`, never the CI artifacts,
which expire 2026-09-12.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-platform-foundation/04-CONTEXT.md
@.planning/phases/04-platform-foundation/04-PATTERNS.md
@.planning/phases/03-ci-spike-prove-llrt-basics-on-windows/03-FINDINGS.md
@.planning/phases/04-platform-foundation/04-01-SUMMARY.md
@.planning/phases/04-platform-foundation/04-02-SUMMARY.md
@.planning/phases/04-platform-foundation/04-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the runtime-probe section — the single os read, the host cache, the version block</name>

  <read_first>
    - `packages/backend/src/index.ts` lines 1-5 and 30-66 — the import block. Built-ins first with
      bare specifiers and a default `path` import; local modules in the fixed order
      `./provider-launch` → `./mcp-runtime` → `./command-resolution` → `./claude-print` →
      `./persistence`.
    - `packages/backend/src/index.ts` lines 68-80 — the `// ── Types (inline to avoid Zod which
      crashes QuickJS) ──` box header and the `Result<T>` / `ok` / `err` definitions.
    - `packages/backend/src/index.ts` lines 82-88 — the exported-constant error-message style
      (`NODE_EXECUTABLE_ERROR`).
    - `packages/backend/src/index.ts` lines 119-129 — the module-level singleton block, including
      `pluginVersion` at `:129`.
    - `packages/backend/src/index.ts` lines 455-476 — `detectPluginVersion()`. It **already exists**,
      already walks four candidate paths with per-candidate `try`/`catch`, and is already called at
      init (`:2957`). D-08 reads the cached module variable; do not write a new manifest reader.
    - `packages/backend/src/index.ts` lines 828-844 — `genUUID()`. Phase 3's `P3-UUID` does **not**
      license replacing this hex loop with `crypto.randomUUID` — it is the only one of the seven
      assertions with zero source-level LLRT confirmation in either direction. Keep it.
    - `packages/backend/src/index.ts` lines 890-894 and 2838-2844 — the mandatory
      `globalThis as typeof globalThis & { process?: … }` guard shape. **Never write a bare
      `process.version`.**
    - `packages/backend/src/platform.ts` and `packages/backend/src/runtime-probe.ts` — the modules
      from plans 04-01 and 04-03, for exact signatures.
    - `.planning/phases/04-platform-foundation/04-RESEARCH.md` § *Code Examples* 1 (the probe sketch)
      and § *Pitfall 8* (`process.execPath` is undefined under Caido — nothing to fix, just do not
      "restore" it).
  </read_first>

  <files>packages/backend/src/index.ts</files>

  <action>
Add imports and a new ASCII-box section. Do not change any existing behaviour in this task.

**Imports.** Add `import os from "os";` to the built-ins block at the top (bare specifier, default
import, matching `import path from "path"`). The `import` statement itself is fine — **D-02 forbids a
module-scope `os.*` *call*, not the import.** `"os"` is the phase's one accepted new specifier:
Phase 3's `P2-OS` measured it and 04-RESEARCH.md § *Environment Availability* records it as the
phase's single hard dependency, with D-05's hard-fail as the agreed handling.

Also add exactly **one** namespace import, used only as a guarded source for the realpath-rung
presence check: `import * as fsPromisesNs from "fs/promises";`. A namespace import is deliberate — it
tolerates a missing member as `undefined`, whereas a named import of an absent export is an ESM link
error, and `realpath` is verified **absent** from `caido/dependency-llrt@main`'s `fs` module. The
specifier `"fs/promises"` is safe because `index.ts:2` already imports from it in shipped 0.1.0 code,
so it is proven-resolvable under Caido's LLRT rather than assumed. `no-duplicate-imports` is not
enabled in `eslint.config.mjs`, so the second statement against the same specifier is fine.

**Do NOT add `import * as fsSync from "fs";`.** The bare `"fs"` specifier has **no** source-verified
resolution under Caido's LLRT — `P2-OS` covered `"os"` only, and 04-RESEARCH.md names `"os"` as this
phase's single hard dependency. A static ESM import cannot be wrapped in `try`/`catch`, and an
unresolvable module-scope specifier kills the **entire** plugin at load on every platform — exactly
the failure mode D-02 exists to prevent. Its only would-be consumer is the rung-1
(`realpathSync.native`) presence check, and 04-RESEARCH.md § *Environment Availability* already
records that rung as **verified absent from the fork's source**. Spending a plugin-death risk to
re-measure a field whose value is already known is a bad trade; rung 1 is therefore reported as
**`not probed`**, which the probe distinguishes from `absent` (see `detectRealpathRung` below).

Then add the three new local imports after `./persistence`: `./platform`, `./fs-retry`,
`./runtime-probe`.

**New section**, placed after the existing types box and before the first function that needs it, with
an ASCII box header in the file's established style:
`// ── Runtime probe ───────────────────────────────────────────────────`

Contents:

1. `type HostFacts = { platform: Platform; tmpdir: string };`
2. `let host: HostFacts | undefined;` — comment: **D-02**, the only cache; every downstream site
   reads this and never `os`. Explain the rejected alternative in one line: a module-level
   `const HOST = { platform: os.platform(), … }` evaluated at plugin load means that if Caido's LLRT
   lacks `os`, the throw happens during module evaluation and the **entire Drift plugin dies** —
   chat and settings included, frontend renders a dead panel — and RUN-05 never gets to speak.
3. `let lastProbeReport: ProbeReport | undefined;` and `let lastFirstWriteAttempts = 0;`
4. `function readVersionBlock(): Record<string, string>` — **D-08**. Every read individually guarded,
   `"unavailable"` on failure. Use the `globalThis` guard shape from `index.ts:2838-2844` for
   `process`; never a bare `process.` reference. Fields: `driftVersion` (from the existing
   `pluginVersion` module variable, `"unavailable"` when empty), `processVersion`
   (`process.version`), `versionsNode` (`process.versions.node`), `versionsLlrt`
   (`process.versions.llrt`), `osPlatform` (`os.platform()`), `osRelease` (`os.release()`). Add a
   comment that `process.versions.node === "0.0.0"` under LLRT is a free LLRT-vs-Node discriminator
   for bug reports.
5. `function detectRealpathRung(): RealpathRung` — a **presence** check only, no filesystem call.
   It probes rung 2 and rung 3 exclusively: returns `"fs.realpath"` when
   `typeof fsPromisesNs.realpath === "function"`, else `"path.resolve"`. Wrap the property read in
   `try`/`catch` and return `"path.resolve"` on any throw. It can **never** return
   `"realpathSync.native"`, because probing rung 1 would require a module-scope `"fs"` import whose
   resolution under Caido's LLRT is not source-verified — see the import note above.

   Add a companion `const REALPATH_NATIVE_PROBE_NOTE = "realpathSync.native: not probed — Drift does not import the bare \"fs\" specifier at module scope (D-02); 04-RESEARCH.md § Environment Availability records it as absent from caido/dependency-llrt@main";`
   and pass it to `buildProbeReport` as `realpathRungNote`. The distinction is load-bearing and must
   survive review: **`not probed` is not the same claim as `absent`.** Reporting rung 1 as absent
   without having probed it would be a measurement Drift did not make, in a report whose entire
   purpose is to be pasted into a bug report as evidence.

   Comment: under Caido the rung reached is expected to be `path.resolve`, which is the answer
   Phase 6 designs against, and it is the highest-value single field in the probe report.
6. `function readWindowsEnvPresence(): Record<string, boolean>` — using the `globalThis` process-env
   guard, returns presence booleans for `USERPROFILE`, `APPDATA`, `LOCALAPPDATA`. **Booleans only —
   never the values.** These are pasted into public bug reports.
7. `function probeRuntime(): Result<HostFacts>` — the structure agreed in **D-02**, written as
   sketched:
   - Declare `let rawPlatform: string | undefined; let tmpdir: string | undefined;` then a single
     `try { rawPlatform = os.platform(); tmpdir = os.tmpdir(); } catch { /* fall through — D-05 */ }`.
     This is the **only** `os.platform()` / `os.tmpdir()` call site in the file.
   - `const normalized = normalizePlatform(rawPlatform);`
   - `const facts = normalized !== undefined && typeof tmpdir === "string" && tmpdir !== "" ? { platform: normalized, tmpdir } : undefined;`
     Keep the explicit `typeof tmpdir !== "string" || tmpdir === ""` check — it is part of the agreed
     structure.
   - Build `lastProbeReport = buildProbeReport({ rawPlatform, normalizedPlatform: normalized, tmpdir, tempRoot: facts === undefined ? undefined : getTempRoot(facts), realpathRung: detectRealpathRung(), realpathRungNote: REALPATH_NATIVE_PROBE_NOTE, windowsEnvPresent: normalized === "win32" ? readWindowsEnvPresence() : undefined, version: readVersionBlock() })`
     — built on **both** the success and failure paths, so `getDiagnostics` always has it.
   - When `facts === undefined`, return `err(formatProbeFailure(lastProbeReport))`. **D-05**: this arm
     is taken on every OS; there is no `platform === "darwin"` fallback to `/tmp`. Add a comment
     stating why: if `os` is genuinely absent from Caido's LLRT the POSIX path cannot reach
     `os.tmpdir()` either, and silently falling back would ship a build whose Windows path is dead
     while POSIX quietly works — the exact failure mode this milestone exists to kill.
   - On success set `host = facts` and `return ok(facts)`.
8. `function genShortToken(): string` — 20 lowercase hex characters, using the same `Math.random`
   hex-loop idiom as `genUUID` (do **not** modify `genUUID` itself; SC-3's shortening changes the
   *consumer*). Comment the MAX_PATH arithmetic: the directory component shrinks from
   `drift-mcp-<36-char UUID>` (47 chars with separator) to `drift-mcp-<20 hex>` (31 chars), saving 16,
   which moves the worst-case constructed path from ~124 to ~108 characters against the 259-character
   budget. Comment the security floor explicitly: **do not shorten below 16 hex characters** — the
   `drift-mcp-` prefix must stay because the orphan sweep filters on
   `name.startsWith("drift-mcp-")` and changing it would silently orphan every pre-upgrade
   token-bearing directory, and the real entropy is bounded by `Math.random`'s PRNG state rather than
   the string length, which is precisely why shortening costs nothing and why `genUUID` is not being
   replaced by `crypto.randomUUID` (blocked by Phase 3's `P3-UUID`).
  </action>

  <verify>
    <automated>pnpm typecheck && pnpm exec eslint packages/backend/src/index.ts --max-warnings 0 && pnpm exec vitest run</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c '^import os from "os";' packages/backend/src/index.ts` returns `1` — the first `os` import in backend **source** (the three pre-existing ones are in test files).
    - `grep -c 'from "fs"' packages/backend/src/index.ts` returns `0` and `grep -c 'from "node:fs"' packages/backend/src/index.ts` returns `0` — the bare `"fs"` specifier is **not** source-verified for Caido's LLRT and a module-scope ESM import of an unresolvable specifier kills the whole plugin at load. (`from "fs/promises"` does not match this pattern and is expected twice: the pre-existing named import at `:2` and the new namespace import.)
    - `grep -c 'import \* as fsPromisesNs from "fs/promises";' packages/backend/src/index.ts` returns `1` — the single guarded namespace source for the rung-2 presence check.
    - `detectRealpathRung`'s return type is `RealpathRung` and its body contains no `realpathSync` reference — `grep -c "realpathSync" packages/backend/src/index.ts` returns `0`. Rung 1 is reported as `not probed`, not as absent: `grep -c "not probed" packages/backend/src/index.ts` is at least `1`.
    - `grep -c 'os\.\(platform\|tmpdir\|release\)()' packages/backend/src/index.ts` returns exactly `4`: `os.platform()` and `os.tmpdir()` once each inside `probeRuntime`'s `try`, plus `os.platform()` and `os.release()` once each inside `readVersionBlock`'s individually-guarded reads. No other `os.` call site exists.
    - Every `os.` call site is lexically inside a `try` block — verified by reading `probeRuntime` and `readVersionBlock`; no `os.` call appears at module scope.
    - `probeRuntime` returns the inline `Result<HostFacts>` and contains no `platform === "darwin"` or `"/tmp"` fallback arm (D-05).
    - `genShortToken()` returns a 20-character lowercase hex string and `genUUID` is byte-unchanged — `git diff packages/backend/src/index.ts | grep -c "^-.*genUUID"` returns `0`.
    - `grep -c "crypto.randomUUID" packages/backend/src/index.ts` returns `0`.
    - `readVersionBlock` contains no bare `process.` reference — every access goes through a `globalThis as typeof globalThis & { process?: … }` guard.
    - `readWindowsEnvPresence` returns `Record<string, boolean>`; `grep -n "USERPROFILE" packages/backend/src/index.ts` shows it used only in a presence check, never assigned into a string field.
    - `pnpm typecheck`, `pnpm exec eslint packages/backend/src/index.ts --max-warnings 0` and `pnpm exec vitest run` all exit 0.
  </acceptance_criteria>

  <done>`index.ts` has a single guarded `os` read cached in `let host`, a D-08 version block, a realpath-rung detector, a Windows env presence check, and a 20-hex-char token generator — with no behaviour change yet.</done>
</task>

<task type="auto">
  <name>Task 2: Reorder startMcpServer, wrap the real first write, and migrate all three /tmp sites</name>

  <read_first>
    - `packages/backend/src/index.ts` lines 1694-1728 — the `startMcpServer` prologue being rewritten,
      including the existing comment block at `:1712-1715` explaining why the asset is copied out of
      `assetsPath` (the Caido plugin path contains "Application Support", whose spaces break Claude
      Code's `--mcp-config` parsing). **That reason still holds** — update the comment's `/tmp` claim,
      do not delete the rationale.
    - `packages/backend/src/index.ts` lines 1673-1692 — `sweepOrphanedMcpTempDirs`, including its
      `readdir("/tmp")`, the `startsWith("drift-mcp-")` filter, the `dir !== mcpTempDir` guard, and
      the `sdk.console.error` catch arm.
    - `packages/backend/src/index.ts` lines 335-338 — `getSessionDebugLogPath`, the third `/tmp` site.
      It already returns `string | undefined`.
    - `packages/backend/src/index.ts` lines 1724-1747 — the four existing `Result` failure arms
      (`const message = "…"` → `cleanupMcpRuntime(sdk, "error", message)` → `return err(message)`).
      Copy that arm shape exactly.
    - `packages/backend/src/index.ts` lines 262-270 — `writeTemp`, which already does the
      `mkdir(0o700)` + `writeFile(0o600)` pair.
    - `packages/backend/src/fs-retry.ts` — `withFsRetry`'s exact signature and outcome shape.
    - `.planning/phases/04-platform-foundation/04-RESEARCH.md` § *Pitfall 1* (trailing separator →
      always `path.join`) and § *Pitfall 6* (probe before sweep).
  </read_first>

  <files>packages/backend/src/index.ts</files>

  <action>
**Reorder `startMcpServer`'s prologue.** After the existing token check and the
`fileExists(mcpScript)` asset check, and **before** anything else:

1. `const probe = probeRuntime();` — on `probe.kind === "Error"`, call
   `await cleanupMcpRuntime(sdk, "error", probe.error)` and `return err(probe.error)`, matching the
   arm shape at `:1724-1728`.
2. Move `await sweepOrphanedMcpTempDirs(sdk, probe.value);` to **after** the probe (it currently runs
   at `:1710`). Add a comment: ORDER MATTERS — the sweep needs `host.tmpdir`. Note in the comment
   that the sweep's existing `dir !== mcpTempDir` guard was comparing against an `undefined`
   `mcpTempDir` at this point today and remains harmless because every candidate is a string.
3. Replace `mcpTempDir = \`/tmp/drift-mcp-${genUUID()}\`` with
   `mcpTempDir = path.join(getTempRoot(probe.value), \`drift-mcp-${genShortToken()}\`)`. **Always
   `path.join`, never template concatenation** — LLRT's `os.tmpdir()` is `std::env::temp_dir()`
   (`GetTempPath2` on Windows, `$TMPDIR` on macOS) and can return a path ending in a separator, while
   Node has stripped trailing separators since v2.0.0. `path.join` normalises it; a template literal
   produces `C:\…\Temp\/drift-mcp-x`. Update the surrounding comment block at `:1712-1715` so the
   `/tmp` claim becomes the resolved temp root, keeping the "Application Support has spaces" rationale
   and the `0o700` rationale intact.
4. **D-07** — wrap the existing `mkdir` + `writeFile` pair in `withFsRetry`:
   `const written = await withFsRetry(async () => { await mkdir(mcpTempDir!, { recursive: true, mode: 0o700 }); await writeFile(mcpScriptLocal, await readFile(mcpScript, "utf-8")); }, { onRetry: (info) => sdk.console.error(\`[drift] Transient filesystem error staging the MCP server (attempt ${String(info.attempt)}, code ${info.code}); retrying in ${String(info.delayMs)}ms\`) });`
   Then `lastFirstWriteAttempts = written.attempts;` and on `written.kind === "Error"` build
   `const message = formatProbeFailure(lastProbeReport!, { firstWriteError: written.error, firstWriteAttempts: written.attempts });`, call `cleanupMcpRuntime(sdk, "error", message)` and
   `return err(message)`. Add a comment: **there is no separate canary file** — a canary can itself
   trip the anti-virus write-then-access race it exists to detect, a false negative on precisely the
   machines that matter, and this write is the failure users actually hit (a read-only or
   Defender-locked temp dir passes a `stat()`-only check and then dies at the copy).
   Leave both `mode:` options **unconditional** — LLRT's `set_mode` is a total no-op returning `Ok(())`
   on non-unix and Node silently ignores `mode` on Windows, so a `platform !== "win32"` guard would
   double the branch count for zero behaviour change and risk a POSIX regression.

**Rewrite `sweepOrphanedMcpTempDirs`** to `async function sweepOrphanedMcpTempDirs(sdk: BackendSDK, hostFacts: HostFacts): Promise<void>`:
- `const roots = getSweepRoots(hostFacts);`
- For each root, run the **existing** `readdir(root)` → `filter(name => name.startsWith("drift-mcp-"))`
  → `map(name => path.join(root, name))` → `filter(dir => dir !== mcpTempDir)` →
  `rm(dir, { recursive: true, force: true }).catch(() => undefined)` chain unchanged.
- Put each root's `readdir` in its **own** `try`/`catch` so a missing or unreadable legacy root cannot
  abort the sweep of the real one; keep the existing
  `sdk.console.error(\`[drift] Failed to sweep orphaned MCP temp dirs: ${String(error)}\`)` message
  form and include the root in it.
- Keep the existing comment explaining that these dirs hold token-bearing wrapper scripts, and add
  the legacy-arm rationale: the shipped 0.1.0 wrote `drift-mcp-*` into `/tmp`, and on macOS
  `os.tmpdir()` is `/var/folders/…`, so without the second root those token-bearing directories would
  never be swept again after upgrade.

**Rewrite `getSessionDebugLogPath`**:
`if (!currentSettings.debugLogging) return undefined;` then `if (host === undefined) return undefined;`
then `return path.join(getTempRoot(host), \`drift-session-${sessionId}.log\`);`.
Comment the `host === undefined` arm: `host` is only populated at MCP start (D-02) but a chat turn can
run with MCP never started; `debugLogging` is opt-in, so silently skipping the log is acceptable and
strictly better than reintroducing a hardcoded `/tmp`.

**Do not** touch `renderExportExecScript`, `writeMcpWrapper`, `writeLaunchScript`, `shellQuote`, or any
`chmod` call — those are held byte-stable by the Phase 5 scope fence. **Do not** change the
`mcp-activity-<sessionId>`, `mcp-approvals-<sessionId>`, `copilot-mcp-<chatId>` or `mcp-<chatId>`
filename derivations — only the directory component changes, because Phase 2's SEC-02 will validate
`sessionId`/`chatId` against those exact derivations.
  </action>

  <verify>
    <automated>pnpm typecheck && pnpm exec eslint packages/backend/src/index.ts --max-warnings 0 && pnpm exec vitest run</automated>
  </verify>

  <acceptance_criteria>
    - `grep -n '"/tmp"' packages/backend/src/index.ts` returns **nothing** — the only remaining `/tmp` literal in the repo's backend source is `getSweepRoots`'s deliberate legacy arm in `platform.ts`.
    - `grep -n '`/tmp/' packages/backend/src/index.ts` returns nothing and `grep -c "drift-mcp-\${genUUID" packages/backend/src/index.ts` returns `0`.
    - In `startMcpServer`, the line number of `probeRuntime()` is **less than** the line number of `sweepOrphanedMcpTempDirs(` which is **less than** the line number of `mcpTempDir =` — verifiable with `grep -n "probeRuntime()\|await sweepOrphanedMcpTempDirs(\|mcpTempDir = path.join" packages/backend/src/index.ts` and checking the ordering of the three line numbers.
    - `mcpTempDir` is built with `path.join(getTempRoot(...), ...)`; `grep -c 'mcpTempDir = `' packages/backend/src/index.ts` returns `0` (no template-literal construction).
    - The `mkdir` + `writeFile` pair at the former `:1717-1720` is inside a `withFsRetry` callback — `grep -c "withFsRetry" packages/backend/src/index.ts` is at least `1`.
    - `grep -c "mode: 0o700" packages/backend/src/index.ts` and `grep -c "mode: 0o600" packages/backend/src/index.ts` are unchanged from their pre-plan values, and no `platform !== "win32"` guard wraps either.
    - `sweepOrphanedMcpTempDirs` takes a second `HostFacts` parameter and iterates `getSweepRoots`.
    - `getSessionDebugLogPath` returns `undefined` when `host` is `undefined`.
    - `git diff packages/backend/src/index.ts` shows **no** change to `renderExportExecScript`, `writeMcpWrapper`, `writeLaunchScript`, `shellQuote`, or any `chmod` call site.
    - `pnpm exec vitest run` is green — 134 pre-existing tests plus everything added in wave 1; `provider-launch.test.ts`'s exact `toEqual([...])` arrays have not moved.
  </acceptance_criteria>

  <done>All three hardcoded `/tmp` sites resolve through the cached `os.tmpdir()`, the probe runs first, the real first write is wrapped by the retry ladder, and the macOS legacy `/tmp` sweep arm ships.</done>
</task>

<task type="auto">
  <name>Task 3: Surface the probe report, version block and retry count in getDiagnostics and the MCP status message</name>

  <read_first>
    - `packages/backend/src/index.ts` lines 2750-2751, 2771-2800 and 2821-2832 — `getDiagnostics`, its
      flat `Record<string, string>` with `?? "not set (MCP not started)"` / `|| "none"` defaults, and
      the existing user-triggered write probe at the tail. Note that tail probe is fine where it is
      (diagnostics is user-triggered, not on the MCP-start hot path) — it is the shape D-07 rejects
      only as a *standalone canary on the start path*.
    - `packages/backend/src/index.ts` lines 349 (`redactDebugText`) — it covers the debug log. The
      probe report is a **different** surface and needs its own no-secrets discipline.
    - `packages/backend/src/runtime-probe.ts` — `formatProbeReportFields`'s exact output shape.
    - `.planning/phases/04-platform-foundation/04-CONTEXT.md` § D-06 (every result, gating or not,
      lands in the probe message **and** `getDiagnostics`) and § D-08 (the block surfaces in the MCP
      status message, `sdk.console`, and `getDiagnostics`).
  </read_first>

  <files>packages/backend/src/index.ts</files>

  <action>
Merge the probe's reported fields into `getDiagnostics`'s existing flat `info` record, immediately
after the existing `mcpTempDir` / `mcpContextFile` group so related fields read together:

- Spread `formatProbeReportFields(lastProbeReport)` when `lastProbeReport !== undefined`; when it is
  `undefined` (MCP never started, so the probe never ran) add a single
  `runtimeProbe: "not run (MCP not started)"` entry following the file's existing
  `"not set (MCP not started)"` convention.
- Add `mcpFirstWriteAttempts: String(lastFirstWriteAttempts)`. This is the agreed mitigation for the
  one RUN-04 behaviour that is not inducible in CI on any runner: a real Defender lock. With the
  attempt count in the support bundle, the reporter's next bug report answers whether the 1,500 ms
  ladder was long enough, and `FS_RETRY_DELAYS_MS` can then be widened on evidence.

Emit the probe report once through `sdk.console` at MCP start — on the success path log a single
compact line summarising the gating results plus the realpath rung; on the failure path the message
already returned by `probeRuntime` is what reaches the user. Use the `[drift]` prefix convention.

Route the D-08 version block into the user-visible MCP status message on the failure path only:
`formatProbeFailure` already composes it, and `startMcpServer`'s existing failure arms already pass
their message through `cleanupMcpRuntime(sdk, "error", message)`, so no new plumbing is needed —
confirm by reading that path rather than adding a parallel channel.

**Security discipline for this task, non-negotiable:** every field added here is a version string, a
path Drift itself constructed or resolved, a boolean, or an integer. Do **not** add any field derived
from enumerating `process.env`, and do not add the Windows env-var *values* — only the presence
booleans that `buildProbeReport` already renders. `mcp-server.mjs` reads named env keys rather than
enumerating, and Phase 3's D-10 no-secret-material CI gate enforces the same discipline on any
workflow or script this phase touches.
  </action>

  <verify>
    <automated>pnpm typecheck && pnpm exec eslint packages/backend/src/index.ts --max-warnings 0 && pnpm exec vitest run</automated>
  </verify>

  <acceptance_criteria>
    - `getDiagnostics`'s returned record contains a `mcpFirstWriteAttempts` key and the flattened probe fields (at minimum keys covering the four `PROBE_CAPABILITIES`, the six D-08 version fields, and the two MAX_PATH metrics).
    - When MCP has never been started, `getDiagnostics` returns `runtimeProbe: "not run (MCP not started)"` rather than throwing or omitting the group.
    - `grep -n "Object.keys(process.env)\|Object.entries(process.env)\|for (const .* in process.env" packages/backend/src/index.ts` returns nothing — no environment enumeration was introduced.
    - No Windows env-var **value** reaches diagnostics: `grep -n "env?.USERPROFILE\|env?.APPDATA\|env?.LOCALAPPDATA" packages/backend/src/index.ts` shows those reads only inside `readWindowsEnvPresence`, and that function's return type is `Record<string, boolean>`.
    - `sdk.console` emits exactly one probe summary line per `startMcpServer` success — `grep -c "\[drift\] runtime probe" packages/backend/src/index.ts` returns `1` (or the equivalent single-emission form chosen).
    - `pnpm typecheck`, `pnpm exec eslint packages/backend/src/index.ts --max-warnings 0` and `pnpm exec vitest run` all exit 0.
  </acceptance_criteria>

  <done>Every probe result — gating and reported — plus the D-08 version block and the retry-attempt count reach `getDiagnostics`, with no environment enumeration and no env-var values.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Caido backend (LLRT) → local filesystem temp root | The created directory holds `mcp-server.mjs`, the MCP wrapper and configs that carry `CAIDO_TOKEN`; other local users are the adversary |
| Upgraded install → filesystem state left by the shipped 0.1.0 | Token-bearing `/tmp/drift-mcp-*` directories from before the `os.tmpdir()` switch |
| Backend → user-visible MCP status message, `sdk.console`, `getDiagnostics` | The probe report and version block cross here and are expected to be pasted into a **public** bug report |
| Windows anti-virus / indexer filter driver → the write→spawn path | Transient `EPERM`/`EBUSY`/`UNKNOWN` failures arrive here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02 | Information Disclosure | `sweepOrphanedMcpTempDirs` | mitigate | Iterate `getSweepRoots(host)` so the sweep covers both the new `os.tmpdir()` root **and**, on non-`win32`, the legacy `/tmp` where the shipped 0.1.0 left token-bearing directories. Keep the `drift-mcp-` prefix in `genShortToken`'s output so pre-upgrade directories still match the filter |
| T-04-01 | Information Disclosure / Elevation | `genShortToken` + `mkdir(mode: 0o700)` | mitigate | 20 hex characters (≈80 bits nominal, PRNG-bounded in practice) with a documented floor of 16, and `mkdir` with `mode: 0o700` closing the read window on POSIX. Do **not** shorten further |
| T-04-03 | Information Disclosure | `mode: 0o700` / `0o600` on Windows | accept | Windows ignores POSIX modes; the accepted control is the per-user `%TEMP%` ACL baseline. Explicit `icacls` hardening is **HRD-01, deferred to v2** in STATE.md § *Deferred Items*. LLRT's `set_mode` is a verified no-op on non-unix, so the modes stay unconditional |
| T-04-04 | Information Disclosure | probe report, version block, `getDiagnostics` | mitigate | Version strings, Drift-constructed paths, booleans and integers only. No `process.env` enumeration; Windows env vars as presence booleans, never values. `redactDebugText` (`index.ts:349`) covers the debug log — this is a separate surface with its own discipline |
| T-04-23 | Denial of Service | transient FS failure at MCP start | mitigate | `withFsRetry` wraps the real first write with the bounded 1,500 ms ladder; failure produces the actionable RUN-05 message plus the attempt count rather than a cryptic error |
| T-04-24 | Spoofing | unrecognised `os.platform()` silently taking the POSIX arm | mitigate | `normalizePlatform` gates (D-06). An unrecognised value is a hard failure, because falling through to POSIX on Windows **is** the reported bug reintroduced one layer up |
| T-04-07 | Tampering | `sessionId`/`chatId` interpolated into filesystem paths | transfer | **Phase 2's SEC-02.** Phase 4's obligation is not to move the goalposts: only the directory component of the temp path changes; every filename derivation stays byte-stable so SEC-02 validates against the same scheme |
| T-04-08 | Denial of Service (self) | stale `gemini`/`codex` MCP registration pointing at the old `/tmp` wrapper path | accept | `registerMcpWithCli` already does a best-effort `mcp remove drift` before every `mcp add` (`index.ts:1587`) and registration re-runs on every `startMcpServer`, so a stale entry is overwritten on the next start. Verify that pre-clean survives this phase; add no migration step |
| T-04-SC | Tampering | npm/pnpm installs | mitigate | Phase 4 installs zero packages (04-RESEARCH.md § *Package Legitimacy Audit*: not applicable). `graceful-fs` and `write-file-atomic` are design references only and must not be installed |
</threat_model>

<verification>
- `pnpm typecheck` exits 0.
- `pnpm lint` exits 0 at `--max-warnings 0`.
- `pnpm exec vitest run` — full suite green, including all wave-1 additions and the untouched `provider-launch.test.ts` exact arrays (CMP-01 tripwire).
- `pnpm build` succeeds — this phase adds the first `import … from "os"` to the backend bundle and a bundler resolution failure would be invisible to vitest.
- `grep -n '"/tmp"' packages/backend/src/index.ts` returns nothing.
- `git diff --stat packages/backend/src/command-resolution.ts` shows no change (D-03: untouched in Phase 4).
</verification>

<success_criteria>
- ROADMAP SC-2 is satisfied: all three hardcoded `/tmp` sites resolve through `os.tmpdir()`, and the macOS orphan sweep still finds Drift's dirs under `/var/folders/…` plus the legacy `/tmp` (CMP-02).
- ROADMAP SC-3's remaining halves are satisfied: the write→spawn hot path retries on `EPERM`/`EBUSY`/`UNKNOWN` with bounded backoff, and the temp-path scheme is shortened for MAX_PATH safety. (Its "copies once at MCP start, not per turn" clause was already true at `index.ts:1719`.)
- ROADMAP SC-4 is satisfied: a runtime-capability probe fails loud at MCP start with an actionable message including a best-effort version block. Honest note for a strict verifier: **the Caido SDK exposes no Caido version anywhere** (`sdk.meta` offers only `db()`, `path()`, `assetsPath()`), so D-08's block is the closest available substitute, not an omission.
- Zero POSIX regressions: the existing suite is green and the Phase 5 scope-fence functions are byte-unchanged.
</success_criteria>

<output>
Create `.planning/phases/04-platform-foundation/04-08-SUMMARY.md` when done. Include the Phase 3 vehicle caveat verbatim: the Windows platform was measured, the Caido runtime is inferred.
</output>
