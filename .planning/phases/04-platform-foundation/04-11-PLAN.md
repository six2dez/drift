---
phase: 04-platform-foundation
plan: 11
type: execute
wave: 5
depends_on: [04-06, 04-10]
files_modified:
  - .planning/phases/04-platform-foundation/04-VALIDATION.md
autonomous: false
requirements: [RUN-03, RUN-04, RUN-05, CMP-02, PERF-02, PERF-03, PERF-04]

must_haves:
  truths:
    - "No hardcoded /tmp remains in packages/backend/src outside getSweepRoots' deliberate legacy arm in platform.ts (RUN-03 static row)"
    - "PERF-04's accumulator count set holds simultaneously: `out +=`/`stderr +=` at 0, createBoundedBuffer( at 6, appendBounded( at 7, drainCompleteLines( at 1, newlineIndex at 0. index.ts has SEVEN accumulator sites, not four and not six — the seventh is resolveCommand's `out` at :862, which the narrower `stdout +=` pattern is structurally blind to, which is exactly why this gate uses the broader `out +=` form"
    - "The probe provably runs before the orphan sweep and before mcpTempDir is assigned (RUN-05 static row)"
    - "SC-9's static cross-check is recorded honestly: buildSpawnEnv legitimately has ZERO call sites in Phase 4 because no spawn site in index.ts passes an env option today — env reaches children through the bash export wrapper that Phase 5 owns. A zero count here is the correct answer, not a failure"
    - "D-04: normalizePathForCompare is still exported and still uncalled, with its lint exemption citing D-04 rather than having been deleted"
    - "Three deviations reach 04-VALIDATION.md's Phase Gate Results, not two: the PERF-02 Buffer.alloc row graded against activity-tail.ts, SC-9's zero call-site count, and the SC-4 substitution (the Caido SDK exposes no Caido version in sdk.meta, so D-08's version block is the closest available substitute). /gsd-verify-work grades SC-4 against this artifact, so an unrecorded substitution reads as a straight requirement failure"
    - "pnpm build passes — this phase adds the first import from os to the backend bundle, and a bundler resolution failure would be invisible to vitest"
    - "The RUN-05 failure message is legible and actionable to a real bug reporter — the one criterion that is not machine-assertable and therefore needs a human read"
  artifacts:
    - path: ".planning/phases/04-platform-foundation/04-VALIDATION.md"
      provides: "The completed per-requirement verification map with real plan/task ids, statuses, and the sign-off block"
      contains: "nyquist_compliant: true"
  key_links:
    - from: "04-VALIDATION.md Plan / Task columns"
      to: "the eleven Phase 4 plans"
      via: "one row per requirement behaviour, filled with the plan that proved it"
      pattern: "04-0[1-9]|04-1[01]"
---

<objective>
Close Phase 4 with the static gates that no single plan owns, then a human read of the one criterion
that is not machine-assertable.

Purpose: four of Phase 4's validation rows are **phase-wide static assertions** rather than unit
tests — "no hardcoded `/tmp` remains", "the probe runs before the sweep", SC-9's `buildSpawnEnv`
call-site cross-check, and PERF-04's "every accumulator site in `index.ts` is bounded" count set.
The last of those is genuinely phase-wide rather than plan-wide: the seven accumulator sites are
split across plan 04-09 (six) and plan 04-10 (the seventh, `resolveCommand`'s `out`), so no single
plan can assert the complete claim. Running them inside any earlier plan would grade an incomplete
tree. Two more
items are graded **N** (not provable in CI on any runner) and one of those, the probe message's
legibility, is exactly the kind of thing that ships broken when nobody reads it out loud.

The SC-9 cross-check needs framing up front so it does not read as a failure: the pattern mapper
verified that `grep -n "spawn(" packages/backend/src/index.ts` returns `:853`, `:1191`, `:1521`,
`:2188` and **none of them passes an `env` option today**. Env reaches children through the bash
`export` wrapper, which Phase 5 rewrites. So `buildSpawnEnv` legitimately has zero consumers in
Phase 4 — the pure function plus its unit test is the whole Phase 4 deliverable, and the static half
of the check is confirming that no spawn site supplies `env` **without** going through it.

Output: updated `.planning/phases/04-platform-foundation/04-VALIDATION.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-platform-foundation/04-VALIDATION.md
@.planning/phases/04-platform-foundation/04-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the phase-wide static gates and record the results in 04-VALIDATION.md</name>

  <read_first>
    - `.planning/phases/04-platform-foundation/04-VALIDATION.md` in full — the 40-row
      per-requirement map whose `Plan / Task` and `Status` columns this task fills, the *Wave 0
      Requirements* checklist, the *Lint note*, and the *Validation Sign-Off* block.
    - `.planning/phases/04-platform-foundation/04-PATTERNS.md` § *No Analog Found* — the recorded
      `buildSpawnEnv` zero-consumer fact and the `normalizePathForCompare` do-not-delete note.
    - `packages/backend/src/platform.ts`, `packages/backend/src/runtime-probe.ts` and
      `packages/backend/src/index.ts` — the files the static assertions run against.
    - The eleven summary files `.planning/phases/04-platform-foundation/04-0*-SUMMARY.md` and
      `04-1*-SUMMARY.md` — for the plan ids and any recorded deviations to carry into the map.
  </read_first>

  <files>.planning/phases/04-platform-foundation/04-VALIDATION.md</files>

  <action>
Run these gates and record each result. Where a gate fails, fix the source (in the owning module) and
re-run — do not weaken the gate.

**Gate 1 — RUN-03, no hardcoded `/tmp`.** Run
`grep -rn '"/tmp"' packages/backend/src --include=*.ts | grep -v '\.test\.ts'`. The **only** permitted
match is inside `getSweepRoots` in `packages/backend/src/platform.ts` — the deliberate legacy arm.
Also run `grep -rn '`/tmp/' packages/backend/src --include=*.ts | grep -v '\.test\.ts'` and require
zero matches. Note the deliberate filter of `.test.ts` files: `command-resolution.test.ts`,
`mcp-server.context.test.ts` and `mcp-server.transport.test.ts` legitimately use `os.tmpdir()` for
their own fixtures and are not production paths.

**Gate 2 — RUN-05, probe ordering.** Run
`grep -n "probeRuntime()\|await sweepOrphanedMcpTempDirs(\|mcpTempDir = path.join" packages/backend/src/index.ts`
and assert the three line numbers are in **strictly ascending** order. Record the three numbers in
the map row so a later reader can re-check without re-deriving the query.

**Gate 3 — SC-9 cross-check.** Run `grep -n "spawn(" packages/backend/src/index.ts` and, for each
match, read the options object. Assert that **either** the site passes no `env` option **or** its
`env` value is produced by `buildSpawnEnv`. Record the count of `env`-supplying spawn sites — the
expected Phase 4 answer is **0**, and record it as such with a one-line explanation: env reaches
children through the bash `export` wrapper Phase 5 owns, so this is a forward-looking guarantee and a
zero count is correct rather than a gap. Also assert `buildSpawnEnv` is exported and unit-tested
(`pnpm exec vitest run packages/backend/src/platform.test.ts -t "buildSpawnEnv"` is green).

**Gate 4 — D-04, `normalizePathForCompare` survives.** Assert
`grep -c "export async function normalizePathForCompare" packages/backend/src/runtime-probe.ts`
returns `1`, that it has zero non-test callers
(`grep -rn "normalizePathForCompare" packages/backend/src --include=*.ts | grep -v '\.test\.ts' | grep -v "runtime-probe.ts"`
is empty), and that if an `eslint-disable` was needed its comment cites **D-04**. Deleting the export
to quiet the linter is a gate failure.

**Gate 5 — the full static and test suite.** Run, in order:
`pnpm typecheck`, `pnpm lint`, `pnpm exec vitest run`, `pnpm build`. All four must exit 0.
`pnpm build` is called out specifically: this phase adds the first `import … from "os"` to the
backend bundle, and a bundler resolution failure would be invisible to vitest.

**Gate 6 — CMP-01 tripwire.** Assert `git diff` shows no change to
`packages/backend/src/provider-launch.ts` or `packages/backend/src/provider-launch.test.ts` across
the whole phase, and no change to `packages/backend/src/command-resolution.ts` (D-03), and no change
to `renderExportExecScript`, `writeMcpWrapper`, `writeLaunchScript`, `shellQuote` or any `chmod` call
site in `index.ts` (Phase 5 scope fence).

**Gate 7 — PERF-04, no unbounded accumulator remains.** Run
`grep -n "out += \|stderr += " packages/backend/src/index.ts` and require **zero** matches, then
`grep -c "createBoundedBuffer(" packages/backend/src/index.ts` (expect `6`),
`grep -c "appendBounded(" packages/backend/src/index.ts` (expect `7` — six accumulator appends plus
the truncation-marker append on `callMcpMethod`'s stdout drop path),
`grep -c "drainCompleteLines(" packages/backend/src/index.ts` (expect `1`) and
`grep -v '^[[:space:]]*[/*]' packages/backend/src/index.ts | grep -c "newlineIndex"` (expect `0`).
Record all five numbers.

**The pattern is `out += `, not `stdout += `, and that is the whole point of this gate.** `index.ts`
has **seven** accumulator sites — not four, and not six:

| # | Site | Appended | Mechanism | Wired by |
|---|------|----------|-----------|----------|
| 1 | `spawnAndWait` stdout | `:1524` | `appendBounded`, `head` | 04-09 task 2 |
| 2 | `spawnAndWait` stderr | `:1525` | `appendBounded`, `tail` | 04-09 task 2 |
| 3 | `sendCliMessage` stdout | `:2484` | `appendBounded`, `both` | 04-09 task 2 |
| 4 | `sendCliMessage` stderr | `:2573` | `appendBounded`, `tail` | 04-09 task 2 |
| 5 | `callMcpMethod` stderr | `:1295` | `appendBounded`, `tail` | 04-09 task 3 |
| 6 | `callMcpMethod` `stdoutBuffer` | `:1268` | `drainCompleteLines` | 04-09 task 3 |
| 7 | `resolveCommand`'s `out` | `:862` | `appendBounded`, `head` | 04-10 task 1 |

Site 6 is a line-**drain** buffer, which is why it goes through `drainCompleteLines` rather than
`appendBounded`. Site 7 hid from every earlier inventory in this phase because its variable is named
`out`, not `stdout`, so the natural query `grep -n "stdout += \|stderr += "` could never see it — the
same query the earlier drafts of plans 04-05, 04-09 and this gate all trusted. `out += ` is a strict
superset (`stdout += ` contains `out += `), so the broader pattern covers all seven with one query
and cannot be defeated by another variable of the same shape. Record that reasoning in the gate
result, not just the number. If any count is off, fix the source in `index.ts`; do not relax the
gate.

**Then update `04-VALIDATION.md`:**
- Fill the `Plan / Task` column of every row with the plan that proved it (`04-01` … `04-10`), using
  `—` for the two **W** rows already answered by Phase 3 and the two **N** rows.
- Set the `Status` column: `✅ green` for each passing row, `⬜ mitigated` / `⬜ manual` for the **N**
  rows, `⬜ deferred` for the Windows-only SC-10 row.
- Tick the seven *Wave 0 Requirements* checkboxes now that the test files exist.
- Record the **seven** gate results in a new `## Phase Gate Results` section, each with the exact
  command run and its output summary — including Gate 3's zero count with its explanation and
  Gate 7's five accumulator counts.
- Tick the *Validation Sign-Off* checkboxes that now hold and set `nyquist_compliant: true` in the
  front matter.

Record any deviation from the map honestly rather than editing the map to match the code. **Three**
are already known and expected, and all three must appear in the `## Phase Gate Results` section —
`/gsd-verify-work` grades against this artifact, so a deviation that is not written here does not
exist as far as the verifier is concerned:

1. **PERF-02 `Buffer.alloc` static row.** Graded against `activity-tail.ts` rather than `index.ts`.
   An improvement, not a miss — it moved the LLRT `copy_from_slice` constraint into a tested module.
2. **SC-9 static row.** Counts **zero** `env`-supplying `spawn` sites. Correct, not a gap: env
   reaches children through the bash `export` wrapper Phase 5 owns, so this is a forward-looking
   guarantee. Recorded with that one-line explanation.
3. **SC-4's "Caido/runtime version" clause — the substitution.** Record it in these words, because
   it is the one deviation a strict verifier will otherwise read as a straight requirement failure:

   > SC-4's literal wording asks for "the Caido/runtime version". **The Caido SDK exposes no Caido
   > version anywhere** — `sdk.meta` offers only `db()`, `path()` and `assetsPath()`. Per **D-08**,
   > the shipped substitute is a best-effort version block: `driftVersion` (from the existing
   > `detectPluginVersion()` at `index.ts:455`), `processVersion`, `versionsNode`, `versionsLlrt`,
   > `osPlatform` and `osRelease`, each individually guarded and rendering `"unavailable"` when its
   > source throws. `process.versions.node === "0.0.0"` under LLRT is a free LLRT-vs-Node
   > discriminator for bug reports. This is the closest available substitute, **not** an omission.
   > Accepted by the maintainer at task 2 step 5.

   Cross-reference: plan 04-08 states the same substitution in its success-criteria section, and
   task 2 step 5 of this plan asks the maintainer to confirm it. This gate result is where it lands
   in the artifact.
  </action>

  <verify>
    <automated>pnpm typecheck && pnpm lint && pnpm exec vitest run && pnpm build && test -z "$(grep -rn '\"/tmp\"' packages/backend/src --include=*.ts | grep -v '\.test\.ts' | grep -v 'platform.ts')"</automated>
  </verify>

  <acceptance_criteria>
    - `pnpm typecheck`, `pnpm lint`, `pnpm exec vitest run` and `pnpm build` each exit 0.
    - `grep -rn '"/tmp"' packages/backend/src --include=*.ts | grep -v '\.test\.ts'` matches **only** lines inside `getSweepRoots` in `platform.ts`.
    - The three line numbers from `grep -n "probeRuntime()\|await sweepOrphanedMcpTempDirs(\|mcpTempDir = path.join" packages/backend/src/index.ts` are strictly ascending, and those three numbers are transcribed into 04-VALIDATION.md.
    - Every `spawn(` site in `index.ts` either supplies no `env` option or supplies one built by `buildSpawnEnv`; the recorded count of `env`-supplying sites is `0` and is annotated as expected.
    - The `## Phase Gate Results` section records **three** deviations, not two: the PERF-02 `Buffer.alloc` row graded against `activity-tail.ts`, SC-9's zero call-site count, and the SC-4 "no Caido version in `sdk.meta`" substitution in D-08's wording. `grep -c "sdk.meta" .planning/phases/04-platform-foundation/04-VALIDATION.md` is at least `1`.
    - `normalizePathForCompare` is still exported from `runtime-probe.ts`, still has no non-test caller, and any lint exemption cites D-04.
    - `git diff --stat packages/backend/src/provider-launch.ts packages/backend/src/provider-launch.test.ts packages/backend/src/command-resolution.ts` shows zero changes across the phase.
    - `grep -c "out += \|stderr += " packages/backend/src/index.ts` returns `0` — the broader pattern, which also matches `stdout += ` and therefore covers site 7; `createBoundedBuffer(` returns `6`; `appendBounded(` returns `7`; `drainCompleteLines(` returns `1`; and `grep -v '^[[:space:]]*[/*]' packages/backend/src/index.ts | grep -c "newlineIndex"` returns `0`.
    - `.planning/phases/04-platform-foundation/04-VALIDATION.md` has `nyquist_compliant: true`, every row's `Plan / Task` filled, every row's `Status` set, all seven Wave 0 checkboxes ticked, and a `## Phase Gate Results` section with the seven commands and their outputs.
  </acceptance_criteria>

  <done>All seven phase gates pass or their failures are fixed at source, and 04-VALIDATION.md is a complete, honest record with `nyquist_compliant: true`.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human read of the RUN-05 probe failure message and the RUN-04 mitigation</name>

  <read_first>
    - `packages/backend/src/runtime-probe.ts` — `formatProbeFailure` and `buildProbeReport`, the code
      that produces the message being read.
    - `packages/backend/src/runtime-probe.test.ts` — everything already machine-asserted about the
      message, so the human read focuses on what the tests cannot grade.
    - `.planning/phases/04-platform-foundation/04-VALIDATION.md` § *Manual-Only Verifications* — the
      two rows this checkpoint closes and the four questions the message must answer.
    - `.planning/phases/04-platform-foundation/04-CONTEXT.md` § D-08 — including the honest note that
      the Caido SDK exposes no Caido version, so this block is the closest available substitute.
  </read_first>

  <what-built>
    Phase 4's runtime-capability probe now fails loud at MCP start with an actionable message: it
    names the missing primitive, states what Drift needed it for, gives a remedy, and appends a
    best-effort version block (Drift version, `process.version`, `process.versions.node` and
    `.llrt`, `os.platform()` + `os.release()` — each rendering `"unavailable"` when its source
    throws) plus the reported-but-non-gating capabilities and the two MAX_PATH metrics.

    Everything machine-assertable about that message is already tested in
    `packages/backend/src/runtime-probe.test.ts`. **Legibility is not machine-assertable**, and
    `04-VALIDATION.md` grades it as the phase's one manual verification. This checkpoint is the read.

    Also confirmed here: the RUN-04 mitigation for the behaviour that is not inducible in CI on any
    runner (a real anti-virus write-then-exec lock). Every retry logs its code and attempt index
    through `sdk.console`, and the attempt count reaches `getDiagnostics` as
    `mcpFirstWriteAttempts`, so the next real bug report answers whether the 1,500 ms ladder was
    long enough.
  </what-built>

  <how-to-verify>
    1. Render one example failure message without starting Caido. From the repo root run:
       `pnpm exec vitest run packages/backend/src/runtime-probe.test.ts --reporter=verbose`
       and read the assertions, then produce a full rendered example — the simplest way is a
       throwaway `node --experimental-strip-types` snippet, or temporarily add a
       `console.log(formatProbeFailure(report))` inside a scratch test that is **not** committed.
       Render two variants: (a) `os.tmpdir()` missing, and (b) the first write failing with
       `EPERM` after 6 attempts.

    2. Read each rendered message as a Windows user who has never seen Drift's source would. It must
       answer all four questions without further investigation:
       - **What is missing?** The primitive is named, not just "a runtime capability".
       - **Why does Drift care?** It says what the primitive was needed for.
       - **What do I do now?** There is a concrete next action.
       - **What do I paste into an issue?** The version block is present, complete, and obviously
         copy-pasteable as one block.

    3. Confirm the message contains **no** secret material: no `CAIDO_TOKEN` value, no `%APPDATA%`
       or `%USERPROFILE%` *value* (presence booleans by variable name are expected and correct), and
       no environment dump. This is the T-04-04 control and the discipline Phase 3's D-10 CI gate
       enforces elsewhere.

    4. Run `pnpm exec vitest run` one final time and confirm the whole suite is green, including the
       pre-existing 134 tests and `provider-launch.test.ts`'s exact `toEqual([...])` arrays, which
       are the CMP-01 regression tripwire.

    5. Confirm the two known-and-accepted gaps are acceptable to you as the maintainer:
       - **RUN-05's Caido version.** SC-4's literal wording says "including the Caido/runtime
         version". `sdk.meta` exposes only `db()`, `path()` and `assetsPath()` — **there is no Caido
         version anywhere in the SDK surface.** D-08's block is the closest available substitute, not
         an omission. Confirm you accept that reading.
       - **A real Defender lock is never exercised.** Not inducible on any CI runner. The mitigation
         shipped is the retry logging plus `mcpFirstWriteAttempts` in diagnostics; final confirmation
         waits on the original Windows reporter in Phase 9/10.

    If any rendered message fails step 2 or step 3, describe what was unclear or what leaked. The fix
    belongs in `formatProbeFailure` in `packages/backend/src/runtime-probe.ts`, with a new
    `toContain` assertion in `runtime-probe.test.ts` so the same gap cannot reopen.
  </how-to-verify>

  <resume-signal>Type "approved" to close Phase 4, or describe what the message failed to communicate / what it leaked.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Backend → user-visible MCP status message and support bundle | The probe failure message crosses here and is expected to be pasted into a **public** bug report |
| Phase 4 source tree → Phase 5-8 assumptions | The static gates are what stop a silently reintroduced `/tmp`, an out-of-order probe, or a re-added unbounded accumulator from becoming a later phase's mystery |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | Information Disclosure | rendered probe failure message | mitigate | Step 3 of the human verification is an explicit read for secret material: no `CAIDO_TOKEN` value, no Windows env-var **values**, no environment dump. Backed by an automated `toContain`-negative assertion in `runtime-probe.test.ts` from plan 04-03 |
| T-04-26 | Repudiation | an unreadable failure message | mitigate | Legibility is not machine-assertable, so it gets a blocking human read against four explicit questions. A failure here is fixed in `formatProbeFailure` **plus** a new assertion, so the gap cannot silently reopen |
| T-04-02 | Information Disclosure | a silently reintroduced hardcoded `/tmp` | mitigate | Gate 1 asserts the only surviving `/tmp` literal in non-test backend source is `getSweepRoots`' deliberate legacy arm. A reintroduced `/tmp` elsewhere would resurrect the orphaned-token-directory class |
| T-04-27 | Tampering | the deliberately-uncalled `normalizePathForCompare` being deleted to quiet the linter | mitigate | Gate 4 asserts the export survives with a D-04-citing exemption. `04-VALIDATION.md` § *Lint note* mandates this explicitly |
| T-04-05 | Denial of Service | an accumulator site silently left unbounded, or a bound weakened to pass a contradictory gate | mitigate | Gate 7 checks all five counts together (`out +=`/`stderr +=` at `0`, `createBoundedBuffer(` at `6`, `appendBounded(` at `7`, `drainCompleteLines(` at `1`, `newlineIndex` at `0`). They are satisfiable simultaneously only when every one of `index.ts`'s **seven** accumulator sites is converted. The pattern is deliberately `out += ` rather than `stdout += `: the narrower form is precisely what let site 7 (`resolveCommand`'s `out` at `:862`) survive both the "four sites" and the "six sites" undercounts, and a gate that repeats the query which caused the miss cannot detect the miss |
| T-04-SC | Tampering | npm/pnpm installs | mitigate | Gate 5's `pnpm build` runs against the committed lockfile. Phase 4 installs zero packages; `git diff package.json pnpm-lock.yaml` must be empty |
</threat_model>

<verification>
- `pnpm typecheck`, `pnpm lint`, `pnpm exec vitest run` and `pnpm build` all exit 0.
- All seven phase gates recorded in `04-VALIDATION.md` § *Phase Gate Results* with their commands and outputs.
- `git diff package.json pnpm-lock.yaml` is empty — zero packages added.
- The human checkpoint returns "approved".
</verification>

<success_criteria>
- ROADMAP SC-8 is proven at the phase level: the existing macOS/Linux unit and snapshot tests are green, and `provider-launch.test.ts`'s exact arrays have not moved.
- ROADMAP SC-9's static half is recorded honestly, with its zero call-site count explained rather than presented as a gap.
- ROADMAP SC-4's substitution is recorded in the artifact `/gsd-verify-work` reads: the Caido SDK has no Caido version, so D-08's version block stands in its place, and that is stated in `## Phase Gate Results` rather than only in a plan's prose.
- The two **N**-bucket criteria are closed the only way they can be: one by a human read, one by shipped diagnostics that let a future bug report answer it.
- `04-VALIDATION.md` is a complete record with `nyquist_compliant: true`, ready for `/gsd-verify-work`.
</success_criteria>

<output>
Create `.planning/phases/04-platform-foundation/04-11-SUMMARY.md` when done. Record the rendered probe failure message verbatim in the summary — it is the artifact a Windows bug reporter will eventually paste, and having it in the phase record means a later reviewer can judge it without re-rendering.
</output>
