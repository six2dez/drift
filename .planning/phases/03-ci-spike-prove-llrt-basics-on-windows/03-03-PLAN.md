---
phase: 03-ci-spike-prove-llrt-basics-on-windows
plan: 03
type: execute
wave: 2
depends_on:
  - 03-01
  - 03-02
files_modified: []
autonomous: true
requirements:
  - CI-02

must_haves:
  truths:
    - "A real GitHub Actions run on a real windows-latest host has executed the probe and produced all seven assertion lines — P0-ENV, P0-TMP, P1-CMD, P1-WHERE, P2-OS, P3-VARS, P3-UUID — captured verbatim with the run URL recorded (D-13)"
    - "The D-10 gate command is run locally over BOTH phase files before anything is pushed — this plan is the first point in the phase where both files are guaranteed to coexist, so it is where the workflow's actual gate command is proven rather than approximated"
    - "The run's per-step conclusions are read from the Actions API rather than inferred from the YAML, following the evidence standard set by decision [01-06]"
    - "A non-zero probe exit is recorded as a finding, not treated as a broken job: D-05 through D-07 define which outcomes gate, and every one of them is a legitimate result this plan must capture rather than retry away"
    - "P1-CMD's result is conclusive — it names which of the four spawn surfaces fired — because an indeterminate P1-CMD leaves Phase 4 with no architectural basis (D-07)"
    - "The four existing Verify (Node N) legs stay green on the same push, proving the new scripts/ directory did not turn the pre-existing quality gate red"
    - "Nothing was published to the public remote beyond a throwaway scratch branch created at an already-committed SHA, and if this plan terminates early the scratch branch is deleted before it stops rather than left alive on a public remote"
  artifacts:
    - path: .planning/phases/03-ci-spike-prove-llrt-basics-on-windows/03-03-SUMMARY.md
      provides: "The verbatim seven assertion lines, the run URL, per-step conclusions, and the probe's measured exit code — the measured input plan 03-05 writes the findings document from (D-12)"
      contains: "P0-ENV"
      min_lines: 40
  key_links:
    - from: "scratch branch pushed to origin"
      to: "windows-latest probe run"
      via: "bare push trigger on the new workflow (D-01)"
      pattern: "windows-llrt-probe"
    - from: "probe stdout on the runner"
      to: "recorded evidence"
      via: "named artifact download into an explicit directory plus gh run view --json jobs"
      pattern: "probe-results\\.txt"
---

<objective>
Run the probe on a real `windows-latest` host and capture what it measured. This plan is the phase's completion bar: per D-13 the phase is complete only when a real CI run has produced all seven assertion lines, recorded with its URL. "The files exist and the YAML is valid" is explicitly not sufficient — that is the false-green pattern Phase 1 existed to eliminate.

Purpose: Phases 4-8 choose their spawn and env-injection architecture from these seven results. Until they exist as measured output rather than expectation, no port code can be written on top of them.

Output: no file in the repository changes. The deliverable is `03-03-SUMMARY.md` carrying the run URL, per-step conclusions read from the API, the probe's measured exit code, and the seven assertion lines quoted verbatim. Plan 03-05 writes the committed findings document from these measurements and from nothing else (D-12).

Before pushing anything, this plan re-runs the local gates. A lint error in `scripts/windows-llrt-probe.mjs` would turn all four pre-existing `Verify (Node N)` legs red, so the pre-flight is what keeps a probe defect from masquerading as a quality-gate regression. The pre-flight also carries a responsibility handed to it by plan 03-02: plans 03-01 and 03-02 ran in parallel in wave 1 on disjoint files, so neither could run the D-10 gate command over both targets. This plan is the first point where both files are guaranteed to exist, so it runs that command in its full two-file form — the identical string the workflow step executes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-ci-spike-prove-llrt-basics-on-windows/03-CONTEXT.md
@.planning/phases/01-restore-the-verification-signal/01-06-SUMMARY.md
@.github/workflows/ci.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Local pre-flight before burning a Windows runner</name>

  <files>none — verification only, no file is modified</files>

  <read_first>
    - `.planning/phases/01-restore-the-verification-signal/01-06-SUMMARY.md` section `### Local pre-flight before pushing the revert-proof` — the established pattern is a four-part local check where every part must hold before anything is pushed, so a botched input is caught before it burns a CI run. That plan's pre-flight matched CI exactly, which is what made it a true dry run rather than a hopeful one.
    - `scripts/windows-llrt-probe.mjs` — the file whose lint status determines whether the existing four-leg matrix stays green.
    - `.github/workflows/windows-llrt-probe.yml` — the file about to be exercised for the first time, and the source of the exact D-10 gate command string this task must replicate.
    - `.planning/phases/03-ci-spike-prove-llrt-basics-on-windows/03-02-SUMMARY.md` — records that the gate self-scan there was scoped to one file and that the two-file run was deferred to this task. Do not treat that as already proven.
    - `.planning/STATE.md` section `### Blockers/Concerns` — note the entry recording that `git add -A` is forbidden repo-wide and why.
  </read_first>

  <action>
    Run every gate below and record its measured exit code. Do not proceed to task 2 unless all of them hold; a failure here costs one command, a failure discovered on CI costs a runner and produces a red run that has to be explained away in the findings document.

    Run `pnpm lint` and require exit 0. Capture it with a redirect and read `$?` immediately — never through a pipe, because `$?` after a pipeline is the last stage's status and would report 0 no matter what lint did. This is the load-bearing check. ESLint's flat config ignores only `node_modules` and `dist`, so the new `scripts/` directory is linted by `eslint . --max-warnings 0` on all four `ubuntu-latest` legs. A `no-empty` or `no-unused-vars` error in the probe turns the pre-existing quality gate red, and the resulting failure would be attributed to Phase 3 breaking CI rather than to a probe defect.

    Run `pnpm -r typecheck` and require exit 0.

    Run `actionlint .github/workflows/windows-llrt-probe.yml` and require exit 0. GitHub will not tell you a workflow is malformed until you push it, and a malformed workflow produces no run at all — which is indistinguishable from a workflow that ran and passed if you are only checking for red.

    RUN THE D-10 GATE COMMAND IN ITS FULL TWO-FILE FORM. This is the responsibility plan 03-02 deferred here. First assert both targets exist with `test -f`, exactly as the workflow step does, so a rename fails loudly rather than being inferred from an exit status. Then run the identical `grep -nE` alternation the workflow runs, over both `.github/workflows/windows-llrt-probe.yml` and `scripts/windows-llrt-probe.mjs`, capturing the status explicitly. Require **exactly 1**. Exit 0 means a match — either a real leak or patterns that match their own source, and the job would fail on its own text. Exit 2 means a path was wrong and the check proved nothing, which is the vacuous-pass failure mode the gate's third branch exists to catch; treat it as a hard stop rather than as absence of evidence. Record the measured status, not a tick.

    Confirm the phase touched no product code: `git diff --name-only` against the merge-base for the working branch must list no path under `packages/`, and `package.json` and `pnpm-lock.yaml` must be unmodified — this phase installs nothing.

    Confirm the working tree has no unexpected untracked file that a careless stage could publish. `git status --porcelain` should show only what this phase created and what was already ignored. `IMPROVEMENT-PLAN.md` is now `.gitignore`d, but the discipline stands: only targeted `git add <path>` is used anywhere in this phase, never `git add -A`.

    Confirm the commits from plans 03-01 and 03-02 exist locally and that `scripts/windows-llrt-probe.mjs` and `.github/workflows/windows-llrt-probe.yml` are both tracked. An untracked workflow file cannot trigger a run, and the resulting silence looks exactly like a run that did not fail.
  </action>

  <verify>
    <automated>pnpm lint > /tmp/pf-lint.txt 2>&1; echo "lint=$?"; pnpm -r typecheck > /tmp/pf-tc.txt 2>&1; echo "typecheck=$?"; actionlint .github/workflows/windows-llrt-probe.yml; echo "actionlint=$?"</automated>
    <automated>for f in .github/workflows/windows-llrt-probe.yml scripts/windows-llrt-probe.mjs; do test -f "$f" || { echo "GATE-TARGET-MISSING: $f"; exit 1; }; done; s=0; grep -nE 'CAIDO_(TOKEN)|secret(s)\.' .github/workflows/windows-llrt-probe.yml scripts/windows-llrt-probe.mjs || s=$?; echo "secret-gate-status=$s (1 clean / 0 match / 2 unreadable)"</automated>
    <automated>git ls-files --error-unmatch scripts/windows-llrt-probe.mjs .github/workflows/windows-llrt-probe.yml && echo "both-tracked"; out=$(git status --porcelain -- packages/ package.json pnpm-lock.yaml); test -z "$out"; echo "product-code-clean=$? (0 = clean, 1 = dirty)"; echo "product-code-dirty: $out"</automated>
  </verify>

  <acceptance_criteria>
    - `pnpm lint` exits 0, measured with a redirect rather than through a pipe. Record the measured value; this is the check that protects the four pre-existing `Verify (Node N)` legs.
    - `pnpm -r typecheck` exits 0.
    - `actionlint .github/workflows/windows-llrt-probe.yml` exits 0 with no output.
    - Both D-10 gate targets exist — the `test -f` loop printed no `GATE-TARGET-MISSING` line — and the two-file gate grep reports `secret-gate-status=1`. A status of 0 means a match; a status of 2 means a path was wrong and the check proved nothing. This is the phase's only pre-push execution of the gate command in the exact form the workflow step runs it, and the summary must record it as such rather than as a repeat of plan 03-02's single-file scan.
    - `git ls-files --error-unmatch` succeeds for both new files — they are tracked and will exist on a pushed branch.
    - `product-code-clean=0`. The command captures `git status --porcelain -- packages/ package.json pnpm-lock.yaml` into a variable and discriminates on emptiness with `test -z`, so 0 means those paths are clean, 1 means they are not, and `product-code-dirty:` names any offending path. The earlier `git status --porcelain; echo "product-code-clean=$?"` form could not report anything but 0 — `git status` exits 0 whether or not it printed a line — so the label was a measurement that measured nothing. That is precisely the `[01-06]` failure mode this plan cites as its governing standard, reproduced inside the plan that cites it.
    - No `git add -A` was used anywhere in this phase.
  </acceptance_criteria>

  <done>
    Every local gate passes with a recorded exit code, the two-file D-10 gate command is proven clean at status 1, both new files are tracked, and no product code or dependency manifest was touched.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run the probe on a real windows-latest host and capture the seven results</name>

  <files>none — verification only; a throwaway scratch branch is created and pushed, then deleted in plan 03-04 (or here, if this plan terminates early)</files>

  <read_first>
    - `.planning/phases/01-restore-the-verification-signal/01-06-SUMMARY.md` — read in full. It is the procedural analog: the scratch-branch pattern, the run table format, the per-leg and per-step conclusion tables read from the API, and two failure modes this task will otherwise hit. Note especially `## Issues Encountered` item 2: `gh run view --log` output carries ANSI escapes and a `<step>\t<timestamp>` prefix, so a naive grep silently matches nothing even when the line is present — and a grep that returns nothing looks identical to a proof that failed.
    - `.planning/phases/03-ci-spike-prove-llrt-basics-on-windows/03-CONTEXT.md` — D-05, D-06, D-07 and D-13 define which outcomes gate and what "complete" means here.
    - `.github/workflows/windows-llrt-probe.yml` — the five step names this task reads conclusions for, and the `name:` expression on the upload step, which embeds `github.run_number` and therefore cannot be assumed to be a fixed string when downloading.
    - `.planning/phases/03-ci-spike-prove-llrt-basics-on-windows/03-01-SUMMARY.md` — the probe's single-emission output contract and the local darwin per-ID grep counts, which are the baseline the CI counts are compared against.
  </read_first>

  <action>
    Create and push a throwaway scratch branch, observe the real run, and capture what it measured.

    BRANCH. Record `git rev-parse HEAD` first. Create `scratch/ci-proof-windows-probe` pointing at that exact SHA and verify the two SHAs match before pushing — the point of the scratch branch is that CI runs on a byte-identical tree, so only the ref name differs. Push only the scratch branch. Never push the working branch: the repository is public, and plan `01-06` recorded a plan-spec defect where the plan said to push the working branch and the execution authorization forbade it. No new commit is needed, because plans 03-01 and 03-02 already committed both files.

    Expect two workflows to fire on this push, because both now carry bare triggers: `CI` (the four `Verify (Node N)` legs) and `Windows LLRT Primitive Probe`. Both are wanted. The `CI` run is the check that the new `scripts/` directory did not break the pre-existing quality gate on real CI rather than only locally.

    OBSERVE. Find the probe run with `gh run list --workflow=windows-llrt-probe.yml --branch=scratch/ci-proof-windows-probe --json databaseId,headSha,status,conclusion,url`, confirm `headSha` equals the recorded SHA, then wait for completion with `gh run watch <id>` — without `--exit-status`, because a probe that exits 1 is a legitimate recorded finding under D-05 through D-07 and must not abort this task.

    Read per-step conclusions from the API, not from the YAML: `gh run view <id> --json jobs` gives each step's name, number and conclusion. Record the conclusion of all five steps by name — `Checkout`, `Setup Node`, `Assert no secret material is reachable (D-10)`, `Run LLRT Windows primitive probe`, and `Upload probe results`. If `Upload probe results` failed with a permissions error, that is the `permissions: contents: read` divergence being disproven: widen the block in `.github/workflows/windows-llrt-probe.yml`, commit to the scratch branch with a targeted `git add`, push, and re-run — recording both runs.

    CAPTURE. Download the artifact by name into an explicit directory. `gh run download <id>` with no `-n` extracts each artifact into a subdirectory named after that artifact, so `probe-results.txt` would land at `./windows-llrt-probe-<run_number>/probe-results.txt` and a grep of `probe-results.txt` in the working directory would print grep's error on stderr and nothing on stdout — reproducing exactly the `01-06` failure mode where a grep that matched nothing was indistinguishable from a proof that failed. Instead:
      - Resolve the artifact's real name rather than reconstructing it: `ART=$(gh api repos/six2dez/drift/actions/runs/<id>/artifacts --jq '.artifacts[0].name')`. The upload step names it `windows-llrt-probe-${{ github.run_number }}`, and the run number is not the run id.
      - Download with both flags: `gh run download <id> -n "$ART" -D /tmp/drift-probe-artifact`. With `-n` naming a single artifact, the contents extract directly into `-D` rather than into a per-artifact subdirectory. Using a path outside the working tree keeps a downloaded copy off the repository entirely, so no `git add` can reach it.
      - Assert the file landed before reading it: `test -f /tmp/drift-probe-artifact/probe-results.txt`. Without this the whole capture step can silently produce nothing.

    Prefer the artifact over the log for the verbatim lines: it is the probe's own output with no ANSI escapes and no step-and-timestamp prefix. Also fetch the log with `gh run view <id> --log` as a cross-check, stripping escapes with `sed -E 's/\x1b\[[0-9;]*m//g'` before any grep. Extract, verbatim and unedited, the one `PASS [<ID>]:` or `FAIL [<ID>]:` line for each of the seven IDs from the `=== Summary ===` block, plus every `INFO [<ID>]:` line, plus the final `PROBE PASSED:`/`PROBE FAILED:` verdict line, plus the banner line carrying `process.version` (pitfall 5: the `.cmd` EINVAL guard only exists on Node >= 18.20.2, so the version belongs in the record). Record the probe's measured exit code as reported by the step conclusion.

    INTERPRET, without editing the evidence. Any of these is a legitimate outcome to record and carry forward:
      - `PASS [P0-ENV]` — the direct-spawn plus env-injection architecture is confirmed for Phases 4-8. Also record the accompanying `INFO [P0-ENV]` replace-versus-merge observation; Phase 4 needs it to decide whether its spawn options must spread `...process.env`.
      - `FAIL [P0-ENV]` — the job exits non-zero and this is the single most consequential result in the phase. Do not retry it away. Capture which of the two failure modes the probe reported (the `env` option not reaching the child at all, versus a cleared environment block breaking the child on this host), because D-05 makes this a blocker that stops Phase 4 until the `.cmd`-launcher fallback is decided, and the blocker text has to name what is being decided.
      - `PASS`/`FAIL [P0-TMP]` — recorded as measured.
      - `PASS [P1-CMD]` naming any of `spawn-threw-sync`, `spawn-error-event`, `ran` or `hung` — all four are conclusive and each selects a different architecture for Phases 4-8. A "wrong" answer is not a failure here.
    One outcome is **not** acceptable as a final result: `FAIL [P1-CMD]: indeterminate`. Per D-07 that leaves Phase 4 with no architectural basis, which is worse than a clear negative. If it occurs, capture the raw surface, exit code, stdout and stderr the probe reported, extend the classifier in `scripts/windows-llrt-probe.mjs` to cover the observed surface, commit with a targeted `git add`, push to the scratch branch and re-run. Cap this at two additional attempts.

    EARLY-TERMINATION TEARDOWN. If P1-CMD is still indeterminate after three runs, stop and record it as an open blocker for plan 03-05 rather than continuing to burn runners — but do not stop with a branch alive on a public remote. The teardown normally lives in plan 03-04, and stopping here means 03-04 never runs. So before stopping, and before stopping for any other reason that skips the hand-off to 03-04: delete `scratch/ci-proof-windows-probe` from `origin` and delete the local ref, then assert `git ls-remote --heads origin 'scratch/*'` returns empty and record the full unfiltered `git ls-remote --heads origin` listing alongside it, so the empty glob is provably empty rather than mistyped. Record the run IDs first — the run records survive the branch deletion and remain valid citations, which is what makes deleting the branch safe.

    CROSS-CHECK THE NEGATIVE LEGS. Read the `CI` run for the same SHA with `gh run list --workflow=ci.yml --branch=scratch/ci-proof-windows-probe` and confirm all four `Verify (Node N)` legs concluded `success`. This applies decision `[01-06]`'s rule that a proof must assert the negative legs stay green: it proves the new `scripts/*.mjs` file passes real-CI lint on all four Node majors, and it means a red probe job is attributable to the probe alone.

    On the normal path — P1-CMD conclusive — leave the scratch branch in place. Plan 03-04 needs a pushed baseline to compare its deliberate-FAIL runs against, and deletes every scratch branch at the end.
  </action>

  <verify>
    <automated>gh run list --workflow=windows-llrt-probe.yml --branch=scratch/ci-proof-windows-probe --limit 3 --json databaseId,headSha,status,conclusion,url</automated>
    <automated>test -f /tmp/drift-probe-artifact/probe-results.txt || { echo "ARTIFACT-NOT-DOWNLOADED"; exit 1; }; for id in P0-ENV P0-TMP P1-CMD P1-WHERE P2-OS P3-VARS P3-UUID; do printf '%s -> ' "$id"; grep -cE "^(PASS|FAIL) \[$id\]:" /tmp/drift-probe-artifact/probe-results.txt; done; printf 'indeterminate -> '; grep -c 'indeterminate' /tmp/drift-probe-artifact/probe-results.txt</automated>
    <automated>gh run list --workflow=ci.yml --branch=scratch/ci-proof-windows-probe --limit 1 --json databaseId,conclusion --jq '.[0]'</automated>
  </verify>

  <acceptance_criteria>
    - A run of `Windows LLRT Primitive Probe` exists on `windows-latest` whose `headSha` equals the recorded local HEAD SHA, and its full run URL is recorded.
    - The artifact was resolved by name and downloaded into `/tmp/drift-probe-artifact`, and `test -f /tmp/drift-probe-artifact/probe-results.txt` succeeded before any grep ran. A bare `gh run download <id>` would have extracted into a `windows-llrt-probe-<run_number>/` subdirectory and every grep below would have measured nothing while looking successful.
    - `probe-results.txt` contains exactly one `PASS [<ID>]:` or `FAIL [<ID>]:` summary line for each of the seven IDs. Each of the seven greps above returns **1** — not 0, which would mean the line is absent, and not 2 or more, which would mean the probe reintroduced inline status emission in violation of plan 03-01's single-emission contract, under which those lines are produced only inside the `=== Summary ===` block.
    - All seven lines are recorded verbatim in the summary, unedited, including their trailing detail. The `P2-OS` line carries `(node-vehicle; LLRT resolver verified by source analysis)` and the `P3-UUID` line carries `(node-vehicle; LLRT crypto surface unverified)`, so the archived evidence cannot be misread as an LLRT result (D-08).
    - `grep -c 'indeterminate' /tmp/drift-probe-artifact/probe-results.txt` returns 0, or the escalation path was followed and the outcome after at most three runs is recorded explicitly (D-07).
    - Per-step conclusions for all five steps are recorded, read from `gh run view <id> --json jobs` rather than inferred from the workflow file.
    - `Upload probe results` concluded `success`, confirming the `permissions: contents: read` divergence from `ci.yml` does not break artifact upload — or the widened-permissions re-run is recorded with both run URLs.
    - The probe's measured exit code is recorded, and if it is non-zero the recorded reason is one of the gating conditions D-05, D-06 or D-07 defines rather than an unexplained failure.
    - All four `Verify (Node N)` legs on the `CI` run for the same SHA concluded `success`.
    - On the normal path, `git ls-remote --heads origin` shows `scratch/ci-proof-windows-probe` and no other `scratch/*` branch, and it is deliberately left in place for plan 03-04. On the early-termination path, `git ls-remote --heads origin 'scratch/*'` is empty, the full unfiltered listing is recorded alongside it, and the run IDs are recorded before deletion so the citations survive.
    - Nothing else was pushed; the working branch was not pushed.
  </acceptance_criteria>

  <done>
    A real `windows-latest` run has executed the probe, all seven assertion lines are captured verbatim from a name-resolved artifact download with the run URL and per-step conclusions, P1-CMD is conclusive, and the four pre-existing `Verify (Node N)` legs are green on the same SHA — or the plan terminated on its documented escalation path with the scratch branch torn down and the teardown asserted.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local working tree → public `origin` | Anything pushed to a scratch branch on a public repository is world-visible, and remains so in run records after branch deletion |
| GitHub Actions run record → project evidence | The run's conclusions become the basis for Phase 4's architecture; a misread or fabricated conclusion propagates silently |
| runner environment → public artifact | The probe's stdout becomes a downloadable, world-readable artifact |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | Information Disclosure | pushing to a public remote | mitigate | The scratch branch is created at an already-committed SHA, so the push adds no new content. Only targeted `git add <path>` is used if a fix commit becomes necessary, never `git add -A` (STATE.md, decision `[01-06]`). Branch names use the `scratch/ci-proof-*` form so a single glob confirms none survive. Teardown normally belongs to plan 03-04, and this plan's early-termination path performs and asserts it inline so no exit route leaves a branch alive |
| T-03-06 | Tampering | integrity of the recorded verification signal | mitigate | Per-step conclusions are read from `gh run view --json jobs` rather than inferred from the YAML, and the seven assertion lines are taken verbatim from the downloaded artifact rather than paraphrased. The artifact is resolved by name and downloaded into an explicit directory with a `test -f` assertion before any grep, so a wrong extraction path cannot produce a silent empty measurement. The log cross-check strips ANSI escapes before grepping, closing the failure mode recorded in `01-06` where a grep that matched nothing was indistinguishable from a proof that failed |
| T-03-11 | Repudiation | a non-zero probe exit being quietly retried until green | mitigate | The plan names in advance which outcomes are legitimate findings to record (`FAIL [P0-ENV]`, `FAIL [P0-TMP]`, any of the four P1-CMD surfaces) and which single outcome justifies a re-run (`FAIL [P1-CMD]: indeterminate`), with a hard cap of three runs and a mandatory escalation. This removes the discretion that would let an inconvenient result be re-rolled |
| T-03-02 | Information Disclosure | probe output in a public artifact | accept | The artifact contains only the three named Windows path variables, a synthetic sentinel, and runtime version strings. On a hosted runner these are runner-scoped values under `C:\Users\runneradmin` with no user or org data. The D-10 gate step runs before the probe and would have failed the job before any token-shaped material could be printed |
| T-03-12 | Denial of Service | a probe defect turning the pre-existing quality gate red | mitigate | Task 1's `pnpm lint` pre-flight runs before anything is pushed and is measured with a redirect rather than through a pipe, and task 2 asserts all four `Verify (Node N)` legs concluded `success` on the same SHA — so a red probe is provably attributable to the probe alone |
| T-03-23 | Tampering | the D-10 gate never being proven in its real two-file form | mitigate | Plans 03-01 and 03-02 run in parallel on disjoint files, so neither can execute the gate command over both targets. Task 1 owns that execution: it asserts both targets exist, runs the identical command string, and requires status exactly 1, treating status 2 as a hard stop rather than as absence of evidence. Tracked under its own ID rather than reusing `T-03-20`, which covers the gate's three-branch construction in plan 03-02; `T-03-24` in plan 03-04 covers the gate's failure path being exercised. Three distinct properties, three stable IDs |
| T-03-SC | Tampering | npm/pnpm installs | mitigate | Nothing is installed. Task 1 asserts `package.json` and `pnpm-lock.yaml` are unmodified, and the probe workflow has no install step |
</threat_model>

<verification>
- `pnpm lint`, `pnpm -r typecheck` and `actionlint` all exit 0 locally before any push, with lint measured via redirect rather than pipe.
- The two-file D-10 gate command reports status exactly 1, with both targets confirmed present first.
- A `windows-latest` probe run exists at the recorded SHA with a recorded URL.
- The artifact was name-resolved, downloaded to an explicit directory, and asserted present before grepping.
- All seven assertion IDs appear exactly once each in the downloaded `probe-results.txt`, quoted verbatim in the summary.
- P1-CMD names one of its four spawn surfaces; no `indeterminate` result is left standing.
- Per-step conclusions for all five steps are read from the Actions API.
- All four `Verify (Node N)` legs concluded `success` on the same SHA.
- Only `scratch/ci-proof-windows-probe` exists on the remote and only on the normal path; on the early-termination path no `scratch/*` branch survives. The working branch was not pushed.
</verification>

<success_criteria>
The seven LLRT/Windows assertions have been measured on a real Windows host, not predicted. The run URL, the probe's exit code, the five per-step conclusions and the seven verbatim assertion lines are recorded in `03-03-SUMMARY.md`, and the pre-existing four-leg quality gate is proven green on the identical commit. This is the evidence D-13 requires and the input D-12 forbids plan 03-05 from writing without.
</success_criteria>

<output>
Create `.planning/phases/03-ci-spike-prove-llrt-basics-on-windows/03-03-SUMMARY.md` when done.

It must contain, at minimum: a run table in the `01-06-SUMMARY.md` shape (purpose, branch, commit, run URL, conclusion); a per-step conclusion table for all five steps read from the API; the two-file D-10 gate status measured in the pre-flight; the resolved artifact name and download path; the probe's measured exit code; the banner line carrying `process.version`; all seven assertion lines quoted verbatim with their measured per-ID grep counts; every `INFO` line, in particular the `P0-ENV` replace-versus-merge observation; the final verdict line; and the four `Verify (Node N)` conclusions for the same SHA. Plan 03-05 writes the committed findings document from this file and the run itself — nothing in it may be paraphrased or predicted.
</output>
