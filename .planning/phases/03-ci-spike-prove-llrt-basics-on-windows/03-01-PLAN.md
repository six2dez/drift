---
phase: 03-ci-spike-prove-llrt-basics-on-windows
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/windows-llrt-probe.mjs
  - .gitignore
autonomous: true
requirements:
  - CI-02

must_haves:
  truths:
    - "Running the probe emits exactly one status line per assertion ID for all seven IDs: P0-ENV, P0-TMP, P1-CMD, P1-WHERE, P2-OS, P3-VARS, P3-UUID"
    - "The probe exits non-zero when P0-ENV fails, when P0-TMP fails, or when P1-CMD cannot be classified into one of its four spawn surfaces (D-05, D-06, D-07)"
    - "The probe exits zero when both P0 assertions pass and P1-CMD is conclusive, regardless of what P1-WHERE, P2-OS, P3-VARS or P3-UUID reported (D-09)"
    - "The P2-OS and P3-UUID output lines each carry a literal non-LLRT vehicle label, so a reader months later cannot mistake them for LLRT results (D-08)"
    - "The only secret-shaped value the probe handles is a synthetic sentinel of the form drift-probe-sentinel-<timestamp>; the probe never reads a Caido token and never dumps process.env wholesale (D-10)"
    - "The probe's final verdict line reaches stdout before the process exits, so a pipe to tee cannot truncate the evidence"
    - "pnpm lint stays at exit 0 with the new scripts/ directory present, so the existing four-leg ci.yml matrix is not turned red by this phase"
  artifacts:
    - path: scripts/windows-llrt-probe.mjs
      provides: "Zero-dependency Node ESM probe covering the seven LLRT/Windows primitives, with a four-surface spawn classifier and the D-05/D-06/D-07 exit-code contract"
      contains: "P0-ENV"
      min_lines: 150
    - path: .gitignore
      provides: "Targeted ignore for the probe's local output file so a stray git add cannot publish it"
      contains: "probe-results.txt"
  key_links:
    - from: "scripts/windows-llrt-probe.mjs"
      to: "child process environment block"
      via: "spawn(process.execPath, ['-e', ...], { env: { SENTINEL } })"
      pattern: "env:\\s*\\{"
    - from: "scripts/windows-llrt-probe.mjs"
      to: "process exit code"
      via: "P0-ENV / P0-TMP / P1-CMD-indeterminate gate, flushed before exit"
      pattern: "process\\.exitCode"
    - from: "scripts/windows-llrt-probe.mjs"
      to: "bare module specifier resolution (P2-OS)"
      via: "deliberate un-prefixed import of the os module"
      pattern: "from \"os\""
---

<objective>
Create `scripts/windows-llrt-probe.mjs` — the zero-dependency Node ESM probe that answers all seven LLRT/Windows assertions (CI-02) — and add a targeted `.gitignore` entry for its local output file.

Purpose: This probe is the instrument the entire Windows port is calibrated against. Phases 4-8 pick their spawn and env-injection architecture from what it measures. Drift today never passes an `env` option to `spawn()` at all (both production call sites, `index.ts:1191` and `index.ts:2188`, pass only `stdio`; environment injection runs through a `#!/bin/bash` wrapper that `export`s variables and `exec`s). That wrapper cannot exist on Windows, so P0-ENV is not confirming an existing mechanism — it is proving the *replacement* mechanism works before code is written on top of it. That is why D-05 makes a P0-ENV failure a project-stopping blocker.

Output: `scripts/windows-llrt-probe.mjs` (new file in a new top-level directory — `scripts/` does not exist in this repo yet) and one line in `.gitignore`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-ci-spike-prove-llrt-basics-on-windows/03-CONTEXT.md
@.planning/phases/03-ci-spike-prove-llrt-basics-on-windows/03-PATTERNS.md
@.planning/phases/03-ci-spike-prove-llrt-basics-on-windows/03-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the seven-assertion probe with a four-surface spawn classifier</name>

  <files>scripts/windows-llrt-probe.mjs</files>

  <read_first>
    - `.planning/phases/03-ci-spike-prove-llrt-basics-on-windows/03-PATTERNS.md` — read in full. It contains the verbatim source excerpts for every pattern this task must copy (module shape, exit-flush, timeout discipline, the `settled`-flag spawn helper, the four-surface table) AND the binding "Stale Upstream Guidance" table at the end. Nine line-referenced items in `03-RESEARCH.md` must NOT be copied; four of the nine apply to this file.
    - `packages/backend/src/index.ts` lines 852-877 only (use Read with `offset: 852, limit: 26`) — the `settled` + `clearTimeout` + `SIGKILL` promise-wrapped spawn this probe's helper must replicate, and the exact `catch { /* ignore */ }` form that keeps `no-empty` quiet.
    - `packages/backend/src/index.ts` lines 1519-1530 only (use Read with `offset: 1519, limit: 12`) — `spawnAndWait`, the shipped helper that resolves on BOTH `close` and `error`.
    - `packages/backend/assets/mcp-server.mjs` lines 1-12 and 696-710 (two targeted reads) — the repo's only other hand-written standalone `.mjs`: header docblock shape, zero-dependency constraint, and the callback-awaited `process.stdout.write` that must precede `process.exit`.
    - `.planning/phases/03-ci-spike-prove-llrt-basics-on-windows/03-RESEARCH.md` lines 199-452 (the annotated probe skeleton) and lines 541-628 (Common Pitfalls 1-6, Assertion Map, Exit Code Policy). Treat the skeleton as a starting shape with four known defects, all named in the PATTERNS stale table — do not transcribe it.
  </read_first>

  <action>
    Create `scripts/windows-llrt-probe.mjs`. New top-level `scripts/` directory; create it as part of writing the file.

    HEADER. Shebang `#!/usr/bin/env node` (inert on Windows, present for consistency with `mcp-server.mjs`), then a block docblock stating: what the file is, that it is invoked as `node scripts/windows-llrt-probe.mjs`, that the runtime vehicle is **Node.js, not LLRT** and why (no standalone LLRT Windows binary exists — upstream dropped it at v0.6.0-beta and `caido/dependency-llrt` has no published releases), and that the file is deleted in Phase 9 when CI-01 lands (D-02).

    IMPORTS — zero npm dependencies, Node built-ins only. One import must be **bare and stay bare**: `import { tmpdir, platform } from "os";`. Put a comment directly above it saying the un-prefixed specifier is deliberate because P2-OS's entire assertion is that the bare specifier resolves — prefixing it to `node:os` would make P2-OS assert nothing. All other imports use the `node:` prefix (`node:child_process` for `spawn`, `node:fs` for `existsSync`/`writeFileSync`/`unlinkSync`, `node:path` for `join`, `node:crypto` for a named `randomUUID` import). Use the named `randomUUID` import rather than a default `crypto` import so the readonly global `crypto` is not shadowed — P3-UUID must be able to test the `globalThis.crypto` surface and the `node:crypto` surface separately.

    OUTPUT GRAMMAR — three emitters, `pass(id, detail)` / `fail(id, reason)` / `info(id, detail)`, producing exactly `PASS [<ID>]: <detail>`, `FAIL [<ID>]: <reason>`, `INFO [<ID>]: <detail>`. `fail` writes to stderr, the other two to stdout. Each `pass`/`fail` pushes `{ id, status, detail }` onto a results array. The seven IDs are `P0-ENV`, `P0-TMP`, `P1-CMD`, `P1-WHERE`, `P2-OS`, `P3-VARS`, `P3-UUID`; every one must appear exactly once as a `PASS`/`FAIL` (`INFO` lines are supplementary and unlimited).

    SPAWN HELPER — `spawnCapture(cmd, args, opts, timeoutMs)` returning a Promise that resolves (never rejects) to `{ surface, stdout, stderr, exitCode, error }` where `surface` is one of the four literals `"throw"`, `"error"`, `"close"`, `"timeout"`. Replicate `index.ts:852-875` exactly: a `settled` boolean guarding all paths, `clearTimeout(timeout)` on the `close` and `error` paths, and `try { child.kill("SIGKILL"); } catch { /* ignore */ }` on the timeout path. Wrap the `spawn()` call itself in `try`/`catch` and resolve with `surface: "throw"` — Node's CVE-2024-27980 guard throws **synchronously** out of `spawn()` for a `.cmd` target, so an async-only handler misses it entirely. Use `stdio: ["ignore", "pipe", "pipe"]`. Default timeout 5000 ms; P1-CMD passes 5000, P1-WHERE passes 5000. Never use a bare `catch (_) {}` — `no-empty` is configured `[2, { allowEmptyCatch: false }]` and an empty catch is a lint ERROR that would turn all four existing `ci.yml` legs red.

    P0-ENV (gates exit code — D-05). Build `sentinel` as the literal prefix `drift-probe-sentinel-` concatenated with `Date.now()`. Spawn `process.execPath` with `["-e", "process.stdout.write((process.env.SENTINEL || 'MISSING') + '|' + (process.env.PATH ? 'PATH-VISIBLE' : 'PATH-ABSENT'))"]` and `{ env: { SENTINEL: sentinel } }` — the `env` object carries that one key and nothing else. `process.execPath` is absolute so the child needs no `PATH` to start. Split the child's stdout on `|`: field 1 must equal `sentinel`; field 2 records whether the `env` option **replaced** the parent environment or merged into it. Emit that as an `INFO [P0-ENV]` line naming replace-vs-merge explicitly — Phases 4-8 need it to decide whether their spawn options must spread `...process.env`, and it is not recoverable from a bare PASS.
    On success: `pass("P0-ENV", ...)` naming the sentinel value and the replace/merge observation.
    On failure the result must be **conclusive about which mechanism broke**, because D-05 turns this into a blocker that halts Phase 4 and the blocker text has to name what is being decided. So on failure, retry once with `{ env: { ...process.env, SENTINEL: sentinel } }` and classify: if the merged retry succeeds, the `env` option is honoured but a fully-cleared environment block breaks the child on this host; if the merged retry also fails, the `env` option is not reaching the child at all. Emit `fail("P0-ENV", ...)` naming which of the two, plus the raw child stdout and any `error.code`/`error.message`. Do not swallow the distinction into a generic message.

    P0-TMP (gates exit code — D-06). Call `tmpdir()` and `platform()` (the bare-imported `os` functions — deliberately not `process.platform`). Emit an `INFO [P0-TMP]` line carrying both raw values. FAIL if `platform()` is not the literal `"win32"`, or `tmpdir()` does not match `/^[A-Za-z]:[\\/]/`, or `existsSync(tmpdir())` is false; PASS only when all three hold. Keep the platform comparison against the single literal `"win32"` on one line — plan 03-04 mutates exactly that literal to prove the gate bites, so it must be a single unambiguous occurrence.

    P1-CMD (conclusive-or-fail — D-07). Write a real `.cmd` into `tmpdir()` at `probe-test-<Date.now()>.cmd` containing `@echo off` and `echo CMD_PROBE_RAN` with CRLF line endings, then `spawnCapture(cmdPath, [], {}, 5000)`. Classify on the returned `surface`, and the classification — not the outcome — is what gates:
      - `"throw"` → conclusive. PASS, naming `spawn-threw-sync` and the raw `error.code` and `error.message` (EINVAL is expected on Node >= 18.20.2; EPERM/EBUSY from a Defender handle on a freshly written executable and ENOENT are equally conclusive).
      - `"error"` → conclusive. PASS, naming `spawn-error-event` and the raw code/message.
      - `"close"` with stdout containing `CMD_PROBE_RAN` → conclusive. PASS, naming `ran` and the exit code.
      - `"timeout"` → conclusive. PASS, naming `hung` and the timeout duration.
      - anything else — in particular a `"close"` whose stdout lacks the marker — is **not** one of D-07's three legitimate outcomes. `fail("P1-CMD", ...)` prefixed with the literal word `indeterminate`, carrying surface, exit code, and the raw stdout/stderr. This FAIL gates the exit code. Do not add a catch-all `pass()` — the superseded research skeleton calls `pass(...)` on every branch, which is precisely the defect D-07 exists to prevent.
    Delete the `.cmd` in a `finally` with `try { unlinkSync(cmdPath); } catch { /* ignore */ }`. Guard the file write itself so a write failure is reported as `indeterminate` rather than crashing the probe.

    P1-WHERE (informational, no gate — D-09). Resolve the binary by absolute path, `join(process.env.SystemRoot ?? "C:\\Windows", "System32", "where.exe")`, because Git Bash's PATH on `windows-latest` may not include System32. Spawn it with `["node"]`. Split stdout on `/\r?\n/`, trim, drop empties. Emit an `INFO [P1-WHERE]` line with the line count and the JSON-stringified lines (the CRLF-parse evidence Phase 6 needs), then PASS naming the first line and whether it ends in `.exe`, or FAIL naming the surface and error when the spawn did not produce output. Either way this assertion must not influence the exit code.

    P2-OS (informational + mandatory vehicle label — D-08). The bare `os` import at the top of the file having succeeded is the assertion; reaching this function proves it. PASS with a detail that ends in the exact literal parenthetical `(node-vehicle; LLRT resolver verified by source analysis)` — LLRT's resolver calls `name.trim_start_matches("node:")`, so `"os"` and `"node:os"` are the same module there. The label is load-bearing, not decorative: without it a reader of the archived artifact will believe LLRT itself was exercised.

    P3-VARS (informational, no gate — D-09). Check `USERPROFILE`, `APPDATA`, `LOCALAPPDATA`. Emit one `INFO [P3-VARS]` line per present variable carrying its value, then PASS if all three are non-empty or FAIL naming the missing ones. Print only these three variables by name — never iterate or dump `process.env`, whose contents on a hosted runner include tokens injected by the Actions runtime.

    P3-UUID (informational + mandatory vehicle label — D-08). Record `typeof globalThis.crypto?.randomUUID === "function"` and `typeof randomUUID === "function"` separately — LLRT and Node do not necessarily expose both surfaces, and Phase 4 needs to know which to target. PASS if either is true, FAIL if neither. The detail must end in the exact literal parenthetical `(node-vehicle; LLRT crypto surface unverified)` — unlike P2-OS there is no source-level LLRT confirmation for this one, and the label must not overstate.

    MAIN AND EXIT CONTRACT. Print a banner carrying `process.version`, `process.platform` and the vehicle caveat (pitfall 5: the `.cmd` EINVAL guard only exists on Node >= 18.20.2, so the version must be in the record). Run the assertions in ID order, awaiting the async ones. Print a `=== Summary ===` block re-emitting one `<STATUS> [<ID>]:` line per result — this block is what the findings document quotes and what the CI-side greps count, so all seven must appear here even if an earlier line went to stderr. Compute the exit code: 1 if `P0-ENV` failed, or `P0-TMP` failed, or `P1-CMD` failed; 0 otherwise. Explicitly do not let P1-WHERE, P2-OS, P3-VARS or P3-UUID influence it. Print a final `PROBE PASSED:` or `PROBE FAILED:` verdict line; on failure also print the documented fallback pointer (a minimal `.cmd` launcher that `set`s the variables, per the STATE.md blocker). Then flush before exiting: wrap the final `process.stdout.write` in a Promise and await its callback exactly as `mcp-server.mjs:699-706` does, and only then call `process.exit(code)`. A bare `process.exit()` truncates pending stdout writes, and under `2>&1 | tee probe-results.txt` on a Windows runner a truncated final line silently deletes the verdict D-13 requires; awaiting the last write drains everything ordered before it. Do not rely on setting `process.exitCode` and falling off the end — a stray un-cleared timer or a killed-but-not-reaped child would hang the job.

    STYLE. `pnpm format`'s glob is root-scoped (`packages/**/src/**` plus root-level `*.{ts,mjs}`) and does not reach `scripts/`, so Prettier will not touch this file. Deliberate decision: do **not** widen the format glob — hand-match Prettier defaults instead (2-space indent, double quotes, semicolons; no `.prettierrc` is committed). Rationale: widening the glob edits `package.json`, a file Phases 4-8 will touch, and enlarges parked backlog item 999.11's scope for a file that Phase 9 deletes. Formatting is not a gate either way — `eslint-config-prettier` is last in the flat config and disables every formatting rule. Use the repo's ASCII box-rule section headers (the `// ── Name ─────` form from `mcp-server.mjs:41`) to delimit the helper block and the seven assertion blocks, applied uniformly.
  </action>

  <verify>
    <automated>pnpm lint 2>&1 | tail -5; echo "lint-exit=$?"</automated>
    <automated>node scripts/windows-llrt-probe.mjs > /tmp/probe-local.txt 2>&1; echo "exit=$?"; for id in P0-ENV P0-TMP P1-CMD P1-WHERE P2-OS P3-VARS P3-UUID; do printf '%s -> ' "$id"; grep -cE "^(PASS|FAIL) \[$id\]:" /tmp/probe-local.txt; done</automated>
  </verify>

  <acceptance_criteria>
    - `pnpm lint` exits 0. This is the load-bearing one: ESLint's flat config ignores only `node_modules` and `dist`, so `scripts/*.mjs` IS linted by `eslint . --max-warnings 0`, and a lint error here turns all four existing `Verify (Node N)` legs red rather than the new workflow.
    - `grep -c 'catch (_)' scripts/windows-llrt-probe.mjs` returns 0 — the empty-binding catch form from the research skeleton is a `no-empty` error under `allowEmptyCatch: false`.
    - `grep -c 'from "os"' scripts/windows-llrt-probe.mjs` returns at least 1 — the bare specifier survived and P2-OS still asserts something.
    - `grep -c 'process.exit(' scripts/windows-llrt-probe.mjs` returns exactly 1, and the line immediately preceding its enclosing block awaits a `process.stdout.write` callback.
    - Running `node scripts/windows-llrt-probe.mjs` on the macOS dev host exits **1**, and the output contains exactly one `PASS [<ID>]:` or `FAIL [<ID>]:` summary line for each of the seven IDs. Exit 1 is the expected and correct local result: `os.platform()` returns `darwin`, so `FAIL [P0-TMP]` fires with a platform-mismatch reason. This local run is the phase's first falsifiability evidence — it proves the P0 gate actually drives the exit code before a Windows runner is ever burned.
    - In that same local run, `PASS [P0-ENV]` is present (POSIX `spawn` env passthrough works), and the accompanying `INFO [P0-ENV]` line states whether the `env` option replaced or merged the parent environment.
    - The `P2-OS` line ends with the literal `(node-vehicle; LLRT resolver verified by source analysis)` and the `P3-UUID` line ends with the literal `(node-vehicle; LLRT crypto surface unverified)`; `grep -c 'node-vehicle' scripts/windows-llrt-probe.mjs` returns at least 2 (D-08).
    - `grep -cE 'CAIDO_(TOKEN)|secret(s)\.' scripts/windows-llrt-probe.mjs` returns 0 (D-10). Both patterns are written so the pattern text does not match itself.
    - `git diff --name-only -- packages/` is empty — this phase touches zero files under `packages/*/src`.
  </acceptance_criteria>

  <done>
    `scripts/windows-llrt-probe.mjs` exists, passes `pnpm lint` at exit 0, emits all seven assertion IDs, and exits 1 on the darwin dev host because P0-TMP correctly rejects a non-`win32` platform.
  </done>
</task>

<task type="auto">
  <name>Task 2: Keep the probe's local output out of git</name>

  <files>.gitignore</files>

  <read_first>
    - `.gitignore` — the file being modified. Note it already carries `*.log` but not `*.txt`, and note the existing comment style: the `IMPROVEMENT-PLAN.md` entry documents the repo's stated rationale, "Ignored rather than merely untracked so a stray `git add -A` cannot publish them."
    - `.planning/phases/03-ci-spike-prove-llrt-basics-on-windows/03-PATTERNS.md` section `### Secrets and the sentinel (D-10)` — names this exact gap and its three options.
  </read_first>

  <action>
    Append a `probe-results.txt` entry to `.gitignore` with a short "why" comment above it, matching the phrasing style of the existing `IMPROVEMENT-PLAN.md` entry: the file is the probe's tee'd output, it is produced by any local `node scripts/windows-llrt-probe.mjs 2>&1 | tee probe-results.txt` run, and it is ignored rather than merely untracked so a stray `git add -A` cannot publish it. Mention in the comment that the durable record is the committed findings document (D-11), not this file, and that the CI copy is an expiring artifact.

    Chosen over the two alternatives PATTERNS lists: renaming the output to `probe-results.log` to catch the existing `*.log` rule would fight `03-RESEARCH.md` and `03-VALIDATION.md`, which both name `probe-results.txt`, and relying only on targeted `git add` discipline leaves the repo one careless command away from publishing an untracked file — the exact failure mode the `IMPROVEMENT-PLAN.md` entry was added to close. Do not add a broad `*.txt` rule; keep the ignore targeted.
  </action>

  <verify>
    <automated>node scripts/windows-llrt-probe.mjs 2>&1 | tee probe-results.txt > /dev/null; git status --porcelain probe-results.txt; git check-ignore -v probe-results.txt; rm -f probe-results.txt</automated>
  </verify>

  <acceptance_criteria>
    - `git check-ignore -v probe-results.txt` exits 0 and names `.gitignore` plus the new line number.
    - After creating `probe-results.txt` locally, `git status --porcelain probe-results.txt` produces no output — the file is not offered for staging.
    - `.gitignore` gains exactly one ignore pattern plus its comment; `git diff --stat .gitignore` shows no removals and no change to any pre-existing line.
    - No broad `*.txt` rule was introduced: `grep -c '^\*\.txt$' .gitignore` returns 0.
  </acceptance_criteria>

  <done>
    `probe-results.txt` is ignored by a targeted rule carrying its rationale, and creating the file locally leaves `git status` clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| probe process → public CI log / artifact | Anything the probe writes to stdout or stderr becomes world-readable on a public repository |
| probe process → child process environment block | The `env` option crosses into a spawned child; this is the mechanism under test |
| local working tree → public `origin` | Files that become tracked can be published by a later push |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | `scripts/windows-llrt-probe.mjs` sentinel handling | mitigate | The only secret-shaped value is `drift-probe-sentinel-<Date.now()>`, generated in-process. The probe never reads a Caido token and the file contains no reference to one — asserted by `grep -cE 'CAIDO_(TOKEN)|secret(s)\.'` returning 0 in Task 1's acceptance criteria, and re-asserted on every push by the workflow gate built in plan 03-02 (D-10) |
| T-03-02 | Information Disclosure | P3-VARS environment reporting | mitigate | The probe prints only `USERPROFILE`, `APPDATA` and `LOCALAPPDATA` by explicit name and never iterates or dumps `process.env`, which on a hosted runner carries Actions-injected tokens. Their values on `windows-latest` are runner-scoped paths under `C:\Users\runneradmin` and carry no user or org data |
| T-03-07 | Denial of Service | `spawnCapture` timeout discipline | mitigate | Every spawn is bounded by a 5000 ms wall-clock timeout with a `settled` flag on all four paths, `clearTimeout` on the `close` and `error` paths, and `SIGKILL` on the timeout path (the shipped `index.ts:852-875` shape). The probe exits via an explicit `process.exit` after a flushed final write rather than relying on the event loop draining, so a killed-but-unreaped child cannot hold a Windows runner open |
| T-03-08 | Tampering | evidence integrity of the probe's own output | mitigate | The final verdict write is callback-awaited before `process.exit`, so a truncated pipe cannot delete the verdict and leave a shorter, still-plausible-looking artifact. P1-CMD's unclassifiable branch fails rather than passes (D-07), so an unrecognised outcome cannot be laundered into a green result |
| T-03-09 | Information Disclosure | `probe-results.txt` in the working tree | mitigate | Targeted `.gitignore` entry (Task 2) so the file cannot be staged by a careless `git add -A`; the repo's stated posture for local artifacts on a public remote |
| T-03-SC | Tampering | npm/pnpm installs | mitigate | This plan installs nothing. The probe imports Node built-ins only, matching the precedent of `mcp-server.mjs`, whose 829 lines import exactly one module. `package.json` and `pnpm-lock.yaml` are untouched and are outside this plan's `files_modified` |
</threat_model>

<verification>
- `pnpm lint` exits 0 with `scripts/` present — the existing four-leg `ci.yml` matrix is not endangered by this phase.
- `pnpm -r typecheck` exits 0 (the `.mjs` probe is outside every `tsconfig` include, so this asserts nothing regressed).
- `node scripts/windows-llrt-probe.mjs` on darwin exits 1, emits all seven assertion IDs, and the sole gating failure is `P0-TMP` on the platform check.
- `git status --porcelain -- packages/` is empty — zero files under `packages/*/src` touched.
- `git check-ignore probe-results.txt` exits 0.
</verification>

<success_criteria>
A zero-dependency Node ESM probe exists at `scripts/windows-llrt-probe.mjs` that emits exactly one status line for each of the seven assertion IDs, gates its exit code on P0-ENV, P0-TMP and P1-CMD-indeterminacy only, labels P2-OS and P3-UUID as non-LLRT results, handles only a synthetic sentinel, flushes its verdict before exiting, and passes `pnpm lint` at exit 0. Its output file is ignored by a targeted `.gitignore` rule.
</success_criteria>

<output>
Create `.planning/phases/03-ci-spike-prove-llrt-basics-on-windows/03-01-SUMMARY.md` when done. Record the probe's local darwin exit code and the verbatim seven summary lines it produced — plan 03-05 uses them as the POSIX control against the Windows run.
</output>
