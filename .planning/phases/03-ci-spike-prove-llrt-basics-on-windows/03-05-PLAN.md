---
phase: 03-ci-spike-prove-llrt-basics-on-windows
plan: 05
type: execute
wave: 4
depends_on:
  - 03-03
  - 03-04
files_modified:
  - .planning/phases/03-ci-spike-prove-llrt-basics-on-windows/03-FINDINGS.md
  - .planning/STATE.md
autonomous: true
requirements:
  - CI-02

must_haves:
  truths:
    - "A committed findings document in the phase directory records all seven assertions with their result, the run URL, and the verbatim log line proving the mechanism — not a green tick (D-11)"
    - "Every line in the findings document is transcribed from the measured run output captured in plans 03-03 and 03-04; nothing is pre-filled, predicted, or paraphrased (D-12)"
    - "The findings document states which architecture each P1-CMD outcome selects for Phases 4-8, so it can be cited as the canonical basis by a later PLAN.md after the CI artifact expires"
    - "The P2-OS and P3-UUID rows carry their non-LLRT vehicle labels, so a reader months later cannot mistake a Node result for an LLRT result (D-08)"
    - "Both falsifiability proofs are recorded — the probe's failure path and the D-10 gate's failure path — because the gate is the only part of this phase that survives Phase 9 and an unfalsified gate would be the weakest link in the evidence chain"
    - "The STATE.md blocker recording that LLRT spawn env-passthrough on Windows is unverified is rewritten in place with the measured outcome and an inline run URL — RESOLVED if P0-ENV passed, or an explicit blocker halting Phase 4 until the .cmd-launcher fallback is decided if it failed (D-05)"
    - "The residual risk that Node is the vehicle rather than LLRT is stated in the verdict rather than buried, since it is the one thing this phase could not close"
  artifacts:
    - path: .planning/phases/03-ci-spike-prove-llrt-basics-on-windows/03-FINDINGS.md
      provides: "The durable, citable verdict for CI-02: seven assertions with results, verbatim evidence lines, run URLs, and the architectural consequence of each for Phases 4-8"
      contains: "P0-ENV"
      min_lines: 60
    - path: .planning/STATE.md
      provides: "The Phase 3 outcome fed back into project state, with the P0-ENV blocker resolved or re-opened per D-05"
      contains: "env-passthrough"
  key_links:
    - from: ".planning/phases/03-ci-spike-prove-llrt-basics-on-windows/03-FINDINGS.md"
      to: "the recorded GitHub Actions runs"
      via: "inline run URLs cited per assertion"
      pattern: "https://github.com/six2dez/drift/actions/runs/"
    - from: ".planning/STATE.md blocker entry"
      to: "Phase 4 gating decision"
      via: "RESOLVED or OPEN prefix with the measured outcome and run URL"
      pattern: "(RESOLVED|OPEN).*env-passthrough"
---

<objective>
Write the committed verdict for CI-02 and feed it back into project state.

Purpose: The CI artifact expires after 30 days and cannot be cited from a `PLAN.md`. Phases 4-8 need this verdict as the canonical basis for their spawn and env-injection architecture, so it has to live in the repository (D-11). It applies decision `[01-06]` verbatim: a CI claim is recorded with its run URL, per-leg conclusion, per-step conclusion and the log line proving the mechanism — never just a green tick.

The order matters and is not negotiable. The document is written **after** reading the real run, from measured output (D-12). Nothing here is pre-filled from a template before results exist. Every row is transcribed from `03-03-SUMMARY.md`, `03-04-SUMMARY.md` and the run records they cite. If a value is not in those sources, it does not go in the document — it goes in an "unmeasured" row.

Output: `03-FINDINGS.md` in the phase directory, and a rewritten blocker entry in `.planning/STATE.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-ci-spike-prove-llrt-basics-on-windows/03-03-SUMMARY.md
@.planning/phases/03-ci-spike-prove-llrt-basics-on-windows/03-04-SUMMARY.md
@.planning/phases/01-restore-the-verification-signal/01-06-SUMMARY.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write 03-FINDINGS.md from the measured run output</name>

  <files>.planning/phases/03-ci-spike-prove-llrt-basics-on-windows/03-FINDINGS.md</files>

  <read_first>
    - `.planning/phases/03-ci-spike-prove-llrt-basics-on-windows/03-03-SUMMARY.md` — the authoritative run, its URL, per-step conclusions, probe exit code, and the seven verbatim assertion lines. This is the primary source and the only place the seven results exist.
    - `.planning/phases/03-ci-spike-prove-llrt-basics-on-windows/03-04-SUMMARY.md` — the two falsifiability runs (probe failure path and D-10 gate failure path) and the teardown assertions.
    - `.planning/phases/03-ci-spike-prove-llrt-basics-on-windows/03-02-SUMMARY.md` — the static half of the evidence: pin comparison against `ci.yml`, parsed trigger mapping, parsed step names, and the three-branch gate structural counts.
    - `.planning/phases/01-restore-the-verification-signal/01-06-SUMMARY.md` — the format analog (`03-PATTERNS.md` names it as such). Copy two structures: the run table at its lines 66-70 (purpose, branch, commit, run URL, conclusion) and the per-leg conclusion tables at lines 74-81, whose defining property is that conclusions are read from the API and not from the YAML.
    - `.planning/phases/03-ci-spike-prove-llrt-basics-on-windows/03-RESEARCH.md` lines 605-628 — the interpretation tables mapping each P1-CMD and P1-WHERE outcome to its Phase 4+ consequence. Use these to fill the consequence column, but only for the outcome that actually occurred.
    - `.planning/phases/03-ci-spike-prove-llrt-basics-on-windows/03-CONTEXT.md` — D-08 for the mandatory vehicle labels, D-11 for what the document must record, and the `### Claude's Discretion` block for the accepted LLRT fidelity ceiling, which the residual-risk section must state accurately rather than overstate.
  </read_first>

  <action>
    Create `.planning/phases/03-ci-spike-prove-llrt-basics-on-windows/03-FINDINGS.md`. Filename and internal structure are the planner's choice under D-11; this structure is the choice. Write it as a standalone evidence document rather than a plan SUMMARY, since it is cited by later phases rather than consumed by the phase machinery.

    OPENING VERDICT. One paragraph stating, in measured terms, what the phase established: which of the seven assertions passed, whether the direct-spawn plus env-injection architecture is confirmed for Phases 4-8 or whether the `.cmd`-launcher fallback is now required, and which P1-CMD surface fired. Lead with the consequence, not the process.

    VEHICLE CAVEAT, stated before any result table so it frames everything below it. The probe ran on Node.js, not LLRT. No standalone LLRT Windows binary exists — upstream dropped it at v0.6.0-beta and `caido/dependency-llrt` publishes no releases — and Caido headless in CI requires a paid Teams plan. Per assertion, state the fidelity basis honestly: P0-ENV and P0-TMP are faithful because LLRT's `child_process` uses `tokio::process::Command` reaching the same Windows `CreateProcess` that Node reaches through libuv, and LLRT's `env` option calls `env_clear()` then `envs()` with no Windows-specific branch; P2-OS is closed by source analysis of LLRT's resolver, which calls `name.trim_start_matches("node:")`; P3-UUID has no such source-level confirmation and is labelled accordingly. State that the user accepted this ceiling as-is and that real-machine confirmation from the original Windows reporter was considered as a completion gate and declined, because it depends on a third party and could block Phase 4 indefinitely — it belongs to Phase 9 or 10.

    Note in the same section that the ROADMAP's success criterion for this phase phrases the tmpdir/platform result as holding "inside the Caido backend runtime", whereas what was measured is Node on `windows-latest`. State the gap plainly so nobody later reads that criterion as literally met, and name D-08's labelling as the adopted mitigation.

    RUN TABLE, in the `01-06-SUMMARY.md` shape: one row per run with purpose, branch (marked deleted), commit SHA, full run URL, and conclusion. At minimum three rows — the authoritative run, the probe-failure falsifiability run, and the D-10-gate falsifiability run — plus any re-run that occurred. Mark both falsifiability runs' `failure` conclusions as intended, the way `01-06` marks its runs 2 and 3, so a future reader does not read a red run as an unresolved problem.

    PER-ASSERTION EVIDENCE TABLE — the core of the document. One row for each of the seven IDs in order: `P0-ENV`, `P0-TMP`, `P1-CMD`, `P1-WHERE`, `P2-OS`, `P3-VARS`, `P3-UUID`. Columns: assertion ID, priority, result, **the verbatim probe output line**, and the consequence for Phases 4-8. The verbatim line is the load-bearing column — it is `[01-06]`'s "log line proving the mechanism", and it is what makes this document evidence rather than a summary. Do not paraphrase, reflow, or trim these lines. The `P2-OS` and `P3-UUID` rows must retain their `(node-vehicle; ...)` labels intact (D-08); if a label is missing from the captured line, that is a defect to record, not to silently repair.

    Include the `INFO` observations that carry architectural weight, in particular the `P0-ENV` replace-versus-merge line. Phase 4 needs to know whether passing an `env` option replaces the child's entire environment or merges into it, because that determines whether its spawn options must spread `...process.env`. Also record the banner's `process.version`, since the `.cmd` EINVAL guard P1-CMD characterises only exists on Node >= 18.20.2 and the result is meaningless without knowing the runtime version.

    EXIT-CODE CONTRACT AS MEASURED. State the policy — P0-ENV, P0-TMP and an indeterminate P1-CMD gate; P1-WHERE, P2-OS, P3-VARS and P3-UUID do not (D-05, D-06, D-07, D-09) — and then record the exit code the run actually produced and which condition drove it.

    FALSIFIABILITY SECTION — two proofs, recorded separately. This is what distinguishes the document from a green tick.

    First, the probe's failure path. Record that a one-character mutation of the P0-TMP platform literal produced a `failure` conclusion at the `Run LLRT Windows primitive probe` step, that `Upload probe results` still concluded `success` on that failing run, that the artifact still carried exactly one summary line for each of the seven assertion IDs, and that the four `Verify (Node N)` legs stayed green on the same commit. State plainly what this closes: `03-RESEARCH.md` assumption A2 and Pitfall 1 — that `tee` could swallow the probe's exit code and leave the job green with a `FAIL` line in the artifact — are now closed by measurement rather than by reasoning about how GitHub maps `shell: bash`.

    Second, the D-10 gate's failure path. Record that a one-line self-labelling canary in a workflow comment produced a `failure` conclusion at the `Assert no secret material is reachable (D-10)` step, that `Run LLRT Windows primitive probe` concluded `skipped`, and that `Upload probe results` concluded `failure` because no results file existed. State what each closes: the gate is proven to bite, which matters more than any other proof here because the gate is the one thing from this phase that Phase 9 does not delete and Phases 4-8 rely on it silently; and the failing upload is the phase's only observation distinguishing `if-no-files-found: error` from `warn`, closing the "a missing results file fails the job rather than passing quietly" claim. Note that the canary named a nonexistent secret and lived only on a deleted branch, so the public run record is self-labelling and carries no real secret material.

    HAND-OFF TO PHASE 4. State the architectural decision each result forces, concretely enough to be cited from a future `PLAN.md`: what P0-ENV's outcome means for the env-injection path that replaces today's `#!/bin/bash` wrapper (noting that Drift's two production `spawn()` call sites, `index.ts:1191` and `index.ts:2188`, pass only `stdio` today and never an `env` option, so this is a new mechanism rather than a confirmed existing one); what the observed P1-CMD surface means for whether a `cmd.exe /c` branch is mandatory; what P1-WHERE's output shape means for replacing the `which` call at `index.ts:852-875` with `where.exe`; and what P3-UUID means for the custom hex-loop UUID generator.

    RESIDUAL RISK. State the one thing this phase could not close: Caido's `dependency-llrt` is a maintained fork that could have diverged from upstream `awslabs/llrt` with custom shims over `child_process`, `os`, or the module resolver. Name where it gets closed — the original reporter or a real Windows Caido installation confirming a later phase's build, deferred to Phase 9 or 10.

    TEARDOWN AND HYGIENE. Record that all three scratch branches were deleted and that the assertion was a `scratch/*` glob backed by a full unfiltered remote listing, and that all three run records survive the branch deletion so the URLs cited above remain valid.

    Close with the D-02 reminder: `.github/workflows/windows-llrt-probe.yml` and `scripts/windows-llrt-probe.mjs` are deleted in Phase 9 when CI-01 lands the permanent regression job. This document is what survives them — along with the D-10 property the gate was keeping true, which Phase 9's replacement job must carry forward.

    If any required value is missing from the source summaries, write an explicit "not measured" row naming what is missing and why. Do not infer it, and do not fill it from `03-RESEARCH.md`'s predictions — the whole point of D-12 is that this document reports what happened rather than what was expected.
  </action>

  <verify>
    <automated>test -f .planning/phases/03-ci-spike-prove-llrt-basics-on-windows/03-FINDINGS.md || { echo "FINDINGS-MISSING"; exit 1; }; for id in P0-ENV P0-TMP P1-CMD P1-WHERE P2-OS P3-VARS P3-UUID; do printf '%s -> ' "$id"; grep -c "$id" .planning/phases/03-ci-spike-prove-llrt-basics-on-windows/03-FINDINGS.md; done</automated>
    <automated>printf 'run-urls='; grep -c 'https://github.com/six2dez/drift/actions/runs/' .planning/phases/03-ci-spike-prove-llrt-basics-on-windows/03-FINDINGS.md; printf 'vehicle-labels='; grep -c 'node-vehicle' .planning/phases/03-ci-spike-prove-llrt-basics-on-windows/03-FINDINGS.md; printf 'gate-proof='; grep -c 'if-no-files-found' .planning/phases/03-ci-spike-prove-llrt-basics-on-windows/03-FINDINGS.md</automated>
  </verify>

  <acceptance_criteria>
    - All seven assertion IDs appear in the document, each with a result and a verbatim probe output line in its evidence column.
    - `grep -c 'https://github.com/six2dez/drift/actions/runs/'` returns at least 3 — the authoritative run, the probe-failure falsifiability run and the D-10-gate falsifiability run are each cited by full URL, not by run number alone.
    - `grep -c 'node-vehicle'` returns at least 2: the `P2-OS` and `P3-UUID` rows retain their D-08 labels verbatim.
    - Every verbatim evidence line in the document appears byte-identically in `03-03-SUMMARY.md` or `03-04-SUMMARY.md`. Spot-check at least the `P0-ENV` and `P1-CMD` lines by diffing the quoted text against the source summary; a reflowed or paraphrased line means the document drifted from the measurement.
    - The falsifiability section records **both** proofs: the probe run's `failure` conclusion with its failing step by name, its `Upload probe results` conclusion and the four green `Verify (Node N)` legs; and the gate run's `failure` conclusion at the D-10 step, the `skipped` probe step, and the `failure` upload step identified as the `if-no-files-found: error` proof.
    - The document states the probe's measured exit code and which gating condition produced it.
    - The hand-off section names a concrete architectural consequence for Phases 4-8 for each of P0-ENV, P1-CMD, P1-WHERE and P3-UUID — not a restatement of the result.
    - The vehicle-caveat section states the ROADMAP-criterion gap explicitly: the tmpdir/platform result was measured under Node on `windows-latest`, not inside the Caido backend runtime.
    - No value in the document is absent from the source summaries. Any gap is an explicit "not measured" row (D-12).
  </acceptance_criteria>

  <done>
    `03-FINDINGS.md` exists in the phase directory, records all seven assertions with verbatim evidence lines and run URLs, states both falsifiability proofs and the residual LLRT-fidelity risk, and gives Phases 4-8 a citable architectural basis that outlives the expiring CI artifact.
  </done>
</task>

<task type="auto">
  <name>Task 2: Feed the verdict back into STATE.md, resolving or re-opening the P0-ENV blocker</name>

  <files>.planning/STATE.md</files>

  <read_first>
    - `.planning/STATE.md` — the file being modified. Read `### Blockers/Concerns` in full. The entry to edit is the one reading "LLRT `spawn({env})` env-passthrough on Windows is unverified (P0 risk). Phase 3 resolves it; if it fails, the documented fallback is a minimal `.cmd` launcher that `set`s env vars." That entry is P0-ENV, and D-05 makes it the one this phase must settle.
    - `.planning/STATE.md` **lines 88-89 and 93-94** — the four entries in `### Blockers/Concerns` that actually carry the format convention: a `RESOLVED <date> (plan NN-NN):` or `OPEN ...` prefix, then the measured fact, then an inline markdown link to the run URL. Read those four, not "the surrounding entries": the entries immediately adjacent to the P0-ENV blocker are line 90 (`check_scope`) and line 92 (Gemini-on-Windows), and **neither carries a prefix at all**, so adjacency is the wrong selector here. Line 88 (`RESOLVED 2026-08-12 (plan 01-06)`) and line 89 (`OPEN DECISION (user, outside git)`) are the two this task's action names directly as the shape to follow for each branch; lines 93-94 are two further `RESOLVED` entries confirming the prefix-plus-measured-fact pattern is the section's convention rather than a one-off. Blocker entries are rewritten in place, not appended to and not duplicated.
    - `.planning/phases/03-ci-spike-prove-llrt-basics-on-windows/03-FINDINGS.md` — just written; the source for every value used here.
  </read_first>

  <action>
    Rewrite the P0-ENV blocker entry in place and record the phase outcome. Take one of two branches based on the measured P0-ENV result — the branch is determined by the run, not chosen.

    IN BOTH BRANCHES, the rewritten entry must contain the literal hyphenated token `env-passthrough`. The existing entry's `spawn({env})` literal does not survive a full rewrite, and that literal is what the entry is currently greppable by; `env-passthrough` appears in the current text too, is branch-independent, and reads naturally in both the resolved and open phrasings. Keeping one stable token is what lets a future planner or checker locate this entry without knowing which branch was taken.

    IF P0-ENV PASSED. Rewrite the entry as `RESOLVED <date> (plan 03-05):` followed by the measured fact and an inline link to the run URL. State that Windows `spawn` env-passthrough — `spawn(node, [script], { env })` — was proven on a real `windows-latest` host to pass the environment to the child, quote the verbatim `PASS [P0-ENV]` line, and record the replace-versus-merge observation, because that detail is what Phase 4 actually builds against. State that the `.cmd`-launcher fallback named in the original entry is therefore not required, and that the vehicle was Node rather than LLRT with the fidelity basis named in one clause. Follow the shape of the existing `RESOLVED 2026-08-12 (plan 01-06)` entry, whose defining feature is that it records how the closure was proven rather than merely asserting it.

    IF P0-ENV FAILED. Rewrite the entry as an explicit `OPEN` blocker, not a resolution. It must state: that Windows `spawn` env-passthrough via the `env` option failed on a real `windows-latest` host; which of the two failure modes the probe reported, since the probe was built to discriminate them; the verbatim `FAIL [P0-ENV]` line; the inline run URL; and — the operative part under D-05 — that **Phase 4 does not start until the fallback is decided**, that fallback being a minimal `.cmd` launcher that `set`s the variables before invoking Node. Frame it as a decision the user must make, in the style of the existing `OPEN DECISION` entry, rather than as a task that can be silently picked up. Drift's entire env-injection architecture rests on this, and today's `#!/bin/bash` wrapper cannot exist on Windows, so there is no fallback already in place to quietly fall back to.

    IN BOTH CASES, also do the following.

    Add a `[03-05]` entry to `### Decisions` under `## Accumulated Context` recording the architectural consequence of the measured P1-CMD surface for Phases 4-8 — this is ROADMAP success criterion 4, results feeding back before Phase 4 begins — with a pointer to `03-FINDINGS.md` as the canonical citable source. Keep it to the one or two sentences a future planner needs, in the style of the existing `[01-06]` entries.

    Update `## Current Position` and the `Last activity` line to reflect that Phase 3's CI spike is complete with its evidence recorded. Do not hand-edit the `progress:` front-matter block; it is clobbered by every SDK writer and the file's own comment says to re-correct it after those run, not to maintain it by hand here.

    Add a one-line note that the probe workflow and script are deleted in Phase 9 when CI-01 lands (D-02), so the debt has a due date visible from project state rather than only from a comment inside a file that Phase 9 will delete. Include in that line that the D-10 no-secret-material gate is the one property from this phase that must survive the deletion, so Phase 9's replacement job carries it forward rather than dropping it.

    Leave every other blocker entry untouched. In particular do not alter the `check_scope` entry, the branch-protection open decision, or the Gemini-on-Windows entry — none of them is in this phase's scope.
  </action>

  <verify>
    <automated>printf 'env-passthrough-token='; grep -c 'env-passthrough' .planning/STATE.md; grep -nE '^\- (RESOLVED|OPEN).*env-passthrough' .planning/STATE.md; printf 'stale-phrase='; grep -c 'is unverified (P0 risk)' .planning/STATE.md; printf 'findings-pointer='; grep -c '03-FINDINGS.md' .planning/STATE.md</automated>
    <automated>git diff --stat .planning/STATE.md; printf 'check_scope='; grep -c 'check_scope' .planning/STATE.md; printf 'open-decision='; grep -c 'OPEN DECISION (user, outside git)' .planning/STATE.md; printf 'run-url='; grep -c 'https://github.com/six2dez/drift/actions/runs/' .planning/STATE.md</automated>
  </verify>

  <acceptance_criteria>
    - The P0-ENV blocker entry is rewritten in place: `grep -c 'is unverified (P0 risk)' .planning/STATE.md` returns 0, and exactly one entry now covers Windows spawn env-passthrough. The entry count in `### Blockers/Concerns` is unchanged — the entry was rewritten, not duplicated or appended to.
    - `grep -c 'env-passthrough' .planning/STATE.md` returns at least 1, and `grep -nE '^\- (RESOLVED|OPEN).*env-passthrough'` matches the rewritten entry. This token is branch-independent and is what keeps the entry locatable after the rewrite; the old `spawn({env})` literal is not required to survive.
    - The rewritten entry begins with `RESOLVED <date> (plan 03-05):` or `OPEN`, matching the section's existing convention, and contains an inline markdown link to a `https://github.com/six2dez/drift/actions/runs/` URL.
    - The entry quotes the verbatim `PASS [P0-ENV]` or `FAIL [P0-ENV]` line from the run.
    - If P0-ENV failed, the entry states explicitly that Phase 4 does not start until the `.cmd`-launcher fallback is decided (D-05), and is phrased as a user decision rather than a queued task.
    - `grep -c '03-FINDINGS.md' .planning/STATE.md` returns at least 1 — project state points at the citable verdict.
    - A `[03-05]` entry exists in `### Decisions` recording the P1-CMD architectural consequence for Phases 4-8.
    - The Phase 9 deletion note is present and names the D-10 gate property as the thing that must survive the deletion.
    - `git diff .planning/STATE.md` shows no change to the `check_scope` entry, the branch-protection `OPEN DECISION` entry, the Gemini-on-Windows entry, or the `progress:` front-matter block.
  </acceptance_criteria>

  <done>
    STATE.md's Windows env-passthrough blocker is settled in place with the measured outcome, its verbatim evidence line and its run URL, and remains greppable by the stable `env-passthrough` token; the P1-CMD consequence is recorded as a decision for Phases 4-8; project state points at `03-FINDINGS.md`; and no unrelated entry was disturbed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| measured run output → committed project record | Everything downstream phases build on flows through this transcription step; a paraphrase here becomes fact for Phases 4-8 |
| findings document → future PLAN.md citations | Later phases cite this document as their architectural basis long after the CI artifact has expired |
| STATE.md blocker → phase gating | The blocker entry is the mechanism that stops Phase 4 when P0-ENV fails |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-16 | Tampering | transcription drift between the run output and the committed verdict | mitigate | Every evidence line is copied verbatim and spot-checked byte-for-byte against `03-03-SUMMARY.md` and `03-04-SUMMARY.md`; any value absent from those sources is recorded as an explicit "not measured" row rather than inferred from `03-RESEARCH.md`'s predictions (D-12) |
| T-03-17 | Repudiation | a Node-vehicle result later being read as an LLRT result | mitigate | The vehicle caveat is stated before the first result table, and the `P2-OS` and `P3-UUID` rows retain their `(node-vehicle; ...)` labels verbatim (D-08). Per-assertion fidelity basis is stated honestly: source-verified for P0-ENV, P0-TMP and P2-OS; explicitly unverified for P3-UUID. The same section records that the ROADMAP criterion's "inside the Caido backend runtime" phrasing was not literally met |
| T-03-18 | Elevation of Privilege | a failed P0-ENV being downgraded to a note and Phase 4 proceeding on an unproven mechanism | mitigate | The failure branch is written into the plan in advance, is selected by the measured result rather than by judgement at write time, and must state that Phase 4 does not start until the fallback is decided (D-05). Drift has no existing `env`-option path to fall back on — both production `spawn()` call sites pass only `stdio` |
| T-03-19 | Tampering | collateral edits to unrelated STATE.md entries | mitigate | The plan names the four regions that must not change — the `check_scope` entry, the branch-protection open decision, the Gemini-on-Windows entry, and the `progress:` front-matter block — and the acceptance criteria assert the diff leaves them untouched |
| T-03-22 | Repudiation | the rewritten blocker becoming unfindable because the rewrite destroyed the token it was greppable by | mitigate | Both rewrite branches are required to carry the literal `env-passthrough`, a token present in the current text and natural in both phrasings, and the plan's `must_haves` and `key_links` key on that token rather than on the `spawn({env})` literal the rewrite removes |
| T-03-01 | Information Disclosure | secret material reaching a committed document | mitigate | The only secret-shaped values anywhere in this phase are the synthetic `drift-probe-sentinel-<timestamp>` and the deliberately nonexistent `DRIFT_PROBE_GATE_CANARY` name used to falsify the D-10 gate. Quoting either verbatim is safe by construction, and no real token exists anywhere in the evidence chain |
| T-03-SC | Tampering | npm/pnpm installs | mitigate | Nothing is installed. This plan writes two documentation files only |
</threat_model>

<verification>
- `03-FINDINGS.md` exists and contains all seven assertion IDs with verbatim evidence lines.
- At least three full run URLs are cited, and the `node-vehicle` labels survive on the P2-OS and P3-UUID rows.
- Both falsifiability proofs are recorded — the probe failure path and the D-10 gate failure path with its `if-no-files-found: error` consequence.
- Quoted evidence lines match their source summaries byte-for-byte on spot-check.
- STATE.md's Windows env-passthrough blocker is rewritten in place with a `RESOLVED`/`OPEN` prefix, an inline run URL, the verbatim P0-ENV line, and the stable `env-passthrough` token.
- A `[03-05]` decision entry records the P1-CMD consequence for Phases 4-8.
- `git diff .planning/STATE.md` shows no change to unrelated blocker entries or to the `progress:` block.
</verification>

<success_criteria>
CI-02 has a durable, citable verdict. All seven assertions are recorded with their result, the run URL and the verbatim log line proving the mechanism; both falsifiability proofs and the residual LLRT-fidelity risk are stated rather than implied; the architectural consequence for Phases 4-8 is concrete enough to cite from a later PLAN.md; and project state either records the env-passthrough mechanism as proven or halts Phase 4 pending the fallback decision, per D-05.
</success_criteria>

<output>
Create `.planning/phases/03-ci-spike-prove-llrt-basics-on-windows/03-05-SUMMARY.md` when done.

Record which D-05 branch was taken and why, the verbatim P0-ENV line that drove it, and the exact STATE.md entry text before and after the rewrite.
</output>
