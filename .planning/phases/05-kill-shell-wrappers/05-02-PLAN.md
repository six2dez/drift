---
phase: 05-kill-shell-wrappers
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - .gitattributes
  - CLAUDE.md
  - .github/workflows/ci.yml
  - .github/scripts/check-ci-windows-job.sh
  # Reachable ONLY through task 1's Tier 2 zip-parity contingency (widen the assets
  # glob). Declared up front so the contingency does not require a mid-execution plan
  # edit. Expected outcome is that this file is untouched. No sibling plan claims it,
  # so declaring it creates no wave-1 file conflict.
  - caido.config.ts
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [CI-01, CI-03, CMP-01]

estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`pnpm build` runs on a shell that exists on windows-latest — the POSIX tail (cp/rm/cd/zip) is gone and `caido-dev build` alone produces dist/plugin_package.zip (CI-01, V-18's precondition)."
    - "The reduced build script produces a zip whose entry-name set and per-entry byte lengths are identical to the pre-change script's, measured on this checkout rather than trusted from RESEARCH.md (CMP-01, assumption A7)."
    - "`.gitattributes` exists at the repo root with `* text=auto eol=lf` (CI-03, V-15)."
    - "A zip-parity mismatch has a named route rather than a dead end: task 1's three-tier contingency ladder (cosmetic cruft / widen the assets glob / escalate). CI-01 and V-18 hang entirely on a portable `pnpm build` and plan 05-06 is blocked behind this plan, so an unrouted STOP would strand two requirements."
    - "`.github/workflows/ci.yml` declares a `windows-latest` job that runs install → typecheck → lint → test → build with the same action pins as the ubuntu `verify` job, `pnpm/action-setup@v6` ahead of `actions/setup-node@v5`, and no `package-manager-cache: false` (CI-01)."
    - "The Windows job carries the D-10 no-secret-material gate in its THREE-branch form: grep status 0 fails, 1 passes, anything else including a missing or unreadable target fails (ROADMAP Phase 9 SC-1a, pulled forward)."
    - "The Windows job is a blocking leg — no `continue-on-error`, no `if:` that would let it skip (D-09)."
    - "CI-01 and CI-03 read `Phase 5` in REQUIREMENTS.md's traceability table AND in both phase rollups, with a dated paragraph explaining the pull-forward — the same structural move that re-targeted RUN-04 at the close of Phase 4 (D-07/D-09)."
  artifacts:
    - .gitattributes
    - .github/workflows/ci.yml
    - package.json
    - .planning/REQUIREMENTS.md
  key_links:
    - "ci.yml's new job consumes `pnpm build`, which only works on Windows because package.json's build script was reduced in the same plan. Landing the job without the script change makes the leg red at its Build step for a reason unrelated to Phase 5's code."
    - ".github/workflows/release.yml consumes dist/plugin_package.zip and reads manifest.json out of it with `unzip -p` — it depends on the artifact, not on how it was zipped."
    - "The no-secret-material gate's scan-target list keeps windows-llrt-probe.yml in it deliberately, so Phase 9's deletion of that file trips the gate's third arm loudly instead of silently narrowing the scan."
  prohibitions:
    - "Never add a GitHub `secrets.` expression or a Caido token reference to any scanned workflow file — the gate this plan installs exists to catch exactly that, and it scans itself."
    - "Never make the Windows leg non-blocking with `continue-on-error` or a conditional `if:`. A job nobody must heed is the weakness D-07 already rejected one layer up."
    - "Never copy `windows-llrt-probe.yml`'s `package-manager-cache: false` into this job. That line exists only because the probe deliberately installs nothing; this job installs."
    - "Never collapse the secret gate's third arm into the second. grep exits 2 on a missing or unreadable target, and an if/else routes that into the pass arm."
    - "Never claim a CI result in this plan. This plan AUTHORS the leg; plan 05-06 runs it and records the run URL, the per-step conclusions and the log line proving the mechanism."
    - "Never run `git add -A`. Every stage is a targeted `git add <path>`."
    - "Never add a packaging dependency (`archiver`, `adm-zip`, `jszip`, or a hand-rolled `scripts/pack.mjs`) to rescue a zip-parity mismatch. That is a Tier 3 escalation to the developer: this phase installs nothing per 05-RESEARCH § Package Legitimacy Audit, and `release.yml` signs the resulting bytes."
    - "Never re-add a file-copy step to `package.json`'s build script to rescue a missing asset. The Tier 2 fix widens `caido.config.ts`'s assets glob instead — a copy step re-imports the exact POSIX portability problem this task removes."
---

<objective>
Land the permanent `windows-latest` CI net (CI-01) and the line-ending guard (CI-03) that D-07 and
D-09 pull forward from Phase 9, and remove the one POSIX shell one-liner that makes `pnpm build`
impossible on a Windows runner.

Purpose: Phase 5 is the first phase with real Windows behaviour to protect and the maintainer
cannot test native Windows locally. Every later phase inherits this net. The build-script blocker is
what decides whether this is a cheap slice or a Phase-9-sized job — and RESEARCH § Finding C-1
measured that the POSIX tail is a no-op, so the fix is a deletion.

Output: a blocking Windows leg in `ci.yml`, a portable `build` script, `.gitattributes`, and the
requirements-table ownership of CI-01/CI-03.

**This plan does not run CI.** It authors the leg and proves everything provable locally. Plan 05-06
pushes, reads the real run, and records the evidence — because the authoritative run must happen
after 05-01's new spawn test and 05-04/05-05's rewrite have landed.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-kill-shell-wrappers/05-CONTEXT.md
@.planning/phases/05-kill-shell-wrappers/05-RESEARCH.md
@.planning/phases/05-kill-shell-wrappers/05-PATTERNS.md
@.github/workflows/ci.yml
@.github/workflows/windows-llrt-probe.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prove Finding C-1 on this checkout, then reduce the build script and land .gitattributes</name>
  <files>package.json, .gitattributes, CLAUDE.md</files>
  <precondition>`zip` and `unzip` are available on the maintainer's host — they are needed to produce and compare the BEFORE artifact, and RESEARCH § Environment Availability confirms `zip` is present locally.</precondition>
  <read_first>
    - package.json line 26 (the current `build` script, verbatim)
    - caido.config.ts (the `assets` glob at line 30 — the reason the POSIX tail is redundant)
    - .github/workflows/release.yml lines 40-80 (how the release consumes dist/plugin_package.zip and reads manifest.json out of it)
    - .planning/phases/05-kill-shell-wrappers/05-RESEARCH.md § Finding C-1 and § Assumptions Log rows A4 and A7
    - CLAUDE.md § Configuration (the stale `dist/drift.zip` "final deliverable" line)
    - .gitignore (confirm `dist/` is ignored before generating artifacts)
  </read_first>
  <action>
Measure before you change anything, because assumption A7 says the entry-name and byte-length parity
was measured in the research session and must be re-verified here rather than trusted.

Step 1 — capture the BEFORE artifact. Remove `dist`, run the current `pnpm build` unchanged, and
copy `dist/plugin_package.zip` to a scratch path outside the repo. Record `unzip -Z1` sorted entry
names and `unzip -l` byte lengths.

Step 2 — capture the AFTER artifact. Remove `dist`, run `pnpm exec caido-dev build` alone, and copy
`dist/plugin_package.zip` to a second scratch path. Record the same two listings.

Step 3 — diff the two listings. The sorted entry-name sets must be identical and every entry's
uncompressed byte length must match. If they do not match, do NOT proceed to step 4 — the
recommendation to delete the tail is only safe because of this parity, and `release.yml` signs these
bytes.

**Contingency ladder, walked in order.** A mismatch must not stall the phase: CI-01 and V-18 depend
entirely on a portable `pnpm build`, and plan 05-06 is blocked behind this plan. Two facts measured
during plan revision narrow what a mismatch can mean — re-verify both, do not merely trust them:
`@caido-community/dev@0.1.6` bundles `jszip` and emits `dist/plugin_package.zip` ITSELF (grep the
installed package for `plugin_package.zip` — 8 hits), which is why the tail's own `rm -f` exists; and
`packages/backend/assets/` holds exactly ONE flat file, `mcp-server.mjs`, with no nesting and no
dotfiles, so `caido.config.ts`'s `./packages/backend/assets/**/*` glob is a superset of the tail's
`cp -r`. So:

- **Tier 1 — the delta is only `.DS_Store` (or comparable OS cruft).** The old tail filtered it with
  `-x '*.DS_Store'`; caido-dev may not. This is a macOS-local artifact that cannot exist on the
  ubuntu runner `release.yml` builds on, so it is not a regression to the signed artifact. Delete the
  stray files from `dist/plugin_package/`, re-measure, and record the delta and this reasoning in the
  SUMMARY. Proceed to step 4.
- **Tier 2 — the AFTER zip is missing files under `backend/assets/`.** Fix `caido.config.ts`'s
  `assets` glob (line 30) so `caido-dev build` emits them itself. Do NOT re-add a copy step: widening
  the glob keeps `build` a single cross-platform command and makes the build tool the one source of
  truth, whereas any copy step re-imports the portability problem this task exists to remove. Add
  `caido.config.ts` to this task's `<files>`, re-run steps 1-3, and require parity before step 4.
- **Tier 3 — the packaging itself diverges** (entry paths reshaped, compression semantics changed, or
  `caido-dev build` alone emits no zip, contradicting the jszip finding above). STOP and escalate to
  the developer in the same turn, naming CI-01, V-18 and 05-06 as blocked. Do NOT reach for
  `archiver`, `adm-zip` or a hand-rolled `scripts/pack.mjs` inside this plan: neither library is
  installed, `05-RESEARCH.md` § *Package Legitimacy Audit* records that this phase installs no
  external packages and touches no lockfile, and adding one would turn this into an install task that
  must run the Package Legitimacy Gate behind a blocking human checkpoint first. That is a scope
  decision for the developer, not for the executor.

In every tier, record which tier was taken — including "Tier 0: parity held on the first
measurement", which is the expected outcome — so the SUMMARY says what happened rather than implying
the happy path.

Step 4 — reduce `package.json`'s `build` script to exactly `caido-dev build`. Everything after it in
the current script is POSIX-only (`cp -r`, a glob, stderr redirection, semicolon sequencing, `rm -f`,
`cd`, `zip -r`, `-x`), pnpm's script shell on Windows is `cmd.exe`, and `zip` is absent from the
windows-latest image manifest. Note in the commit body that `dist/drift.zip` is referenced only by
the deleted `rm -f` and is produced by nothing.

Step 5 — create `.gitattributes` at the repo root containing `* text=auto eol=lf` plus a short
header comment stating its job in this phase is prophylactic — nothing currently asserts on
newline-bearing generated content (no snapshot matchers exist in the repo; exactly one test reads a
file and writes it itself into a temp dir) — and that it is landed anyway because the surviving
POSIX MCP wrapper still emits newline-joined shell text and Phase 7 will test it.

Step 6 — correct CLAUDE.md § Configuration: it still calls `dist/drift.zip` "the final deliverable"
created by the root `build` script. Replace that with the accurate statement that `caido-dev build`
emits `dist/plugin_package/` and `dist/plugin_package.zip`, and that the release workflow signs and
publishes the latter. Change only that stale claim; do not restructure the section.
  </action>
  <verify>
    <automated>rm -rf dist && pnpm build && test -f dist/plugin_package.zip && unzip -Z1 dist/plugin_package.zip | grep -q 'backend/assets/mcp-server.mjs' && grep -q 'eol=lf' .gitattributes</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "const p=require('./package.json'); process.exit(p.scripts.build.trim()==='caido-dev build'?0:1)"` exits 0.
    - `rm -rf dist && pnpm build` exits 0 and `dist/plugin_package.zip` exists.
    - `unzip -Z1 dist/plugin_package.zip | sort` contains `backend/assets/mcp-server.mjs`, `backend/index.js`, `frontend/index.css`, `frontend/index.js` and `manifest.json`.
    - The BEFORE/AFTER entry-name and byte-length comparison from steps 1-3 is recorded in the SUMMARY with the actual listings, not summarised as "identical".
    - The SUMMARY names which contingency tier step 3 took, explicitly including `Tier 0 — parity held on the first measurement` when nothing went wrong. Silence is not the same claim as a stated Tier 0.
    - If Tier 2 was taken, `git diff --stat package.json` shows the build script reduced to `caido-dev build` and NOT re-grown with a copy step; the fix is visible in `caido.config.ts` instead.
    - `grep -c 'eol=lf' .gitattributes` is 1 and `grep -c 'text=auto' .gitattributes` is 1.
    - `grep -c 'drift\.zip' package.json` is 0.
    - `grep -c 'drift\.zip' CLAUDE.md` is 0.
    - `pnpm exec vitest run` reports 0 failures — the build change must not move the suite.
  </acceptance_criteria>
  <done>`pnpm build` is a single cross-platform command that produces a byte-equivalent plugin package, `.gitattributes` exists, and CLAUDE.md no longer names an artifact nothing produces.</done>
</task>

<task type="auto">
  <name>Task 2: Author the blocking windows-latest job in ci.yml with the three-arm no-secret-material gate</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml in full (the `verify` job's step order, action pins, and the comment above `Setup pnpm` explaining why it precedes `Setup Node`)
    - .github/workflows/windows-llrt-probe.yml lines 1-10 (the dated-deletion header form), 44-60 (`permissions` and `timeout-minutes` with stated reasons), 140-185 (the three-arm secret gate verbatim, and the `shell: bash` / pipefail rationale)
    - .planning/phases/05-kill-shell-wrappers/05-RESEARCH.md § Finding C-2, § Finding C-3, § Verifying D-09's Measurement
    - .planning/ROADMAP.md § Phase 9 (SC-1, SC-1a, SC-2 — the criteria this job is pulling forward)
  </read_first>
  <action>
Add a second job to `.github/workflows/ci.yml`, alongside `verify`, named `Verify (Windows)` with
`runs-on: windows-latest`. It is a sibling job, not a matrix entry on `verify` — the ubuntu leg runs
four Node versions and this one runs a single Node 20, so folding them into one matrix would either
quadruple Windows minutes or dilute the Node matrix.

Step order, copied from `verify` and in this sequence: `Checkout` with `actions/checkout@v5`;
`Setup pnpm` with `pnpm/action-setup@v6` and no version input; `Setup Node` with
`actions/setup-node@v5`, `node-version: '20'`, `cache: pnpm`; the secret gate (below); `Install
dependencies` running `pnpm install --frozen-lockfile`; `Typecheck` running `pnpm -r typecheck`;
`Lint` running `pnpm lint`; `Test` running `pnpm exec vitest run`; `Build` running `pnpm build`.
Carry over the comment explaining that `Setup pnpm` must stay ahead of `Setup Node`, and add the
measured reason from Finding C-2 — `actions/setup-node@v5` defaults `package-manager-cache` to true
and auto-enables pnpm caching from the `packageManager` field, so a Windows job without a prior pnpm
step dies at `Setup Node`. Do NOT set `package-manager-cache: false`.

Set `timeout-minutes: 20` with a comment stating it is a starting value, that the probe's `10` was
sized for a dependency-free single step, and that plan 05-06 replaces it with a number derived from
the first real run's measured duration and records that run's URL beside it. Set
`permissions: contents: read` on this job with the same in-comment rationale the probe workflow
carries; do not change the workflow-level or `verify`-job permissions.

Add the no-secret-material gate as a `shell: bash` step named `Gate: no secret material in CI
configuration`, placed BEFORE `Install dependencies` so it cannot be skipped by an install failure.
Copy the probe's implementation shape exactly: initialise `status=0`, run a single `grep -nE` over
the scan targets with `|| status=$?` on the left of a condition context so `-e` cannot abort the
step, then branch three ways — status 0 means a match and fails, status 1 means clean and passes,
anything else including a missing or unreadable target fails. Carry the probe's comment block
explaining why the third arm exists and that it must not be collapsed. Scan targets are
`.github/workflows/ci.yml` and `.github/workflows/windows-llrt-probe.yml`; add a comment recording
that the probe file is deliberately still in the list so its Phase 9 deletion trips the third arm
loudly rather than silently narrowing the scan. Keep the pattern self-non-matching by the same
device the probe uses — the literal source text of the pattern must not itself match the pattern
when read as text. Verify that property by running the gate locally before committing.

Add a `shell: bash` line to every `run:` step in the new job. The default shell on a Windows runner
is pwsh; `shell: bash` maps to `bash --noprofile --norc -eo pipefail {0}` identically on every OS,
and `pipefail` is what makes a piped command return the real exit code. State that reason in a
comment above the first such step, matching the probe's wording.

Do not add `continue-on-error`, do not add an `if:` to the job, and do not touch the `verify` job or
the workflow-level `on:` / `concurrency:` blocks.

**Comment-text discipline for this file.** Three of this task's acceptance criteria are NEGATIVE greps
over `ci.yml` itself — `continue-on-error` is 0, `package-manager-cache` is 0, and the secret gate's
pattern must not match its own source text. A YAML comment is text like any other, so a well-meant
`# do not set package-manager-cache: false` or `# no continue-on-error here` would zero out the very
gate it was explaining and fail the task for a comment rather than for the config. Write the reasoning
WITHOUT the forbidden literals: say "the Node setup action's pnpm cache default is deliberately left
alone" and "this leg is blocking by design", and put the mechanical detail in the commit body and the
SUMMARY, where nothing greps for it. Run the three greps after writing the comments, not just after
writing the YAML.
  </action>
  <verify>
    <automated>bash .github/scripts/check-ci-windows-job.sh</automated>
  </verify>
  <acceptance_criteria>
    - Author `.github/scripts/check-ci-windows-job.sh` as part of this task so the gate below is a runnable command rather than prose. It must exit non-zero on any failed assertion and print which one failed.
    - `grep -c 'windows-latest' .github/workflows/ci.yml` is 1.
    - `grep -c 'continue-on-error' .github/workflows/ci.yml` is 0.
    - `grep -c 'package-manager-cache' .github/workflows/ci.yml` is 0.
    - `grep -c 'timeout-minutes' .github/workflows/ci.yml` is 1.
    - `grep -c 'shell: bash' .github/workflows/ci.yml` is at least 6 — every `run:` step in the new job pins it.
    - `grep -c 'pnpm/action-setup@v6' .github/workflows/ci.yml` is 2 and `grep -c 'actions/setup-node@v5' .github/workflows/ci.yml` is 2 — the pins match the existing `verify` job exactly.
    - The Setup pnpm step appears at a lower line number than the Setup Node step inside the Windows job. Assert it by comparing the two `grep -n` line numbers, not by eye.
    - The secret gate proves it FIRES, not just that it passes: run the gate's grep locally against the two scan targets and confirm status 1 (clean); then run it against a scratch copy of `ci.yml` with a seeded token-shaped literal appended and confirm status 0; then run it against a nonexistent path and confirm status 2. Record all three statuses in the SUMMARY. A zero from an unproven scanner is not evidence.
    - `grep -c 'windows-llrt-probe.yml' .github/workflows/ci.yml` is at least 1 — the probe file is in the scan-target list on purpose.
    - The gate step's own pattern text does not match the gate's own pattern. Prove it by running the gate over `.github/workflows/ci.yml` after the edit and confirming status 1.
  </acceptance_criteria>
  <done>`ci.yml` carries a blocking `windows-latest` job with matching pins, a `shell: bash` pin on every run step, and a three-arm secret gate that has been demonstrated to fire on all three statuses.</done>
</task>

<task type="auto">
  <name>Task 3: Re-target CI-01 and CI-03 into Phase 5 in REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md § Traceability (the table, the Coverage block and the Phase → requirement rollup) and the `Re-targeted to Phase 5 at the close of Phase 4` note in full — it is the structural template this task copies
    - .planning/ROADMAP.md § Phase 9 (what remains there after CI-01/CI-03 move)
    - .planning/phases/05-kill-shell-wrappers/05-CONTEXT.md D-07 and D-09
  </read_first>
  <action>
Make Phase 5 the structural owner of CI-01 and CI-03, the same way RUN-04 was re-targeted at the
close of Phase 4 — in the table and in both rollups, not in a paragraph alone.

Change the traceability table rows for `CI-01` and `CI-03` from `Phase 9` to `Phase 5`. Leave their
Status as `Pending`; this plan authors the job, it does not run it.

Update the Phase → requirement rollup: add `CI-01, CI-03` to the Phase 5 line so it reads
`RUN-01, RUN-02, RUN-04, HLT-01, HLT-02, CMP-01, CI-01, CI-03`, and remove them from the Phase 9
line. Phase 9's line then has no requirements left, so replace it with an explicit statement that
Phase 9 retains no requirement IDs of its own after the pull-forward and now narrows to making the
Windows leg required-for-merge and green for the right reasons — do not leave a bare empty list,
which reads as an editing accident.

Leave the `Coverage` counts unchanged in total (43 mapped, 0 unmapped) but re-verify them after the
edit rather than assuming; if the count moved, the edit was wrong.

Add a dated section after the existing `Re-targeted to Phase 5 at the close of Phase 4` note, titled
`Pulled forward into Phase 5 during Phase 5 planning`, covering both IDs. For CI-01 state that
Phase 5 is the first phase with real Windows behaviour to protect, that every later phase inherits
the net, and that investing in `windows-llrt-probe.yml` — already stamped for deletion in Phase 9 —
would put SC-2's evidence in a job nobody must heed and that then vanishes. For CI-03 state D-09's
three measurements (no snapshot matchers anywhere in the repo, exactly one test reads a file and
writes it itself into a temp dir, and no `.gitattributes` existed) and that they are what make the
leg blocking from day one rather than a narrow subset. Record explicitly what stays in Phase 9: the
job becomes required for merge, `windows-llrt-probe.yml` and `scripts/windows-llrt-probe.mjs` are
deleted, and the `caido:plugin` vitest alias is the deferred item that would raise SC-2's fidelity.

Do not mark anything Complete in this task.
  </action>
  <verify>
    <automated>grep -c '| CI-01 | Phase 5 | Pending |' .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'CI-01 | Phase 5' .planning/REQUIREMENTS.md` is 1 and `grep -c 'CI-03 | Phase 5' .planning/REQUIREMENTS.md` is 1.
    - `grep -c 'CI-01 | Phase 9' .planning/REQUIREMENTS.md` is 0 and `grep -c 'CI-03 | Phase 9' .planning/REQUIREMENTS.md` is 0.
    - The Phase 5 rollup line contains all eight IDs: RUN-01, RUN-02, RUN-04, HLT-01, HLT-02, CMP-01, CI-01, CI-03. Assert each with a separate grep over that one line.
    - The Phase 9 rollup line contains neither CI-01 nor CI-03 and is not an empty list — it carries a sentence explaining the pull-forward.
    - Counting the traceability table rows still yields 43 mapped IDs and 0 unmapped. Compute it, do not restate it.
    - `grep -c 'Pulled forward into Phase 5' .planning/REQUIREMENTS.md` is 1.
    - No requirement's Status changes to Complete in this task: `git diff .planning/REQUIREMENTS.md` shows no line where `Pending` became `Complete`.
  </acceptance_criteria>
  <done>CI-01 and CI-03 are owned by Phase 5 in the table and in both rollups, with a dated paragraph explaining why and what remains in Phase 9.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
Created or newly referenced by THIS plan (exclude from drift verification):

**New files**
- `.gitattributes` (repo root — none existed at HEAD)
- `.github/scripts/check-ci-windows-job.sh` (the runnable static gate for task 2's acceptance criteria)

**New CI job / step names in `.github/workflows/ci.yml`**
- job: `Verify (Windows)` on `windows-latest`
- step: `Gate: no secret material in CI configuration`
- steps reused by name from the `verify` job: `Checkout`, `Setup pnpm`, `Setup Node`, `Install dependencies`, `Typecheck`, `Lint`, `Test`, `Build`

**Changed config keys**
- `package.json` `scripts.build` reduced to `caido-dev build`
- `.planning/REQUIREMENTS.md`: CI-01 and CI-03 re-targeted to Phase 5; new section `Pulled forward into Phase 5 during Phase 5 planning`
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repository source → public GitHub Actions log | Anything a workflow prints is world-readable on a public repo |
| Repository source → published release artifact | `dist/plugin_package.zip` is signed and shipped to the Caido store |
| CI runner → pnpm registry | `pnpm install --frozen-lockfile` fetches third-party code |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-06 | Information Disclosure | `.github/workflows/ci.yml` | high | mitigate | The three-arm no-secret-material gate runs before `Install dependencies` and scans the workflow files themselves. Proven to fire on all three grep statuses (0/1/2) as an acceptance criterion, not assumed. |
| T-05-07 | Tampering | `package.json` `scripts.build` | high | mitigate | The reduced script must produce an artifact with an identical entry-name set and identical per-entry byte lengths, measured on this checkout (assumption A7). `release.yml` signs the bytes, so an unmeasured change to the packaging step is a supply-chain change to a signed artifact. |
| T-05-08 | Tampering | `pnpm install` on a new runner | medium | mitigate | `--frozen-lockfile` is preserved verbatim from the `verify` job; the lockfile is not touched by this phase; the three action pins are the ones already in `ci.yml`, so no new supply-chain surface is introduced. |
| T-05-09 | Denial of Service | the Windows leg | low | accept | A hung spawn would hold a Windows runner. `timeout-minutes: 20` bounds it, versus the 360-minute default. Accepted with the value re-derived from the first real run in plan 05-06. |
| T-05-SC | Tampering | npm/pip/cargo installs | high | mitigate | **Not applicable.** `05-RESEARCH.md` § Package Legitimacy Audit records that this phase installs no external packages and touches no lockfile. `pnpm install --frozen-lockfile` is unchanged, and the three GitHub Actions referenced are already pinned in this repo at exactly those majors. If an install task is ever added here, the researcher must run the Package Legitimacy Gate protocol first and this row becomes a blocking human checkpoint. |
</threat_model>

<verification>
1. `rm -rf dist && pnpm build` exits 0 and produces `dist/plugin_package.zip` containing `backend/assets/mcp-server.mjs`.
2. The BEFORE/AFTER zip listings are recorded in the SUMMARY with actual entry names and byte lengths.
3. `bash .github/scripts/check-ci-windows-job.sh` exits 0.
4. The secret gate is demonstrated at all three statuses (clean → 1, seeded → 0, missing target → 2), recorded in the SUMMARY.
5. `pnpm exec vitest run` reports 0 failures; `pnpm -r typecheck` and `pnpm lint` exit 0.
6. `git status --porcelain` captured into a variable and discriminated with `test -z` — `git status` exits 0 regardless of what it printed.
</verification>

<success_criteria>
- `pnpm build` is portable and byte-equivalent (CI-01's precondition, V-18).
- `.gitattributes` exists with `* text=auto eol=lf` (CI-03, V-15).
- `ci.yml` carries a blocking `windows-latest` leg with matching pins and the carried-forward secret gate (CI-01, ROADMAP Phase 9 SC-1a).
- CI-01 and CI-03 are structurally owned by Phase 5 (D-07, D-09).
- No CI result is claimed here — 05-06 owns the run and the evidence.
</success_criteria>

<output>
Create `.planning/phases/05-kill-shell-wrappers/05-02-SUMMARY.md` when done.
Record in it: the two zip listings and their comparison verbatim, the three demonstrated grep
statuses for the secret gate, the exact job and step names as shipped, and the explicit non-claim
that no `windows-latest` run has been executed yet.
</output>
