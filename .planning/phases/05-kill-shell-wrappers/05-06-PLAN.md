---
phase: 05-kill-shell-wrappers
plan: 06
type: execute
wave: 4
depends_on: ["05-02", "05-05"]
files_modified:
  - .github/workflows/ci.yml
  - .planning/REQUIREMENTS.md
  - .planning/phases/05-kill-shell-wrappers/05-VALIDATION.md
  - .planning/phases/05-kill-shell-wrappers/05-REPORT.md
autonomous: false
requirements: [RUN-01, RUN-02, RUN-04, HLT-01, HLT-02, CMP-01, CI-01, CI-03]

estimate:
  tokens: 84000
  raw_tokens: 84000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "Every phase-wide static gate is EXECUTED with its raw output recorded, never inferred from a diff — Phase 4 recorded two exact-count gates silently zeroed by a formatter-shaped argument wrap while typecheck, lint and the suite all stayed green."
    - "05-VALIDATION.md's V-5 gate is corrected: the published form counts double-quoted occurrences only and that count is already satisfied at HEAD, so it is vacuous. The corrected form is comment-stripped and quote-agnostic, and its expected value is 1, not 2, because writeMcpWrapper's default path now comes from getMcpWrapperPath()."
    - "A real windows-latest run exists, and every CI claim carries its run URL, its per-job and per-step conclusions and the log line proving the mechanism — written AFTER reading the run, never pre-filled (Phase 3 D-11/D-12/D-13)."
    - "`timeout-minutes` on the Windows leg is set from the first real run's measured duration and the run URL is recorded beside it."
    - "The phase report states every bucket-N row as an explicit NON-claim, and the D-08 vehicle caveat appears verbatim: this phase proves the spec, the server and the spawn contract on Windows — not index.ts's wiring of them, and not a real Claude CLI connecting."
    - "The three probe-surfaced rows that could not be classified are carried as explicitly flagged assumptions rather than auto-resolved or dropped."
    - "REQUIREMENTS.md statuses change only for requirements whose evidence was actually produced, and every deferral is recorded with its reason — the same call Phase 4 made eleven consecutive times."
  artifacts:
    - .planning/phases/05-kill-shell-wrappers/05-REPORT.md
    - .planning/REQUIREMENTS.md
    - .planning/phases/05-kill-shell-wrappers/05-VALIDATION.md
    - .github/workflows/ci.yml
  key_links:
    - "The authoritative Windows run must happen AFTER 05-01's spawn test and 05-04/05-05's rewrite have landed, which is why the CI evidence lives here and not in 05-02."
    - "A CI log-extraction pipeline must be validated against an independently-known nonzero expected count before its result is trusted — three separate Phase 3 plans hit a silently-empty pipeline that reads identically to a proof that failed."
    - "The scratch-branch teardown must capture the ls-remote output into a variable and discriminate with `test -z`; `git ls-remote` exits 0 whether or not the glob matched."
  prohibitions:
    - "Never pre-fill a CI claim. Write it after reading the real run, and include the log line that proves the mechanism, not just the green tick."
    - "Never record a human read as a machine-verified result, and never record a machine result as a human read. The distinction is the whole reason a bucket for unverifiable criteria exists."
    - "Never mark a requirement Complete on a claim no gate executed, and never mark one Complete for a path the phase deliberately did not reach."
    - "Never let the integration spawn test's green be reported as Claude-on-Windows end-to-end. That is PRV-01 and it belongs to Phase 7."
    - "Never run `git add -A`. Every stage is a targeted `git add <path>`, and any CI-proof branch is named `scratch/ci-*` and torn down with a `test -z` discrimination over the captured glob plus the full unfiltered remote listing."
    - "Never retune a gate threshold to make it pass. If a gate is genuinely wrong, correct it in 05-VALIDATION.md and say so in the report; if the code is wrong, fix the code."
---

<objective>
Close the phase on evidence: execute every static gate, get a real `windows-latest` run and record it
the way this project records CI claims, reconcile the validation contract with what actually shipped,
and state the phase's non-claims plainly enough that nobody later reads them as claims.

Purpose: this phase's most convincing artifact — an integration test that spawns the production spec
against the real MCP server — is also the one most likely to attract more confidence than it earns.
Three of the five unverifiable rows are one underlying gap seen from three angles: the thing being
changed lives in a module no test can import, and the runtime it must work on cannot be executed by
any test this project can run. Saying that out loud is the deliverable.

Output: `05-REPORT.md`, a corrected `05-VALIDATION.md`, an evidence-backed `timeout-minutes`, and
requirement statuses that match what was proven.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-kill-shell-wrappers/05-CONTEXT.md
@.planning/phases/05-kill-shell-wrappers/05-RESEARCH.md
@.planning/phases/05-kill-shell-wrappers/05-VALIDATION.md
@.planning/phases/03-ci-spike-prove-llrt-basics-on-windows/03-FINDINGS.md
@.planning/phases/03-ci-spike-prove-llrt-basics-on-windows/03-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Execute the phase-wide static gate set and reconcile 05-VALIDATION.md with what shipped</name>
  <files>.planning/phases/05-kill-shell-wrappers/05-VALIDATION.md</files>
  <precondition>Plans 05-01 through 05-05 are all committed; `packages/backend/src/mcp-server-spec.ts`, `.gitattributes` and the `windows-latest` job all exist on the working branch.</precondition>
  <read_first>
    - .planning/phases/05-kill-shell-wrappers/05-VALIDATION.md in full (every row V-1 through V-24 and the Wave 0 list)
    - .planning/phases/05-kill-shell-wrappers/05-04-SUMMARY.md and 05-05-SUMMARY.md (the gate outputs already recorded, so this task can confirm rather than rediscover)
    - .planning/phases/05-kill-shell-wrappers/05-RESEARCH.md § Code Examples 4 and § Complete Site Inventory
    - .planning/STATE.md the `[Phase 04]: [04-10]` entry on gates silently zeroed by a formatter-shaped argument wrap
  </read_first>
  <action>
Run every gate below on the current tree and paste its literal output into the SUMMARY. Run them;
do not infer them from a diff. Phase 4 recorded two exact-count gates that read zero instead of one
because a formatter wrapped an argument, while typecheck, lint and the suite all stayed green.

Gate 1 — shell literals. Over a comment-stripped view (`sed -e 's://.*::' packages/backend/src/index.ts`),
`grep -c '\.sh'` must be 1, and `grep -n '\.sh'` must name only the line inside `getMcpWrapperPath`.

Gate 2 — chmod discrimination. Over the same view, `grep -c 'spawnAndWait("chmod"'` must be 1. The
whole-file `grep -c 'chmod'` must be greater than zero, and `enforceOwnerOnlyDir`'s body must be
unchanged against the pre-phase commit. Record the non-zero count together with the sentence that
explains it: a zero chmod count would mean a POSIX security control was deleted.

Gate 3 — site inventory. `grep -n "writeMcpWrapper(\|validateCaidoAuth(\|tryRegisterMcpForProviders(\|callMcpMethod(\|writeChatMcpConfig(" packages/backend/src/index.ts`
must show one definition and one call for `writeMcpWrapper`, one definition and two calls for
`validateCaidoAuth`, and one definition and two calls for `tryRegisterMcpForProviders`.

Gate 4 — the parent-spread gate, two-sided. Over the comment-stripped view, the count of lines
matching `(^|[[:space:],{(])env:` must be at least 3, AND filtering that list to remove
`env: spec.env`, `env: buildSpawnEnv(` and `env: options.env` must print nothing. Both halves are
required: the filter alone passes vacuously on a file with no env sites at all.

Gate 5 — dated deletion notices. `grep -rn 'DELETED IN PHASE [0-9]\+ (' .github/workflows packages/backend/src | wc -l`
must be 4.

Gate 6 — the launch indirection is gone. `grep -c 'launchCommand'`, `grep -c 'launchArgs'` and
`grep -c 'writeLaunchScript'` over `index.ts` must each be 0.

Gate 7 — the compatibility tripwire (V-11). `git diff --stat <pre-phase-commit> -- packages/backend/src/provider-launch.ts packages/backend/src/provider-launch.test.ts`
must report no change, and `pnpm exec vitest run packages/backend/src/provider-launch.test.ts` must
pass.

Gate 8 — line endings. `grep -q 'eol=lf' .gitattributes` and `grep -q 'text=auto' .gitattributes`.

Gate 9 — the whole suite and both static checks (V-14). `pnpm -r typecheck`, `pnpm lint`,
`pnpm exec vitest run`. Record the final test count against the 263 pre-phase baseline and name every
test removed as obsolete, individually, with its reason.

Then reconcile `05-VALIDATION.md` with what shipped. Two corrections are already known and must be
made explicitly rather than silently:

First, V-5's published command counts only double-quoted occurrences, and that count is ALREADY
satisfied on the pre-phase tree — it is a vacuous gate. Replace it with the comment-stripped,
quote-agnostic form, and change its expected value from 2 to 1, because `writeMcpWrapper`'s inline
default join was replaced by a call to `getMcpWrapperPath()`, leaving that function as the single
source of truth for the surviving wrapper path. Record both the vacuity and the count change in the
row and in the report.

Second, V-6's expected `spawnAndWait("chmod"` count is 1 after this phase, and the gate must be run
over the comment-stripped view so a rationale comment mentioning the call cannot break it.

Mark each row V-1 through V-19 with its actual status and the command that proved it. Leave V-20
through V-24 as non-claims and do not mark them anything else. Set `nyquist_compliant` and
`wave_0_complete` to reflect reality, and set `status` to `validated` only if every V-1..V-19 row is
green.
  </action>
  <verify>
    <automated>sed -e 's://.*::' packages/backend/src/index.ts | grep -c '\.sh' | grep -qx 1</automated>
  </verify>
  <acceptance_criteria>
    - All nine gates are executed and their literal output is pasted into the SUMMARY. A gate reported as "passed" without its output does not count.
    - Gate 1 returns 1; Gate 2 returns 1 with a non-zero whole-file chmod count; Gate 3 matches the expected definition/call-site counts exactly; Gate 4's raw count is at least 3 and its filter prints nothing; Gate 5 returns 4; Gate 6 returns 0 three times; Gate 7 reports no change and passes; Gate 8 succeeds; Gate 9 exits 0 with 0 failures.
    - `05-VALIDATION.md`'s V-5 row carries the corrected command and the expected value 1, plus a sentence recording that the published form was vacuous.
    - `05-VALIDATION.md`'s V-6 row carries the comment-stripped form and the expected value 1.
    - Every row V-1..V-19 has a non-pending status and names the command that proved it.
    - Rows V-20..V-24 are unchanged as non-claims.
    - The final test count and every individually named removed test are recorded.
  </acceptance_criteria>
  <done>Nine gates executed with raw output, and the validation contract now matches the code rather than the plan's intent.</done>
</task>

<task type="auto">
  <name>Task 2: Get a real windows-latest run, record it with evidence, and set timeout-minutes from it</name>
  <files>.github/workflows/ci.yml</files>
  <precondition>The `windows-latest` job authored by plan 05-02 exists on the working branch, `pnpm build` is the reduced single-command script, and the `gh` CLI is authenticated against this repository.</precondition>
  <read_first>
    - .planning/phases/03-ci-spike-prove-llrt-basics-on-windows/03-CONTEXT.md D-11, D-12 and D-13 (how a CI claim is recorded in this project)
    - .planning/STATE.md the `[Phase 03]: [03-04]` entries on scratch-branch teardown and on log-extraction pipelines, and the `[Phase 03]: [03-03]` entry on the setup-node pnpm trap
    - .planning/phases/05-kill-shell-wrappers/05-RESEARCH.md § Verifying D-09's Measurement, including the two additional first-run risks (spawn timing on a Windows runner, and pnpm long paths) and assumption A3
    - .github/workflows/ci.yml as authored by plan 05-02
    - .planning/phases/05-kill-shell-wrappers/05-02-SUMMARY.md (the three demonstrated secret-gate statuses)
  </read_first>
  <action>
Create a throwaway branch named with the `scratch/ci-` prefix, stage ONLY the paths this phase
changed with targeted `git add <path>` calls, and push it. Never `git add -A`: an untracked document
at the repository root would be published to the public remote.

Read the resulting run with `gh run view --json jobs`. Record, for the Windows job: the run URL, the
job conclusion, and the conclusion of every step by name. Then extract the log line that proves each
mechanism you claim — the secret gate's pass message, the test step's summary line with the file and
test counts, and the build step's completion. A green tick is not a claim; the log line is.

Before trusting any log-extraction pipeline, validate it against an independently-known nonzero
expected count. `gh run view --log` prefixes every line with the job name, the step name and a
timestamp, and three separate Phase 3 plans hit a pipeline that silently produced nothing —
indistinguishable from a proof that failed. Strip the prefix with a field cut, then strip the ANSI
escapes and the leading timestamp with a real regex engine rather than a `sed` whose escapes are
platform-dependent. Prove the pipeline returns the expected nonzero count on a line you already know
is present before using it on the lines you are trying to prove.

If the first run is red, READ it rather than retrying it. The two candidate causes named in advance
are spawn timing (the integration spawn tests use a wall-clock timeout around a real child, and a
Windows runner is materially slower at process creation while Defender scans a freshly written
script) and pnpm's deep dependency tree against the Windows path-length limit at the install step.
The mitigation for the first is to raise the timeout constant, never to add a retry. Record which one
it was, with the log line.

Once green, replace `timeout-minutes` with a value derived from the measured wall-clock duration of
the Windows job — enough headroom that a normal run cannot flake, not so much that a hung spawn holds
a runner for hours — and put the run URL and the measured duration in the comment beside it.

Tear down the scratch branch locally and on the remote. Capture the `git ls-remote --heads origin`
glob output into a variable and discriminate with `test -z`; the command exits 0 whether or not the
glob matched, so a bare exit-status check reports success with every branch still alive. Record the
full unfiltered remote listing alongside the filtered one so an empty match is provably empty rather
than a mistyped pattern. Do the same `test -z` discrimination on `git status --porcelain`.
  </action>
  <verify>
    <automated>grep -n 'timeout-minutes' .github/workflows/ci.yml</automated>
  </verify>
  <acceptance_criteria>
    - A real `windows-latest` run URL is recorded, with the job conclusion and every step's conclusion by name.
    - The Test step's log line showing the file and test counts is quoted verbatim, and the count matches the local post-phase count from task 1. This one line carries V-16 (`--validate-auth` on Windows), V-17 (the three self-test methods on Windows) and V-19 (the whole suite green on Windows) — record all three row IDs beside it, and record separately that the two integration cases were among the executed tests rather than skipped.
    - The secret gate's pass message is quoted verbatim from the Windows run's log.
    - The Build step concluded success, and the run's artifact or log confirms `dist/plugin_package.zip` was produced.
    - The log-extraction pipeline was validated against a known nonzero expected count BEFORE being used for any claim, and that validation is recorded.
    - `timeout-minutes` in `ci.yml` differs from the placeholder authored in 05-02 unless the measured duration justifies keeping it, and the comment beside it carries the run URL and the measured duration.
    - The scratch branch is gone: the captured `git ls-remote --heads origin` glob output is empty by `test -z`, and the full unfiltered listing is recorded.
    - `git status --porcelain` captured into a variable is empty by `test -z`.
    - No `git add -A` appears anywhere in the task's command history.
  </acceptance_criteria>
  <done>V-16 through V-19 rest on a real run with its URL, its per-step conclusions and its proving log lines, and the leg's timeout is a measured number rather than a guess.</done>
</task>

<task type="auto">
  <name>Task 3: Write 05-REPORT.md — the non-claims, the flagged assumptions, and the D-08 caveat — and mark REQUIREMENTS.md</name>
  <files>.planning/phases/05-kill-shell-wrappers/05-REPORT.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/phases/05-kill-shell-wrappers/05-VALIDATION.md as corrected by task 1 (rows V-20..V-24 verbatim)
    - .planning/phases/03-ci-spike-prove-llrt-basics-on-windows/03-FINDINGS.md § Vehicle caveat and § ROADMAP success-criterion gap — the voice this report copies
    - .planning/REQUIREMENTS.md § Traceability, both rollups, and the `Re-targeted to Phase 5 at the close of Phase 4` note plus the `Pulled forward into Phase 5` note added by plan 05-02
    - all five SUMMARY files from plans 05-01 through 05-05
    - .planning/phases/05-kill-shell-wrappers/05-CONTEXT.md D-08 and § Deferred Ideas
  </read_first>
  <action>
Write `05-REPORT.md`. Lead with what the phase does NOT claim, before what it does — the ordering is
the point, because the integration spawn test is the phase's most convincing artifact and therefore
attracts more confidence than it earns.

Carry the D-08 caveat verbatim and phrase it so a reader cannot mistake it for a full end-to-end
Windows claim: this phase proves the spec, the server and the spawn contract on Windows; it does not
prove `index.ts`'s wiring of them, and it is not a real Claude CLI reading a config and connecting.

Reproduce every bucket-N row as an explicit non-claim with its reason and its closing condition:
a real Defender lock on real hardware is not inducible in CI and RUN-04 is satisfied structurally
instead; `index.ts`'s wiring is not test-reachable because the module cannot be imported and the
`caido:plugin` alias was deliberately deferred to Phase 9; the env contract under the real runtime is
invisible to a Node vehicle that back-fills eleven names, which is why the vehicle-independent static
gate exists; behaviour under the real Caido runtime at all is closed only by a real Windows install in
Phase 9 or 10; and a real Claude CLI connecting on Windows is Phase 7's PRV-01 and out of scope here.
Name the row IDs explicitly — V-20, V-21, V-22, V-23 and V-24 — so a later reader can check the list
against the validation contract rather than trusting that it is complete. V-23 in particular carries
the standing vehicle caveat: every Windows result in this phase was measured on Node, not on Caido's
runtime, and source analysis raised confidence without closing the gap.

Add a section titled `Flagged assumptions — surfaced, not resolved` carrying the three edge-coverage
rows the automated probe could not classify, one for RUN-01, one for RUN-02 and one for HLT-01. State
for each that no category-specific probe applied, what the manual reading of it is, and what evidence
this phase produced against it. Record the reconciliation explicitly: the planning input described
four unclassified rows while the coverage report contains three, and all three are carried here so
nothing is silently dropped. Do not auto-resolve them with a backstop.

Add a short section recording the two forward-looking notes this phase surfaced but does not act on:
the surviving POSIX wrapper's persisted entries in the external CLIs' own config files, which Phase 7
must clean up for a user who upgrades mid-phase; and SEC-02, still owned by Phase 2 and still Not
started, whose stakes this phase raises because the chat-scoped MCP config is now token-bearing.
Also record, for Phase 10 only, that the runtime does not parse the console-hiding spawn option at
all, so UX-04 must not be treated as partially satisfied by anything here.

Then update `.planning/REQUIREMENTS.md`. Mark a requirement Complete only where this phase produced
the evidence:
- RUN-01, RUN-02, HLT-01, HLT-02 and CMP-01 rest on plan 05-01's unit and integration proof, plans
  05-04/05-05's static gates, and the Windows run recorded in task 2. Mark them Complete only if
  every corresponding V-row in the corrected `05-VALIDATION.md` is green AND task 2's run is green;
  otherwise leave them Pending and say why.
- RUN-04: state the structural argument in the row's note — after this phase no write-then-execute
  pair remains on any Windows-reachable path, and the retry ladder now has more than one production
  call site — and record that real-machine confirmation is Phase 9/10. Decide Complete or Pending on
  that basis and write the reasoning into REQUIREMENTS.md so it survives the phase boundary, exactly
  as the RUN-04 re-target note does.
- CI-01 and CI-03: these were pulled forward by plan 05-02 and rest on task 2's run. Record
  explicitly what remains for Phase 9 — making the job required for merge, deleting the probe
  workflow and its script, and the deferred `caido:plugin` vitest alias.

Re-verify the coverage counts after editing rather than restating them, and confirm no requirement
outside this phase's set changed status.
  </action>
  <verify>
    <automated>test -f .planning/phases/05-kill-shell-wrappers/05-REPORT.md && grep -c 'not index.ts' .planning/phases/05-kill-shell-wrappers/05-REPORT.md</automated>
  </verify>
  <acceptance_criteria>
    - `05-REPORT.md` exists and its FIRST substantive section is the non-claims, not the accomplishments.
    - All five bucket-N rows (V-20 through V-24) appear as explicit non-claims with a reason and a closing condition each.
    - The D-08 caveat appears with both halves: what is proven and what is not.
    - The `Flagged assumptions` section carries three entries covering RUN-01, RUN-02 and HLT-01, and records the three-versus-four reconciliation.
    - Neither the Phase 7 persisted-registration note, the SEC-02 raised-stakes note, nor the Phase 10 console-hiding note is omitted.
    - Every requirement whose status changed in REQUIREMENTS.md has a stated evidence basis; every one left Pending has a stated reason.
    - The coverage counts are recomputed after the edit and still reconcile to 43 mapped and 0 unmapped.
    - `git diff .planning/REQUIREMENTS.md` shows no status change for any requirement outside this phase's eight IDs.
  </acceptance_criteria>
  <done>The phase's ceiling is written down before its achievements, three unclassified edges are carried as flagged assumptions rather than dropped, and every status change names its evidence.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Blocking human read — is the phase report's ceiling legible to someone who has not read Drift's source?</name>
  <what-built>
Phase 5 is complete on disk: the direct-`node`-spawn keystone is wired into all three orchestration
sites, the launch-script indirection is deleted, the POSIX Gemini/Codex wrapper survives behind a
platform guard with a dated deletion notice, a blocking `windows-latest` CI leg exists with a real
run behind it, and `05-REPORT.md` states the phase's non-claims.

Two things remain that no gate can produce, which is exactly why this checkpoint exists. Both are
recorded as a HUMAN READ with a date, never dressed up as a machine result.
  </what-built>
  <read_first>
    - .planning/phases/05-kill-shell-wrappers/05-REPORT.md as written by task 3
    - the diff of `packages/backend/src/index.ts` across the whole phase
  </read_first>
  <how-to-verify>
**First — legibility.** Read the non-claims section of `.planning/phases/05-kill-shell-wrappers/05-REPORT.md`
as someone encountering this project for the first time would: a future maintainer, the original
Windows bug reporter, or a reviewer of the store release. Answer one question: can that section be
misread as a claim that Claude works on Windows today? The check is whether a stranger would draw the
wrong conclusion, not whether the sentences are technically accurate. If it can be misread, say how
and the wording changes.

**Second — the V-21 code review.** `index.ts`'s wiring of the keystone is the one criterion in this
phase whose only possible evidence is a human reading a diff. Run
`git diff <pre-phase-commit> -- packages/backend/src/index.ts` and read it with three questions in
front of you:

  1. Does any `spawn` or `spawnAndWait` hand a child an environment that was not merged with the
     parent block? Every one should be `spec.env`, `buildSpawnEnv(...)` or the forwarded
     `options.env`.
  2. Does any surviving removal call in `finalize()` operate on a path that could be the user's own
     `claude` binary? The `launchCommand` guard and its removal should both be gone.
  3. Is the Gemini/Codex wrapper genuinely unreachable on Windows on BOTH paths — MCP start AND
     settings save / token sync? Both call sites should reach `tryRegisterMcpForProviders`, and the
     platform guard should live inside it.

Answer all three. A partial answer does not close this checkpoint.
  </how-to-verify>
  <resume-signal>Type "approved" once both reads are done and recorded, or describe the wording problem or the code issue you found. If either answer is negative, the issue is fixed and this checkpoint is re-presented.</resume-signal>
  <verify>
    <human-check>The developer has read the non-claims section and confirmed it cannot be misread as a Claude-on-Windows claim, and has read the phase's index.ts diff and answered all three V-21 questions. Both answers are recorded in 05-REPORT.md as a dated human read, explicitly labelled as a human read rather than a verified result.</human-check>
  </verify>
  <acceptance_criteria>
    - `05-REPORT.md` contains a dated entry labelled as a human read covering the legibility question.
    - `05-REPORT.md` contains a dated entry labelled as a human read covering the three V-21 code-review questions, with the answer to each.
    - Neither entry is phrased as a machine-verified result, and neither is attributed to a gate.
    - Any wording change requested by the developer is applied and re-presented before the checkpoint closes.
  </acceptance_criteria>
  <done>Both human reads are recorded with a date and labelled as human reads, and the report's ceiling has survived a stranger-legibility test.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
Created by THIS plan (exclude from drift verification):

**New files**
- `.planning/phases/05-kill-shell-wrappers/05-REPORT.md`

**Changed**
- `.github/workflows/ci.yml`: `timeout-minutes` re-derived from the measured Windows run, with the run URL in its comment
- `.planning/phases/05-kill-shell-wrappers/05-VALIDATION.md`: corrected V-5 and V-6 gate commands and expected values; V-1..V-19 statuses; frontmatter `status`, `nyquist_compliant`, `wave_0_complete`
- `.planning/REQUIREMENTS.md`: statuses and evidence notes for RUN-01, RUN-02, RUN-04, HLT-01, HLT-02, CMP-01, CI-01, CI-03
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Working branch → public GitHub remote | A scratch branch push publishes whatever was staged |
| Repository source → public Actions log | Anything a workflow prints on a public repo is world-readable |
| Phase artifacts → future maintainers and the store reviewer | A claim recorded here is trusted later without re-derivation |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-26 | Information Disclosure | the scratch-branch push | high | mitigate | Targeted `git add <path>` only; `git add -A` is prohibited because an untracked document at the repository root would be published. Teardown is proven with a `test -z` discrimination over the captured `git ls-remote` glob plus the full unfiltered listing, because the command exits 0 whether or not the glob matched. |
| T-05-27 | Information Disclosure | the Windows CI job's log | high | mitigate | The three-arm no-secret-material gate runs before install on every Windows run, and plan 05-02 demonstrated it firing on all three grep statuses. Its pass message is quoted from this run's log as the proof it executed. |
| T-05-28 | Repudiation | the phase report | high | mitigate | Non-claims lead the document; every bucket-N row is reproduced with its reason and closing condition; the D-08 caveat carries both halves; human reads are dated and labelled as human reads. A green integration test being reported as PRV-01 is the specific misreading this structure prevents. |
| T-05-29 | Repudiation | a CI claim | medium | mitigate | Every claim carries its run URL, its per-step conclusions and the log line proving the mechanism, written after reading the run. The log-extraction pipeline is validated against a known nonzero expected count first, because a silently empty pipeline reads identically to a proof that failed. |
| T-05-30 | Tampering | gate thresholds | medium | mitigate | Retuning a threshold to make it pass is prohibited. The two gate corrections made here are declared as corrections with their reasons — one vacuous pattern and one count change caused by a real code decision — and recorded in both the validation contract and the report. |
| T-05-SC | Tampering | npm/pip/cargo installs | high | mitigate | **Not applicable.** `05-RESEARCH.md` § Package Legitimacy Audit records that this phase installs no external packages and touches no lockfile; the Windows job runs `pnpm install --frozen-lockfile` against the unchanged lockfile. If an install is ever added, the Package Legitimacy Gate protocol runs first and this row becomes a blocking human checkpoint. |
</threat_model>

<verification>
1. Nine static gates executed with raw output recorded (task 1).
2. A real `windows-latest` run URL with per-step conclusions and quoted proving log lines (task 2).
3. `05-VALIDATION.md` rows V-1..V-19 all green and V-20..V-24 unchanged as non-claims.
4. `05-REPORT.md` exists, leads with non-claims, carries the D-08 caveat and three flagged assumptions.
5. `.planning/REQUIREMENTS.md` coverage recomputes to 43 mapped, 0 unmapped, with no out-of-phase status change.
6. The scratch branch is provably gone by `test -z` over the captured glob plus the full unfiltered remote listing.
7. Both human reads are recorded, dated, and labelled as human reads.
</verification>

<success_criteria>
- Every phase-wide gate was executed, not inferred (RUN-01, RUN-02, CMP-01).
- The Windows leg has a real run behind it, recorded the way this project records CI claims (CI-01, CI-03, HLT-01, HLT-02).
- RUN-04's structural argument and its residual real-machine gap are both written into REQUIREMENTS.md.
- The phase's five unverifiable rows and three unclassified edges are stated as such, in a document that leads with them.
- No requirement is marked Complete on evidence this phase did not produce.
</success_criteria>

<output>
Create `.planning/phases/05-kill-shell-wrappers/05-06-SUMMARY.md` when done.
Record in it: the raw output of all nine gates, the Windows run URL with its per-step conclusions and
quoted log lines, the measured duration behind the new `timeout-minutes`, the two gate corrections
with their reasons, the final requirement statuses with their evidence basis, and both dated human
reads labelled as human reads.
</output>
