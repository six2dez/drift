---
phase: 07-provider-spawn-registration
plan: 05
type: execute
wave: 5
depends_on: ["07-04"]
files_modified:
  - README.md
  - .planning/phases/07-provider-spawn-registration/07-VALIDATION.md
autonomous: false
requirements: [PRV-01, PRV-04, PRV-05]

estimate:
  tokens: 56000
  raw_tokens: 56000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The README's provider table describes what actually ships after this phase — not the pre-phase not-yet-supported sentences, and not a claim stronger than the evidence supports (SC-4's in-README half, Pitfall E)."
    - "A user reads, in the README, that a Drift crash may leave a Drift entry in an external CLI's configuration and that the next Drift start removes it — so the residual this phase accepted is stated rather than discovered."
    - "The phase's validation contract names real task ids, real commands and real files, and the commands it names are the ones this repo actually has."
    - "Every claim this phase makes about the CLIs' behaviour is written with its vehicle caveat: source-verified is not measured, a Node runner is not Caido's runtime, and a CI spawn contract is not a real Claude turn on a real Windows desktop."
    - "PRV-01's end-to-end claim is recorded at exactly the strength the evidence supports, with the reporter confirmation named as the residual and placed in the phase that owns it."
    - "The limitation sentence a Codex user reads has been read by a human and judged to disambiguate on its own — the one criterion in this phase no gate can reach."
  artifacts:
    - README.md
    - .planning/phases/07-provider-spawn-registration/07-VALIDATION.md
  key_links:
    - "The shared capability table's limitation sentences -> the provider card (07-02) -> the README rows: one voice across three surfaces, so a user seeing two of them recognises the same system talking."
    - "The `windows-latest` leg -> the phase's only real Windows evidence -> the recorded run URL and per-step conclusions."
  prohibitions:
    - "Never state that Drift has proven a specific installed CLI version on a specific machine forwards its environment. The evidence is that Drift supplies the variables and that the CLI's source forwards them."
    - "Never record a green tick as a CI claim. A claim carries its run URL, its per-leg conclusion, its per-step conclusion and the log line proving the mechanism."
    - "Never record a human read as a machine-verified result, and if the approval arrived through an orchestrating workflow rather than being observed first-hand, say so — a relayed human read is still a human read but is a weaker record."
    - "Never mark a requirement complete on the strength of a plan that merely touched it."
    - "Never claim the milestone's blocking must-have is closed end-to-end. The reporter confirmation is the residual and it belongs to a later phase."
---

<objective>
Close the phase honestly: say in the README what actually ships, fill the validation contract with the
task ids and commands that now exist, and put the phase's only real Windows evidence on the record
with its run URL and its caveat.

Purpose: SC-4's README half, SC-5's status wording, and the evidence discipline this project has
applied since Phase 1 — a claim carries what it did not prove alongside what it did.
Output: two rewritten README rows plus a residual-behaviour line, a filled validation document, a
recorded `windows-latest` run, and one human read of the sentence no gate can grade.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-provider-spawn-registration/07-CONTEXT.md
@.planning/phases/07-provider-spawn-registration/07-RESEARCH.md
@.planning/phases/07-provider-spawn-registration/07-01-SUMMARY.md
@.planning/phases/07-provider-spawn-registration/07-02-SUMMARY.md
@.planning/phases/07-provider-spawn-registration/07-03-SUMMARY.md
@.planning/phases/07-provider-spawn-registration/07-04-SUMMARY.md
</context>

<artifacts_this_plan_produces>
No new symbols. This plan rewrites two README table rows, adds one README line about post-crash
residue, and fills the phase's validation document.
</artifacts_this_plan_produces>

<!-- planner-discipline-allow: not yet supported -->
<!-- planner-discipline-allow: pnpm test -->

<tasks>

<task type="auto">
  <name>Task 1: Rewrite the README's provider rows to what actually ships</name>
  <files>README.md</files>
  <read_first>
    - `README.md` lines 36-50 (the provider setup paragraph and the four-row provider table, including the two rows carrying the pre-phase not-yet-supported sentences and the paragraph below the table about the experimental contract)
    - `packages/shared/src/cli-providers.ts` as written in 07-02 — the limitation sentences, so the README reads in the same voice rather than inventing a second wording
    - `.planning/phases/07-provider-spawn-registration/07-CONTEXT.md` § decision D-08 and § specific ideas (the one-voice requirement)
    - `.planning/phases/07-provider-spawn-registration/07-RESEARCH.md` § Common Pitfalls, Pitfall E, and § Q4 "Upstream Windows reliability — the honest PRV-04 / SC-5 status"
    - `.planning/phases/07-provider-spawn-registration/07-03-SUMMARY.md` and `07-04-SUMMARY.md` (what actually shipped, including the checkpoint's answer on the Codex token)
  </read_first>
  <action>
    Rewrite the two external-CLI rows. Remove the pre-phase sentences saying Drift MCP is not yet
    supported for them on Windows — that is now false, and leaving it would be the most visible wrong
    claim in the repository. Replace each row's MCP column with what the CLI is actually registered
    with: the validated Node executable plus the server script plus an explicit environment, written
    into that CLI's own configuration.

    For the CLI with the working channel, the status column says approvals and the MCP activity trace
    work, and states the evidence at its real strength: the environment forwarding is established from
    that project's own source, not measured against an installed binary, and its sanitisation is
    tightening upstream. Do not write a sentence that a reader could take as "Drift tested this".

    For the CLI without the channel, the status column reuses the limitation sentence from the shared
    table verbatim in meaning — attached and working for read-only tools, sensitive tools unavailable
    because that CLI builds a clean environment for its MCP servers — so a user who sees the provider
    card and then the README sees one voice, and cannot come away thinking there are two different
    mechanisms.

    Add one line below the table stating the residual this phase accepted: after a Drift crash or hard
    kill, a Drift entry may persist in an external CLI's configuration file, that entry carries a Caido
    session token, and the next Drift start removes it from every scope. Give the manual removal
    commands for both CLIs, matching the remediation strings 07-04 already emits, so the README and the
    error message agree.

    Leave the paragraph under the table about the shared runtime contract intact except where it is now
    factually wrong; the two CLIs are no longer registered through a wrapper, so any wording implying
    that must change. Do not expand the table with new columns and do not add a Windows section — the
    Windows install and prerequisites documentation is a later phase's requirement and is explicitly
    not this one's.
  </action>
  <acceptance_criteria>
    - `grep -c 'not yet supported' README.md` returns 0 — the two pre-phase sentences are gone.
    - Source assertion: both external-CLI rows describe registration by executable plus arguments plus environment, and neither describes a wrapper.
    - Source assertion: the CLI without a channel is described as attached and usable with sensitive tools unavailable, in the same terms as the shared table's sentence — not as unsupported and not as broken.
    - Source assertion: the working-channel row states the evidence as established from upstream source rather than as measured or tested.
    - Source assertion: a post-crash residue line exists below the table, names the token, names both CLIs' configuration files, states that the next start removes it, and carries removal commands matching what the backend prints.
    - Source assertion: no Windows install or prerequisites section was added.
    - `pnpm lint` exits 0 (unchanged — this task edits documentation only).
  </acceptance_criteria>
  <verify>
    <automated>test "$(grep -c 'not yet supported' README.md)" = 0 && grep -q 'crash' README.md && pnpm lint</automated>
  </verify>
  <done>The README describes the shipped registration shape, states each CLI's approval capability at the strength its evidence supports, and tells a user what a crash leaves behind and when it is cleaned.</done>
</task>

<task type="auto">
  <name>Task 2: Fill the phase validation contract with real ids, real commands and the vehicle caveat</name>
  <files>.planning/phases/07-provider-spawn-registration/07-VALIDATION.md</files>
  <read_first>
    - `.planning/phases/07-provider-spawn-registration/07-VALIDATION.md` in full — the seeded requirement-to-test map, the wave-zero list, the manual-only table and the sign-off checklist
    - All four prior plan summaries in this phase, for the task ids and the commands each task actually ran
    - `package.json` scripts and `.github/workflows/ci.yml` lines 40-60 and 190-210 — the commands this repo genuinely has
    - `.planning/phases/07-provider-spawn-registration/07-RESEARCH.md` § Q5 "Vehicle caveat, to be written verbatim into the plan and the phase report"
  </read_first>
  <action>
    Correct the document's test-infrastructure block first. The seeded full-suite command names a
    package script this repository does not define; the real full-suite invocation is the direct vitest
    run that both CI legs use. Fix the quick-run and full-suite rows and the sampling-rate section to
    the commands that exist, and note in one line that the seeded value was wrong and was corrected
    against the workflow, so a reader does not assume the document was never checked.

    Fill the per-task verification map with the real task ids from all five plans, each row naming the
    requirement, the behaviour, the test type, the command that proves it and whether the file exists
    now. Mark the wave-zero items complete only where the file genuinely exists. Add rows the seed did
    not anticipate: the capability predicate's fail-closed cases, the registration planner's two
    refusal arms, the removal policy's dual-scope output, the failure formatter's value-free signature,
    and the deletion checklist's counts.

    Add the vehicle caveat as its own section, written in the voice the earlier phases established and
    without softening: the phase proves the spawn plan, the escaping and the interpreter contract on
    Windows under a Node vehicle; it does not prove the backend entry module's wiring of them, because
    that module cannot be imported under the test runner; it does not prove Caido's own runtime, where
    the verbatim-arguments option is established from the fork's source but never executed; it does not
    prove that any installed CLI version on any machine forwarded its environment, only that Drift
    supplies the variables and that the CLI's source forwards them; and it does not prove that a real
    Claude Code CLI connected to Drift on a real Windows desktop. Name the reporter confirmation as
    the residual and name the phase that owns it.

    Update the manual-only table to match reality: the reporter confirmation and the real-machine
    reliability check stay, placed in their owning phase and explicitly not gates on this one; the
    limitation-sentence read becomes this plan's human-verify checkpoint rather than an unscheduled
    item. Then complete the sign-off checklist honestly — set the compliance flags only if the
    conditions genuinely hold, and if any does not, say which and why rather than setting it anyway.
  </action>
  <acceptance_criteria>
    - The document names `pnpm exec vitest run` as the full-suite command and no longer names a script this repository does not define, with a one-line note recording the correction.
    - Every task in plans 07-01 through 07-05 appears in the per-task verification map with the command that proves it.
    - Every wave-zero item is marked complete or is marked incomplete with a stated reason; none is left ambiguous.
    - The vehicle-caveat section exists and names all five things the phase did not prove.
    - The manual-only table places the reporter confirmation and the reliability check in their owning phase and marks them as not gating this phase.
    - The sign-off flags in the front matter reflect the measured state; if `nyquist_compliant` is not set true, the blocking reason is written into the document.
    - Front matter remains valid and the status field is advanced only if the conditions for it hold.
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'pnpm exec vitest run' .planning/phases/07-provider-spawn-registration/07-VALIDATION.md && test "$(grep -c 'pnpm test' .planning/phases/07-provider-spawn-registration/07-VALIDATION.md)" = 0</automated>
  </verify>
  <done>The phase's validation contract names the commands this repository has, maps every task to the check that proves it, and states in one place everything the phase's evidence does not cover.</done>
</task>

<task type="auto">
  <name>Task 3: Run the phase tree on windows-latest and record the evidence</name>
  <files>.planning/phases/07-provider-spawn-registration/07-VALIDATION.md</files>
  <precondition>`gh auth status` succeeds and `git remote get-url origin` resolves to the six2dez/drift remote — this task needs a real GitHub Actions run and cannot be satisfied locally.</precondition>
  <read_first>
    - `.github/workflows/ci.yml` lines 1-20 and 70-115 (the every-branch trigger and the blocking Windows sibling job)
    - `.planning/phases/07-provider-spawn-registration/07-01-SUMMARY.md` (the scratch-branch procedure and teardown assertion this task repeats, and the pre-existing unrelated scratch branch recorded there)
    - `.planning/STATE.md` § the `[01-06]`, `[03-04]` and `[03-05]` entries — the run-record discipline, the log-extraction pipeline, and the teardown discrimination
  </read_first>
  <action>
    Push the complete phase tree to a throwaway branch named for this plan, staging only tracked paths
    with targeted arguments — the repository root still carries an untracked review document that must
    never reach the public remote.

    Record the run as a claim, not as a tick: the run URL, the conclusion of every job leg including
    all four Node versions on the Linux matrix and the single Windows job, the per-step conclusions for
    the Windows job's type-check, lint, test and build steps, and the log lines proving the phase's new
    Windows-gated cases actually executed on that host rather than skipping. Strip the log prefix with
    a field cut and remove the escape sequences and the leading timestamp with a Perl substitution; the
    platform stream editor silently no-ops on both, which returns nothing and is indistinguishable from
    a proof that failed. Validate the pipeline against an independently known non-zero expected count
    before trusting any count it produces.

    Compare the result against the run 07-01 recorded. If a leg that was green there is red here, the
    phase introduced the regression and the plan does not close until it is understood — a red leg is
    never explained away as flakiness without evidence.

    Write the run URL and the per-step conclusions into the validation document's vehicle-caveat
    section, so the phase's single strongest piece of evidence is stored beside the statement of what
    it does not cover.

    Tear the branch down locally and remotely. Assert the teardown by capturing the remote listing and
    discriminating on the specific branch name rather than on a glob — the glob is non-empty for an
    unrelated pre-existing branch, and a glob listing exits zero whether or not it matched. Record the
    full unfiltered listing beside the assertion.
  </action>
  <acceptance_criteria>
    - A run URL is recorded together with every job leg's conclusion and the Windows job's four per-step conclusions.
    - The log lines proving the Windows-gated cases RAN on that host are recorded verbatim; a run in which they skipped is not evidence.
    - All four Linux matrix legs and the Windows leg are green, and any difference from the run recorded in 07-01 is explained with evidence rather than dismissed.
    - `git ls-remote --heads origin` no longer lists this plan's scratch branch; the captured unfiltered listing is recorded, and the unrelated pre-existing scratch branch is named as pre-existing and left alone.
    - `git status --porcelain` is clean for tracked paths after teardown, and the untracked repository-root review document was never staged.
  </acceptance_criteria>
  <verify>
    <automated>gh run view "$(gh run list --branch "$(git rev-parse --abbrev-ref HEAD)" --json databaseId -q '.[0].databaseId' --limit 1)" --json jobs -q '.jobs[] | {name, conclusion, steps: [.steps[] | {name, conclusion}]}'</automated>
  </verify>
  <done>The phase tree is green on all five CI legs, the Windows evidence is recorded with its run URL and per-step conclusions beside the statement of what it does not prove, and the scratch branch is gone from the public remote with the teardown asserted.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: read the limitation sentence a Codex user actually sees</name>
  <what-built>
    The provider card in Settings now renders a third state. A provider that is installed, resolved and
    registered but cannot receive approval prompts shows an amber status dot and a limitation sentence,
    instead of the green-or-red dot and the error-only text that shipped before this phase. The
    sentence comes from a static table shared with the tool policy, and the same wording appears in the
    README row for that provider.

    This is the one criterion in the phase no gate can reach. The predicate behind the sentence is
    unit-tested; whether the sentence READS correctly is a human judgement, and D-08 makes the wording
    requirement binding rather than advisory — the map it travels through carries no discriminator, so
    the sentence has to disambiguate on its own.
  </what-built>
  <how-to-verify>
    1. Build and load the plugin, then open Settings and scroll to CLI Providers.
    2. Find the provider whose sensitive tools are disabled by this phase. Confirm its status dot is
       amber, not red, and that its resolved path is shown — it is a working provider.
    3. Read the limitation sentence beneath it. Confirm, without looking at any other field, that it
       tells you the provider IS attached and that a specific capability is unavailable, and that it
       says why. Confirm it does not read as "Drift could not determine" — the evidence is a
       source-verified negative and the sentence should say so.
    4. Compare it against the older wording style for a provider Drift never registered at all.
       Confirm a user seeing both across an upgrade would read them as the same system describing two
       different states, and would not conclude there are two different mechanisms.
    5. Confirm the command field shows its placeholder hint when empty, and that typing a nonsense path
       produces the backend's precise error rather than a new frontend validation message.
    6. Read the two rewritten README provider rows and confirm they match what the card says.
  </how-to-verify>
  <resume-signal>Reply "approved", or describe which sentence reads wrong and how. If the approval reaches the executor through the orchestrating workflow rather than being observed first-hand, that must be recorded in the summary as a relayed human read — still a human read, but a weaker record.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local working tree -> the public git remote | A scratch branch push publishes everything staged and everything already committed on the branch. |
| Phase claims -> future readers and downstream phases | A recorded claim stronger than its evidence becomes a trap for the phase that relies on it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-18 | Information Disclosure | The scratch-branch push to a public remote | high | mitigate | Targeted path staging only, never an add-everything flag; the repository root carries an untracked review document that would otherwise be published. Teardown asserted by discriminating on the specific branch name and recording the full unfiltered remote listing, because a glob listing exits zero whether or not it matched. |
| T-07-19 | Repudiation | A CI claim recorded as a green tick | medium | mitigate | Every claim carries its run URL, per-leg conclusion, per-step conclusion and the log line proving the mechanism — the standing rule since Phase 1, and the reason a later reader can trust the record after the artifacts expire. |
| T-07-20 | Spoofing | A README or report claim stronger than the evidence | high | mitigate | The vehicle caveat is written verbatim and names all five uncovered areas; the working-channel row states source-verified rather than measured; the blocking must-have is recorded with the reporter confirmation as its residual. Enforced by the plan's prohibitions and by source criteria on the README wording. |
| T-07-21 | Repudiation | A relayed human approval recorded as first-hand | low | mitigate | The resume signal requires the provenance to be recorded when the approval arrives through the orchestrating workflow rather than being observed directly. |
| T-07-SC | Tampering | npm/pnpm dependency surface | high | mitigate | Documentation-only plan; no package installed and no lockfile change. |
</threat_model>

<flagged_assumptions>
- **PRV-01 (unclassified edge probe, unresolved).** The phase closes PRV-01's CI-provable half and
  nothing more. Nothing in CI executes the turn path, so the claim recorded is the spawn contract plus
  the static wiring gates. The reporter confirmation is the residual and belongs to a later phase; SC-1
  marks it "where possible" and it must not become a gate here.
- **PRV-04 / SC-5 (unclassified edge probe, unresolved).** The premise behind SC-5's gate — unresolved
  upstream Windows MCP issues for the scoped CLI — was not reproduced by a title-and-query search, but
  that search read no issue bodies against a very large backlog. A search finding nothing is weak
  evidence, so the checkpoint stays and the status wording says best-effort.
- **PRV-05 (unclassified edge probe, unresolved).** The evidence is source-verified per CLI, not
  measured. No CLI binary was executed anywhere in this phase, and the manual real-run confirmation
  was deliberately deferred as the companion to the reporter confirmation.
- **A2 (unresolved).** Caido's own runtime honouring the verbatim-arguments option is established from
  the fork's source and from the vendored type surface, and was never executed. Only a real Windows
  Caido closes it.
</flagged_assumptions>

<verification>
- `pnpm exec vitest run`, `pnpm -r typecheck` and `pnpm lint` green locally.
- All five CI legs green on the phase tree, recorded with run URL and per-step conclusions.
- The README carries no pre-phase not-yet-supported sentence and does carry the post-crash residue line.
- The validation document names the commands this repository has and states everything the phase did not prove.
- One human read of the limitation sentence, recorded with its provenance.
</verification>

<success_criteria>
- The README describes the shipped registration shape and each CLI's real approval capability, at the strength the evidence supports.
- The phase's validation contract is filled with real task ids and real commands, and its vehicle caveat is written in full.
- The phase's only real Windows evidence is on the record as a claim, not a tick.
- The limitation sentence has been read by a human and judged to disambiguate on its own.
- No requirement is marked complete on the strength of a plan that merely touched it.
</success_criteria>

<output>
Create `.planning/phases/07-provider-spawn-registration/07-05-SUMMARY.md` when done.
</output>
