---
phase: 01-restore-the-verification-signal
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-lock.yaml
  - eslint.config.mjs
  - caido.config.ts
  - CLAUDE.md
  - .planning/codebase/STACK.md
  - .planning/codebase/CONVENTIONS.md
autonomous: true
requirements: [SIG-02]

must_haves:
  truths:
    - "`pnpm lint` invokes a real, installed ESLint 10 against a committed flat config"
    - "The lint run covers the whole repo, including packages/backend/assets/mcp-server.mjs, which the old ./packages/**/src glob never reached"
    - "No --fix appears anywhere on the `pnpm lint` path; --fix lives only in lint:fix"
    - "ESLint loads without the MODULE_TYPELESS_PACKAGE_JSON reparse warning"
    - "The lockfile lands in the same commit as package.json, so `pnpm install --frozen-lockfile` cannot fail in CI"
  artifacts:
    - path: "eslint.config.mjs"
      provides: "Root ESLint 10 flat config covering TypeScript, Vue SFCs, .mjs, and test files"
      min_lines: 40
      contains: "eslint-config-prettier/flat"
    - path: "package.json"
      provides: "8 exact-pinned ESLint devDependencies; lint / lint:fix scripts; widened format glob"
      contains: "--max-warnings 0"
    - path: "pnpm-lock.yaml"
      provides: "Resolved dependency tree for the 8 new devDependencies"
      contains: "eslint"
  key_links:
    - from: "package.json scripts.lint"
      to: "eslint.config.mjs"
      via: "`eslint .` resolves the flat config upward from each linted file"
      pattern: "\"lint\":\\s*\"eslint \\."
    - from: "eslint.config.mjs"
      to: "eslint-config-prettier/flat"
      via: "final config entry disables every rule Prettier already owns"
      pattern: "eslint-config-prettier/flat"
---

<objective>
Install a real ESLint 10 toolchain and land a committed flat config, so `pnpm lint` stops being a script
that names a binary the repo does not have.

Purpose: SIG-02's danger is not the debt — measured at 4 errors / 20 warnings, and 0/0 after the
recommended rule config. The danger is the current script, `eslint ./packages/**/src --fix`, which in CI
would rewrite source in the runner's checkout, report success, and discard the changes: a textbook false
pass. 17 of the 24 measured findings are auto-fixable, so this is not theoretical. The glob is also stale —
it never reaches `packages/backend/assets/mcp-server.mjs`, all 829 lines of it.

Output: 8 exact-pinned devDependencies plus lockfile, `eslint.config.mjs`, `lint` / `lint:fix` scripts, a
`pnpm format` glob that can reach root-level files, and the four stale ESLint claims in the project docs
corrected.

Scope note: this plan makes the linter real and provably repo-wide. It does NOT clear the measured debt —
that is plan `01-04`, because one of the four errors (`settings.ts:78`) lives in plan `01-01`'s file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-restore-the-verification-signal/01-RESEARCH.md
@.planning/phases/01-restore-the-verification-signal/01-PATTERNS.md
@.planning/phases/01-restore-the-verification-signal/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install the ESLint stack exact-pinned and rewrite the package scripts</name>

  <files>package.json, pnpm-lock.yaml, caido.config.ts</files>

  <read_first>
    - `package.json` — the whole file. Note `scripts` at `:21-27` and `devDependencies` at `:28-40`: all 11
      existing root devDependencies are exact-pinned with no caret, alphabetically sorted. Note also that
      the root has no `"type": "module"`.
    - `.planning/phases/01-restore-the-verification-signal/01-RESEARCH.md` -> `## Standard Stack`
      (lines 135-171) — the eight packages, their roles, and their peer constraints.
    - `.planning/phases/01-restore-the-verification-signal/01-RESEARCH.md` -> `## Package Legitimacy Audit`
      (lines 175-193) — all eight audited `[OK]`, official repos, zero postinstall scripts, lowest weekly
      download count 3.6M. Read the operator note: `slopcheck install` is NOT read-only; if you re-run the
      audit, use `slopcheck scan`.
    - `.planning/phases/01-restore-the-verification-signal/01-RESEARCH.md` -> Assumption A2 (line 948) —
      the pnpm 9 strict-peer-resolution risk and its escalation path.
    - `.planning/phases/01-restore-the-verification-signal/01-PATTERNS.md` lines 299-331 — the exact-pin
      convention conflict and how it resolves.
  </read_first>

  <action>
Install the eight packages with exact pins. House convention wins over RESEARCH's `^` ranges: 11 of 11
existing root devDependencies are exact-pinned; carets appear only in `packages/frontend/package.json`.
Use `-E` / `--save-exact`:

`pnpm add -Dw -E eslint@10.8.1 @eslint/js@10.0.1 typescript-eslint@8.67.0 eslint-plugin-vue@10.10.0 vue-eslint-parser@10.4.1 eslint-config-prettier@10.1.8 globals@17.11.0 @vitest/eslint-plugin@1.6.27`

All eight versions were re-confirmed resolvable on the npm registry during planning. `-w` is required: the
flat config lives at the repo root and ESLint 10 resolves plugins relative to the config file.

If the install fails on peer resolution (assumption A2): STOP and report. Do not add `.npmrc` overrides,
`--no-strict-peer-dependencies`, `pnpm.overrides`, or `pnpm.peerDependencyRules` to work around it. All
non-`eslint` peers are declared optional via `peerDependenciesMeta`, so a failure here is new information
that needs a decision, not a silent workaround.

Supply-chain check on the install output: the audit recorded zero postinstall scripts across all eight. If
pnpm's output mentions any lifecycle script (`postinstall`, `prepare`, `install`) for one of these eight
packages, STOP and report before committing — that contradicts the audit.

Rewrite `scripts` in `package.json`:
- `"lint": "eslint . --max-warnings 0"` — no `--fix`, ever. The `--max-warnings 0` ratchet goes in on day
  one: measured residual with the recommended rule config is 0 errors / 0 warnings, so there is nothing to
  grandfather, and a ratchet with slack is a ratchet that slips. Putting it in the script rather than only
  in the CI invocation gives one source of truth for local and CI runs.
- `"lint:fix": "eslint . --fix"` — local convenience only. First colon-namespaced script in this file;
  that is the ecosystem standard and is acceptable.
- `"format": "prettier --write \"packages/**/src/**/*.{vue,ts,js,json}\" \"*.{ts,mjs}\""` — the current
  glob is `packages/**/src/**` and cannot reach the new root-level `vitest.setup.ts` or `eslint.config.mjs`.
  Widening to root `*.{ts,mjs}` closes that drift hazard permanently. Bounded blast radius: at the root this
  matches only `caido.config.ts`, `vitest.config.ts`, `vitest.setup.ts`, and `eslint.config.mjs`.

Bring `caido.config.ts` under the widened glob: it is currently not Prettier-clean (one over-long
`description:` line that Prettier wraps onto two). Run `pnpm exec prettier --write caido.config.ts` and
commit the resulting whitespace-only change. Do NOT run bare `pnpm format` in this task — `vitest.setup.ts`
and `vitest.config.ts` are owned by plan `01-02` in the same wave (which formats them itself), and this plan
must not write to another plan's file.

Leave `engines`, `packageManager`, `.nvmrc`, and the `build` / `watch` / `typecheck` scripts untouched.
The Node 20 matrix leg stays for this phase; raising `.nvmrc` off EOL Node 20 is a separate, user-facing
support decision, not a CI one.

Commit `pnpm-lock.yaml` in the same commit as `package.json`. CI runs `pnpm install --frozen-lockfile` on
every one of the four matrix legs; if the lockfile lags, all four fail.
  </action>

  <verify>
    <automated>pnpm exec eslint --version && node -p "require('./package.json').scripts.lint" && pnpm exec prettier --check caido.config.ts</automated>
  </verify>

  <acceptance_criteria>
    - SIG-02a — `pnpm exec eslint --version` prints a version starting with `v10.`.
    - All eight packages present and exact-pinned. Run this and expect it to print `pins OK`:
      `node -e "const d=require('./package.json').devDependencies,w={eslint:'10.8.1','@eslint/js':'10.0.1','typescript-eslint':'8.67.0','eslint-plugin-vue':'10.10.0','vue-eslint-parser':'10.4.1','eslint-config-prettier':'10.1.8',globals:'17.11.0','@vitest/eslint-plugin':'1.6.27'};for(const k in w){if(d[k]!==w[k])throw new Error(k+'='+d[k]+' want '+w[k])}console.log('pins OK')"`
    - devDependencies still alphabetically sorted with zero range specifiers. Expect `sorted+exact OK`:
      `node -e "const d=require('./package.json').devDependencies,k=Object.keys(d);if(JSON.stringify(k)!==JSON.stringify([...k].sort()))throw new Error('not sorted');if(Object.values(d).some(v=>/[\^~]/.test(v)))throw new Error('range found');console.log('sorted+exact OK')"`
    - SIG-02e (package.json half) — `node -p "require('./package.json').scripts.lint"` prints
      `eslint . --max-warnings 0` and contains no `--fix`.
    - `node -p "require('./package.json').scripts['lint:fix']"` prints `eslint . --fix`.
    - The format glob reaches the root: `node -p "require('./package.json').scripts.format"` contains
      the substring `*.{ts,mjs}`.
    - `pnpm exec prettier --check caido.config.ts` exits `0`. Check `caido.config.ts` only: plan `01-02`
      owns `vitest.config.ts` and runs in this same parallel wave, so asserting on it here would couple two
      sibling plans. It is re-asserted at settled state in plan `01-04` task 3 step F.
    - Lockfile moved with the manifest: `git status --porcelain pnpm-lock.yaml package.json` lists both.
    - Untouched support surface: `node -p "require('./package.json').engines.node"` prints `>=20` and
      `cat .nvmrc` prints `20`.
  </acceptance_criteria>

  <done>
    ESLint 10.8.1 and its seven companions are installed as exact-pinned root devDependencies with the
    lockfile updated, `pnpm lint` is `eslint . --max-warnings 0` with `--fix` isolated to `lint:fix`, the
    format glob reaches root-level config files, and `caido.config.ts` is Prettier-clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write eslint.config.mjs, prove the lint surface, correct the stale ESLint docs</name>

  <files>eslint.config.mjs, CLAUDE.md, .planning/codebase/STACK.md, .planning/codebase/CONVENTIONS.md</files>

  <read_first>
    - `.planning/phases/01-restore-the-verification-signal/01-RESEARCH.md` -> `### Pattern 3: ESLint 10 Flat
      Config for a pnpm TS+Vue Monorepo` (lines 454-524). This exact config was executed read-only against
      this repo and measured at 4 errors / 20 warnings. There is no repo analog (confirmed in 01-PATTERNS.md
      `## No Analog Found`), so Pattern 3 is the source of truth.
    - `.planning/phases/01-restore-the-verification-signal/01-RESEARCH.md` -> `## Measured Lint Debt`
      (lines 623-660) — the per-rule disposition rationale behind every override in the config.
    - `.planning/phases/01-restore-the-verification-signal/01-RESEARCH.md` -> Pitfalls 5, 6, 7
      (lines 688-701) — the `.mjs` extension requirement, the `--fix` false pass, and ESLint's exit-code
      matrix (a glob matching no files exits `2`, not `0`).
    - `.planning/phases/01-restore-the-verification-signal/01-RESEARCH.md` -> `## Don't Hand-Roll`
      (lines 608-619) — in particular why `vue-eslint-parser` must stay the top-level parser for `.vue`.
    - `CLAUDE.md` lines 70-76 and 112-116 — the two stale ESLint claims.
    - `.planning/codebase/STACK.md` line 96 and `.planning/codebase/CONVENTIONS.md` lines 34-37 — the same
      claims in the codebase map that CLAUDE.md is assembled from.
  </read_first>

  <action>
Create `eslint.config.mjs` at the repo root. The `.mjs` extension is mandatory — the root `package.json`
has no `"type": "module"`, so `eslint.config.js` triggers `[MODULE_TYPELESS_PACKAGE_JSON] Warning: ...
Reparsing as ES module ... This incurs a performance overhead` on every run (reproduced during research).
Implement RESEARCH Pattern 3 exactly. Required structure, in order:

1. Imports: `js` from `@eslint/js`, `tseslint` from `typescript-eslint`, `pluginVue` from
   `eslint-plugin-vue`, `vitest` from `@vitest/eslint-plugin`, `prettier` from
   `eslint-config-prettier/flat`, `globals` from `globals`.
2. `export default tseslint.config(` ... `);`
3. `{ ignores: ["**/node_modules/**", "dist/**", "**/dist/**"] }` — this exact ignore set is what produced
   the measured file count.
4. `js.configs.recommended`, `...tseslint.configs.recommended`, `...pluginVue.configs["flat/recommended"]`.
   Do NOT use `recommendedTypeChecked`: measured at 49 errors, 33 of them parse errors, because both
   package tsconfigs `exclude` `./src/**/*.test.ts` and `.vue` needs `parserOptions.extraFileExtensions`.
   Type-aware linting is deliberately deferred to backlog 999.10.
5. `{ files: ["**/*.vue"], languageOptions: { globals: globals.browser, parserOptions: { parser: tseslint.parser } } }`
   — the TS parser must be nested under `parserOptions.parser`, leaving `vue-eslint-parser` as the
   top-level parser that `flat/recommended` installs. Setting `@typescript-eslint/parser` at the top level
   for `.vue` breaks template analysis entirely; it is the single most common Vue-flat-config mistake.
6. `{ files: ["packages/frontend/**/*.ts"], languageOptions: { globals: globals.browser } }`
7. `{ files: ["packages/backend/**/*.ts", "packages/shared/**/*.ts"], languageOptions: { globals: globals.node } }`
8. `{ files: ["**/*.mjs", "*.config.ts", "vitest.setup.ts"], languageOptions: { globals: globals.node } }`
   — `packages/backend/assets/mcp-server.mjs` is a standalone Node process, not part of the QuickJS bundle.
9. `{ files: ["**/*.test.ts"], ...vitest.configs.recommended }` — this brings in `no-focused-tests`, the
   only thing that catches a committed `describe.only` / `it.only` reducing the suite to one test and
   calling it green. Grep cannot see `.each.only`.
10. A rules block containing:
    - `"@typescript-eslint/no-unused-vars": ["error", { argsIgnorePattern: "^_", varsIgnorePattern: "^_", caughtErrorsIgnorePattern: "^_" }]`
      — the repo already uses the `_` prefix for intentionally-unused bindings (five `_sdk` RPC handler
      params in `index.ts`, `_content` in `chat.ts`).
    - `"vue/attribute-hyphenation": "off"` and `"vue/v-on-event-hyphenation": "off"` — all 14 findings are
      PrimeVue's camelCase public API (`optionLabel`, `optionValue`, `autoResize`, `modelValue`, `minSize`,
      `inputClass`, `@update:modelValue`). Hyphenating them would be a behavioural change dressed as
      formatting. Add a one-line comment saying so.
11. `{ files: ["**/*.test.ts"], rules: { "vue/one-component-per-file": "off" } }` — inline stub components
    in `ChatInput.test.ts` are intentional.
12. `prettier` as the LAST entry. It removes 168 of the 188 baseline warnings in one line.

`vue/no-v-html` stays ON. Do not add it to the rules-off block. `MessageBubble.vue` is handled with a
justified inline disable in plan `01-04`, so the rule remains a tripwire for the next `v-html`.

Include a short header comment in the repo's "why, not what" style naming the two constraints that shaped
this file: the `.mjs` extension (root package is CommonJS-typed) and ESLint being dev-time only — it emits
no runtime code and ships nothing into `dist/plugin_package/`, so CLAUDE.md's QuickJS constraints (no Zod,
no `import.meta`, no dynamic require) are unaffected.

Write the file to Prettier defaults, then run `pnpm exec prettier --write eslint.config.mjs`. Do NOT run
bare `pnpm format` — `vitest.setup.ts` and `vitest.config.ts` belong to plan `01-02` in this same wave.

Then correct the four stale ESLint claims. All four assert the same two false things: that `pnpm lint` is
`eslint ./packages/**/src --fix`, and that there is no project-level ESLint config.
- `CLAUDE.md:73` (build/tooling list)
- `CLAUDE.md:114-115` (Conventions)
- `.planning/codebase/STACK.md:96`
- `.planning/codebase/CONVENTIONS.md:35-36`

Replace with the truth: ESLint 10.8.1 flat config at `eslint.config.mjs` (root, `.mjs` because the root
package is CommonJS-typed), covering TypeScript, Vue SFCs, `.mjs`, and test files; `pnpm lint` runs
`eslint . --max-warnings 0` with no `--fix`; `pnpm lint:fix` is the local auto-fix entry point. Do not touch
`CLAUDE.md:316` or anything below it — that section is managed by `generate-claude-profile`.

Expect a nonzero error count from `pnpm lint` at the end of this task: the measured debt has not been
cleared yet. That is plan `01-04`. Record the exact error and warning counts, and the linted-file count, in
the SUMMARY so `01-04` has a baseline to close against.
  </action>

  <verify>
    <automated>pnpm exec eslint . -f json > /tmp/drift-eslint-0103.json 2>/dev/null; node -e "const r=require('/tmp/drift-eslint-0103.json'),f=r.map(x=>x.filePath);if(r.length<59)throw new Error('only '+r.length+' files linted');for(const n of ['assets/mcp-server.mjs','eslint.config.mjs'])if(!f.some(p=>p.endsWith(n)))throw new Error('missing '+n);console.log('files='+r.length,'errors='+r.reduce((a,x)=>a+x.errorCount,0),'warnings='+r.reduce((a,x)=>a+x.warningCount,0))"</automated>
  </verify>

  <acceptance_criteria>
    - SIG-02b — the config loads with no reparse warning:
      `test "$(pnpm lint 2>&1 | grep -c MODULE_TYPELESS)" = "0"` succeeds. The file is named
      `eslint.config.mjs`; `test -f eslint.config.js` must fail.
    - SIG-02c — the lint surface is repo-wide, not a stale glob. The `<verify>` command above must exit `0`,
      printing a `files=` count of at least `59` and confirming both
      `packages/backend/assets/mcp-server.mjs` and `eslint.config.mjs` are in the linted set. (Research
      measured 59 lintable files pre-phase; this phase adds `eslint.config.mjs`, `vitest.setup.ts`, and
      `packages/frontend/src/__storage-shim.test.ts`, so the settled count is ~62 and is re-asserted in plan
      `01-04`.) A count of `0` or an exit code of `2` means a broken glob or over-broad `ignores` — fix it,
      do not accept the exit code.
    - SIG-02e (workflow half is plan `01-05`; this half): `grep -c -- "--fix" eslint.config.mjs` returns `0`.
    - `vue/no-v-html` is NOT disabled globally:
      `grep -c 'vue/no-v-html' eslint.config.mjs` returns `0`.
    - The Vue parser is nested, not top-level:
      `grep -c 'parserOptions: { parser: tseslint.parser }' eslint.config.mjs` returns `1` or more, and
      `eslint.config.mjs` contains no top-level `parser:` key inside a `**/*.vue` `languageOptions` block.
    - `prettier` is the final config entry: the last non-empty line before the closing `);` is `prettier,`.
    - `@vitest/eslint-plugin` is wired for test files:
      `grep -c 'vitest.configs.recommended' eslint.config.mjs` returns `1`.
    - `pnpm exec prettier --check eslint.config.mjs` exits `0`.
    - Docs corrected — no stale claim survives:
      `grep -c 'eslint ./packages/\*\*/src --fix' CLAUDE.md .planning/codebase/STACK.md .planning/codebase/CONVENTIONS.md`
      returns `0` for each file, and
      `grep -c 'No project-level ESLint config file' CLAUDE.md .planning/codebase/CONVENTIONS.md` returns
      `0` for each file.
    - `grep -c 'generate-claude-profile' CLAUDE.md` still returns `1` (the managed section was not disturbed).
  </acceptance_criteria>

  <done>
    `eslint.config.mjs` exists, loads without the reparse warning, lints at least 59 files including
    `mcp-server.mjs`, keeps `vue/no-v-html` enabled, nests the TS parser correctly for `.vue`, and ends with
    `eslint-config-prettier/flat`. The four stale ESLint claims in CLAUDE.md and the codebase map now
    describe reality. The residual error/warning counts are recorded for plan `01-04`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry -> repo `node_modules` and lockfile | Eight new third-party dev dependencies enter the build environment. Untrusted code crosses here at install time and at every lint invocation. |
| CI runner checkout -> reported job status | A tool that can modify the checkout and then report success is a false-pass channel. |
| Lint configuration -> security signal | Rules that flag security-relevant patterns can be silenced to make the build pass. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering | npm installs: `eslint`, `@eslint/js`, `typescript-eslint`, `eslint-plugin-vue`, `vue-eslint-parser`, `eslint-config-prettier`, `globals`, `@vitest/eslint-plugin` | mitigate | All eight audited `[OK]` in RESEARCH `## Package Legitimacy Audit` (slopcheck 0.6.1 plus per-package `npm view`): official source repos, zero postinstall scripts, lowest weekly download count 3.6M. No `[SUS]` or `[SLOP]` package is present, so no blocking legitimacy checkpoint is required before install. Install uses exact pins (no `^`), `pnpm-lock.yaml` is committed in the same commit as `package.json`, and the install output is inspected for any lifecycle script — a lifecycle script on any of the eight contradicts the audit and halts the task. |
| T-01-09 | Tampering | `eslint --fix` on any CI-reachable path | mitigate | `scripts.lint` is `eslint . --max-warnings 0`; `--fix` exists only in `scripts["lint:fix"]`, which CI never calls. 17 of the 24 measured findings are auto-fixable, so an accidental `--fix` in CI would silently erase most of the debt and go green. Asserted by SIG-02e against `package.json` here and against `ci.yml` in plan `01-05`. |
| T-01-10 | Spoofing | ESLint exiting `0` because it linted zero files | mitigate | SIG-02c asserts the linted-file count is at least 59 and names `packages/backend/assets/mcp-server.mjs` explicitly — the file the previous glob never reached. A no-match glob exits `2` (measured), which is fatal but not self-explanatory, so the count assertion is the real control. |
| T-01-11 | Repudiation | Silencing a security-relevant rule to clear debt | mitigate | `vue/no-v-html` stays enabled globally and is explicitly asserted absent from the rules-off block. The only two rules turned off (`vue/attribute-hyphenation`, `vue/v-on-event-hyphenation`) are non-security formatting rules whose findings are all PrimeVue's camelCase public API, with the rationale recorded in RESEARCH `## Measured Lint Debt`. |
| T-01-12 | Tampering | Lockfile drift between local and CI | mitigate | Lockfile committed alongside `package.json`; CI keeps `pnpm install --frozen-lockfile` on all four matrix legs (plan `01-05`). |
| T-01-13 | Elevation of privilege | Malicious postinstall in a new dev dependency | mitigate | `npm view <pkg> scripts.postinstall` returned none for all eight during the audit; the install-output inspection above is the runtime re-check. |
</threat_model>

<verification>
Run from the repo root:

1. `pnpm exec eslint --version` -> `v10.x`
2. `node -p "require('./package.json').scripts.lint"` -> `eslint . --max-warnings 0`
3. `test "$(pnpm lint 2>&1 | grep -c MODULE_TYPELESS)" = "0"` -> exit 0 (SIG-02b)
4. The `<verify>` JSON-report command in Task 2 -> exit 0, `files=` at least 59 (SIG-02c)
5. `pnpm exec prettier --check caido.config.ts eslint.config.mjs` -> exit 0 (`vitest.config.ts` belongs
   to plan `01-02` in this wave; it is checked at settled state in plan `01-04`)
6. `git status --porcelain -- package.json pnpm-lock.yaml eslint.config.mjs caido.config.ts CLAUDE.md .planning/codebase/STACK.md .planning/codebase/CONVENTIONS.md`
   -> only those seven paths. Path-scoped, not bare: plans `01-01` and `01-02` run concurrently in this wave
   in the same working tree, so a bare `git status --porcelain` cannot pass for more than one of the three.
   The negative half — nothing outside `files_modified` changed — is asserted repo-wide at settled state in
   plan `01-04` task 3.

`pnpm lint` is expected to exit nonzero at the end of this plan. The 0/0 gate (SIG-02d) belongs to plan
`01-04`, which owns the three remaining error sites plus the Vue warnings.
</verification>

<success_criteria>
- ESLint 10.8.1 plus the seven companion packages are installed as exact-pinned root devDependencies with
  `pnpm-lock.yaml` updated in the same commit.
- `eslint.config.mjs` exists, loads warning-free, and lints at least 59 files including
  `packages/backend/assets/mcp-server.mjs`.
- `pnpm lint` carries no `--fix` and enforces `--max-warnings 0`; `pnpm lint:fix` is the only `--fix` entry.
- `pnpm format` can now reach root-level `*.{ts,mjs}` files; `caido.config.ts` is Prettier-clean.
- CLAUDE.md and the codebase map no longer make false claims about ESLint.
- No file outside `files_modified` changed — asserted path-scoped here (wave-1 concurrency), and repo-wide
  at settled state in plan `01-04` task 3 (CMP-inv).
</success_criteria>

<output>
Create `.planning/phases/01-restore-the-verification-signal/01-03-SUMMARY.md` when done.
Record: the exact `pnpm exec eslint --version` output, the linted-file count, the residual error and warning
counts with their file:line locations (the baseline plan `01-04` must close), and confirmation that the
install produced no lifecycle scripts.
</output>
