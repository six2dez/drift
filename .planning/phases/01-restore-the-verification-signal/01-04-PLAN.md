---
phase: 01-restore-the-verification-signal
plan: 04
type: execute
wave: 2
depends_on: ["01-01", "01-02", "01-03"]
files_modified:
  - packages/backend/assets/mcp-server.mjs
  - packages/backend/src/index.ts
  - packages/backend/src/claude-print.test.ts
  - packages/frontend/src/components/chat/MessageBubble.vue
autonomous: true
requirements: [SIG-01, SIG-02]

must_haves:
  truths:
    - "`pnpm lint` exits 0 with --max-warnings 0 across the whole repo"
    - "A deliberately introduced lint error makes `pnpm lint` exit 1 — the gate is proven to bite"
    - "The full suite is green on Node 22, 24 and 26 locally, with at least 125 tests and no reduction in test count"
    - "vue/no-v-html stays enabled at config level; the single legitimate use is suppressed by a narrowly scoped in-template disable/enable pair with a rationale, and MessageBubble.vue still compiles and mounts"
    - "No file in the Phase 5-8 spawn path changed anywhere in this phase"
  artifacts:
    - path: "packages/backend/assets/mcp-server.mjs"
      provides: "GraphQL timeout error that preserves its cause"
      contains: "cause"
    - path: "packages/frontend/src/components/chat/MessageBubble.vue"
      provides: "Attribute order fixed and a justified element-scoped vue/no-v-html disable/enable pair"
      contains: "eslint-disable vue/no-v-html"
  key_links:
    - from: "packages/frontend/src/components/chat/MessageBubble.vue"
      to: "eslint.config.mjs vue/no-v-html"
      via: "template disable/enable pair wrapping the single v-html element, outside the start tag"
      pattern: "eslint-(disable|enable) vue/no-v-html"
    - from: "pnpm lint"
      to: "CI Lint step"
      via: "same script, same --max-warnings 0 ratchet, no --fix on either side"
      pattern: "--max-warnings 0"
---

<objective>
Close the measured lint debt to 0 errors / 0 warnings, prove the gate actually bites, and run the
phase-local green gate across every Node major available on this machine.

Purpose: SIG-02's "passes clean" branch is reachable in one task — research measured the residual at
0/0 after the recommended rule config, so no debt-tracking document is needed. But "0 problems" is
worthless if nobody proved the command can report a problem, and "the suite is green" is worthless if it
was only ever run on a Node version where the bug does not exist. This plan does both.

Output: three one-line error fixes, the `MessageBubble.vue` attribute reorder plus an element-scoped
`vue/no-v-html` disable/enable pair with a verified rationale, and a recorded green gate on
Node 22.23.2 / 24.13.0 / 26.7.0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-restore-the-verification-signal/01-RESEARCH.md
@.planning/phases/01-restore-the-verification-signal/01-PATTERNS.md
@.planning/phases/01-restore-the-verification-signal/01-VALIDATION.md
@.planning/phases/01-restore-the-verification-signal/01-01-SUMMARY.md
@.planning/phases/01-restore-the-verification-signal/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clear the three remaining lint errors</name>

  <files>packages/backend/assets/mcp-server.mjs, packages/backend/src/index.ts, packages/backend/src/claude-print.test.ts</files>

  <read_first>
    - `packages/backend/assets/mcp-server.mjs` lines 262-284 — the GraphQL `catch (error)` block. Note the
      two throw sites: the `new Error(...)` timeout at `:278` and the bare `throw error;` at `:280`.
    - `packages/backend/src/index.ts` lines 705-720 — `writeApprovalDecision`, where
      `let current: Record<string, ApprovalDecision> = {};` at `:710` is immediately reassigned in both the
      `try` and the `catch`.
    - `packages/backend/src/claude-print.test.ts` lines 214-235 — the `let state = consumeClaudePrintChunk(...)`
      at `:217`; confirm whether `state` is reassigned anywhere in that `it` block before choosing `const`.
    - `.planning/phases/01-restore-the-verification-signal/01-PATTERNS.md` lines 454-464 — the confirmed
      current code at each of the four error sites and the note that none of them sits in the Phase 5-8
      spawn path.
    - `.planning/phases/01-restore-the-verification-signal/01-RESEARCH.md` -> `### The 4 errors` (lines
      633-642) — the rule that fires at each site and why it is new in ESLint 10's `recommended`.
    - `.planning/phases/01-restore-the-verification-signal/01-03-SUMMARY.md` — the residual error and warning
      counts recorded as this task's baseline.
  </read_first>

  <action>
Three one-line fixes. The fourth measured error (`packages/frontend/src/stores/settings.ts:78`,
`no-useless-assignment`) was already cleared by plan `01-01` — do not touch `settings.ts`.

1. `packages/backend/assets/mcp-server.mjs:278` — rule `preserve-caught-error` (new in ESLint 10's
   `recommended`). The unattributed rethrow is the **`new Error(...)` at line 278**, not the `throw error;`
   at line 280. Attach the cause: `throw new Error(\`GraphQL request timed out after ${timeoutMs}ms\`, { cause: error });`
   The message string must stay byte-identical, and the `throw error;` at `:280` must stay untouched —
   `packages/backend/src/mcp-server.transport.test.ts` and the other two `mcp-server.*.test.ts` files
   assert against this module's behaviour and must stay green. `{ cause }` is supported on every Node the
   MCP server runs on (Node >= 18 host requirement).

2. `packages/backend/src/index.ts:710` — rule `no-useless-assignment`. Drop the dead `= {}` initialiser:
   `let current: Record<string, ApprovalDecision>;`. Both the `try` and the `catch` assign it before use, so
   TypeScript's definite-assignment analysis stays satisfied. Nothing else in `writeApprovalDecision` changes.

3. `packages/backend/src/claude-print.test.ts:217` — rule `prefer-const`. Change `let state = ...` to
   `const state = ...` **only if `state` is never reassigned inside that `it` block**. Read the block first;
   if it is reassigned, the correct fix is different and you should report rather than force it.

Scope fence (assertion CMP-inv): none of these sites is inside `renderExportExecScript` (`index.ts:320`),
`writeMcpWrapper` (`:743`), `writeLaunchScript` (`:299`), `shellQuote` (`:398`), or any `chmod` call site.
Do not edit those functions, and do not let an editor auto-format sweep touch them. In particular: these
three files are **not** Prettier-clean today and this phase does not make them Prettier-clean. Do not run
`pnpm format`, `prettier --write`, or a format-on-save sweep over any of them — reformatting
`packages/backend/src/index.ts` rewrites the bodies of all five fenced identifiers and breaks CMP-inv.
Hand-edit the three lines only.
  </action>

  <verify>
    <automated>pnpm exec eslint packages/backend/assets/mcp-server.mjs packages/backend/src/index.ts packages/backend/src/claude-print.test.ts --max-warnings 0 && pnpm exec vitest run packages/backend && pnpm -r typecheck</automated>
  </verify>

  <acceptance_criteria>
    - `pnpm exec eslint packages/backend/assets/mcp-server.mjs packages/backend/src/index.ts packages/backend/src/claude-print.test.ts --max-warnings 0` exits `0`.
    - The cause is attached at the right site:
      `grep -c 'cause: error' packages/backend/assets/mcp-server.mjs` returns `1` or more, and
      `grep -c 'GraphQL request timed out after' packages/backend/assets/mcp-server.mjs` returns `1`
      (message unchanged), and
      `grep -c '^\s*throw error;' packages/backend/assets/mcp-server.mjs` is unchanged from before the edit.
    - `grep -v '^\s*//' packages/backend/src/index.ts | grep -c 'let current: Record<string, ApprovalDecision> = {}'` returns `0`.
    - `grep -c 'let state = consumeClaudePrintChunk' packages/backend/src/claude-print.test.ts` returns `0`.
    - All backend tests green: `pnpm exec vitest run packages/backend` reports `0 failed` across 9 files —
      in particular the three `mcp-server.*.test.ts` files.
    - `pnpm -r typecheck` exits `0`.
    - CMP-inv, spawn path untouched. **Compare against the working tree, not `HEAD`** — an uncommitted
      breach must not slip through as an empty diff. Expect no output from:
      `git diff 2c169d0 -- packages/backend/src/index.ts | grep -E '^[-+]' | grep -v '^[-+][-+]' | grep -E 'renderExportExecScript|writeMcpWrapper|writeLaunchScript|shellQuote|chmod'`
    - Token counts in `packages/backend/src/index.ts` are unchanged from the pre-phase baseline:
      `renderExportExecScript`=4, `writeMcpWrapper`=4, `writeLaunchScript`=3, `shellQuote`=3, `chmod`=4
      (`grep -c <token> packages/backend/src/index.ts` for each).
  </acceptance_criteria>

  <done>
    All three remaining measured ESLint errors are cleared with one-line changes, the MCP server's timeout
    error carries its cause without changing its message, the backend suite and typecheck are green, and the
    Phase 5-8 spawn path is provably untouched.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix MessageBubble attribute order and add the element-scoped vue/no-v-html disable</name>

  <files>packages/frontend/src/components/chat/MessageBubble.vue</files>

  <read_first>
    - `packages/frontend/src/components/chat/MessageBubble.vue` lines 96-115 — the `v-html` div. Confirmed
      current layout: the start tag spans `:104-108` with `v-html` on `:105`, `style` on `:106`, and a
      ~500-character `class` on `:107`. Both `vue/attributes-order` warnings point at `:106` and `:107`.
    - `packages/frontend/src/components/chat/MessageBubble.vue` lines 1-24 — the `<script setup>` block.
      `md` is constructed at `:18` and `renderedHtml` is computed at `:21-24`. The disable rationale must be
      verified against these lines, not assumed.
    - `.planning/phases/01-restore-the-verification-signal/01-PATTERNS.md` lines 466-480
      (`### The vue/no-v-html element-scoped disable`) — why the two edits must be done together
      (reordering moves `v-html` inside the multi-line start tag) and why `eslint-disable-next-line` is
      ruled out. That section was corrected during plan review; it now agrees with the action below.
    - `.planning/phases/01-restore-the-verification-signal/01-RESEARCH.md` -> `## Security Domain`, ASVS V5
      row (line 926) — the disposition this task implements.
    - `eslint.config.mjs` (written by plan `01-03`) — confirm `vue/comment-directive` is **not** in the
      rules-off block. The template disable below only works because `vue/comment-directive` ships in
      `pluginVue.configs["flat/base"]`, which `flat/recommended` pulls in.
  </read_first>

  <action>
Two coupled edits to the same element. The suppression mechanism is already decided — implement it as
written and do not substitute another one.

**Why `eslint-disable-next-line` is not usable here, and do not retry it.** `vue/no-v-html` reports on the
`v-html` **attribute** node, not on the element, so a comment above the `<div` line does not suppress it
once the attributes are reordered. And after edit 1 below, `v-html` becomes the last attribute *inside* a
multi-line start tag, so "the line immediately above the `v-html` attribute" is inside `<div ... />`. An
HTML comment there is a parse error, verified against this repo's own `@vue/compiler-sfc@3.5.29`:
`Illegal '/' in tags.` plus `Element is missing end tag.`. Collapsing the element onto one line is not a
stable escape either — the `class` value is ~500 characters, so Prettier re-expands the tag. The mechanism
in edit 2 was verified against the same compiler and parses with zero errors.

1. **`vue/attributes-order` (2 warnings).** Reorder the attributes on the div whose start tag currently
   spans `MessageBubble.vue:104-108` so that `style` and `class` come **above** `v-html`. This is the
   auto-fixable order — `vue/attributes-order` places the `CONTENT` group, which `v-html` belongs to, last.
   Apply it by hand rather than with `--fix` so the diff is reviewed. Keep each attribute on its own line
   and keep the element self-closing.

2. **`vue/no-v-html` (1 warning).** Wrap that one element in a scoped disable/enable pair, both comments
   placed **outside** the start tag at the same indentation as `<div`:
   - On the line(s) immediately before `<div`, an HTML comment carrying the rationale.
   - On the next line, on its own, the directive comment `<!-- eslint-disable vue/no-v-html -->`.
   - On the line immediately after the element's closing `/>`, on its own,
     `<!-- eslint-enable vue/no-v-html -->`.

   Keep the rationale in its own separate HTML comment rather than appending it to the directive with the
   `--` description separator. Two reasons: a plain directive comment has no parsing ambiguity to reason
   about, and `eslint-disable -- <reason>` with no rule name is a *file-level disable of every rule* wearing
   a rationale, which an acceptance criterion below explicitly rejects.

   **The `enable` half is mandatory.** Without it, `vue/no-v-html` stays off for the remainder of the file,
   which is a file-level disable wearing a different hat. Everything below this element in the template
   must still be covered by the rule.

   The rationale must name the two real sanitizers and be **verified against `<script setup>` before you
   write it**: `md` is `new MarkdownIt({ html: false, ... })` at `:18`, and `renderedHtml` returns
   `DOMPurify.sanitize(md.render(...))` at `:23`. If either fact is not true in the current code, STOP and
   report — the disable would then be silencing a real finding.

**Forbidden alternatives**, all of which an acceptance criterion checks for:
- adding `vue/no-v-html` to `eslint.config.mjs` in any form (plan `01-03` asserted it is absent from the
  rules-off block and it must stay absent);
- a bare `<!-- eslint-disable -->` with no rule name, which disables every Vue rule for the rest of the file;
- the description form `<!-- eslint-disable -- reason -->`, which is the same file-level disable with prose
  attached;
- a `/* eslint-disable */` block in `<script setup>`;
- an `eslint-disable` directive with no matching `eslint-enable`.

**Do not restructure the template.** Do not change `renderedHtml`, the markdown pipeline, the DOMPurify
call, the Tailwind class string, or the inline style, and do not extract the class or style values into
`<script setup>` constants. This is an attribute reorder plus three added comment lines.

`MessageBubble.vue` is not Prettier-clean today and this phase does not make it Prettier-clean. Do not run
`pnpm format` or `prettier --write` on it — hand-edit only, matching the surrounding indentation.
  </action>

  <verify>
    <automated>pnpm exec eslint packages/frontend/src/components/chat/MessageBubble.vue --max-warnings 0 && pnpm --filter frontend typecheck && pnpm exec vitest run packages/frontend/src/components/chat/MessageBubble.test.ts</automated>
  </verify>

  <acceptance_criteria>
    - `pnpm exec eslint packages/frontend/src/components/chat/MessageBubble.vue --max-warnings 0` exits `0`.
    - **The SFC still parses.** Both of these must hold, and they are the ACs that catch a comment placed
      inside a start tag:
      `pnpm --filter frontend typecheck` (`vue-tsc --noEmit`) exits `0`, and
      `pnpm exec vitest run packages/frontend/src/components/chat/MessageBubble.test.ts` reports `0 failed`
      — that file `mount()`s the component, so it only passes if `@vitejs/plugin-vue` compiled the SFC.
    - The disable is element-scoped and closed:
      `grep -c 'eslint-disable vue/no-v-html' packages/frontend/src/components/chat/MessageBubble.vue`
      returns `1`, and
      `grep -c 'eslint-enable vue/no-v-html' packages/frontend/src/components/chat/MessageBubble.vue`
      returns `1`.
    - No blanket disable at file level:
      `grep -cE 'eslint-disable([[:space:]]+--[^-]|[[:space:]]*(-->|\*/|$))' packages/frontend/src/components/chat/MessageBubble.vue`
      returns `0`. The first alternation branch is load-bearing and was added after a tested hole: the plain
      form `eslint-disable\s*(-->|\*/|$)` catches `<!-- eslint-disable -->` and `/* eslint-disable */` but
      **misses** ESLint's `--` description form, `<!-- eslint-disable -- because reasons -->`, which is also
      a file-level disable of every rule (`\s*` eats the space, then the alternation needs `-->`/`*/`/EOL and
      instead finds `-- reason -->`). Verified against this environment's grep and system grep, the form
      above returns `1` for `<!-- eslint-disable -->`, `1` for `/* eslint-disable */`, `1` for
      `<!-- eslint-disable -- because reasons -->` and `1` for `/* eslint-disable -- reason */`, while
      correctly returning `0` for `<!-- eslint-disable vue/no-v-html -->`,
      `<!-- eslint-enable vue/no-v-html -->` and `<!-- eslint-disable-next-line ... -->`.
    - Belt and braces on the same hole — the `--` description separator appears nowhere in the file:
      `grep -cE 'eslint-disable[[:space:]]+--[[:space:]]' packages/frontend/src/components/chat/MessageBubble.vue`
      returns `0`. The action's "keep the rationale in its own separate HTML comment" is the instruction;
      this is its enforcement layer.
    - The rationale names both sanitizers: the comment block immediately above the directive contains both
      `html: false` (or `html:false`) and `DOMPurify`.
    - The rule is still globally enabled: `grep -c 'vue/no-v-html' eslint.config.mjs` returns `0`.
    - `vue/comment-directive` was not disabled to make this work:
      `grep -c 'vue/comment-directive' eslint.config.mjs` returns `0`.
    - Attribute order fixed: in the reordered start tag, the line numbers of the `style` and `class`
      attributes are both lower than the line number of the `v-html` attribute.
    - The change is an ordering edit plus comments, nothing else. Against the pre-phase baseline
      (working tree, not `HEAD`):
      `git diff 2c169d0 -- packages/frontend/src/components/chat/MessageBubble.vue | grep -cE '^-[^-]'`
      returns `3` or fewer, and
      `git diff 2c169d0 -- packages/frontend/src/components/chat/MessageBubble.vue | grep -cE '^\+[^+]'`
      returns `9` or fewer.
  </acceptance_criteria>

  <done>
    `MessageBubble.vue` lints clean at `--max-warnings 0` with `style` and `class` ordered before `v-html`
    and a closed `eslint-disable` / `eslint-enable vue/no-v-html` pair around that one element whose
    rationale cites the two real sanitizers; the SFC still type-checks and still mounts; and the rule stays
    a tripwire for every other `v-html` in the repo, including the rest of this file.
  </done>
</task>

<task type="auto">
  <name>Task 3: Phase-local green gate — 0/0 lint, negative lint proof, and the cross-version suite</name>

  <files>(verification only — no files modified)</files>

  <read_first>
    - `.planning/phases/01-restore-the-verification-signal/01-VALIDATION.md` — the full Per-Task Verification
      Map (lines 56-81) and `## What would constitute a false pass` (lines 87-104). Every assertion this task
      claims is listed there by ID.
    - `.planning/phases/01-restore-the-verification-signal/01-RESEARCH.md` -> Pitfall 7 (lines 697-700) —
      ESLint's exit-code matrix. Warnings alone exit `0` without `--max-warnings`; a glob matching no files
      exits `2`.
    - `.planning/phases/01-restore-the-verification-signal/01-01-SUMMARY.md` and `01-02-SUMMARY.md` — the
      test counts recorded there are the baseline the count assertion below compares against.
    - `.planning/ROADMAP.md` line 15 — the Phase 5-8 scope fence that step F below is deliberately narrowed
      to avoid breaching, and `### Phase 999.11` — where the repo-wide Prettier sweep is parked.
  </read_first>

  <action>
No source changes. Run the gate, record every number in the SUMMARY, and stop on any failure rather than
adjusting the assertion.

**A. Repo-wide lint gate (SIG-02d).** `pnpm lint` must exit `0`. If it does not, the remaining findings are
in scope for this plan — fix them at the source; do not add rule-off entries or `eslint-disable` comments to
reach zero.

**B. Negative lint proof (SIG-02f).** The gate is worthless unless it can report a problem. One-shot,
manual, revert immediately:
- Create `./__lint-negative-probe.ts` at the repo root containing exactly `const unused = 1;`
  (root level so `eslint .` reaches it; ESLint 10 flat config does not read `.gitignore`).
- Run `pnpm lint`; it must exit `1` and name `@typescript-eslint/no-unused-vars`.
- Delete the file. Confirm `git status --porcelain -- __lint-negative-probe.ts` prints nothing, and that
  `pnpm lint` exits `0` again. Path-scoped, not bare: plan `01-05` is also wave 2 and shares this working
  tree, so a bare `git status --porcelain` would report its two workflow files and fail on work that is
  not yours. The assertion is about the probe leaving no trace; scoping it to the probe says exactly that.

**C. Cross-version suite (SIG-01a/b/c).** Run the full suite on all three locally available Node majors,
invoking vitest directly so the pnpm shim cannot resolve a different Node:
- Node 26 (default, the version that reproduces the bug — SIG-01a):
  `node --version` (must print `v26.`) then `pnpm exec vitest run`
- Node 22 (SIG-01b): `PATH="/opt/homebrew/opt/node@22/bin:$PATH" node node_modules/vitest/vitest.mjs run`
- Node 24 (SIG-01c): `PATH="$HOME/.nvm/versions/node/v24.13.0/bin:$PATH" node node_modules/vitest/vitest.mjs run`

All three must report `0 failed` with identical file and test counts. **A differing count between legs is a
finding, not noise** — it means the legs are exercising different code paths, which is the exact blind spot
this phase exists to close. Node 20 has no local binary; it is CI-only (SIG-01d, plan `01-06`).

**D. Build (all legs run it in CI, so prove it locally).** `pnpm build` must succeed and produce
`dist/drift.zip`.

**E. Typecheck.** `pnpm -r typecheck` must exit `0`.

**F. The files this phase created or formatted are Prettier-clean — and nothing else is touched.**

This is a **read-only `--check` over a fixed five-file list**, not a format run:
`pnpm exec prettier --check vitest.setup.ts eslint.config.mjs caido.config.ts vitest.config.ts packages/frontend/src/__storage-shim.test.ts`

**No `pnpm format` anywhere in this phase, and no `prettier --write` on any file outside the five-file list
above.** Each of the five is formatted by the plan that creates or edits it, in wave 1, where formatting is
still allowed: `vitest.setup.ts` and `vitest.config.ts` by plan `01-02` task 1,
`packages/frontend/src/__storage-shim.test.ts` by plan `01-02` task 2, `caido.config.ts` by plan `01-03`
task 1, and `eslint.config.mjs` by plan `01-03` task 2. By the time this step runs they are already clean
and step F only has to confirm it; step F itself stays read-only and stops on failure.

The reason the sweep is out of scope, measured on the current tree: 46 files under `packages/**/src` are not
Prettier-clean, including `packages/backend/src/index.ts`, `provider-launch.ts`, `command-resolution.ts`,
and `MessageBubble.vue`. A repo-wide sweep would rewrite the bodies of `renderExportExecScript`,
`writeMcpWrapper`, `writeLaunchScript`, `shellQuote` and the `chmod` call sites — precisely the code
ROADMAP line 15 fences off for Phases 5-8, and precisely what this task's own CMP-inv assertion forbids.
There is no ordering that satisfies both, so the sweep is out of scope.

Plan `01-03` widened the `pnpm format` glob so it can *reach* the two new root files. Widening the glob is
in scope; executing the sweep is not. The repo-wide Prettier sweep is its own change with its own commit and
is parked at ROADMAP `### Phase 999.11`. Do not promote it here.

The five files above are the complete set this phase is accountable for: `vitest.setup.ts` and
`packages/frontend/src/__storage-shim.test.ts` are new (plan `01-02`), `eslint.config.mjs` is new (plan
`01-03`), `caido.config.ts` was explicitly formatted by plan `01-03` task 1, and `vitest.config.ts` was
already Prettier-clean pre-phase and is edited and re-formatted by plan `01-02`. The pre-existing files this
phase merely hand-edits (`index.ts`, `mcp-server.mjs`, `claude-print.test.ts`, `settings.ts`,
`settings.test.ts`, `MessageBubble.vue`, `package.json`) were not Prettier-clean before the phase and are
not expected to be after it.

**G. CMP-inv and V2-inv.** Run the two static invariants described in the acceptance criteria below. Both
compare the **working tree** against the pre-phase baseline commit `2c169d0` — note the single-commit form
`git diff 2c169d0 -- <path>`, not `2c169d0..HEAD`. The two-dot form only sees committed work reachable from
the asserting branch, so an uncommitted breach of the scope fence would show up as an empty diff and pass
vacuously.

Record all counts, the exit codes, and the three per-Node results in the SUMMARY. If any check fails, stop
and report — do not relax an assertion to make the gate pass.
  </action>

  <verify>
    <automated>pnpm lint && pnpm -r typecheck && pnpm exec vitest run && PATH="/opt/homebrew/opt/node@22/bin:$PATH" node node_modules/vitest/vitest.mjs run && PATH="$HOME/.nvm/versions/node/v24.13.0/bin:$PATH" node node_modules/vitest/vitest.mjs run && pnpm build && test -f dist/drift.zip && pnpm exec prettier --check vitest.setup.ts eslint.config.mjs caido.config.ts vitest.config.ts packages/frontend/src/__storage-shim.test.ts</automated>
  </verify>

  <acceptance_criteria>
    - SIG-02d — `pnpm exec eslint . --max-warnings 0` exits `0`, and the JSON report shows `errorCount` 0 and
      `warningCount` 0 across all files.
    - SIG-02c re-asserted at settled state — the linted-file count is at least `59` and the set includes
      `packages/backend/assets/mcp-server.mjs`, `eslint.config.mjs`, `vitest.setup.ts`, and
      `packages/frontend/src/__storage-shim.test.ts`.
    - SIG-02f — with `./__lint-negative-probe.ts` present containing `const unused = 1;`, `pnpm lint` exits
      `1`; after deleting the file, `pnpm lint` exits `0` and
      `git status --porcelain -- __lint-negative-probe.ts` prints nothing. Path-scoped, not bare: plan
      `01-05` is also wave 2 and shares this working tree, so a bare `git status --porcelain` would report
      its two workflow files.
    - SIG-01a — on Node `v26.x`, `pnpm exec vitest run` reports `0 failed` and at least `125` tests across
      `23` test files.
    - SIG-01b — the Node 22.23.2 run reports `0 failed` with the same file and test counts as the Node 26 run.
    - SIG-01c — the Node 24.13.0 run reports `0 failed` with the same file and test counts as the Node 26 run.
    - No focused tests slipped through: `pnpm lint` passing implies `@vitest/eslint-plugin`'s
      `no-focused-tests` found none; additionally
      `grep -rcE '(describe|it|test)\.only' packages --include=*.test.ts | grep -v ':0$' | wc -l` returns `0`.
    - `pnpm -r typecheck` exits `0`; `pnpm build` exits `0` and `dist/drift.zip` exists.
    - Phase-owned files are Prettier-clean:
      `pnpm exec prettier --check vitest.setup.ts eslint.config.mjs caido.config.ts vitest.config.ts packages/frontend/src/__storage-shim.test.ts`
      exits `0`.
    - No sweep happened — `git diff 2c169d0 --name-only -- packages/` lists exactly seven paths: this plan's
      four `files_modified`, plus `packages/frontend/src/stores/settings.ts` and
      `packages/frontend/src/stores/settings.test.ts` (plan `01-01`) and
      `packages/frontend/src/__storage-shim.test.ts` (plan `01-02`). Nothing else under `packages/` may
      appear — in particular
      `git diff 2c169d0 --name-only | grep -cE 'provider-launch\.ts|command-resolution\.ts|mcp-runtime\.ts|persistence\.ts'`
      returns `0`. This is the settled-state, repo-wide form of CMP-inv that the wave-1 plans deliberately
      scoped down to their own paths, because they run concurrently in a shared working tree.
    - CMP-inv — working-tree comparison, no `..HEAD`. Expect no output from:
      `git diff 2c169d0 --name-only | grep -E 'provider-launch\.ts|command-resolution\.ts'`
      and no output from:
      `git diff 2c169d0 -- packages/backend/src/ | grep -E '^[-+]' | grep -v '^[-+][-+]' | grep -E 'renderExportExecScript|writeMcpWrapper|writeLaunchScript|shellQuote|chmod'`
    - V2-inv — the fail-closed abort survives. The invariant is that `startMcpServer` still refuses to start
      on an empty token, so assert that block *structurally*, anchored on the function name. Do NOT use a
      line-number window (`sed -n '1690,1705p'`) and do NOT assert a whole-file count of `1` — the string
      appears at six sites (`:1013`, `:1698` = the `startMcpServer` fail-closed site, and two pairs at
      `:1933-1934` and `:1995-1996`), and any window keyed to absolute line numbers breaks the moment
      anything above shifts. All five of these must hold:
      - `grep -c 'async function startMcpServer' packages/backend/src/index.ts` returns `1`
        (the anchor is unique, so the `-A` window below is unambiguous).
      - `grep -A 10 'async function startMcpServer' packages/backend/src/index.ts | grep -c 'caidoToken === ""'`
        returns `1`.
      - `grep -A 10 'async function startMcpServer' packages/backend/src/index.ts | grep -c 'setMcpAuthStatus("invalid", message)'`
        returns `1` AND
        `grep -A 10 'async function startMcpServer' packages/backend/src/index.ts | grep -c 'return err(message)'`
        returns `1` — the guard still marks auth invalid and still returns early rather than falling through.
      - `grep -c 'No Caido access token is available' packages/backend/src/index.ts` returns `6` — the
        measured pre-phase baseline, unchanged. This phase's only `index.ts` edit is task 1's one-line
        `no-useless-assignment` fix at `:710`, which touches none of the six sites, so `6` is stable across
        the whole phase. A different number means a token-path edit slipped in.
      - `git diff 2c169d0 -- packages/backend/src/index.ts | grep -E '^[-+]' | grep -v '^[-+][-+]' | wc -l`
        returns `2` (exactly the one-line `no-useless-assignment` change).
  </acceptance_criteria>

  <done>
    `pnpm lint` exits 0 at `--max-warnings 0` and is proven to exit 1 on a real error; the full suite is
    green with identical counts on Node 22.23.2, 24.13.0 and 26.7.0; typecheck and build are clean; the five
    files this phase owns pass `prettier --check` with no repo-wide sweep having run; and both CMP-inv and
    V2-inv hold against the working tree. Every number is recorded in the SUMMARY.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Model-generated markdown -> rendered DOM in the Caido webview | `MessageBubble.vue` injects LLM output via `v-html`. This is the repo's only `v-html` and the highest-value XSS sink in the product. |
| Lint configuration -> reported build health | Any change that reduces what the linter looks at is indistinguishable from a fix, unless the gate is proven to bite. |
| Phase 1 edits -> Phase 5-8 spawn path | This phase must not touch code that later phases rewrite; a silent overlap would make both phases' validation ambiguous. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-14 | Repudiation | `vue/no-v-html` at `MessageBubble.vue:105` | mitigate | Rule stays enabled at config level; the single legitimate use gets a closed `eslint-disable` / `eslint-enable vue/no-v-html` pair wrapping that one element, whose rationale must be verified against the actual `<script setup>` (`markdown-it({ html: false })` at `:18` plus `DOMPurify.sanitize` at `:23`) before it is written. The mandatory `eslint-enable` keeps the rest of the file covered; acceptance criteria reject a config-level entry, a missing `enable`, a bare unnamed `eslint-disable`, and — via the `--`-aware regex plus a dedicated second grep — the `eslint-disable -- <reason>` description form, which is the same file-level disable with prose attached. Any *new* `v-html` anywhere, including elsewhere in this file, still trips the lint. |
| T-01-14b | Spoofing | An unparseable SFC passing the grep-based disable assertions | mitigate | The prescribed `eslint-disable-next-line` placement was a Vue parse error (`Illegal '/' in tags.`, reproduced against `@vue/compiler-sfc@3.5.29`) that greps alone would have green-lit. Two independent parse proofs are now acceptance criteria: `vue-tsc --noEmit` exits `0`, and `MessageBubble.test.ts` — which `mount()`s the component — reports `0 failed`. |
| T-01-15 | Spoofing | `pnpm lint` reporting 0 problems | mitigate | SIG-02f introduces a real error and requires exit `1` before the clean result is believed. Without it, a lint command that silently lints nothing is indistinguishable from a clean repo (measured: a no-match glob exits `2`, warnings-only exits `0`). |
| T-01-16 | Tampering | `preserve-caught-error` fix altering MCP error semantics | mitigate | Only `{ cause: error }` is appended to the `new Error(...)` at `mcp-server.mjs:278`; the message string and the bare `throw error;` at `:280` stay byte-identical, and all three `mcp-server.*.test.ts` files must stay green. |
| T-01-17 | Tampering | Scope-fence breach into the Phase 5-8 spawn path | mitigate | CMP-inv runs a diff-level grep for the five fenced identifiers against the **working tree** (`git diff 2c169d0 -- <path>`, single-commit form) so an uncommitted breach cannot pass as an empty diff, plus a token-count check against the pre-phase baseline (`renderExportExecScript`=4, `writeMcpWrapper`=4, `writeLaunchScript`=3, `shellQuote`=3, `chmod`=4). |
| T-01-17b | Tampering | A repo-wide `pnpm format` sweep rewriting fenced spawn-path code | mitigate | 46 files under `packages/**/src` are measurably not Prettier-clean, `index.ts` among them, so a sweep would rewrite all five fenced identifiers. Step F is a read-only `prettier --check` over a fixed five-file list of phase-owned files; `pnpm format` is forbidden phase-wide and `prettier --write` is forbidden on any file outside that five-file list (each of the five is formatted by its owning wave-1 plan). The sweep is parked at ROADMAP `### Phase 999.11`. |
| T-01-18 | Spoofing | Token guard silently failing open after the phase's edits | mitigate | V2-inv re-confirms `startMcpServer`'s empty-token abort *block* is structurally intact — a `grep -A 10` window anchored on the unique function name, requiring the `caidoToken === ""` test, the `setMcpAuthStatus("invalid", message)` call and the `return err(message)` early return — plus an unchanged whole-file count of `6` occurrences of the abort string, plus proof that the only change to `packages/backend/src/index.ts` across the entire phase is the single `no-useless-assignment` line, measured against the working tree. The count is asserted at its measured baseline rather than at `1`, and the window is anchored on the function name rather than on line numbers, so neither assertion can be invalidated by unrelated line shifts. |
| T-01-19 | Repudiation | A committed `it.only` reducing the suite to one test and reporting green | mitigate | `@vitest/eslint-plugin`'s `no-focused-tests` is active via plan `01-03`; the gate additionally asserts an absolute test count of at least 125 and greps for `.only` directly. |
| T-01-SC | Tampering | npm/pip/cargo installs | n/a | This plan installs nothing. |
</threat_model>

<verification>
Run from the repo root, in order. Every step must pass before the plan is complete.

1. `pnpm lint` -> exit 0 (SIG-02d)
2. SIG-02f negative probe -> `pnpm lint` exit 1 with the probe present, exit 0 after deletion,
   `git status --porcelain -- __lint-negative-probe.ts` prints nothing (path-scoped; plan `01-05` shares
   wave 2 and this working tree)
3. `node --version` -> `v26.x`; `pnpm exec vitest run` -> 23 files, at least 125 tests, `0 failed` (SIG-01a)
4. `PATH="/opt/homebrew/opt/node@22/bin:$PATH" node node_modules/vitest/vitest.mjs run` -> same counts,
   `0 failed` (SIG-01b)
5. `PATH="$HOME/.nvm/versions/node/v24.13.0/bin:$PATH" node node_modules/vitest/vitest.mjs run` -> same
   counts, `0 failed` (SIG-01c)
6. `pnpm -r typecheck` -> exit 0; `pnpm --filter frontend typecheck` -> exit 0 (SFC parse proof)
7. `pnpm build` -> exit 0, `dist/drift.zip` exists
8. `pnpm exec prettier --check vitest.setup.ts eslint.config.mjs caido.config.ts vitest.config.ts packages/frontend/src/__storage-shim.test.ts`
   -> exit 0. Do NOT run `pnpm format`; see task 3 step F.
9. CMP-inv and V2-inv, working-tree form (`git diff 2c169d0 -- <path>`) plus the `grep -A 10 'async function
   startMcpServer'` structural window and the unchanged abort-string count of `6` -> all as specified in
   task 3's acceptance criteria

Node 20 is not installed locally; SIG-01d is observed on the CI matrix leg in plan `01-06`.
</verification>

<success_criteria>
- 0 errors and 0 warnings repo-wide at `--max-warnings 0`, achieved by fixing source, not by disabling rules.
- The lint gate is proven to exit 1 on a real error and the probe leaves no trace.
- Identical, green suite results on Node 22.23.2, 24.13.0 and 26.7.0 — the same code path on every leg.
- `MessageBubble.vue` still parses: `vue-tsc --noEmit` exits 0 and the mount test passes.
- `pnpm -r typecheck` and `pnpm build` are clean, and the five phase-owned files pass `prettier --check`.
- No repo-wide Prettier sweep ran; the Phase 5-8 fenced code is byte-untouched.
- CMP-inv and V2-inv both hold against the working tree.
- No file outside `files_modified` changed.
</success_criteria>

<output>
Create `.planning/phases/01-restore-the-verification-signal/01-04-SUMMARY.md` when done.
Record: final lint error/warning/file counts, the SIG-02f exit codes both ways, the per-Node file and test
counts for all three legs, the `pnpm build` result, the `prettier --check` result for the five phase-owned
files, and the exact output of both invariant greps.
</output>
