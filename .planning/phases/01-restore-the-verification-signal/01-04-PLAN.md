---
phase: 01-restore-the-verification-signal
plan: 04
type: execute
wave: 2
depends_on: ["01-01", "01-02", "01-03"]
files_modified:
  - packages/backend/assets/mcp-server.mjs
  - packages/backend/src/index.ts
  - packages/backend/src/claude-print.test.ts
  - packages/frontend/src/components/chat/MessageBubble.vue
autonomous: true
requirements: [SIG-01, SIG-02]

must_haves:
  truths:
    - "`pnpm lint` exits 0 with --max-warnings 0 across the whole repo"
    - "A deliberately introduced lint error makes `pnpm lint` exit 1 — the gate is proven to bite"
    - "The full suite is green on Node 22, 24 and 26 locally, with at least 125 tests and no reduction in test count"
    - "vue/no-v-html remains enabled repo-wide; the single legitimate use is disabled inline with a rationale"
    - "No file in the Phase 5-8 spawn path changed anywhere in this phase"
  artifacts:
    - path: "packages/backend/assets/mcp-server.mjs"
      provides: "GraphQL timeout error that preserves its cause"
      contains: "cause"
    - path: "packages/frontend/src/components/chat/MessageBubble.vue"
      provides: "Attribute order fixed and a justified inline vue/no-v-html disable"
      contains: "eslint-disable-next-line vue/no-v-html"
  key_links:
    - from: "packages/frontend/src/components/chat/MessageBubble.vue"
      to: "eslint.config.mjs vue/no-v-html"
      via: "inline disable comment immediately preceding the v-html attribute"
      pattern: "eslint-disable-next-line vue/no-v-html"
    - from: "pnpm lint"
      to: "CI Lint step"
      via: "same script, same --max-warnings 0 ratchet, no --fix on either side"
      pattern: "--max-warnings 0"
---

<objective>
Close the measured lint debt to 0 errors / 0 warnings, prove the gate actually bites, and run the
phase-local green gate across every Node major available on this machine.

Purpose: SIG-02's "passes clean" branch is reachable in one task — research measured the residual at
0/0 after the recommended rule config, so no debt-tracking document is needed. But "0 problems" is
worthless if nobody proved the command can report a problem, and "the suite is green" is worthless if it
was only ever run on a Node version where the bug does not exist. This plan does both.

Output: three one-line error fixes, the `MessageBubble.vue` attribute reorder plus a justified inline
`vue/no-v-html` disable, and a recorded green gate on Node 22.23.2 / 24.13.0 / 26.7.0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-restore-the-verification-signal/01-RESEARCH.md
@.planning/phases/01-restore-the-verification-signal/01-PATTERNS.md
@.planning/phases/01-restore-the-verification-signal/01-VALIDATION.md
@.planning/phases/01-restore-the-verification-signal/01-01-SUMMARY.md
@.planning/phases/01-restore-the-verification-signal/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clear the three remaining lint errors</name>

  <files>packages/backend/assets/mcp-server.mjs, packages/backend/src/index.ts, packages/backend/src/claude-print.test.ts</files>

  <read_first>
    - `packages/backend/assets/mcp-server.mjs` lines 262-284 — the GraphQL `catch (error)` block. Note the
      two throw sites: the `new Error(...)` timeout at `:278` and the bare `throw error;` at `:280`.
    - `packages/backend/src/index.ts` lines 705-720 — `writeApprovalDecision`, where
      `let current: Record<string, ApprovalDecision> = {};` at `:710` is immediately reassigned in both the
      `try` and the `catch`.
    - `packages/backend/src/claude-print.test.ts` lines 214-235 — the `let state = consumeClaudePrintChunk(...)`
      at `:217`; confirm whether `state` is reassigned anywhere in that `it` block before choosing `const`.
    - `.planning/phases/01-restore-the-verification-signal/01-PATTERNS.md` lines 454-464 — the confirmed
      current code at each of the four error sites and the note that none of them sits in the Phase 5-8
      spawn path.
    - `.planning/phases/01-restore-the-verification-signal/01-RESEARCH.md` -> `### The 4 errors` (lines
      633-642) — the rule that fires at each site and why it is new in ESLint 10's `recommended`.
    - `.planning/phases/01-restore-the-verification-signal/01-03-SUMMARY.md` — the residual error and warning
      counts recorded as this task's baseline.
  </read_first>

  <action>
Three one-line fixes. The fourth measured error (`packages/frontend/src/stores/settings.ts:78`,
`no-useless-assignment`) was already cleared by plan `01-01` — do not touch `settings.ts`.

1. `packages/backend/assets/mcp-server.mjs:278` — rule `preserve-caught-error` (new in ESLint 10's
   `recommended`). The unattributed rethrow is the **`new Error(...)` at line 278**, not the `throw error;`
   at line 280. Attach the cause: `throw new Error(\`GraphQL request timed out after ${timeoutMs}ms\`, { cause: error });`
   The message string must stay byte-identical, and the `throw error;` at `:280` must stay untouched —
   `packages/backend/src/mcp-server.transport.test.ts` and the other two `mcp-server.*.test.ts` files
   assert against this module's behaviour and must stay green. `{ cause }` is supported on every Node the
   MCP server runs on (Node >= 18 host requirement).

2. `packages/backend/src/index.ts:710` — rule `no-useless-assignment`. Drop the dead `= {}` initialiser:
   `let current: Record<string, ApprovalDecision>;`. Both the `try` and the `catch` assign it before use, so
   TypeScript's definite-assignment analysis stays satisfied. Nothing else in `writeApprovalDecision` changes.

3. `packages/backend/src/claude-print.test.ts:217` — rule `prefer-const`. Change `let state = ...` to
   `const state = ...` **only if `state` is never reassigned inside that `it` block**. Read the block first;
   if it is reassigned, the correct fix is different and you should report rather than force it.

Scope fence (assertion CMP-inv): none of these sites is inside `renderExportExecScript` (`index.ts:320`),
`writeMcpWrapper` (`:743`), `writeLaunchScript` (`:299`), `shellQuote` (`:398`), or any `chmod` call site.
Do not edit those functions, and do not let an editor auto-format sweep touch them.
  </action>

  <verify>
    <automated>pnpm exec eslint packages/backend/assets/mcp-server.mjs packages/backend/src/index.ts packages/backend/src/claude-print.test.ts --max-warnings 0 && pnpm exec vitest run packages/backend && pnpm -r typecheck</automated>
  </verify>

  <acceptance_criteria>
    - `pnpm exec eslint packages/backend/assets/mcp-server.mjs packages/backend/src/index.ts packages/backend/src/claude-print.test.ts --max-warnings 0` exits `0`.
    - The cause is attached at the right site:
      `grep -c 'cause: error' packages/backend/assets/mcp-server.mjs` returns `1` or more, and
      `grep -c 'GraphQL request timed out after' packages/backend/assets/mcp-server.mjs` returns `1`
      (message unchanged), and
      `grep -c '^\s*throw error;' packages/backend/assets/mcp-server.mjs` is unchanged from before the edit.
    - `grep -v '^\s*//' packages/backend/src/index.ts | grep -c 'let current: Record<string, ApprovalDecision> = {}'` returns `0`.
    - `grep -c 'let state = consumeClaudePrintChunk' packages/backend/src/claude-print.test.ts` returns `0`.
    - All backend tests green: `pnpm exec vitest run packages/backend` reports `0 failed` across 9 files —
      in particular the three `mcp-server.*.test.ts` files.
    - `pnpm -r typecheck` exits `0`.
    - CMP-inv, spawn path untouched. Expect no output from:
      `git diff 2c169d0..HEAD -- packages/backend/src/index.ts | grep -E '^[-+]' | grep -v '^[-+][-+]' | grep -E 'renderExportExecScript|writeMcpWrapper|writeLaunchScript|shellQuote|chmod'`
    - Token counts in `packages/backend/src/index.ts` are unchanged from the pre-phase baseline:
      `renderExportExecScript`=4, `writeMcpWrapper`=4, `writeLaunchScript`=3, `shellQuote`=3, `chmod`=4
      (`grep -c <token> packages/backend/src/index.ts` for each).
  </acceptance_criteria>

  <done>
    All three remaining measured ESLint errors are cleared with one-line changes, the MCP server's timeout
    error carries its cause without changing its message, the backend suite and typecheck are green, and the
    Phase 5-8 spawn path is provably untouched.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix MessageBubble attribute order and add the justified vue/no-v-html disable</name>

  <files>packages/frontend/src/components/chat/MessageBubble.vue</files>

  <read_first>
    - `packages/frontend/src/components/chat/MessageBubble.vue` lines 96-115 — the `v-html` div. Confirmed
      current layout: `v-html` on `:105`, `style` on `:106`, `class` on `:107`. Both `vue/attributes-order`
      warnings point at `:106` and `:107`.
    - `packages/frontend/src/components/chat/MessageBubble.vue` — the `<script setup>` block, to confirm
      how `renderedHtml` is produced. The disable rationale must be accurate, not assumed.
    - `.planning/phases/01-restore-the-verification-signal/01-PATTERNS.md` lines 466-476 — why the two edits
      must be done together (reordering shifts the line the disable comment must precede).
    - `.planning/phases/01-restore-the-verification-signal/01-RESEARCH.md` -> `## Security Domain`, ASVS V5
      row (line 926) — the disposition this task implements.
  </read_first>

  <action>
Two coupled edits to the same element. Do them together: reordering the attributes shifts the line the
`eslint-disable-next-line` comment must sit above, so doing them separately produces a comment on the wrong
line.

1. `vue/attributes-order` (2 warnings): move `style` and `class` **above** `v-html` on the div currently at
   `MessageBubble.vue:104-108`. This is the auto-fixable order (`vue/attributes-order` puts `CONTENT`
   directives last); apply it by hand rather than with `--fix` so the diff is reviewed.

2. `vue/no-v-html` (1 warning): add `<!-- eslint-disable-next-line vue/no-v-html -->` immediately above the
   `v-html` attribute line, with a rationale naming the two sanitizers that make this safe:
   `markdown-it` is constructed with `{ html: false }` and the rendered output passes through DOMPurify
   before it reaches `renderedHtml`. **Verify both facts against the `<script setup>` block before writing
   the comment** — if either is not actually true in the current code, STOP and report; the disable would
   then be silencing a real finding.

   The rule stays enabled repo-wide (plan `01-03` asserted it is absent from the rules-off block). The
   inline disable is deliberately scoped to one line so any *new* `v-html` anywhere in the codebase trips
   the lint. Do not convert this into a file-level or config-level disable.

Do not change `renderedHtml`, the markdown pipeline, the DOMPurify call, the Tailwind class string, or any
other attribute value. This is an ordering change plus one comment.
  </action>

  <verify>
    <automated>pnpm exec eslint packages/frontend/src/components/chat/MessageBubble.vue --max-warnings 0 && pnpm exec vitest run packages/frontend/src/components/chat/MessageBubble.test.ts</automated>
  </verify>

  <acceptance_criteria>
    - `pnpm exec eslint packages/frontend/src/components/chat/MessageBubble.vue --max-warnings 0` exits `0`.
    - The disable is inline and scoped:
      `grep -c 'eslint-disable-next-line vue/no-v-html' packages/frontend/src/components/chat/MessageBubble.vue`
      returns `1`.
    - No blanket disable was used:
      `grep -cE 'eslint-disable +vue/no-v-html|eslint-disable$' packages/frontend/src/components/chat/MessageBubble.vue`
      returns `0`.
    - The rationale names both sanitizers: the comment text (or the line directly above/below it) contains
      both `html: false` (or `html:false`) and `DOMPurify`.
    - The rule is still globally enabled: `grep -c 'vue/no-v-html' eslint.config.mjs` returns `0`.
    - Rendering behaviour unchanged: `pnpm exec vitest run packages/frontend/src/components/chat/MessageBubble.test.ts`
      reports `0 failed`, and the diff for this file removes no line other than the three reordered attribute
      lines — `git diff -- packages/frontend/src/components/chat/MessageBubble.vue | grep -E '^-' | grep -v '^---' | wc -l`
      returns `3` or fewer.
  </acceptance_criteria>

  <done>
    `MessageBubble.vue` lints clean at `--max-warnings 0` with `style` and `class` ordered before `v-html`
    and a single-line `vue/no-v-html` disable whose rationale cites the two real sanitizers; the rule stays
    a tripwire for every other `v-html` in the repo.
  </done>
</task>

<task type="auto">
  <name>Task 3: Phase-local green gate — 0/0 lint, negative lint proof, and the cross-version suite</name>

  <files>(verification only — no files modified)</files>

  <read_first>
    - `.planning/phases/01-restore-the-verification-signal/01-VALIDATION.md` — the full Per-Task Verification
      Map (lines 56-81) and `## What would constitute a false pass` (lines 87-104). Every assertion this task
      claims is listed there by ID.
    - `.planning/phases/01-restore-the-verification-signal/01-RESEARCH.md` -> Pitfall 7 (lines 697-700) —
      ESLint's exit-code matrix. Warnings alone exit `0` without `--max-warnings`; a glob matching no files
      exits `2`.
    - `.planning/phases/01-restore-the-verification-signal/01-01-SUMMARY.md` and `01-02-SUMMARY.md` — the
      test counts recorded there are the baseline the count assertion below compares against.
  </read_first>

  <action>
No source changes. Run the gate, record every number in the SUMMARY, and stop on any failure rather than
adjusting the assertion.

**A. Repo-wide lint gate (SIG-02d).** `pnpm lint` must exit `0`. If it does not, the remaining findings are
in scope for this plan — fix them at the source; do not add rule-off entries or `eslint-disable` comments to
reach zero.

**B. Negative lint proof (SIG-02f).** The gate is worthless unless it can report a problem. One-shot,
manual, revert immediately:
- Create `./__lint-negative-probe.ts` at the repo root containing exactly `const unused = 1;`
  (root level so `eslint .` reaches it; ESLint 10 flat config does not read `.gitignore`).
- Run `pnpm lint`; it must exit `1` and name `@typescript-eslint/no-unused-vars`.
- Delete the file. Confirm `git status --porcelain` shows no trace of it and `pnpm lint` exits `0` again.

**C. Cross-version suite (SIG-01a/b/c).** Run the full suite on all three locally available Node majors,
invoking vitest directly so the pnpm shim cannot resolve a different Node:
- Node 26 (default, the version that reproduces the bug — SIG-01a):
  `node --version` (must print `v26.`) then `pnpm exec vitest run`
- Node 22 (SIG-01b): `PATH="/opt/homebrew/opt/node@22/bin:$PATH" node node_modules/vitest/vitest.mjs run`
- Node 24 (SIG-01c): `PATH="$HOME/.nvm/versions/node/v24.13.0/bin:$PATH" node node_modules/vitest/vitest.mjs run`

All three must report `0 failed` with identical file and test counts. **A differing count between legs is a
finding, not noise** — it means the legs are exercising different code paths, which is the exact blind spot
this phase exists to close. Node 20 has no local binary; it is CI-only (SIG-01d, plan `01-06`).

**D. Build (all legs run it in CI, so prove it locally).** `pnpm build` must succeed and produce
`dist/drift.zip`.

**E. Typecheck.** `pnpm -r typecheck` must exit `0`.

**F. Formatting is a no-op.** Now that every file in the phase has landed, `pnpm format` must change
nothing: run it and confirm `git status --porcelain` is empty afterwards. This proves the widened glob from
plan `01-03` and the hand-formatted root files from plan `01-02` agree.

**G. CMP-inv and V2-inv.** Run the two static invariants described in the acceptance criteria below.

Record all counts, the exit codes, and the three per-Node results in the SUMMARY. If any check fails, stop
and report — do not relax an assertion to make the gate pass.
  </action>

  <verify>
    <automated>pnpm lint && pnpm -r typecheck && pnpm exec vitest run && PATH="/opt/homebrew/opt/node@22/bin:$PATH" node node_modules/vitest/vitest.mjs run && PATH="$HOME/.nvm/versions/node/v24.13.0/bin:$PATH" node node_modules/vitest/vitest.mjs run && pnpm build && test -f dist/drift.zip</automated>
  </verify>

  <acceptance_criteria>
    - SIG-02d — `pnpm exec eslint . --max-warnings 0` exits `0`, and the JSON report shows `errorCount` 0 and
      `warningCount` 0 across all files.
    - SIG-02c re-asserted at settled state — the linted-file count is at least `59` and the set includes
      `packages/backend/assets/mcp-server.mjs`, `eslint.config.mjs`, `vitest.setup.ts`, and
      `packages/frontend/src/__storage-shim.test.ts`.
    - SIG-02f — with `./__lint-negative-probe.ts` present containing `const unused = 1;`, `pnpm lint` exits
      `1`; after deleting the file, `pnpm lint` exits `0` and `git status --porcelain` is empty.
    - SIG-01a — on Node `v26.x`, `pnpm exec vitest run` reports `0 failed` and at least `125` tests across
      `23` test files.
    - SIG-01b — the Node 22.23.2 run reports `0 failed` with the same file and test counts as the Node 26 run.
    - SIG-01c — the Node 24.13.0 run reports `0 failed` with the same file and test counts as the Node 26 run.
    - No focused tests slipped through: `pnpm lint` passing implies `@vitest/eslint-plugin`'s
      `no-focused-tests` found none; additionally
      `grep -rcE '(describe|it|test)\.only' packages --include=*.test.ts | grep -v ':0$' | wc -l` returns `0`.
    - `pnpm -r typecheck` exits `0`; `pnpm build` exits `0` and `dist/drift.zip` exists.
    - Formatting is settled: `pnpm format` followed by `git status --porcelain` produces no output.
    - CMP-inv — expect no output from:
      `git diff 2c169d0..HEAD --name-only | grep -E 'provider-launch\.ts|command-resolution\.ts'`
      and no output from:
      `git diff 2c169d0..HEAD -- packages/backend/src/ | grep -E '^[-+]' | grep -v '^[-+][-+]' | grep -E 'renderExportExecScript|writeMcpWrapper|writeLaunchScript|shellQuote|chmod'`
    - V2-inv — the fail-closed abort survives:
      `grep -c 'No Caido access token is available' packages/backend/src/index.ts` returns `1`, and
      `git diff 2c169d0..HEAD -- packages/backend/src/index.ts | grep -E '^[-+]' | grep -v '^[-+][-+]' | wc -l`
      returns `2` (exactly the one-line `no-useless-assignment` change).
  </acceptance_criteria>

  <done>
    `pnpm lint` exits 0 at `--max-warnings 0` and is proven to exit 1 on a real error; the full suite is
    green with identical counts on Node 22.23.2, 24.13.0 and 26.7.0; typecheck, build and `pnpm format`
    are clean; and both CMP-inv and V2-inv hold. Every number is recorded in the SUMMARY.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Model-generated markdown -> rendered DOM in the Caido webview | `MessageBubble.vue` injects LLM output via `v-html`. This is the repo's only `v-html` and the highest-value XSS sink in the product. |
| Lint configuration -> reported build health | Any change that reduces what the linter looks at is indistinguishable from a fix, unless the gate is proven to bite. |
| Phase 1 edits -> Phase 5-8 spawn path | This phase must not touch code that later phases rewrite; a silent overlap would make both phases' validation ambiguous. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-14 | Repudiation | `vue/no-v-html` at `MessageBubble.vue:105` | mitigate | Rule stays enabled repo-wide; the single legitimate use gets a one-line `eslint-disable-next-line` whose rationale must be verified against the actual `<script setup>` (`markdown-it({ html: false })` plus DOMPurify) before it is written. A blanket or config-level disable is explicitly rejected by an acceptance criterion, so any *new* `v-html` still trips the lint. |
| T-01-15 | Spoofing | `pnpm lint` reporting 0 problems | mitigate | SIG-02f introduces a real error and requires exit `1` before the clean result is believed. Without it, a lint command that silently lints nothing is indistinguishable from a clean repo (measured: a no-match glob exits `2`, warnings-only exits `0`). |
| T-01-16 | Tampering | `preserve-caught-error` fix altering MCP error semantics | mitigate | Only `{ cause: error }` is appended to the `new Error(...)` at `mcp-server.mjs:278`; the message string and the bare `throw error;` at `:280` stay byte-identical, and all three `mcp-server.*.test.ts` files must stay green. |
| T-01-17 | Tampering | Scope-fence breach into the Phase 5-8 spawn path | mitigate | CMP-inv runs a diff-level grep for the five fenced identifiers over the whole phase range, plus a token-count check against the pre-phase baseline (`renderExportExecScript`=4, `writeMcpWrapper`=4, `writeLaunchScript`=3, `shellQuote`=3, `chmod`=4). |
| T-01-18 | Spoofing | Token guard silently failing open after the phase's edits | mitigate | V2-inv re-confirms `startMcpServer`'s empty-token abort string is still present and that the only change to `packages/backend/src/index.ts` across the entire phase is the single `no-useless-assignment` line. |
| T-01-19 | Repudiation | A committed `it.only` reducing the suite to one test and reporting green | mitigate | `@vitest/eslint-plugin`'s `no-focused-tests` is active via plan `01-03`; the gate additionally asserts an absolute test count of at least 125 and greps for `.only` directly. |
| T-01-SC | Tampering | npm/pip/cargo installs | n/a | This plan installs nothing. |
</threat_model>

<verification>
Run from the repo root, in order. Every step must pass before the plan is complete.

1. `pnpm lint` -> exit 0 (SIG-02d)
2. SIG-02f negative probe -> `pnpm lint` exit 1 with the probe present, exit 0 after deletion,
   `git status --porcelain` empty
3. `node --version` -> `v26.x`; `pnpm exec vitest run` -> 23 files, at least 125 tests, `0 failed` (SIG-01a)
4. `PATH="/opt/homebrew/opt/node@22/bin:$PATH" node node_modules/vitest/vitest.mjs run` -> same counts,
   `0 failed` (SIG-01b)
5. `PATH="$HOME/.nvm/versions/node/v24.13.0/bin:$PATH" node node_modules/vitest/vitest.mjs run` -> same
   counts, `0 failed` (SIG-01c)
6. `pnpm -r typecheck` -> exit 0
7. `pnpm build` -> exit 0, `dist/drift.zip` exists
8. `pnpm format` then `git status --porcelain` -> no output
9. CMP-inv and V2-inv diff greps -> no output

Node 20 is not installed locally; SIG-01d is observed on the CI matrix leg in plan `01-06`.
</verification>

<success_criteria>
- 0 errors and 0 warnings repo-wide at `--max-warnings 0`, achieved by fixing source, not by disabling rules.
- The lint gate is proven to exit 1 on a real error and the probe leaves no trace.
- Identical, green suite results on Node 22.23.2, 24.13.0 and 26.7.0 — the same code path on every leg.
- `pnpm -r typecheck`, `pnpm build`, and `pnpm format` are all clean.
- CMP-inv and V2-inv both hold across the whole phase diff.
- No file outside `files_modified` changed.
</success_criteria>

<output>
Create `.planning/phases/01-restore-the-verification-signal/01-04-SUMMARY.md` when done.
Record: final lint error/warning/file counts, the SIG-02f exit codes both ways, the per-Node file and test
counts for all three legs, the `pnpm build` result, and the exact output of both invariant greps.
</output>
